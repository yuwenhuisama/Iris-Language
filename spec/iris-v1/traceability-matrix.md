# Iris v1 Traceability Matrix

Status: Iris v1 draft, frozen semantics.

IRIS-V1-TRACE-C019: This matrix is normative for semantic decision coverage. Every frozen decision appears exactly once and maps to manifest-authoritative semantic clauses and only direct chapter vector definitions.

## Decision Rows

| Decision | Frozen draft heading | Semantic clause anchors | Exact example or vector coverage | Migration row | Status |
| --- | --- | --- | --- | --- | --- |
| `D-000` | All formal Iris v1 specification, grammar, core-library/ABI boundary, migration, and conformance documents will live under `spec/iris-v1/`. Existing historical artifacts in `Document/` remain archaeology inputs and are not moved or rewritten. | [IRIS-V1-TRACE-C013](README.md), [IRIS-V1-TRACE-C014](README.md), [IRIS-V1-TRACE-C002](README.md) | metadata-only: specification artifact location and inventory metadata | none | `normative` |
| `D-001` | Revived Iris is a modern successor to legacy Iris, not a source-compatible restoration and not an unrelated new language. Preserve its defining language identity while allowing syntax and problematic semantics to be redesigned. Every intentional incompatibility must be documented in a migration/divergence ledger. | [IRIS-V1-IDENTITY-C003](01-language-identity.md), [IRIS-V1-MIGRATION-C008](11-migration-divergence.md) | `IRIS-V1-IDENTITY-V001`, `IRIS-V1-IDENTITY-V012` | none | `normative` |
| `D-002` | The revived language must preserve all of these constraints: every value is an object; operators are dynamically dispatched messages/methods; method dispatch remains runtime-dynamic; composition uses single inheritance plus modules and explicit interfaces/protocols; lexical closures and trailing blocks remain core; classes and methods have runtime identity and support constrained dynamic mutation; any object may be raised as an exception; and the language remains designed for host embedding and native extensions. Exact syntax and mutation/ABI boundaries remain separate decisions. | [IRIS-V1-IDENTITY-C004](01-language-identity.md), [IRIS-V1-RUNTIME-C002](03-runtime-object-model.md), [IRIS-V1-CONTROL-C016](04-bindings-callables-control-flow.md) | `IRIS-V1-IDENTITY-V002`, `IRIS-V1-IDENTITY-V013` | none | `normative` |
| `D-003` | `Integer` is semantically arbitrary precision. Implementations may represent common values as immediate/fixed-width SmallInt and promote losslessly to BigInt, but this representation split is not observable as different Iris types or dispatch behavior. Integer arithmetic therefore has no fixed-width overflow or wrapping semantics; resource exhaustion is a separate runtime failure. Native boundaries must perform explicit checked conversions when targeting fixed-width host integers. | [IRIS-V1-RUNTIME-C101](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V052` | none | `normative` |
| `D-004` | Iris exposes multiple floating-point widths as observably distinct language types rather than specifying one universal `Float` representation. The initial width set, literal defaults, conversions, mixed-width arithmetic, equality, NaN behavior, and any shared `Float` supertype remain separate decisions. | [IRIS-V1-RUNTIME-C102](03-runtime-object-model.md) | metadata-only: superseded by exact Float32 and Float64 type clauses | none | `superseded wording consolidated` |
| `D-005` | The first language specification exposes exactly `Float32` and `Float64`, each with the corresponding IEEE-754 binary interchange semantics and observable through annotations, reflection, and dispatch. `Float16` and `Float128` are not built-in first-version types. Literal defaults, suffixes, conversions, promotion, and mixed arithmetic remain separate decisions. | [IRIS-V1-RUNTIME-C102](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V053` | none | `normative` |
| `D-006` | A floating-point literal without an explicit width suffix, including exponent notation, has type `Float64`. This decision does not imply an implicit narrowing conversion when the surrounding context expects `Float32`; contextual conversion remains a separate decision. | [IRIS-V1-GRAMMAR-C029](02-lexical-grammar.md), [IRIS-V1-RUNTIME-C102](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V053` | none | `normative` |
| `D-007` | `f32` and `f64` suffixes select `Float32` and `Float64` respectively, including exponent-form literals (for example `1.0f32` and `1e10f64`). Suffix case sensitivity follows the future global case-sensitivity decision. A suffix establishes literal type only and does not imply any implicit numeric conversion. | [IRIS-V1-GRAMMAR-C029](02-lexical-grammar.md), [IRIS-V1-RUNTIME-C102](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V053`, `IRIS-V1-RUNTIME-V054` | none | `normative` |
| `D-008` | `a OP b` has the same observable semantics as sending selector `OP` to the runtime class of the evaluated left operand with the evaluated right operand as its argument; this equivalence does not require a physical generic method lookup or call. Evaluation and dispatch order is fixed: evaluate the left operand, evaluate the right operand, then resolve and invoke the operator method visible at that point. Arithmetic has no language-wide implicit conversion or promotion table: the selected receiver method declares whether it accepts the other operand, how it interprets or converts it, and its result type. Heterogeneous arithmetic need not be symmetric or commutative. Implementations may use primitives, representation-specialized fast paths, inline caches, unboxing, specialized machine code, or eliminated/hoisted guards when behavior remains observationally equivalent. Such optimized code must guard or depend on every relevant receiver class, operand representation, resolved method identity/version, lookup hierarchy version, and dynamic dispatch hook; changing any dependency must invalidate the optimized code or force fallback/deoptimization before a later affected send executes. Hidden `SmallInt`/`BigInt` representations remain the visible `Integer` class and cannot alter user-visible dispatch. Static checking uses the selected receiver method contract when provable; unsupported dynamic sends fail through the normal Iris message/type error mechanism. | [IRIS-V1-RUNTIME-C022](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C027](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V055` | none | `normative` |
| `D-009` | The built-in `Integer`, `Float32`, and `Float64` classes may accept operands from the other built-in numeric classes in their operator methods. Each receiver class defines its own conversion, precision, exceptional-value, and result-type contract; accepting heterogeneous operands does not introduce a global promotion rule or symmetry requirement. Exact directional contracts remain separate decisions. | [IRIS-V1-RUNTIME-C103](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C104](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C105](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C106](03-runtime-object-model.md) | metadata-only: consolidated by directional heterogeneous numeric method clauses | none | `superseded wording consolidated` |
| `D-010` | For built-in arithmetic methods where the receiver is `Integer` and the argument is `Float32` or `Float64`, convert the receiver to the argument's floating-point width using the corresponding IEEE-754 rounding rules, perform the operation at that width, and return that same floating-point type. Loss of integer precision during conversion is permitted and observable. Conversion beyond the finite range is governed by the future exceptional-float-value decision. This is the contract of the built-in `Integer` operator method, not a language-wide promotion rule. | [IRIS-V1-RUNTIME-C104](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V056` | none | `normative` |
| `D-011` | For built-in arithmetic methods where the receiver is `Float32` or `Float64` and the argument is `Integer`, convert the argument to the receiver's floating-point width using the corresponding IEEE-754 rounding rules, perform the operation at that width, and return the receiver's floating-point type. Loss of integer precision is permitted and observable. Conversion beyond the finite range is governed by the future exceptional-float-value decision. This remains the independent contract of each built-in float operator method. | [IRIS-V1-RUNTIME-C105](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V056` | none | `normative` |
| `D-012` | Built-in arithmetic between `Float32` and `Float64` always converts the `Float32` operand to `Float64`, executes at `Float64` width, and returns `Float64`, independent of operand order. Same-width arithmetic returns the same width. The two receiver classes declare matching directional method contracts; this consistency does not create a language-wide promotion mechanism for user-defined numeric classes. A `Float64` operand is never implicitly narrowed merely because the receiver is `Float32`. | [IRIS-V1-RUNTIME-C106](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V056` | none | `normative` |
| `D-013` | `Float32` and `Float64` division follows non-trapping IEEE-754 semantics. Division of a nonzero finite value by positive or negative zero produces the correspondingly signed infinity; zero divided by zero produces NaN. These outcomes do not raise an Iris division-by-zero exception. Signed zero, infinities, and NaNs are observable float values. Integer division by zero remains a separate decision. | [IRIS-V1-RUNTIME-C110](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V057` | none | `normative` |
| `D-014` | When finite `Float32` or `Float64` arithmetic exceeds the target format's maximum finite magnitude, the result is the correspondingly signed infinity under IEEE-754 rounding semantics. Converting an arbitrary-precision `Integer` beyond the target float format's finite range likewise produces signed infinity. Neither case raises `FloatOverflowError` or `NumericConversionError`, and values are not saturated to the maximum finite float. | [IRIS-V1-RUNTIME-C111](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V058` | none | `normative` |
| `D-015` | Numeric equality for `Float32` and `Float64` follows IEEE-754 unordered NaN semantics: every comparison `nan == value` is `false`, and every `nan != value` is `true`, including comparisons with the same NaN value or another NaN. Object identity, hash-key equivalence, ordering, and NaN-payload inspection are separate protocols and must not be inferred from numeric `==`. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C130](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V059` | none | `normative` |
| `D-016` | Positive and negative floating-point zero are numerically equal under `==` and unequal under neither same-width nor mixed-width comparison. Mixed `Float32`/`Float64` comparison first follows the established widening rule. Their IEEE-754 bit patterns and signs remain observable through dedicated bit/sign APIs and operations such as reciprocal division; numeric equality is not bit equality or object identity. | [IRIS-V1-RUNTIME-C130](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V036` | none | `normative` |
| `D-017` | Built-in `Integer`, `Float32`, and `Float64` values compare by exact mathematical value across type boundaries. Comparison must not first round an arbitrary-precision `Integer` into a float: a float equals an integer only when its finite numeric value is exactly that integer. Equality is symmetric even though arithmetic dispatch is receiver-directed. Mixed float widths compare after exact interpretation/widening. NaN remains unequal to every value under D-015, and signed zeros remain equal under D-016. Hash-key equivalence remains a separate decision but must later respect the chosen equality contract where applicable. | [IRIS-V1-RUNTIME-C130](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V071` | none | `normative` |
| `D-018` | If either operand is NaN, each ordinary numeric ordering operator `<`, `<=`, `>`, and `>=` returns `false` and does not raise an exception. These operators therefore do not define a total order over floats. Sorting, ordered containers, and deterministic serialization that require a total order must use a separately specified total-comparison protocol. NaN inequality remains governed by D-015. | [IRIS-V1-RUNTIME-C131](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V059` | none | `normative` |
| `D-019` | Every built-in `Integer` division, integer-division, modulo, or remainder operation whose divisor is zero raises the ordinary catchable Iris exception `DivisionByZeroError`. `Integer` has no infinity or NaN values for this case, and the operations do not return `nil` or a result wrapper. The eventual exception-object fields and hierarchy remain part of the exception specification. | [IRIS-V1-RUNTIME-C121](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V060` | none | `normative` |
| `D-020` | Built-in `/` between two `Integer` values performs real-number division and always returns `Float64`, including exactly divisible operands. Both arbitrary-precision integers are interpreted under the established `Float64` conversion and IEEE-754 rules, so precision loss and signed infinity are permitted. Integer quotient division is a separate operation, and this decision does not introduce a built-in `Rational` type. | [IRIS-V1-RUNTIME-C120](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V046` | none | `normative` |
| `D-021` | Every method with exactly one ordinary argument may be invoked either with member-call syntax `receiver.method(argument)` or named infix syntax `receiver method argument`; therefore `a.div(b)` and `a div b` are equivalent observable sends. Parsing never consults method tables, declarations, arity metadata, or runtime state: syntactically valid `expression identifier expression` is parsed as a named infix send, while method existence and the one-argument contract are checked statically when provable and otherwise by normal runtime dispatch. Defining, removing, or replacing a method cannot change how already-valid source text parses. Eligibility and parsing do not imply a physical generic call, under D-008. Precedence, associativity, newline boundaries, and interactions with trailing blocks remain separate grammar decisions. | [IRIS-V1-GRAMMAR-C044](02-lexical-grammar.md), [IRIS-V1-RUNTIME-C026](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V061` | none | `normative` |
| `D-022` | All named infix calls share one grammar-defined, left-associative precedence level. That level is lower than symbolic arithmetic and comparison operators, but higher than logical `&&`/`||` and assignment. Thus `a div b + c` parses as `a div (b + c)`, while `a div b div c` parses as `(a div b) div c`; parentheses are required for other grouping. Method declarations cannot customize precedence or associativity. Built-in integer quotient division is the one-argument `div` method and may be written as either `a.div(b)` or `a div b`. Its negative-number rounding and paired remainder semantics remain separate decisions. | [IRIS-V1-GRAMMAR-C044](02-lexical-grammar.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V061` | none | `normative` |
| `D-023` | `Integer.div` computes floor division for every nonzero integer divisor: the quotient is the mathematical floor of the exact rational value `a / b`, including negative operands. Consequently `-5 div 2 == -3`, `5 div -2 == -3`, and `-5 div -2 == 2`. Division by zero raises `DivisionByZeroError` under D-019. The paired remainder operation must later satisfy `a == (a div b) * b + remainder`; its surface name and complete sign contract remain separate decisions. | [IRIS-V1-RUNTIME-C121](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V039` | none | `normative` |
| `D-024` | The remainder operation paired with floor `Integer.div` is the one-argument built-in method `mod`, callable as either `a.mod(b)` or named infix `a mod b` under D-021/D-022. The first specification does not provide `%` as an alias and does not define a separate truncating `rem` operation. Division by zero raises `DivisionByZeroError`. Exact negative-operand result signs remain a separate decision. | [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V102` | none | `normative` |
| `D-025` | For nonzero `b`, `a mod b` is defined exactly as `a - (a div b) * b`. It therefore satisfies `a == (a div b) * b + (a mod b)` and `abs(a mod b) < abs(b)`; every nonzero result has the same sign as divisor `b`. Thus `-5 mod 2 == 1`, `5 mod -2 == -1`, and `-5 mod -2 == -1`. Exact division produces zero. A zero divisor raises `DivisionByZeroError`. | [IRIS-V1-RUNTIME-C122](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V040` | none | `normative` |
| `D-026` | Built-in `Integer ** Integer` returns arbitrary-precision `Integer` for every nonnegative exponent. A negative integer exponent converts the computation to `Float64` reciprocal-power semantics and returns `Float64`, subject to the established IEEE-754 rounding, underflow, overflow, infinity, and NaN rules. Result type therefore depends on exponent sign. This decision does not introduce `Rational`. Zero-base edge cases remain separate decisions. | [IRIS-V1-RUNTIME-C123](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V062` | none | `normative` |
| `D-027` | `Integer(0) ** Integer(0)` raises the ordinary catchable Iris exception `DomainError`; it does not return `Integer(1)`, NaN, or `nil`. Positive integer exponents of zero still return `Integer(0)`. Negative-exponent zero behavior and floating-point zero-power behavior remain separate decisions. | [IRIS-V1-RUNTIME-C124](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V041` | none | `normative` |
| `D-028` | For every negative `Integer` exponent, an `Integer(0)` base follows D-026's `Float64` reciprocal-power path and returns `Float64(+Infinity)` under non-trapping IEEE-754 division-by-zero semantics. It does not raise `DivisionByZeroError` or `DomainError`. Iris `Integer` has no signed zero, so no negative-infinity case exists for this operation. | [IRIS-V1-RUNTIME-C124](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V112` | none | `normative` |
| `D-029` | For both `Float32` and `Float64`, every combination of positive or negative zero base and positive or negative floating-point zero exponent raises the ordinary catchable Iris exception `DomainError`. The operation does not return one, NaN, or infinity. This establishes a uniform cross-numeric rule that zero raised to zero is a domain error. Floating zero with a negative nonzero exponent remains a separate decision. | [IRIS-V1-RUNTIME-C125](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V041` | none | `normative` |
| `D-030` | For `Float32` and `Float64` zero bases with a negative nonzero `Integer` exponent, use IEEE/libm signed-zero parity behavior and retain the base width: positive zero produces positive infinity; negative zero with an odd-magnitude exponent produces negative infinity; negative zero with an even-magnitude exponent produces positive infinity. These operations do not raise an exception. Non-integer exponent cases remain separate decisions. | [IRIS-V1-RUNTIME-C125](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V062` | none | `normative` |
| `D-031` | When a negative finite `Float32` or `Float64` base is raised to a floating-point exponent that is not an integer-valued number and no real result exists, the operation returns NaN at the established result width and does not raise `DomainError`. Same-width operands retain that width; mixed float widths follow D-012 and return `Float64`. Ordinary floats never change their runtime result type to `Complex` based on operand values; any future complex exponentiation requires an explicit `Complex` receiver. The zero-to-zero exception in D-029 remains unchanged. | [IRIS-V1-RUNTIME-C126](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V042` | none | `normative` |
| `D-032` | Built-in float exponentiation follows the same width policy as other floating arithmetic: `Float32 ** Integer` and `Float32 ** Float32` return `Float32`; `Float64 ** Integer` and `Float64 ** Float64` return `Float64`; any operation mixing `Float32` and `Float64` widens the `Float32` operand and returns `Float64`, independent of operand order. This width rule does not override the separately frozen zero, infinity, NaN, or real-domain behavior. | [IRIS-V1-RUNTIME-C126](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V062` | none | `normative` |
| `D-033` | Symbolic `**` is right-associative and binds more tightly than prefix unary negation. Therefore `2 ** 3 ** 2` parses as `2 ** (3 ** 2)`, while `-2 ** 2` parses as `-(2 ** 2)`; a negative base requires parentheses, as in `(-2) ** 2`. Unary negation is accepted directly in exponent position, so `2 ** -3` parses as `2 ** (-3)`. | [IRIS-V1-GRAMMAR-C041](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C042](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V003` | none | `normative` |
| `D-034` | The first revived specification provides symbolic `Integer` bitwise operators `&`, `|`, `^`, prefix `~`, `<<`, and `>>`. Legacy `<<<` and `>>>` rotate operators are intentionally removed because arbitrary-precision `Integer` has no intrinsic rotation width. A future fixed-width `BitVector` family may define rotation operations independently. This incompatibility must appear in the legacy divergence ledger. Negative-integer bit semantics and shift-count behavior remain separate decisions. | [IRIS-V1-GRAMMAR-C016](02-lexical-grammar.md), [IRIS-V1-RUNTIME-C127](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C155](03-runtime-object-model.md), [IRIS-V1-MIGRATION-C008](11-migration-divergence.md) | `IRIS-V1-RUNTIME-V043` | none | `normative` |
| `D-035` | Bitwise operations on `Integer` are defined through an abstract infinite sign-extended two's-complement model, independent of physical BigInt storage. Consequently `~x == -x - 1`, `~0 == -1`, and right shift is arithmetic: for example `-1 >> n == -1` for nonnegative `n`, and `-3 >> 1 == -2`. Implementations may use sign-magnitude or any other representation when results are observationally equivalent. Shift-count validation remains a separate decision. | [IRIS-V1-RUNTIME-C127](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V043` | none | `normative` |
| `D-036` | `Integer` shifts accept counts of any sign. A negative count reverses the shift direction and uses the exact arbitrary-precision absolute value of the count: `x << -n` is equivalent to `x >> n`, and `x >> -n` is equivalent to `x << n`, for every positive `n`. Zero remains an identity shift. Negative counts do not raise `RangeError`. All mathematical, sign-extension, and resource rules of the resulting opposite-direction operation apply unchanged; therefore a huge negative right-shift count may require a huge left-shift allocation and raise `ResourceError`. | [IRIS-V1-RUNTIME-C128](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V063` | none | `normative` |
| `D-037` | For every nonnegative shift count, `Integer >> count` has a mathematical result and does not raise merely because the count exceeds the operand's finite magnitude bits. Under infinite sign extension, every nonnegative value eventually stabilizes at `0`, while every negative value eventually stabilizes at `-1`. Implementations may compute these stable results without iterating or allocating in proportion to the count. Resource policies do not alter this result. | [IRIS-V1-RUNTIME-C129](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V063` | none | `normative` |
| `D-038` | An operation such as a huge left shift remains mathematically valid regardless of result size. If the runtime or a host-configured quota cannot allocate the result, it raises the ordinary catchable Iris exception `ResourceError`, with `MemoryLimitError` available as a more specific subtype. Resource exhaustion must not surface as `RangeError`, `nil`, a host panic, process abort, or undefined behavior. Host limits may determine whether an operation succeeds, but never alter the mathematical result of a successful operation. The same principle applies to other controlled allocations such as huge integer powers, strings, and containers. | [IRIS-V1-RUNTIME-C129](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V064` | none | `normative` |
| `D-039` | Integer literals support binary `0b`, octal `0o`, unprefixed decimal, and hexadecimal `0x` forms. A leading zero alone never selects octal, so values such as `0755` are decimal subject to the future leading-zero lexical rule. Legacy or host-style implicit octal notation is intentionally unsupported. Prefix case sensitivity and digit separators remain separate lexical decisions. | [IRIS-V1-GRAMMAR-C024](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V144` | none | `normative` |
| `D-040` | Integer and floating-point literals permit a single underscore only between two digits that are valid within the same lexical digit segment. Forms such as `1_000`, `0xFF_FF`, `1.234_567`, `1e1_000`, and `1.0e+1_024` are valid. Separators at a literal boundary, adjacent to a radix prefix, decimal point, exponent marker/sign, or float suffix, and consecutive separators are invalid; examples include `_1`, `1_`, `1__0`, `0x_FF`, `1_.0`, `1._0`, `1e_3`, `1e+_3`, and `1_f32`. Separators do not affect value. | [IRIS-V1-GRAMMAR-C025](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V145` | none | `normative` |
| `D-041` | Unprefixed decimal integer literals may contain any number of leading zeros and are always interpreted as decimal: `00755 == 755` and `00 == 0`. Leading zeros do not select octal and do not inherently produce a warning. Formatters may canonicalize them only under an explicit formatting policy that does not change program value. | [IRIS-V1-GRAMMAR-C024](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V144`, `IRIS-V1-GRAMMAR-V146` | none | `normative` |
| `D-042` | Radix prefixes accept both cases (`0b`/`0B`, `0o`/`0O`, `0x`/`0X`); hexadecimal digits accept `a-f` and `A-F`; exponent markers accept `e` and `E`. Float-width suffixes have one canonical lexical spelling and are lowercase-only: `f32` and `f64`; forms such as `F32` and `F64` are lexical errors. This decision does not determine case sensitivity for ordinary identifiers or keywords. | [IRIS-V1-GRAMMAR-C048A](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V147` | none | `normative` |
| `D-043` | After a radix prefix begins a numeric literal, the lexer consumes the contiguous candidate literal segment as one token for validation. If it contains a digit or identifier character invalid for that radix, the whole token is an invalid numeric literal with a radix-specific diagnostic; it is not split at the longest valid prefix. Thus `0b102`, `0o89`, and `0xFG` each produce one primary bad-literal error rather than misleading secondary parse errors. Exact candidate-token boundaries and recovery after punctuation/whitespace remain lexer-spec details. | [IRIS-V1-GRAMMAR-C026](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V148` | none | `normative` |
| `D-044` | A radix prefix must be followed immediately by at least one digit valid for that radix. Empty forms (`0x`, `0b`, `0o`, in either accepted prefix case) and separator-first forms such as `0x_FF`, `0b_1010`, and `0o_755` are each one invalid numeric-literal token with a diagnostic that the radix prefix lacks an immediately valid digit. They are not split into decimal zero plus an identifier and are never interpreted as zero. | [IRIS-V1-GRAMMAR-C048A](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V149` | none | `normative` |
| `D-045` | Decimal floating literals may omit digits on either one side of the decimal point, so `.5` equals `0.5` and `1.` equals `1.0`; both are unsuffixed `Float64` under D-006. At least one side must contain decimal digits, so a lone `.` is punctuation/member-access syntax rather than a numeric literal. Existing separator constraints still prohibit forms such as `._5`, `1._0`, and `1_.`. | [IRIS-V1-GRAMMAR-C027](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V150` | none | `normative` |
| `D-046` | Decimal scientific notation forms a floating literal even without a decimal point. The exponent marker is `e` or `E`, followed by an optional single `+` or `-` and at least one decimal digit. Unsuffixed forms such as `1e3` and `2E-4` are `Float64`; lowercase `f32`/`f64` selects the explicit width. Digit separators remain valid only between exponent digits. The mantissa is decimal, and malformed/missing exponent digits are invalid-literal diagnostics. | [IRIS-V1-GRAMMAR-C027](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V151` | none | `normative` |
| `D-047` | The first specification supports C99-style hexadecimal floating literals with a `0x`/`0X` hexadecimal significand and a mandatory `p`/`P` binary-exponent marker whose exponent digits are decimal with an optional sign. Unsuffixed literals are `Float64`; lowercase `f32`/`f64` selects explicit width. Binary- and octal-prefixed literals never accept a radix point or floating exponent. Exact hexadecimal significand point requirements and separator boundaries remain separate lexical decisions. | [IRIS-V1-GRAMMAR-C028](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V152` | none | `normative` |
| `D-048` | Hexadecimal floating significands may omit the radix point (`0x1p0`) or omit digits on either one side of a present point (`0x1.p0`, `0x.8p0`), but must contain at least one hexadecimal digit overall. A `p`/`P` exponent is always mandatory, so a form such as `0x1.` is invalid and `0x1e3` remains a hexadecimal `Integer`. Exponent digits are decimal with an optional single sign; unsuffixed and lowercase-width-suffixed typing follows D-047. | [IRIS-V1-GRAMMAR-C028](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V152`, `IRIS-V1-GRAMMAR-V153` | none | `normative` |
| `D-049` | Decimal and hexadecimal floating source literals are converted to their target `Float32` or `Float64` bit pattern with correctly rounded IEEE-754 `roundTiesToEven` semantics (nearest representable value, exact midpoint chooses an even significand). Compilation must not depend on the host process floating environment, current rounding mode, locale, or an inaccurately rounded host parser such as an unsuitable `strtod`. Equivalent source literals must produce the same bits across interpreter, JIT, target platform, and compiler host. Literal overflow and underflow outcomes remain separate decisions. | [IRIS-V1-GRAMMAR-C030](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C057](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V154` | none | `normative` |
| `D-050` | Correctly rounded source literals use IEEE-754 gradual underflow: magnitude overflow produces signed infinity; sufficiently small values first round to subnormal values and eventually to signed zero. These remain valid literals. The compiler emits a default-enabled precision warning when a finite nonzero source literal rounds to infinity or to zero, but not merely for a representable finite/subnormal result. Tool policy may promote the warning to an error without changing the normative value or bit pattern. | [IRIS-V1-GRAMMAR-C031](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V155` | none | `normative` |
| `D-051` | Iris reserves no global `nan`/`inf` literal tokens or identifiers. Canonical special values are exposed through width-explicit type constants `Float32.nan`, `Float32.infinity`, `Float64.nan`, and `Float64.infinity`; negative infinity is produced with ordinary unary negation. NaN payload construction and exact bit patterns belong to future bit-construction APIs. Whether built-in type constants can be dynamically replaced remains a separate mutation decision. | [IRIS-V1-RUNTIME-C116](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V065` | none | `normative` |
| `D-052` | `nan` and `infinity` are not constant slots; they are get-only properties on the `Float32` and `Float64` class objects. Expressions such as `Float32.nan` and `Float64.infinity` perform ordinary property-read/message semantics and return the canonical value of that width. No property writer exists, so source assignment such as `Float64.infinity = value` is invalid. Whether the built-in getter implementation itself may be replaced through dynamic method mutation remains a separate decision. | [IRIS-V1-RUNTIME-C116](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V065` | none | `normative` |
| `D-053` | The built-in getter implementations for `Float32.nan`, `Float32.infinity`, `Float64.nan`, and `Float64.infinity` are not sealed. Users may replace or remove them through the ordinary dynamic class-object mutation mechanism and accept the resulting semantic consequences; after replacement, these expressions are no longer guaranteed to return canonical IEEE values. Optimizers may fold or specialize an access only while guarding/depending on the exact getter and relevant method/property-table versions, and mutation must invalidate or deoptimize dependent code under D-008. Whether users may add a setter to these initially get-only properties remains a separate decision. | [IRIS-V1-RUNTIME-C117](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V103` | none | `normative` |
| `D-054` | Dynamic mutation may add a setter to an initially get-only built-in property, including the special-value properties on float class objects. Adding a setter is only the addition of an assignment-message implementation: it does not automatically allocate backing storage, replace the getter, or synchronize getter and setter behavior. User code defines and assumes responsibility for all state and side effects. Property/method shape versions must change and invalidate or deoptimize dependent inline caches and compiled code. | [IRIS-V1-RUNTIME-C117](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C155](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V103` | none | `normative` |
| `D-055` | Every primitive built-in `Float32` and `Float64` basic arithmetic step (`+`, `-`, `*`, `/`) is correctly rounded to its established result width using IEEE-754 `roundTiesToEven`. Iris exposes no mutable global or thread-local rounding mode that changes these operators, and results must not depend on the host floating environment. Directed or alternate rounding, if added, must use explicit APIs with separate contracts. Contraction and excess intermediate precision remain separate decisions. | [IRIS-V1-RUNTIME-C107](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V066` | none | `normative` |
| `D-056` | Source-level separate multiply and add operations round independently according to the expression tree. Implementations must not automatically contract `a * b + c` (or equivalent subtract forms) into a fused multiply-add when doing so could change observable bits, exceptions, or special-value behavior. Debug, release, interpreter, and JIT semantics are identical. Contraction is permitted only when proven bit-for-bit observationally equivalent. A future explicit `mul_add` method may expose true fused one-rounding semantics. | [IRIS-V1-RUNTIME-C108](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V067` | none | `normative` |
| `D-057` | Every primitive `Float32` or `Float64` result is semantically rounded to its declared result width before it becomes an operand of a subsequent operation. Implementations may physically retain a wider register representation only when every later observation and operation is exactly equivalent to using the already-rounded target-width bits. Excess precision must never make results depend on storage, register lifetime, target architecture, interpreter/JIT choice, or optimization level. | [IRIS-V1-RUNTIME-C109](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V066` | none | `normative` |
| `D-058` | The first standard numeric API provides the ordinary dynamically replaceable instance method `a.mul_add(b, c)`. It computes the exact product-plus-add expression and performs one final `roundTiesToEven` rounding at the established result width; the product is not rounded separately. It may therefore differ from `a * b + c`. Its two arguments make it ineligible for named infix syntax. Implementations may use hardware FMA or a correctly rounded software path, but optimized direct lowering must depend on the exact method/version and invalidate or deoptimize after dynamic replacement. Mixed-type width and exceptional-value contracts remain separate decisions. | [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V067` | none | `normative` |
| `D-059` | Built-in `Float32.mul_add` and `Float64.mul_add` accept `Integer`, `Float32`, and `Float64` for both arguments. The common result width is `Float64` if any of receiver, multiplier, or addend is `Float64`; otherwise it is the receiver's width. `Integer` operands participate as exact mathematical integers in the fused expression and are not first rounded to a float. The exact product-plus-add value is rounded once to the common result width under D-055. Unsupported user-defined numeric types follow ordinary method contracts/dispatch. Exceptional IEEE combinations remain separate decisions. | [IRIS-V1-RUNTIME-C118](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V067` | none | `normative` |
| `D-060` | If either fused multiplicand is positive or negative infinity and the other is numeric zero, built-in `mul_add` returns NaN at the D-059 common result width and does not raise an Iris exception. Numeric zero includes `Integer(0)` and either signed zero of either float width. The addend cannot make this invalid product valid. NaN bit/payload selection remains a separate decision. | [IRIS-V1-RUNTIME-C119](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V047` | none | `normative` |
| `D-061` | If the exact fused product is positive infinity and the addend is negative infinity, or the product is negative infinity and the addend is positive infinity, built-in `mul_add` returns NaN at the D-059 common width and raises no Iris exception. If infinite product and infinite addend have the same sign, the result is that signed infinity unless another already-frozen NaN/invalid rule applies. NaN bit/payload selection remains separate. | [IRIS-V1-RUNTIME-C119](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V048` | none | `normative` |
| `D-062` | When a built-in floating operation propagates an input NaN or creates a NaN through an invalid IEEE operation, Iris guarantees only a quiet NaN at the established result width. Ordinary arithmetic does not specify which input payload is propagated, the result payload, or the NaN sign bit, and those bits may differ across hardware while preserving numeric semantics. Code requiring exact NaN payload/sign construction or preservation must use explicit bit-level APIs rather than ordinary arithmetic. | [IRIS-V1-RUNTIME-C112](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V104` | none | `normative` |
| `D-063` | The first standard numeric API provides `Float32#to_bits()` and `Float64#to_bits()` instance methods plus `Float32.from_bits(bits)` and `Float64.from_bits(bits)` class-object methods. These are exact IEEE interchange-format bit reinterpretations rather than numeric conversions and must round-trip every bit pattern, including signed zero, infinities, subnormals, quiet/signaling encodings on input, and NaN payload/sign bits. Ordinary arithmetic may still quiet or alter NaNs under D-062. The bit-container types and accepted numeric ranges remain separate decisions. | [IRIS-V1-RUNTIME-C113](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V068` | none | `normative` |
| `D-064` | `Float32#to_bits()` returns a nonnegative `Integer` in `0..2^32-1`, and `Float64#to_bits()` returns one in `0..2^64-1`. The corresponding `from_bits` accepts exactly that width's range; negative and oversized values raise the ordinary catchable Iris `RangeError`. Inputs are never truncated, reduced modulo a power of two, rounded, or interpreted through host endianness. The first specification introduces no `UInt32`/`UInt64` solely for this API. | [IRIS-V1-RUNTIME-C113](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V069` | none | `normative` |
| `D-065` | `from_bits(bits).to_bits() == bits` holds for every valid 32- or 64-bit interchange pattern, including signaling-NaN encodings. Construction, storage, argument passing, return, copying, and `to_bits` must not automatically quiet or canonicalize an sNaN. When such a value participates in ordinary floating arithmetic, D-062 applies and the result is only guaranteed to be a quiet NaN. Non-arithmetic classification and comparison handling remain separate decisions. | [IRIS-V1-RUNTIME-C114](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V068` | none | `normative` |
| `D-066` | `is_nan`, `is_signaling_nan`, `is_infinite`, `is_finite`, `is_normal`, `is_subnormal`, `is_zero`, and `sign_bit` are pure bit-classification methods. They perform no ordinary floating arithmetic, never quiet an sNaN, preserve payload and sign, raise no floating exception, and leave `to_bits()` unchanged. A given interchange bit pattern must receive the same classification across interpreter, JIT, and platform. | [IRIS-V1-RUNTIME-C115](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V068` | none | `normative` |
| `D-067` | A signaling NaN participates in ordinary comparisons with the same non-trapping results as any NaN: `==` is `false`, `!=` is `true`, and `<`, `<=`, `>`, and `>=` are all `false`. Comparison raises no Iris floating exception and does not mutate either operand's stored bit pattern. Any internal quieting used by hardware is not observable through the original values. | [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V068` | none | `normative` |
| `D-068` | `Float32` and `Float64` instances are immutable value objects: their stored interchange bit pattern cannot change after creation. Arithmetic, conversions, and `from_bits` produce values rather than mutating an existing instance; `to_bits` and classification are pure reads. Dynamic mutation may replace class-level methods and property protocols but cannot alter the numeric bits held by an already-existing float value. This permits sharing, unboxing, constant reuse, and register representation while method/version guards remain required for dynamically replaceable behavior. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V070` | none | `normative` |
| `D-069` | Every `Integer` instance is an immutable value object independent of hidden SmallInt/BigInt representation. Arithmetic, exponentiation, bitwise operations, shifts, and representation promotion produce values and cannot mutate an existing integer's mathematical value. Dynamic class methods remain replaceable. Implementations may reuse or mutate uniquely owned internal BigInt storage only when no Iris observation can distinguish it from creating a new immutable value; internal representation must never change alias-visible behavior. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C160](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V070` | none | `normative` |
| `D-070` | `Integer`, `Float32`, and `Float64` expose value and bit-value semantics but no stable object identity. A future identity protocol must reject these values rather than return implementation-dependent results. Implementations may use immediates, interning, constant merging, reboxing, shared BigInt storage, or different interpreter/JIT representations without Iris observation. Float bit distinctions such as signed zero and NaN payload remain observable through explicit bit APIs but are not object identity. | [IRIS-V1-RUNTIME-C160](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V070` | none | `normative` |
| `D-071` | Every pair of built-in numeric values for which ordinary `==` is `true` must produce the same hash/key hash, including equality across `Integer`, `Float32`, and `Float64` and equality of positive and negative zero. Hashing therefore cannot simply include the concrete numeric type or raw float bits. Unequal values may collide. The exact stable/randomized hash algorithm and NaN key behavior remain separate decisions. | [IRIS-V1-RUNTIME-C136](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V071` | none | `normative` |
| `D-072` | No quiet or signaling NaN of either float width is a valid Hash key. Hash construction, insertion, or key update with a NaN raises the ordinary catchable Iris `InvalidKeyError`; containers do not introduce a NaN-specific equality relation. Code that needs payload-sensitive mapping must explicitly use `to_bits()` (and encode width separately when needed) as a non-NaN key. Ordinary NaN equality remains unchanged. | [IRIS-V1-RUNTIME-C134](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V072` | none | `normative` |
| `D-073` | Built-in numeric `hash` returns a specification-defined value stable across processes, platforms, interpreter/JIT modes, and conforming implementations/versions unless a future spec revision explicitly versions the algorithm. Hash containers do not use that public value directly as a bucket index: each runtime/container mixes it with a secret randomized seed to resist collision attacks. This internal mixing is not observable through public `hash` and must preserve D-071 equality consistency. Result width and algorithm remain separate decisions. | [IRIS-V1-RUNTIME-C137](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-074` | The public stable `hash` protocol returns an ordinary nonnegative `Integer` in the fixed range `0..2^64-1`. It introduces no `UInt64`, does not depend on pointer width or host `size_t`, and yields the same 64-bit result for the same specification value across process, platform, interpreter, and JIT. Hash containers may secretly remix and reduce this value for their own table geometry without exposing that internal result. Exact algorithm and evolution policy remain separate decisions. | [IRIS-V1-RUNTIME-C136](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C155](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-075` | The exact public `hash` mapping is part of a language-specification major-version contract: every conforming implementation of the same major version returns the same hash for every specification value, and minor/patch revisions cannot change it. A later major version may adopt a new algorithm only while exposing explicit versioned legacy entry points (for example `hash_v1`) sufficient to read, migrate, or reproduce persisted data. Unversioned `hash` follows the active major specification. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-076` | The first public numeric hash maps every built-in numeric value to a unique canonical mathematical-value encoding before hashing, so values equal under D-017 share the same bytes regardless of `Integer`, `Float32`, or `Float64` representation and signed zeros share one encoding. Exact integral floats reuse the integer-value encoding; other finite floats use a canonical exact binary-rational encoding. NaN remains outside the Hash-key domain under D-072, while public hashing behavior for direct NaN method calls remains to be specified. The canonical bytes are hashed through a specification-fixed BLAKE3 derivation and reduced to the D-074 64-bit result. Dynamic user method replacement cannot alter this built-in normative mapping while the built-in hash method remains selected; replacing that method invokes the ordinary dynamic-mutation consequences. Domain separation, byte grammar, and 64-bit extraction remain separate decisions. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C136](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C140](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-077` | Compute the specification's domain-separated standard 32-byte BLAKE3 digest over the canonical numeric encoding, take digest bytes `0..8`, interpret those eight bytes as an unsigned little-endian integer independent of host byte order, and return that value as the nonnegative Iris `Integer` public hash. No XOR folding or alternate digest segment is used. The exact domain identifier and canonical encoding grammar remain separate decisions. | [IRIS-V1-RUNTIME-C138](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C146](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-078` | First-version public numeric hashing uses BLAKE3 derive-key mode with the exact ASCII context string `Iris Language v1 stable numeric hash`. The canonical numeric encoding is the derive-key input material; the context is not manually concatenated to it. Context bytes, capitalization, spaces, and version marker are normative. Digest extraction then follows D-077. | [IRIS-V1-RUNTIME-C138](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C146](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-079` | Canonical numeric hash encodings begin with one byte: `0x00` for every finite mathematical value, `0x01` for positive infinity, and `0x02` for negative infinity. Concrete numeric type and float width are omitted, so equal `Integer`, `Float32`, and `Float64` values share bytes; integer zero and both signed float zeros share the finite-zero encoding, and infinities share encoding across widths. NaN has no Hash-key canonical encoding under D-072. Direct public `hash` behavior on NaN remains separate. | [IRIS-V1-RUNTIME-C139](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-080` | Calling the built-in public `hash` method on any quiet or signaling NaN of either width raises the ordinary catchable Iris `InvalidKeyError`, matching NaN's exclusion from Hash keys. It returns no canonical or payload-dependent numeric hash. Code that intentionally hashes a NaN bit pattern must explicitly call `to_bits().hash` and encode width separately when required. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C134](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V072` | none | `normative` |
| `D-081` | Every nonzero finite built-in numeric value has the unique exact form `sign × odd_significand × 2^exponent`, where `sign` is positive or negative, `odd_significand` is a positive odd arbitrary-precision integer, and `exponent` is a signed arbitrary-precision integer. All factors of two are removed from the significand and added to the exponent. Zero has a separate encoding. This normal form is independent of concrete numeric type/width and therefore identical for equal `Integer`, `Float32`, and `Float64` values. | [IRIS-V1-RUNTIME-C140](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-082` | Immediately after top-level finite tag `0x00`, one byte identifies the normalized finite case: `0x00` for zero, `0x01` for positive nonzero, and `0x02` for negative nonzero. The zero encoding ends after these two bytes, so `Integer(0)` and both widths/signs of floating zero encode exactly as `[0x00, 0x00]`. Positive/negative encodings continue with the normalized odd significand and exponent. | [IRIS-V1-RUNTIME-C140](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-083` | A normalized positive odd significand is encoded as `ULEB128(byte_length) || big_endian_magnitude`. `byte_length` uses the shortest canonical ULEB128 and is at least one. Magnitude uses the shortest unsigned big-endian byte sequence with no leading zero; decoded magnitude must be positive and odd. Zero, even magnitude, redundant ULEB128, and leading-zero magnitude are invalid canonical encodings. The length explicitly frames the following exponent field. | [IRIS-V1-RUNTIME-C141](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C125](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-084` | The arbitrary-precision signed exponent is mapped to a nonnegative arbitrary-precision integer with ZigZag ordering (`0→0`, `-1→1`, `1→2`, `-2→3`, ...), then encoded as the shortest canonical ULEB128. Redundant ULEB128 encodings are invalid. No fixed-width two's-complement limit applies. The exact arbitrary-precision ZigZag formula remains to be stated separately to prevent host-width implementation errors. | [IRIS-V1-RUNTIME-C142](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C135](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-085` | For exponent `e`, ZigZag encoding is the mathematical piecewise mapping `2*e` when `e >= 0`, and `-2*e - 1` when `e < 0`. Decoding nonnegative `u` yields `u/2` when `u` is even and `-(u+1)/2` when odd. This definition applies to arbitrary-precision `Integer`; host-width shift/XOR formulas may be used only when proven exactly equivalent without overflow or width dependence. The mapped value is then encoded by D-084's shortest ULEB128. | [IRIS-V1-RUNTIME-C142](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-086` | Positive infinity is encoded exactly as the single byte `[0x01]`, and negative infinity exactly as `[0x02]`; each encoding ends immediately after its top-level tag. No width, sign, payload, length, or trailing field is present. Consequently equal infinities of `Float32` and `Float64` hash identically. Any trailing byte would make the encoding noncanonical. | [IRIS-V1-RUNTIME-C139](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C143](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-087` | The canonical byte encoding used to define built-in numeric `hash` is specification-internal and is not exposed through an Iris method such as `canonical_numeric_bytes`. It is not a general serialization, persistence, or network format. Implementations and conformance tools may inspect it internally, and non-normative debugging tools may display it, but user programs observe only the final public hash. Float bit access remains provided by `to_bits`/`from_bits`. The internal encoding remains major-version constrained because it determines public hash results. | [IRIS-V1-RUNTIME-C144](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V074` | none | `normative` |
| `D-088` | Ordinary ordered comparisons among finite `Integer`, `Float32`, and `Float64` values use exact mathematical values and remain symmetric across operand order. Implementations must not round arbitrary-precision integers to a float before comparison. Equal values follow D-017/D-016; infinities compare by their mathematical extended-real positions; any NaN operand follows D-018 and makes all ordering operators false. Dynamic method replaceability remains a separate decision from the built-in method contract. | [IRIS-V1-RUNTIME-C131](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V105` | none | `normative` |
| `D-089` | The built-in `Integer`, `Float32`, and `Float64` implementations of `==`, `!=`, `<`, `<=`, `>`, and `>=` are ordinary dynamically replaceable methods. The frozen numeric rules specify their unmodified standard behavior only. Comparison fast paths, inline caches, and JIT code must depend on the exact methods and relevant lookup versions and invalidate/fallback/deopt after mutation. Users may thereby violate symmetry, transitivity, ordering, or equality/hash consistency and assume responsibility for resulting Hash, set, and sorting behavior. Default derivation relationships among comparison operators remain a separate decision. | [IRIS-V1-RUNTIME-C133](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C155](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V106` | none | `normative` |
| `D-090` | `==`, `!=`, `<`, `<=`, `>`, and `>=` each remain an ordinary one-argument method slot. Their unmodified default implementations derive Boolean results from the current visible `<=>` response, subject to D-097's identity-first rule for ordinary identity-bearing objects. For `<=> == -1/0/1`: `==` tests zero, `!=` tests nonzero, `<` tests `-1`, `<=` tests `-1 or 0`, `>` tests `1`, and `>=` tests `0 or 1`. A standard `<=>` result of `nil` means unordered and maps to `== false`, `!= true`, and all four ordered comparisons `false`. This is method-level default delegation, not parser desugaring, so normal dynamic dispatch and evaluation order apply. | [IRIS-V1-RUNTIME-C084](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V075` | none | `normative` |
| `D-091` | Users may dynamically replace any of `==`, `!=`, `<`, `<=`, `>`, or `>=`; once a slot is replaced, calls to that operator execute the replacement and need not consult `<=>`. Replacing `<=>` affects every still-unmodified/default-delegating comparison slot, but does not override a user-replaced slot. Replacing one derived comparison slot does not alter `<=>` or the other slots. Inline caches and JIT specializations must depend on both the selected operator method and every delegated `<=>` method/version actually used; either mutation must invalidate, fallback, or deoptimize affected code. Users assume responsibility for any resulting equality, ordering, or hash inconsistency. | [IRIS-V1-RUNTIME-C087](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V106` | none | `normative` |
| `D-092` | `<=>` is an independent, dynamically replaceable one-argument operator method. Its unmodified built-in numeric implementation performs exact cross-type mathematical comparison and returns `Integer(-1)`, `Integer(0)`, or `Integer(1)` for less/equal/greater, without first rounding arbitrary-precision `Integer`; if either operand is NaN it returns `nil` for unordered. The six standard comparison methods derive from the current visible `<=>` only while their own default implementations remain installed, under D-090/D-091. | [IRIS-V1-RUNTIME-C084](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V105` | none | `normative` |
| `D-093` | A default-delegating `==`, `!=`, `<`, `<=`, `>`, or `>=` accepts from the invoked `<=>` only exact `Integer(-1)`, `Integer(0)`, `Integer(1)`, or `nil`. Any other integer, any other type, or a contract-violating dynamic value raises the ordinary catchable Iris `ComparisonContractError`; default comparison never coerces by sign or truthiness and never silently returns `false`. A user-replaced comparison slot may define its own behavior because it no longer uses the default delegation. | [IRIS-V1-RUNTIME-C084](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V107` | none | `normative` |
| `D-094` | In the first gradual type system, `<=>` is annotated to return `Integer?`. `nil` represents unordered; non-nil protocol-valid results are restricted dynamically to exact `-1`, `0`, or `1`. A non-`Integer`/non-`nil` result violates the ordinary return type contract and raises `TypeError`; another `Integer` satisfies the broad static type but causes a default-delegating comparison to raise `ComparisonContractError` under D-093. This decision introduces neither literal unions nor a nominal `Comparison` type. | [IRIS-V1-RUNTIME-C085](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V107` | none | `normative` |
| `D-095` | The built-in numeric `<=>` methods accept `other: Object` and return `Integer?`. They perform the frozen exact comparison for `Integer`, `Float32`, and `Float64`; NaN or a nonnumeric object yields `nil` rather than `TypeError`. Therefore still-default-derived comparison methods produce `== false`, `!= true`, and all ordered comparisons `false` for numeric-vs-nonnumeric operands. User replacements may narrow or change their own contracts subject to the gradual type and override rules. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C084](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C132](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V105` | none | `normative` |
| `D-096` | Root `Object` provides `<=>(other: Object) -> Integer?`, whose default implementation returns `nil` for every operand. Consequently every object accepts the comparison message, and subclasses obtain value ordering by overriding `<=>`; no global address/object-ID order is exposed. How root default `==` treats two references to the same ordinary object remains a separate identity/equality decision and may override the generic `<=>` derivation at `Object`. | [IRIS-V1-RUNTIME-C083](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V075` | none | `normative` |
| `D-097` | For identity-bearing objects whose `==` slot still uses the root/default implementation, equality first tests whether both references denote the same object; if so it returns `true` without consulting `<=>`. Otherwise it invokes the current visible `<=>` and returns `true` only for exact `Integer(0)`, with `nil` mapping to `false` and invalid results following D-093. Default `!=` is complementary while its own default slot remains installed. A subclass/user may replace `==` or `!=` independently. Built-in numeric value objects have no observable identity under D-070 and use their numeric `<=>` value protocol only. | [IRIS-V1-RUNTIME-C086](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V075` | none | `normative` |
| `D-098` | Iris provides the non-overloadable primitive `same?` for explicit object-identity comparison, usable as `a same? b` and `a.same?(b)`. It evaluates the left operand and then the right operand once each and returns `Bool`; it bypasses method lookup, `<=>`, `==`, proxies, and user replacement. Only identity-bearing operands are accepted; built-in numeric value objects raise the ordinary catchable `IdentityError`. To support this surface consistently, an ordinary method identifier may end in one trailing `?`, which is part of its name; detailed identifier grammar and whether other suffix punctuation is allowed remain separate decisions. Special singleton/value categories such as `nil`, booleans, and class objects remain to be classified for identity. | [IRIS-V1-RUNTIME-C029](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C155](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V109` | none | `normative` |
| `D-099` | `nil`, `true`, and `false` are unique identity-bearing singleton objects. Class, Module, Contract, Method, Closure/Block, and other runtime definition/meta entities have stable observable identity and may participate in `same?`. `Integer`, `Float32`, and `Float64` remain identity-less immutable values and cause `IdentityError` when used as either operand. Future immutable value types must explicitly declare whether they are identity-less; identity is not inferred merely from implementation allocation. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C155](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V019` | none | `normative` |
| `D-100` | Reopening an existing class mutates the same identity-bearing Class object rather than creating a replacement. References captured before and after reopening satisfy `same?`; existing instances remain instances of that Class object. Reopen updates relevant method/property/shape/contract versions and invalidates or deoptimizes dependent caches/code. Rebinding a name to a newly created Class object is a distinct operation with distinct identity and is not class reopen. Exact rebinding permissions remain separate. | [IRIS-V1-RUNTIME-C010](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V011` | none | `normative` |
| `D-101` | Replacing a method slot creates a new identity-bearing Method object and installs it in the slot, incrementing relevant versions. Previously captured Method objects retain their identity and old body and remain explicitly invocable through reflection; already-running old frames complete the old body. Future ordinary lookup uses the new Method. Removing a slot likewise does not invalidate an old Method object that remains referenced. Caches/JIT depending on the old slot or Method identity must invalidate, fallback, or deoptimize. The lookup context used by `super` inside a retained old Method remains a separate decision. | [IRIS-V1-RUNTIME-C013](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C022](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V012`, `IRIS-V1-RUNTIME-V013` | none | `normative` |
| `D-102` | Every Method object retains the identity of its lexical owner Class/Module. When an old retained Method is explicitly invoked and executes `super`, lookup uses the receiver's current, versioned MRO at invocation time and continues after that lexical owner; the Method does not preserve a historical hierarchy/MRO snapshot. Hierarchy mutation therefore affects future reflective invocations of old bodies, while already-resolved active calls follow normal frame semantics. Behavior when the owner is absent from the receiver's current MRO remains separate. | [IRIS-V1-RUNTIME-C014](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C022](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V015` | none | `normative` |
| `D-103` | When a reflectively invoked retained Method executes `super`, its lexical owner must be locatable in the receiver's current MRO. If absent, execution raises the ordinary catchable Iris `InvalidSuperError`; lookup does not restart from the receiver class, guess from a same-named method, use a historical MRO, follow the owner's unrelated hierarchy, or return `nil`. Diagnostics expose the Method identity/name, lexical owner, receiver class, current MRO/version, and `super` source location. Whether owner membership is required at Method invocation entry even when the body never executes `super` remains separate. | [IRIS-V1-RUNTIME-C014](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V015` | none | `normative` |
| `D-104` | Explicit reflective invocation validates at entry that the receiver's current MRO contains the Method's lexical owner: a Class owner requires an instance whose MRO includes that Class; a Module owner requires the Module in the receiver MRO. Incompatibility immediately raises the ordinary catchable Iris `MethodBindingError`, even if a particular body path would not access state or execute `super`. This makes validity independent of body contents. D-103 still governs a later `super` if reentrant/concurrent hierarchy mutation removes the owner after entry validation. Bound-method capture and revalidation remain separate. | [IRIS-V1-RUNTIME-C015](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V014` | none | `normative` |
| `D-105` | Reading/binding an instance method creates a BoundMethod that captures the invocation receiver and the exact Method object's identity resolved at binding time, but no historical MRO snapshot. For identity-bearing receivers it captures receiver identity; for identity-less immutable values it captures the value representation needed to invoke with that value as `self` without creating observable receiver identity. Later slot replacement does not redirect the BoundMethod. Each invocation revalidates that the receiver's current Class/MRO contains the Method's lexical owner, raising `MethodBindingError` if absent; `super` uses invocation-time current MRO under D-102. BoundMethod itself is identity-bearing. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C038](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C039](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V017` | none | `normative` |
| `D-106` | Each evaluation that reads/binds `obj.method` creates a new identity-bearing BoundMethod, even when receiver and resolved Method are unchanged. Therefore `obj.method same? obj.method` is `false` because the two operands are separately evaluated bindings, while a saved BoundMethod reference is `same?` itself. Implementations may elide allocation only when identity observations remain exactly equivalent; no interning or weak-cache behavior is part of the language contract. Equality by captured contents remains separate. | [IRIS-V1-RUNTIME-C040](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V016`, `IRIS-V1-RUNTIME-V017` | none | `normative` |
| `D-107` | BoundMethod does not override root `<=>` or define value equality by captured receiver/Method. Its default `==` is `true` only for the same BoundMethod identity under D-097; separately created bindings compare unequal even when they capture the same receiver identity and Method identity. Reflection may expose those components for explicit comparison. Users may dynamically replace BoundMethod comparison/hash behavior and assume responsibility for contracts. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C041](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C155](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V077` | none | `normative` |
| `D-108` | Each evaluation of a closure/block expression creates a new identity-bearing Closure/Block object with its own captured environment, even when code location and captured values are equal. Default equality is identity-only through root Object behavior: a saved closure equals/is `same?` itself, while separately evaluated closures compare unequal. Iris does not structurally compare executable code or captured environments. Implementations may elide allocation only when identity and capture observations remain equivalent. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C042](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V077` | none | `normative` |
| `D-109` | Method objects use root identity-first equality and do not define structural/source equality. Only the same Method identity compares equal by default; distinct Method objects remain unequal even when owner, name, signature, visibility, types, and body are equivalent. Method replacement therefore yields old/new Methods that are both `!=` and not `same?`. Reflection may compare individual metadata explicitly. Users may dynamically replace Method comparison/hash behavior and assume responsibility for contracts. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C036](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V078` | none | `normative` |
| `D-110` | Class, Module, and Contract runtime objects compare equal by default only when they are the same identity. Equal qualified names, structure, source location, signatures, or members do not establish equality across distinct objects/load contexts/rebindings. Class reopen remains equal before/after because D-100 preserves the same identity. Users may dynamically replace comparison/hash behavior and assume responsibility for contracts. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V078` | none | `normative` |
| `D-111` | Ordinary instances and identity-bearing runtime/meta objects may be Hash keys and use a runtime-stable identity hash returned as `Integer` in `0..2^64-1`. The same object retains that hash throughout its lifetime and across movement by GC; distinct identities may collide. The value is not stable across processes, runtimes, or restarts and must not expose a raw memory address. Implementations may assign it lazily/randomly, and Hash tables may secretly remix it further. This identity-hash contract is distinct from D-073's cross-platform stable built-in numeric hash. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C088](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V079` | none | `normative` |
| `D-112` | Although `nil`, `true`, and `false` are identity-bearing singleton objects for `same?`, their built-in public `hash` overrides generic identity hashing and is specification-stable across process, platform, runtime, and conforming implementation within a language major version. Each is a valid Hash key and returns a `0..2^64-1` Integer; table-internal randomized mixing still applies. Exact derivation/domain and values remain separate decisions. | [IRIS-V1-RUNTIME-C006](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C145](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C146](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-113` | `nil`, `false`, and `true` hashes use BLAKE3 derive-key mode with the exact ASCII context `Iris Language v1 stable singleton hash` and one-byte inputs `[0x00]`, `[0x01]`, and `[0x02]` respectively. The standard 32-byte digest is reduced by D-077's first-eight-bytes little-endian rule. This domain is separate from numeric hashing. Formal conformance vectors must publish the resulting 64-bit values. | [IRIS-V1-RUNTIME-C145](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C146](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V073` | none | `normative` |
| `D-114` | Built-in `Bool#<=>(other: Object) -> Integer?` defines a total order within Bool: equal singleton values return `0`, `false <=> true` returns `-1`, and `true <=> false` returns `1`; a non-Bool operand returns `nil`. Therefore default derived relations make `false < true`. Bool remains distinct from numeric types: it is neither equal nor ordered with `Integer(0/1)` and retains its separate stable singleton hashes. Methods remain dynamically replaceable under the general comparison rules. | [IRIS-V1-RUNTIME-C091](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V076` | none | `normative` |
| `D-115` | Built-in `nil#<=>(other: Object) -> Integer?` returns `Integer(0)` only when `other` is the same `nil` singleton and returns `nil` for every non-nil value. Thus nil is equal/order-equivalent only to itself and is neither a global minimum nor maximum. Default derived equality/ordering follows D-090, while identity-first equality also recognizes the singleton. Methods remain dynamically replaceable. | [IRIS-V1-RUNTIME-C092](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V076` | none | `normative` |
| `D-116` | Hash key insertion, lookup, update, and deletion use the key's current dynamically dispatched `hash` and `==` methods uniformly for value and identity-bearing objects; identity keys do not secretly switch to `same?`. Users replacing either method must preserve equal-implies-same-hash and related container invariants or accept broken lookup/duplicate behavior and rebuild affected containers. Hash does not freeze historical Method objects merely because a key was inserted. Automatic version detection/rehashing remains separate. | [IRIS-V1-COLLECTIONS-C028](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V275` | none | `normative` |
| `D-117` | Existing Hash containers do not automatically detect `==`/`hash` method-version changes, track every key-class dependency, or rebuild during global mutation. The modifier/user must call `table.rehash()` or recreate the container. Rehash recomputes each current key's current dynamic hash/equality and reapplies runtime-secret table mixing. Until then, lookup behavior may be inconsistent and is the modifier's responsibility. Conflict resolution when formerly distinct keys become equal remains separate. | [IRIS-V1-COLLECTIONS-C030](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V276` | none | `normative` |
| `D-118` | `rehash()` builds and validates a temporary replacement using current key `hash`/`==`. If two previously distinct entries now belong to one equality class, it raises the ordinary catchable Iris `KeyConflictError`, publishes no partial result, and leaves the original Hash contents, buckets, key objects, values, and iteration state unchanged. It never silently chooses first/last or aggregates values. An explicit conflict-merge API remains separate. | [IRIS-V1-COLLECTIONS-C031](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V277` | none | `normative` |
| `D-119` | Supplying a trailing block to `rehash` resolves each equality-class collision. The block receives `(kept_key, kept_value, incoming_key, incoming_value)` and must return a two-element `(key, value)` result for the replacement candidate. It may preserve either key identity, create a new equivalent key, and merge values. The returned key must be equal to the collision class and obey the current hash/equality contract; shape/contract errors, new inconsistencies, or block exceptions abort atomically and leave the original Hash entirely unchanged. Successful rebuild is published once. Ordering of kept/incoming remains tied to the future Hash iteration/order decision. | [IRIS-V1-COLLECTIONS-C032](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V278` | none | `normative` |
| `D-120` | Hash exposes no insertion-order, stable-hash-order, sorted-order, or cross-run iteration guarantee. Entry traversal may vary with implementation, capacity, mutation history, runtime-secret bucket mixing, and process execution. Updating, deleting, reinserting, or rehashing may change observed order. Programs requiring deterministic order must sort/copy through an explicitly ordered structure. The collision order supplied to block-driven `rehash` remains a separate decision rather than inheriting a nonexistent public insertion order. | [IRIS-V1-COLLECTIONS-C033](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V279` | none | `normative` |
| `D-121` | `rehash` provides no deterministic order for collision resolution or for assigning `kept` versus `incoming`; it follows implementation/runtime traversal and rebuild order, which may vary with seed, buckets, capacity, mutation history, or version. Merge blocks that are not commutative/associative may therefore produce different valid outcomes, and the runtime does not attempt to prove those properties. Deterministic merging requires the user to extract entries, explicitly total-order/sort them, merge, and construct a new Hash. | [IRIS-V1-COLLECTIONS-C033](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V280` | none | `normative` |
| `D-122` | While an iterator/traversal is active, adding/removing a key, clearing, rehashing, or any mutation that changes entry structure increments a structural version; the iterator raises ordinary catchable `ConcurrentModificationError` on its next advance rather than continuing with duplicates/omissions. Updating the value associated with an already-existing key is non-structural and allowed. An entry not yet yielded reads its latest value at yield time. Mutation does not retroactively alter a value already delivered to user code; exact yielded-value capture remains to be confirmed separately. | [IRIS-V1-COLLECTIONS-C034](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V281` | none | `normative` |
| `D-123` | When traversal yields `(key, value)`, both are bound using ordinary argument semantics at that moment. Replacing the Hash slot's value afterward does not rewrite the already-bound local `value`; a later Hash lookup sees the replacement, and an entry not yet yielded observes its latest value when yielded. If the captured value is itself a mutable identity-bearing object, ordinary shared-reference mutations remain observable; that is distinct from replacing the slot. | [IRIS-V1-COLLECTIONS-C035](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V282` | none | `normative` |
| `D-124` | Hash iterators provide `remove_current()` to remove exactly the entry most recently yielded by that iterator without causing that same iterator to fail-fast. It may be called at most once after each successful yield and before the next advance; calls before the first yield, twice for one yield, or after completion raise an iterator-state error. The iterator updates its own expected structural version. Every other active iterator observes the structural change and raises `ConcurrentModificationError` on next advance. It cannot remove arbitrary or not-yet-yielded entries. How traversal blocks access the iterator remains separate. | [IRIS-V1-COLLECTIONS-C036](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V283` | none | `normative` |
| `D-125` | `Hash#each` yields exactly `(key, value)`. `Hash#each_with_iterator` yields exactly `(key, value, iterator)`, and that explicit iterator is the only block-traversal surface for `remove_current()`. No implicit/current iterator context exists. Ordinary structural mutation through the Hash during either traversal still causes fail-fast on the next advance, except the owning iterator's valid `remove_current`. Nested traversals receive distinct iterator objects. | [IRIS-V1-COLLECTIONS-C037](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V284` | none | `normative` |
| `D-126` | Invalid sequencing of iterator-specific stateful operations raises the ordinary catchable Iris `IteratorStateError`. For Hash `remove_current`, this includes invocation before a successful yield, more than once for one yielded entry, or after completion. This error is distinct from `ConcurrentModificationError`, which reports external structural-version mismatch, and from key/range errors. Return-false/nil is not used to hide invalid state. | [IRIS-V1-COLLECTIONS-C020](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V285` | none | `normative` |
| `D-127` | Explicit `iterator.next()` returns the built-in nominal `Iteration` result type. A produced element is represented by `Iteration.yield(value)`, which can carry any Iris value including `nil`; normal exhaustion returns the unique singleton `Iteration.done`. Repeated `next()` after exhaustion continues returning that same done singleton. `for`, `each`, and other traversal facilities inspect the result and terminate on `done`; they do not catch `StopIteration`, and normal completion never performs Iris exception unwinding. Actual iterator faults continue to raise ordinary exceptions, including `IteratorStateError` and `ConcurrentModificationError`. The semantic result object does not require heap allocation: interpreters/JITs may lower it to a tag plus value, registers, or direct control flow when observations remain equivalent. | [IRIS-V1-COLLECTIONS-C013](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V286` | none | `normative` |
| `D-128` | `Iteration.yield(value)` is an immutable built-in value object with no observable identity; applying `same?` raises `IdentityError`. Its default equality compares the variant and delegates payload comparison through the payload's current dynamic `==`: two yield results are equal only when their payloads compare equal. `Iteration.done` remains a unique identity-bearing singleton and is never equal to a yield result. Implementations may represent yield results without allocation. Hash behavior and public inspection APIs remain separate decisions. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V287` | none | `normative` |
| `D-129` | `Iteration` exposes get-only properties `yield?`, `done?`, and `value`. For `Iteration.yield(payload)`, `yield?` is `true`, `done?` is `false`, and `value` returns the payload including legitimate `nil`. For singleton `Iteration.done`, `yield?` is `false`, `done?` is `true`, and reading `value` raises `IteratorStateError` rather than returning a sentinel. The internal tag is not otherwise exposed. Under maximum dynamicity these getters may later be replaced; built-in traversal may bypass them only with valid method/property-version guards. | [IRIS-V1-COLLECTIONS-C014](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V288` | none | `normative` |
| `D-130` | `Iteration.done` is a valid stable-hash singleton key. `Iteration.yield(payload)` is a valid key exactly when invoking the payload's current public `hash` succeeds; its built-in hash derives from the yield variant plus that public payload hash and must give equal wrapper hashes whenever wrapper equality is true. If payload hashing raises `InvalidKeyError` or another contract failure (including NaN), wrapper hashing propagates the failure and insertion is rejected. It never falls back to wrapper identity because yield results are identity-less. Exact derivation/domain remains separate. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V289` | none | `normative` |
| `D-131` | Use BLAKE3 derive-key mode with exact ASCII context `Iris Language v1 stable iteration hash`. `Iteration.done` hashes input `[0x00]`; `Iteration.yield(payload)` hashes `[0x01] || payload_public_hash_u64_le`, where the payload's current public hash is encoded as exactly eight little-endian bytes. Digest reduction follows D-077. Payload hash failure propagates. The domain is independent from numeric and language-singleton domains and remains major-version stable. | [IRIS-V1-COLLECTIONS-C087](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C091](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V290` | none | `normative` |
| `D-132` | Built-in `Iteration#<=>(other: Object) -> Integer?` returns `0` for `done <=> done`; returns `nil` between `done` and any yield, between yield and done, or with a non-Iteration; and for `yield(a) <=> yield(b)` invokes the current dynamic `a <=> b` and returns that result. Default `==`, `!=`, and ordering then derive under D-090/D-093, so payload equality/order/unordered behavior is preserved. Users may dynamically replace Iteration comparison slots. Validation/error propagation for an invalid payload `<=>` result remains separate. | [IRIS-V1-COLLECTIONS-C015](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V291` | none | `normative` |
| `D-133` | After `yield(a) <=> yield(b)` invokes the payload's current `<=>`, the Iteration method itself accepts only exact `Integer(-1)`, `Integer(0)`, `Integer(1)`, or `nil`. Any other integer or type immediately raises `ComparisonContractError` with wrapper, payload types, delegated Method, actual result/type, and expected protocol context. It does not normalize by sign, convert to `nil`, or defer validation to an outer comparison operator. | [IRIS-V1-COLLECTIONS-C015](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V292` | none | `normative` |
| `D-134` | Explicit Iterator objects are mutable identity-bearing cursor/state-machine objects. Every iterator creation produces a new identity, including repeated `container.iterator()` calls. Default equality is identity-only; source container, current position, expected version, and current entry do not establish value equality. `same?` is valid, and two distinct iterators remain unequal even at equivalent positions. Iterator key eligibility remains separate. | [IRIS-V1-COLLECTIONS-C016](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V293` | none | `normative` |
| `D-135` | Iterator objects may be Hash keys through the ordinary runtime-stable identity hash. Advancing, exhaustion, `remove_current`, or invalidation does not change identity or hash; source container and cursor state are excluded. The value is not cross-runtime stable. Dynamic replacement of Iterator `==`/`hash` invokes the ordinary user-responsibility and explicit-rehash rules. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V294` | none | `normative` |
| `D-136` | An Iterator holds a strong reference to its source container so the container and iteration state remain alive and usable even when no other Iris reference exists. Behavior never depends on GC timing and no `CollectedSourceError` exists for ordinary iterators. This may extend the lifetime of the container and reachable elements. Whether the strong reference is released immediately on exhaustion/close or only with iterator collection remains separate. | [IRIS-V1-COLLECTIONS-C017](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V295` | none | `normative` |
| `D-137` | On the first advance that determines exhaustion and returns singleton `Iteration.done`, the Iterator enters permanent done state and immediately clears its strong source-container reference, current-entry reference, and unneeded traversal state. Later `next()` returns the same done singleton without touching the source; `remove_current()` raises `IteratorStateError`. Iterator identity/hash remain unchanged. Retaining an exhausted Iterator therefore does not retain the source object graph. Early close remains separate. | [IRIS-V1-COLLECTIONS-C017](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C018](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V296` | none | `normative` |
| `D-138` | Iterator provides idempotent `close()`. Calling it before exhaustion immediately releases source/current-entry/traversal references and enters the same permanent done state as natural exhaustion. Subsequent `next()` returns singleton `Iteration.done`; `remove_current()` raises `IteratorStateError`; repeated `close()` succeeds with no additional effect. Identity/hash remain unchanged. Traversal constructs' obligation to close on nonlocal exit remains separate. | [IRIS-V1-COLLECTIONS-C018](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V297` | none | `normative` |
| `D-139` | Language/standard traversal constructs guarantee iterator cleanup on every exit path. Natural exhaustion already releases under D-137; `break`, return/nonlocal block exit, body exception, iterator exception, cancellation, or outer unwinding invoke idempotent `close()` with finally/defer-equivalent semantics before propagating the original control flow. This applies even when `each` does not expose the iterator. The protocol is suitable for future resource-backed iterators. Conflict handling when close itself fails remains separate. | [IRIS-V1-CONTROL-C046](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V021`, `IRIS-V1-CONTROL-V037`, `IRIS-V1-CONTROL-V282` | none | `normative` |
| `D-140` | If traversal already has a primary `ExceptionContext` E and iterator `close()` raises C, E remains primary and C's `ExceptionContext` is appended to `E.suppressed`; neither is lost. If the pending exit was normal completion, `break`, return, or another non-exception control transfer, a close failure becomes the primary propagated context and prevents that control transfer from completing. Ordering is governed by D-141. | [IRIS-V1-CONTROL-C047](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V035`, `IRIS-V1-CONTROL-V283` | none | `normative` |
| `D-141` | Cleanup remains LIFO and continues after failures. If a primary `ExceptionContext` exists, each cleanup failure's `ExceptionContext` is appended to its `suppressed` list in exact occurrence order; if none exists, the first cleanup failure context becomes primary and later failures append to it. Entries are never sorted, deduplicated, or discarded and retain identity, stack, cause, and nested suppressed state. | [IRIS-V1-CONTROL-C047](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V284` | none | `normative` |
| `D-142` | Every `ExceptionContext` exposes a get-only immutable ordered collection/view of suppressed `ExceptionContext` entries. Only runtime unwinding/cleanup machinery appends; user code cannot insert, delete, replace, reorder, or fabricate entries, but may iterate/copy. Raised values themselves do not inherit or store this channel. The public getter may be dynamically replaced under D-143. | [IRIS-V1-CONTROL-C066](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C065](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V285` | none | `normative` |
| `D-143` | The public `ExceptionContext.suppressed` getter may be dynamically replaced or removed, affecting ordinary Iris property reads only. The actual runtime chain remains an unforgeable internal channel used by unwinding, debugger, uncaught formatter, host/native bridge, and reporting tools; it cannot be altered through the getter. JIT property access remains version-guarded. | [IRIS-V1-CONTROL-C065](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V286` | none | `normative` |
| `D-144` | Throwing a new exception `N` while handling active exception `E` automatically records `E` as `N`'s cause. Throw syntax provides an explicit `from C` form to replace the automatic cause and `from nil` to suppress it. Cause is a single chain distinct from the ordered suppressed-cleanup list; cleanup failures continue to follow D-140/D-141 rather than becoming cause. Public/runtime ownership of the cause property and final exception keyword remain separate decisions. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V028`, `IRIS-V1-CONTROL-V287` | none | `normative` |
| `D-145` | Revived Iris uses `raise error`, `raise error from cause`, and `raise error from nil` as the sole formal throw syntax. Legacy `groan` is intentionally removed and recorded in the migration/divergence ledger; no permanent synonym is provided. Cause semantics follow D-144. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V288` | none | `normative` |
| `D-146` | Revived Iris uses `try`, `catch`, and `finally` as the sole formal exception-handling keywords. Legacy `order`, `serve`, and `ignore` are intentionally removed and recorded in the migration/divergence ledger; no permanent aliases are provided. Detailed catch matching, ordering, binding, and finally control-flow rules remain separate decisions. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V289` | none | `normative` |
| `D-147` | A typed catch may name a nominal Class or explicit Contract. Class matching accepts that Class/subclasses; Contract matching accepts Classes with explicit nominal conformance, never structural auto-conformance. V1 catch has no arbitrary patterns, destructuring, or guards. | [IRIS-V1-CONTROL-C059](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V290` | none | `normative` |
| `D-148` | Catch clauses are tested in written order and only the first matching Class/Contract clause executes. Iris computes no cross-hierarchy “most specific” match and never executes multiple catches. Statically unreachable later clauses are compile errors. Dynamic matching uses current runtime Class/Contract state. | [IRIS-V1-CONTROL-C059](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V291` | none | `normative` |
| `D-149` | Each exception dispatch uses current Class hierarchy and explicit Contract conformance when selection begins. Catch/JIT caches depend on hierarchy/contract versions and target identity; compatible runtime implementation revisions invalidate/deopt as needed. Once entered, later mutation does not retroactively reselect. Declared conformance itself remains immutable under D-173. | [IRIS-V1-CONTROL-C059](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V292` | none | `normative` |
| `D-150` | An untyped `catch error { ... }` catches every raised object and binds it; `catch { ... }` is the equivalent catch-all without a binding. A catch-all must be the final catch clause, and any following catch is a compile-time unreachable-clause error. It does not replace or suppress `finally`; normal propagation depends on the catch body's behavior. | [IRIS-V1-CONTROL-C060](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V027`, `IRIS-V1-CONTROL-V293` | none | `normative` |
| `D-151` | A named catch creates a new immutable binding visible only within that catch body, shadowing but not modifying an outer same-named binding. It may be captured by Closures. The caught object may remain mutable. Typed catches narrow to the named Class/Contract; catch-all binding is Object under D-152. | [IRIS-V1-CONTROL-C060](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V294` | none | `normative` |
| `D-152` | `raise` accepts every Iris value/object without requiring Exception inheritance or a throwable Contract. Catch receives the original value unchanged; catch-all binding is Object. Cause, suppressed failures, and stack live in the per-propagation `ExceptionContext`, not on the raised value. Typed Class/Contract catches match the original object. Re-raise/context lifetime follow D-153 through D-163. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V295` | none | `normative` |
| `D-153` | Every explicit `raise object` creates a new propagation record containing its own stack, cause, suppressed list, source location, and state, even when the same object/value was raised before or is a singleton/immutable value. Metadata never permanently accumulates on the object. Catch still receives the original value. Only a bare re-raise may continue the active record; syntax and scope remain separate. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C056](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V296` | none | `normative` |
| `D-154` | Parameterless `raise` continues the currently handled propagation record and is valid throughout the synchronous dynamic extent of a catch, including helper calls made before that catch exits. Once the catch dynamic extent ends, the active record is unavailable; a stored closure invoked later or any unrelated call to bare `raise` raises ordinary catchable `NoActiveExceptionError`. Bare re-raise does not create a fresh record. Stack-trace presentation remains separate. | [IRIS-V1-CONTROL-C058](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V030`, `IRIS-V1-CONTROL-V031`, `IRIS-V1-CONTROL-V297` | none | `normative` |
| `D-155` | A propagation record preserves the complete original raise stack and an ordered `re_raise_sites` sequence. Each bare `raise` appends one site with source location and current callable/context metadata, without replacing the root stack or duplicating the full current stack. Multiple sites retain occurrence order. Uncaught diagnostics present the original stack followed by re-raise sites, separately from cause and suppressed chains. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V030`, `IRIS-V1-CONTROL-V298` | none | `normative` |
| `D-156` | `raise value from cause` requires `cause` to be an identity-bearing handle to an existing propagation event, or literal/value `nil` to suppress chaining. A plain arbitrary raised object is not sufficient because it does not identify which of potentially many propagation records/stacks is intended. Automatic chaining uses the current active propagation handle. Passing any other value raises/produces a type-contract error according to static knowledge. The public handle type and catch access syntax remain separate. | [IRIS-V1-CONTROL-C057](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V028`, `IRIS-V1-CONTROL-V029`, `IRIS-V1-CONTROL-V299` | none | `normative` |
| `D-157` | Each propagation event has a distinct identity-bearing, runtime-owned, read-only `ExceptionContext`, separate from the original raised object. Typed catch may bind only the object (`catch error: Type`) or both object and context (`catch error: Type, context`); catch-all analogously supports `catch error, context`. Re-raising the same object creates a distinct context. Explicit `from` accepts this context under D-156. Context-only catch syntax remains separate. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V027`, `IRIS-V1-CONTROL-V300` | none | `normative` |
| `D-158` | `_` is a language-wide discard binding/pattern: it accepts a value without creating a readable binding, may appear repeatedly in one binding/destructuring/parameter list, cannot be captured, and is a compile error when read as an expression. Catch may therefore ignore the raised object while binding context as `catch _, context`, or in typed form `catch _: Type, context`. This discard semantics applies consistently to future block parameters and destructuring. | [IRIS-V1-CONTROL-C006](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V301` | none | `normative` |
| `D-159` | `ExceptionContext` exposes get-only `value: Object`, `cause: ExceptionContext?`, `suppressed`, `original_stack`, `re_raise_sites`, and `raise_location`. They expose the original raised value, cause context, immutable ordered cleanup-failure collection, immutable original stack, immutable ordered bare-re-raise sites, and initial raise source location. Public getters remain dynamically replaceable, while runtime diagnostics/unwinding/native bridges read protected internal records. Exact element type of `suppressed` remains separate. | [IRIS-V1-CONTROL-C065](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V302` | none | `normative` |
| `D-160` | `ExceptionContext.suppressed` is an immutable ordered collection/view of `ExceptionContext` objects, one per suppressed cleanup-failure propagation event. Each entry preserves its own raised `value`, original stack, cause, re-raise sites, and nested suppressed contexts. Bare raised objects or formatted strings are not substituted. The public collection type may later be named as an immutable Array/View abstraction. | [IRIS-V1-CONTROL-C066](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V035`, `IRIS-V1-CONTROL-V303` | none | `normative` |
| `D-161` | Runtime-owned `cause` and `suppressed` edges may not create a cycle, including mixed paths across both edge kinds. Before automatic cause linkage, explicit `from context`, or suppressed append, the runtime performs an identity-based cycle check. A cycle attempt raises ordinary catchable `ExceptionChainError` and leaves the existing graph unchanged. System formatters, debuggers, and native bridges still use visited-set defenses against corrupted plugin/native/runtime data. User-visible properties cannot directly mutate the graph. | [IRIS-V1-CONTROL-C066](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V036`, `IRIS-V1-CONTROL-V304` | none | `normative` |
| `D-162` | `ExceptionContext` uses root identity-only equality and is a valid Hash key through runtime-stable identity hash. Distinct propagation events are unequal even when raised value, stack, cause, and suppressed structure are equivalent. Identity/hash remain stable throughout context lifetime and are not cross-runtime stable. This supports user-visible visited sets and graph analysis. Dynamic method replacement remains subject to ordinary comparison/hash responsibility. | [IRIS-V1-CONTROL-C067](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V305` | none | `normative` |
| `D-163` | A context retained beyond catch remains fully readable, identity/hash-stable, and valid as an explicit future `from` cause. It strongly retains its reachable propagation graph and raised values. Ending catch removes only the active dynamic record used by bare `raise`; a saved context cannot later enable bare re-raise but can be linked explicitly after acyclicity validation. | [IRIS-V1-CONTROL-C067](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V306` | none | `normative` |
| `D-164` | `return`, `break`, and `continue` are legal in `finally` and override any pending normal transfer or exception. If they discard pending exception context `E`, ordinary callers observe only the new control transfer, while runtime records `E` in a protected internal `discarded_contexts` diagnostic channel visible to debuggers/trace/reporting hooks. The exception no longer propagates normally. A new exception raised by finally is preserved through cause/suppressed rules rather than discarded; exact relationship remains separate. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V307` | none | `normative` |
| `D-165` | If pending exception context `E` exists and `finally` explicitly raises new value/context `N`, `N` becomes the new primary propagation and `E` is automatically linked as `N.cause`, unless explicit `from another_context` replaces it or `from nil` suppresses it. `E` is not placed in discarded or suppressed for this case. Cleanup failures continue to use suppressed rules. Acyclicity validation applies. Bare raise in finally remains separate. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C064](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V034`, `IRIS-V1-CONTROL-V308` | none | `normative` |
| `D-166` | Parameterless `raise` in `finally` continues the same pending propagation record, appends a re-raise site, and propagates outward when an exception is still pending. If try completed normally or a catch fully handled the exception so no pending record remains, bare raise instead raises a fresh `NoActiveExceptionError`; it never resurrects a merely recent handled context and never silently succeeds. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V309` | none | `normative` |
| `D-167` | Control order is try, then the selected first-match catch if any, then finally, then outward commitment of the resulting normal transfer/return/loop transfer/exception. An unmatched exception remains pending through finally. A catch re-raise or replacement exception becomes pending before finally. Finally always runs and may override under D-164/D-165. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V310` | none | `normative` |
| `D-168` | The construct is an expression. On normal try completion its last expression is the provisional result; when a catch handles an exception normally, that catch body's last expression is the provisional result. An empty/no-value body yields `nil`. A normally completing finally does not replace the provisional result, while finally return/break/continue/raise may override control flow under existing rules. Static result typing must merge normal-producing branches without treating finally's ordinary value as a branch result. | [IRIS-V1-CONTROL-C062](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V032`, `IRIS-V1-CONTROL-V033`, `IRIS-V1-CONTROL-V311` | none | `normative` |
| `D-169` | A finally body executes fully and evaluates its last expression for all side effects, but its ordinary value is discarded and never replaces the provisional try/catch expression result. Only nonlocal control transfer or raise from finally can override. Optimizers may remove finally expressions only under ordinary proof of no observable effect. | [IRIS-V1-CONTROL-C062](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V033`, `IRIS-V1-CONTROL-V312` | none | `normative` |
| `D-170` | When a selected catch reaches normal completion, the caught propagation is considered handled and its last-expression value becomes the provisional try result. The original exception does not continue automatically. Continuing the same record requires bare `raise`; replacing/wrapping it requires explicit `raise value` with automatic/explicit cause semantics. No truthiness or catch kind changes this rule. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C061](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V032`, `IRIS-V1-CONTROL-V313` | none | `normative` |
| `D-171` | Bare `raise` alone continues the active propagation record. Any value-bearing `raise value`, even when value is the exact caught binding/object, creates a fresh ExceptionContext with a new original stack; the old active context is automatically linked as cause unless explicit `from` overrides/suppresses it. No identity, equality, alias, or binding-origin special case converts explicit raise into re-raise. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V314` | none | `normative` |
| `D-172` | Matching performs checked narrowing once and gives the immutable catch binding that static Class/Contract type. A Class revision cannot remove or weaken declared Contract conformance, member names/signatures, typed properties, or other supporting static facts. Dynamic implementation replacement may invalidate caches but must validate before atomic commit; incompatibility raises `TypeContractError` and preserves prior implementation. | [IRIS-V1-CONTROL-C054](04-bindings-callables-control-flow.md), [IRIS-V1-TYPES-C034](05-types-contracts-generics.md) | `IRIS-V1-CONTROL-V315` | none | `normative` |
| `D-173` | For a given nominal Class declaration, the declared Contract set and every contract-visible method/property name and signature form a static spine. Metaprogramming cannot add, remove, rename, weaken, or incompatibly replace those facts in place. Dynamic methods, accessors, modules, and bodies may change only when the candidate state still satisfies the complete static spine; validation happens before atomic publication. A different static contract requires a distinct class revision/declaration created through the explicit class-evolution system rather than monkey-patching the active contract. | [IRIS-V1-TYPES-C045](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V436`, `IRIS-V1-TYPES-V200` | none | `normative` |
| `D-174` | The declared superclass is an immutable nominal subtype fact and minimum static contract for every runtime evolution. It is not an unchecked writable property. Transactional runtime-superclass changes must preserve all static subtype/Contract assumptions, instance/native-layout safety, and MRO validity, then publish atomically or fail. Revision/instance responsibility follows D-258 through D-266. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V436`, `IRIS-V1-TYPES-V201` | none | `normative` |
| `D-175` | Module composition changes only through candidate transactions. Before commit Iris recomputes MRO and verifies declared superclass/Contract requirements, typed members, visibility, and native/layout invariants. Any false/ambiguous static promise raises `TypeContractError`/MRO conflict and preserves the Class. Success atomically updates revisions and invalidates dependencies. | [IRIS-V1-TYPES-C053](05-types-contracts-generics.md), [IRIS-V1-TYPES-C045](05-types-contracts-generics.md) | `IRIS-V1-META-V436`, `IRIS-V1-TYPES-V202` | none | `normative` |
| `D-176` | Iris Class design is explicitly two-layered. The static spine contains nominal identity, declared superclass bound, declared Contracts, contract-visible member names/signatures, typed properties, and native layout obligations; compiled code and typed bindings may rely on these facts permanently for that class revision. The dynamic surface contains compatible Method bodies, non-contract members, getter/setter implementations, Module composition, class-object state, and user metadata. Every structural meta-operation builds a candidate revision/state, validates it against the static spine, and commits atomically with version invalidation; invalid candidates never partially mutate the live Class. This boundary is intended to prevent unconstrained Ruby-style monkey patching while retaining deliberate metaprogramming. | [IRIS-V1-TYPES-C053](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-META-V436`, `IRIS-V1-TYPES-V203` | none | `normative` |
| `D-177` | `class Name ... { ... }` and `module Name ... { ... }` are origin declarations, not order-dependent reopen syntax. Subsequent structural metaprogramming must use explicit `open class Name { ... }` or `open module Name { ... }`, making developer intent auditable. Each open block is one candidate-revision transaction: all body changes are staged, validated against the target's immutable static spine, and atomically published together or entirely rolled back. `contract` declarations are static and cannot be opened; `open contract` is a compile error. The `open` boundary does not create an unrestricted monkey-patch zone: incompatible contract names/signatures, declared Contract changes, or static-bound violations fail before commit. Source/link ordering relative to the origin declaration remains separate. | [IRIS-V1-TYPES-C053](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-META-V437`, `IRIS-V1-TYPES-V204` | none | `normative` |
| `D-178` | Within one compilation unit, `open class/module` may appear textually before its origin declaration because declaration collection/static-spine resolution precedes open transaction scheduling. Across modules/packages, an opener must explicitly depend on the origin-defining module; origin linking and initialization complete before the open transaction executes. Iris does not create unresolved placeholder classes or queue runtime opens waiting for future definitions. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V437`, `IRIS-V1-TYPES-V205` | none | `normative` |
| `D-179` | Declarative `open class/module` is statically resolvable sugar over `A.open`/`M.open` with a trailing block. Aliases/reflection/dynamically selected Class/Module objects use the same API. The block receives stable logical identity while structural operations target an implicit candidate. Normal completion validates/atomically commits; exception/nonlocal failure/validation error rolls back. Contract has no `open`. Candidate visibility follows D-180. | [IRIS-V1-TYPES-C053](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-META-V437`, `IRIS-V1-TYPES-V206` | none | `normative` |
| `D-180` | The open block and its synchronous dynamic call chain read their own candidate structural state after writes (read-your-writes). Code outside that transaction context and other threads continue reading the old active revision until successful atomic publication. Intermediate Method/MRO/Module/property states are never globally visible; rollback leaves no externally observed partial state. Whether ordinary instance sends inside the transaction use candidate MRO remains separate. | [IRIS-V1-TYPES-C066](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V437`, `IRIS-V1-TYPES-V207` | none | `normative` |
| `D-181` | Origin declarations define universal static spine. Compiler-resolvable declarative open may export static API extensions visible only to direct importers; failed runtime publication fails module initialization. Programmatic open never expands nominal static API; such members require reflection or `Dynamic<A>`, which checks A/subtype while permitting arbitrary ordinary sends. Conversion back to static types rechecks contracts. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V438`, `IRIS-V1-TYPES-V208` | none | `normative` |
| `D-182` | Iris supports user-defined generic Class, Contract, and Method declarations with runtime-preserved arguments/metadata. Constraints use ordinary nominal union/intersection algebra from D-184. Instantiations are invariant and dispatch remains dynamic without overload. V1 excludes specialization, SFINAE, arbitrary compile-time execution, non-type/const parameters, dependent/higher-kinded/variadic type parameters, conditional/mapped types, and type-level metaprogramming. Internal JIT specialization is unobservable. | [IRIS-V1-TYPES-C084](05-types-contracts-generics.md), [IRIS-V1-TYPES-C092](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V209` | none | `normative` |
| `D-183` | Iris uses C#-style angle brackets for generic declarations and type application, with constraints in `where` clauses: e.g. `class Box<T> where T: Object, Printable { ... }`, `fun map<T, U>(value: T) -> U where T: Serializable { ... }`, and `Box<String>`. The parser distinguishes generic brackets only in declaration/type grammar contexts; expression `<`, `>`, `<=`, `>=`, `<<`, and `>>` retain operator meanings and tokenization must support nested generic closers without changing expression shifts. Exact constraint ordering/grammar remains separate. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C054](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V210` | none | `normative` |
| `D-184` | `A & B` and `A | B` are first-class type expressions for constraints and annotations. Intersection requires all constituent Class/Contract static contracts; union requires at least one. `&` binds tighter than `|`. Types are reified, support narrowing, and do not add conditional/mapped types, structural auto-conformance, overload, or arbitrary compile-time computation. Union exposes only common safe members; intersection combines compatible guarantees. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C025](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V211` | none | `normative` |
| `D-185` | `T?` is exactly the same type as `T | Nil`, not a distinct constructor. Type identity, reflection, hashing, assignability, runtime guards, generic arguments, narrowing, and JIT semantics are identical. Duplicate Nil/members collapse. Formatting prefers `T?` when the normalized union is exactly one non-Nil type plus Nil; more complex optional-union display remains separate. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C012](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V212` | none | `normative` |
| `D-186` | Union and intersection are commutative, associative, idempotent, and flattened. Members are canonicalized in stable Type-identity order and the normalized composite Type object is runtime-interned, so reordered/grouped equivalents are the same Type identity. Nominal subtype absorption applies: if `Dog <: Animal`, `Dog | Animal` normalizes to `Animal`, while `Dog & Animal` normalizes to `Dog`. Duplicate members collapse. Distribution and impossible-type handling remain separate decisions. | [IRIS-V1-TYPES-C027](05-types-contracts-generics.md), [IRIS-V1-TYPES-C019](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V213` | none | `normative` |
| `D-187` | Canonical Type identity never expands intersections over unions or unions over intersections into DNF/CNF. Assignability, overlap, and narrowing algorithms may reason with distributive equivalence on demand without materializing/interning exponential expanded Type graphs. Representation and diagnostics remain compact and independent of thresholds/optimization. Base flatten/order/dedup/subtype absorption still applies. | [IRIS-V1-TYPES-C023](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V214` | none | `normative` |
| `D-188` | Iris has an intrinsic bottom type `Never`. When nominal/final/constraint reasoning proves an intersection has no possible value, it normalizes to the interned `Never` Type. `Never` has no normally constructible runtime values and is a subtype of every type. It may annotate functions/expressions that cannot complete normally (raise, nonterminating paths) and drives unreachable-flow analysis. Whether source permits arbitrary Never variables and its algebraic simplifications remain separate. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C010](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V215` | none | `normative` |
| `D-189` | `T | Never` normalizes to `T`; `T & Never` to `Never`; duplicate Never collapses; `Never?` is `Never | Nil` and normalizes to `Nil`. The empty union identity is `Never`. Raise/nonterminating/unreachable paths may contribute Never without widening branch results. The empty-intersection/top identity remains separate. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C027](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V216` | none | `normative` |
| `D-190` | Because every Iris value is an object, `Object` is the nominal top type and the identity of an empty intersection. Every type, including value/meta/runtime types, is a subtype of Object unless nilability semantics explicitly distinguish Nil membership. `Object` exposes only its declared static contract. `Dynamic` is not the top type and does not serve as an algebraic identity; it represents disabled static member checking subject to its optional nominal bound. Iris introduces no separate `Any` top. Whether Nil is included in non-optional Object remains separate. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C027](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V217` | none | `normative` |
| `D-191` | `Nil <: Object`; nil is a real object with Class, identity, methods, hash, and raise eligibility. `Object` therefore contains every runtime value and is the true top type. Concrete types such as `String`/`User` do not implicitly include Nil, but `T?` adds it unless already present. Normalization includes `Object | Nil -> Object`, `Object? -> Object`, and `Nil & Object -> Nil`. The nilability principle is membership-based rather than a universal non-null-reference rule. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C027](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V218` | none | `normative` |
| `D-192` | A type parameter with no explicit `where` constraint has implicit upper bound `Object`, not Dynamic and not an implicit non-null bound. `Nil` is therefore a valid type argument, and generic bodies must assume values of unconstrained `T` may be nil while using only Object's static contract. Excluding Nil requires an explicit constraint mechanism. | [IRIS-V1-TYPES-C056](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V219` | none | `normative` |
| `D-193` | Iris provides intrinsic marker Protocol/type `NonNil`, satisfied by every type/value except `Nil`; Nil can never acquire it through metaprogramming. It declares no methods and adds no runtime superclass. It is usable in ordinary type algebra and generic constraints, e.g. `where T: NonNil & Hashable`. `Object & NonNil` denotes all non-nil values, and `Nil & NonNil` normalizes to `Never`. Detailed normalization with unions/type variables remains separate. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C013](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V220` | none | `normative` |
| `D-194` | Intersecting a union with `NonNil` removes its Nil member semantically: `(A | Nil) & NonNil` becomes `A & NonNil`, then subtype absorption reduces to `A` when A is known non-nil. `Object & NonNil` remains the canonical non-nil top. For an unconstrained/unknown type variable `T`, `T & NonNil` remains explicit and is not simplified to T. This rule drives nil-check flow narrowing without introducing general type subtraction. | [IRIS-V1-TYPES-C022](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V221` | none | `normative` |
| `D-195` | A member is statically callable on `A | B | ...` only when every union branch provides that member and a call signature exists that is valid for every branch. For each argument position, callers are restricted to the intersection of accepted input domains; the result type is the normalized union of branch return types. If arity, parameter modes, visibility, effects/contracts, or types cannot produce one safe call contract, the member is unavailable until flow narrowing. Runtime method selection remains based on the actual receiver. Optional/variadic arity combination remains separate. | [IRIS-V1-TYPES-C025](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V222` | none | `normative` |
| `D-196` | For each same-named member across union branches, model each branch's accepted argument-count set (including required/optional/variadic shapes) and permit a call arity only if it belongs to every set. Static checking then computes safe parameter domains for the actual positions supplied. The common API need not be representable as one simplified declaration. Variadic acceptance never overrides another branch's finite restriction. Other parameter modes follow the same all-branches-safe principle. | [IRIS-V1-TYPES-C025](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V223` | none | `normative` |
| `D-197` | `A & B` requires one runtime Method/member implementation to satisfy every same-named contract simultaneously; Iris does not create overload sets, choose by static argument type, prefer source order, or select arbitrarily. If incompatibility is statically certain, the intersection normalizes to `Never` when the type itself is impossible or the declaration/constraint is rejected as unsatisfiable. If openness prevents proof, runtime conformance guards validate candidate classes/revisions. Exact function-signature variance rules remain separate. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C026](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V224` | none | `normative` |
| `D-198` | A Method implementation satisfies a typed Contract/intersection/base promise using standard function subtyping: parameters contravariant and return covariant, recursively through callable positions. It must accept every permitted call shape and return within the promise. This creates no overload. Dynamic replacement validates before atomic commit or raises `TypeContractError`. | [IRIS-V1-TYPES-C037](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V225` | none | `normative` |
| `D-199` | V1 has no declaration-site variance, use-site projection, or `Actual as Exposed` generic syntax. `G<A>` and `G<B>` have no subtype/assignment relation merely because `A <: B`, for generic Classes and Contracts alike. `as` cannot cross invariant arguments. Collection conversion is explicit user code; Iris does not implicitly share, copy, or convert elements. | [IRIS-V1-TYPES-C059](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V226` | none | `normative` |
| `D-200` | At a call site, omitted Method type arguments are inferred only from actual arguments' static types and an explicit/available expected result type. Inference does not inspect the Method body, runtime values, arbitrary downstream uses, or whole-program state. A call succeeds only when inference yields one unique most-specific substitution satisfying every `where` constraint; otherwise diagnostics require explicit `<...>` type arguments. Exact candidate merge rules remain separate. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C068](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V227` | none | `normative` |
| `D-201` | Multiple lower-bound candidates for one type parameter combine as their normalized union; multiple upper-bound candidates combine as their normalized intersection. The result then undergoes ordinary flatten/dedup/subtype/Never/NonNil normalization and must be a unique substitution satisfying every lower/upper bound and `where` expression. Otherwise inference fails and explicit type arguments are required. Runtime values never choose among candidates. | [IRIS-V1-TYPES-C069](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V228` | none | `normative` |
| `D-202` | A clear local expected type from an annotated assignment, return contract, argument position, or equivalent immediate context may by itself infer omitted Method type arguments, including argument-free factories (`user: User = make()` infers `T=User`). A standalone call with no constraining context does not default T to Object and must specify type arguments. Inference never propagates backward from arbitrary later uses or runtime behavior. | [IRIS-V1-TYPES-C070](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V229` | none | `normative` |
| `D-203` | When a generic Method has N type parameters, an explicit angle-bracket list must contain N positions. Each `_` requests local inference for that position; explicit positions are fixed and all constraints are solved jointly. Omitting trailing positions is an arity error, preventing ambiguity with future defaults. Failure to infer any `_` uniquely and compatibly is a compile error. `_` here is a type-argument placeholder, not a binding or fresh user-named type variable. | [IRIS-V1-TYPES-C071](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V230` | none | `normative` |
| `D-204` | Generic construction may use a full-arity type-argument list containing `_`, e.g. `Box<_>.new(value)`, inferred from constructor arguments and immediate expected result type by D-200/D-202. `_` is forbidden in persistent type positions such as variable/property/parameter/return annotations, Contract declarations, base types, stored reflection metadata, and generic constraints; those must be fully closed types. | [IRIS-V1-TYPES-C072](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V231` | none | `normative` |
| `D-205` | For `class Box<T>`, bare `Box` denotes the identity-bearing generic Class definition/meta object and may be reflected/opened, but is not an instance type or raw generic. Instance annotations/construction require a fully constructed `Box<Type>` or construction-site `Box<_>`. `Box` never defaults to `Box<Object>` or Dynamic, and `Box.new` is a missing-type-argument compile error. Closed-construction identity remains separate. | [IRIS-V1-TYPES-C061](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V232` | none | `normative` |
| `D-206` | Within a runtime, a closed generic Class/Type is uniquely interned by `(generic definition identity, normalized ordered type-argument Type identities)`. Repeated `Box<String>` references are the same Class/Type identity and satisfy `same?`; differing definition or any differing argument yields a different closed identity. Interning is runtime-wide rather than per module and supports stable reflection/contracts/JIT caches. Evolution of the generic definition and closed revisions remains separate. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C062](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V233` | none | `normative` |
| `D-207` | Opening a generic definition builds a candidate definition plus substituted candidate revisions for every already-interned closed construction. All static spines, constraints, Contracts, MRO/modules, and native/layout obligations are validated as one transaction; any closed failure rolls back everything. Success atomically publishes definition and closed-Class active revisions while preserving their stable identities. Existing instances remain instances of their logical closed Class and subsequent ordinary sends use the newly active revision under D-258; already-entered frames and retained Method/BoundMethod objects continue captured Method identities. Future closed constructions instantiate from the new definition revision. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C063](05-types-contracts-generics.md) | `IRIS-V1-META-V439`, `IRIS-V1-TYPES-V234` | none | `normative` |
| `D-208` | `open class Box<String>` and programmatic `Box<String>.open` are compile/runtime meta-operation errors in v1. All semantic/API revisions target the unapplied generic definition `Box<T>` and propagate through D-207. Iris has no source/runtime partial or explicit generic specialization. JIT/runtime may internally specialize machine code/layout for concrete type arguments only when unobservable and contract-equivalent. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-META-V439`, `IRIS-V1-TYPES-V235` | none | `normative` |
| `D-209` | A class-level property declared on `Generic<T>` is instantiated separately for each interned closed Class such as `Generic<String>` and `Generic<User>`. Values and typed contracts substitute T independently and mutations do not cross closed constructions. The unapplied generic definition does not itself own these per-closed values. Definition-wide shared state requires a distinct explicit feature. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C064](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V236` | none | `normative` |
| `D-210` | Generic definitions may declare `shared class property name: Type`. One storage slot belongs to the generic definition and is shared by every closed construction. Its declared type and initializer may not reference the definition's type parameters directly or indirectly, because no single type argument applies to shared storage. Ordinary `class property` remains per-closed under D-209. Access paths and initialization timing remain separate. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C064](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V237` | none | `normative` |
| `D-211` | A definition-wide shared property is read/written only through the unapplied generic definition meta object (e.g. `Cache.count`). Closed Classes such as `Cache<String>` do not inherit/forward that accessor and `Cache<String>.count` does not resolve to the shared slot. Closed Classes may have their own per-closed ordinary class properties, but cannot ambiguously shadow a shared definition property through the same access path. Reflection marks definition ownership/shared storage. | [IRIS-V1-TYPES-C065](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V238` | none | `normative` |
| `D-212` | Shared property initializers run exactly once during the origin module's initialization, in source declaration order. They do not wait for first access, closed construction, or instance creation. Failure aborts the module initialization transaction and publishes neither partial shared state nor partial declarations; other threads cannot observe half-initialized values. Later generic opens do not rerun initializers, though explicit transactional assignments remain ordinary state changes. | [IRIS-V1-TYPES-C066](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V239` | none | `normative` |
| `D-213` | The first request that materializes a closed Class executes its ordinary per-closed class-property initializers exactly once in source order inside the closed-Class creation transaction. Concurrent requests for the same normalized construction wait for/share that transaction. Success interns/publishes the closed Class and state atomically. Failure publishes/interns nothing; later requests may retry initialization. Side-effect rollback on failed attempts remains separate. | [IRIS-V1-TYPES-C066](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V240` | none | `normative` |
| `D-214` | Failure discards candidate Class/property/method/MRO/contract/interning/JIT-cache state and publishes nothing. It does not automatically undo filesystem/network/database/native/logging effects or mutations to already-published objects. Initializers must use cleanup/compensation to be retry-safe; later materialization retries rerun them and the author owns duplicate external side effects. | [IRIS-V1-TYPES-C066](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V241` | none | `normative` |
| `D-215` | Closed generic materialization semantically validates every normalized `where` constraint before interning/publishing. Compiler/JIT may eliminate a physical guard only with proof based on still-valid immutable static spines/contracts. Reflection, Dynamic, plugin, deserialization, and native construction paths perform runtime validation. Failure raises `TypeContractError`, publishes/interns no closed Class, and follows D-214 rollback. | [IRIS-V1-TYPES-C067](05-types-contracts-generics.md), [IRIS-V1-TYPES-C007](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V242` | none | `normative` |
| `D-216` | A generic declaration's `where` expressions may reference any type parameter from that same declaration, including relations such as `where U: T & Serializable`; declaration order does not restrict references. The compiler/runtime jointly solve the dependency graph under union/intersection normalization and reified validation. Unsatisfiable or non-uniquely resolvable cyclic relations are compile-time declaration errors. Self/F-bounded constraints remain separate. | [IRIS-V1-TYPES-C057](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V243` | none | `normative` |
| `D-217` | V1 permits nominal self bounds such as `where T: Comparable<T>`. A concrete argument satisfies them only through explicit Class/Contract conformance after substitution; matching shape is insufficient. Higher-kinded/arbitrary recursive type functions and fixed-point programs are excluded; invalid recursive constraints are rejected and materialization revalidates. | [IRIS-V1-TYPES-C058](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V244` | none | `normative` |
| `D-218` | Every generic Class/Contract/Method application has fixed full arity and no declaration defaults. Closed annotations supply all arguments; calls may use full-position `_` inference. Missing trailing arguments never mean Object/default. Future defaults require major-version compatibility rules. | [IRIS-V1-TYPES-C060](05-types-contracts-generics.md), [IRIS-V1-TYPES-C074](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V245` | none | `normative` |
| `D-219` | Modules may declare type parameters/where constraints and be applied/mixed as closed Modules such as `ComparableHelpers<User>`. Generic Module definitions and closed constructions are runtime-reified/interned and follow generic constraint/materialization rules. Modules have no instances or per-instance layout. Opens target only the unapplied generic Module definition and atomically propagate to all interned closed Modules/classes using them; a closed Module cannot be opened independently. Exact binding of Module parameters to host Class types remains separate. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V246` | none | `normative` |
| `D-220` | Generic Module type arguments are supplied explicitly at the mix/include site and are not inferred from the host Class. Intrinsic `Self` denotes the eventual receiver/host and may appear in Module `where` constraints. Composition validates those constraints transactionally. The same closed Module may be composed into multiple compatible Classes. First-version Module composition does not support `_` inference for its type arguments. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C073](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V247` | none | `normative` |
| `D-221` | Declarative `mixin` in Class/typed-open headers contributes static API and may implement declared Contracts. Programmatic `include`/removal in construction/open transactions affects runtime dynamic surface unless separately declared. Both share candidate MRO, generic Module/Self validation, atomicity, reflection, and invalidation, and cannot violate static spine. | [IRIS-V1-META-C053](08-modules-metaprogramming.md), [IRIS-V1-META-C054](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V340` | none | `normative` |
| `D-222` | Origin bodies execute ordinary control flow/metaprogramming in a candidate; unqualified include targets current Class/Module. Header mixin is static, conditional include dynamic-only. Success plus full MRO/static-Contract validation publishes atomically; failure publishes nothing. Every branch that commits must satisfy declared Contracts. | [IRIS-V1-META-C022](08-modules-metaprogramming.md), [IRIS-V1-META-C034](08-modules-metaprogramming.md), [IRIS-V1-META-C042](08-modules-metaprogramming.md) | `IRIS-V1-META-V341` | none | `normative` |
| `D-223` | During origin/open body execution, `self` is the stable logical Class/Module object while structural messages route to the transaction candidate. Unqualified calls such as `include(D)` are equivalent to `self.include(D)` after lexical locals/parameters/captures and constants/type names fail to resolve a callable. Instance methods enter candidate instance Method tables only through explicit declaration syntax; ordinary body calls do not implicitly define them. | [IRIS-V1-META-C023](08-modules-metaprogramming.md), [IRIS-V1-META-C024](08-modules-metaprogramming.md) | `IRIS-V1-META-V342` | none | `normative` |
| `D-224` | A `fun`/method declaration inside origin/open Class/Module body does not close over lexical locals created by that executable body. Its lexical environment contains ordinary definition/module scope, type parameters, `Self`/owner metadata, and method parameters, not transaction-temporary locals. Shared runtime state must be explicit class/shared properties. Code intentionally requiring capture must create a Closure and install/use it through explicit metaprogramming APIs, making lifetime/state visible. | [IRIS-V1-META-C026](08-modules-metaprogramming.md) | `IRIS-V1-META-V342`, `IRIS-V1-META-V343` | none | `normative` |
| `D-225` | Each origin/open execution has an independent lexical local environment. Nested closures may capture locals normally and escaping closures may extend their lifetime. Helpers see them only via ordinary arguments/capture, not transaction membership. Locals do not persist across opens or become class properties. This does not change D-224: declared Methods do not automatically capture them. | [IRIS-V1-META-C027](08-modules-metaprogramming.md) | `IRIS-V1-META-V344` | none | `normative` |
| `D-226` | Members/composition declarations executed inside `if`, loops, blocks, or other runtime control flow in an origin/declarative-open body affect only the candidate runtime dynamic surface when that path executes; they never enter compiler-visible static API. Static API collection includes only header composition and body-top-level unconditional declarations. Thus statically typed receivers need `Dynamic<A>`/reflection for conditional members, and a branch that does not execute creates no such member. | [IRIS-V1-META-C028](08-modules-metaprogramming.md), [IRIS-V1-META-C046](08-modules-metaprogramming.md) | `IRIS-V1-META-V345` | none | `normative` |
| `D-227` | Top-level unconditional members from ordinary `open class/module` extend static API only inside the defining module. Cross-module static visibility requires `export open ...`, and a consumer must directly import that extension module; transitive imports do not merge it. Conditional/dynamic body effects are never exported. Runtime transaction execution/initialization must succeed before a module exporting its extension is considered initialized. | [IRIS-V1-META-C045](08-modules-metaprogramming.md), [IRIS-V1-META-C016](08-modules-metaprogramming.md) | `IRIS-V1-META-V346` | none | `normative` |
| `D-228` | When multiple directly imported declarative extensions add the same member to one Class/Module, a later-loaded extension replaces the earlier implementation only when its complete static signature/visibility/contract is compatible with the already merged member under D-198. No overload set is formed. Incompatible declarations fail compilation/linking or module initialization before publication. Static callers retain one stable contract while runtime dispatch uses the last successfully loaded compatible Method revision. The normative module load order remains separate and cannot be implementation-dependent. | [IRIS-V1-META-C048](08-modules-metaprogramming.md), [IRIS-V1-META-C052](08-modules-metaprogramming.md) | `IRIS-V1-META-V347` | none | `normative` |
| `D-229` | A consumer applies exported extension modules in the written order of its direct imports after each dependency initializes; later compatible extensions win. Within one module, open blocks execute in source order and later compatible definitions win. Filesystem enumeration, concurrent scheduling, module-name sorting, or arbitrary topological choice never determines behavior. Import cycles that prevent a unique initialization sequence are module-initialization errors. | [IRIS-V1-META-C019](08-modules-metaprogramming.md), [IRIS-V1-META-C048](08-modules-metaprogramming.md) | `IRIS-V1-META-V348` | none | `normative` |
| `D-230` | A direct import that would replace any already-merged extension member must be explicitly marked `override` at the consumer import site; otherwise compilation fails even when signatures are compatible. The mark authorizes compatible replacements contributed by that imported extension under source-order semantics, but never authorizes signature/static-contract incompatibility. This prevents ordinary import insertion/reordering from silently changing behavior. Exact syntax placement may be standardized later. Same-module open override marking remains separate. | [IRIS-V1-META-C049](08-modules-metaprogramming.md) | `IRIS-V1-META-V349` | none | `normative` |
| `D-231` | Any declaration in an open/origin executable body that replaces an already-resolved member must carry member-level `override`; omission is a compile-time error for statically known declarations and transaction failure for dynamic paths. `override` on a name with no target is also an error. It never permits incompatible static signatures/contracts. This applies to instance/class methods, getters, setters, and other replaceable member kinds, including conditional dynamic replacements. Import-site override authorization and member-level override intent are both required where applicable. | [IRIS-V1-META-C050](08-modules-metaprogramming.md) | `IRIS-V1-META-V350` | none | `normative` |
| `D-232` | Replacing a member resolved from superclass or Module requires member-level `override`; missing target/incompatible signature are errors. Contract implementation marking follows D-233. | [IRIS-V1-META-C050](08-modules-metaprogramming.md), [IRIS-V1-TYPES-C046](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V351` | none | `normative` |
| `D-233` | A member intended to satisfy a declared Contract requirement must use the explicit lowercase keyword `impl`. The compiler rejects `impl` when no declared contract member is satisfied and rejects an unmarked Class-provided implementation where an explicit implementation is required. If the same declaration also replaces an inherited/Module Method, both modifiers are written (`override impl fun ...`). Iris intentionally uses the concise high-frequency declaration pair `fun` + `impl`. Compatibility follows D-198. | [IRIS-V1-TYPES-C046](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V440`, `IRIS-V1-TYPES-V018`, `IRIS-V1-TYPES-V248` | none | `normative` |
| `D-234` | A single `impl` member is automatically associated with every declared Contract requirement of the same member name that its one dynamic Method signature compatibly satisfies. It need not list targets. Incompatible same-name Contracts cannot be implemented by multiple overloads or by implements-order preference under the ordinary model; they require the explicit qualified-Contract mechanism. | [IRIS-V1-TYPES-C047](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V440`, `IRIS-V1-TYPES-V018`, `IRIS-V1-TYPES-V249` | none | `normative` |
| `D-235` | Ordinary message identity is the selector name (plus its ordinary call shape), and the runtime receiver Class/revision/MRO chooses the concrete Method. Static annotations, inferred types, union branches, generic arguments, and expected return types may validate the message contract and optimize guards but never silently choose a different Method implementation/selector. To resolve incompatible same-name Contract members, Iris provides an explicit qualified Contract selector whose identity contains the Contract slot; only explicit source syntax may choose it. Even then, the receiver's runtime Class dynamically supplies the implementation for that qualified slot. | [IRIS-V1-TYPES-C040](05-types-contracts-generics.md), [IRIS-V1-RUNTIME-C023](03-runtime-object-model.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-RUNTIME-V080` | none | `normative` |
| `D-236` | An explicit qualified call is written `(value as ContractType)..member(args...)`. `as ContractType` performs/proves the checked nominal Contract view; the dedicated double-dot token `..member` explicitly chooses that view's Contract slot. A qualified implementation is declared `impl ContractType::member(...) ...` (with `override` as additionally required), keeping `::` for declaration/name qualification rather than view-call dispatch. Ordinary `value.member(...)` always sends the unqualified selector and is never redirected by static Contract annotations or expected types. Qualified calls still dynamically select the receiver Class revision's implementation of that slot. Double-dot remains lexically distinct from range syntax. | [IRIS-V1-TYPES-C049](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md), [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md) | `IRIS-V1-TYPES-V018` | none | `normative` |
| `D-237` | A checked `value as Contract` produces a storable/passable/capturable Contract view carrying the receiver plus Contract identity/conformance. It may be represented without allocation. Ordinary `view.member()` remains an unqualified message forwarded to the underlying receiver; qualified invocation must explicitly use `view..member()`. Static/view type never silently changes selector. View identity/equality/hash follow D-239-D-241 under the renamed concept. | [IRIS-V1-TYPES-C032](05-types-contracts-generics.md), [IRIS-V1-TYPES-C049](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V018`, `IRIS-V1-TYPES-V250` | none | `normative` |
| `D-238` | `view.member()` searches only the underlying receiver's unqualified `member` selector. It never searches, falls back to, or reports ambiguity among Contract-qualified slots; absence raises ordinary `MessageNotFoundError`. `view..member()` searches only the view's Contract-qualified slot. Thus unqualified `member`, `Parser::member`, and `Validator::member` are distinct selector identities. | [IRIS-V1-TYPES-C049](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md), [IRIS-V1-RUNTIME-C022](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V081` | none | `normative` |
| `D-239` | A Contract view is the immutable pair of an underlying receiver and Contract identity/conformance, and `same?` is invalid because the view itself has no independent object identity. Repeated checked view construction may be allocation-free and produces equal capability values when receiver relation and Contract identity match. For identity-bearing receivers, built-in view equality requires the same receiver identity plus the same Contract identity. | [IRIS-V1-TYPES-C050](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V018`, `IRIS-V1-TYPES-V251` | none | `normative` |
| `D-240` | When a view's receiver is an identity-less value object such as `Integer`/`Float`, built-in view equality requires receiver values equal under their current equality plus the same Contract identity. Its hash combines the receiver's current public hash with Contract identity and preserves equal-implies-same-hash. If receiver hashing is invalid (e.g. NaN), the view is likewise invalid as a Hash key. Identity-bearing receiver views continue using receiver identity semantics. | [IRIS-V1-TYPES-C050](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V252` | none | `normative` |
| `D-241` | Contract-view public hash uses BLAKE3 derive-key mode with exact ASCII context `Iris Language v1 contract view hash` and input `receiver_public_hash_u64_le || contract_type_hash_u64_le`. Digest reduction follows the first-eight-bytes little-endian rule. Hash stability inherits its components: stable value receivers plus stable named Contract Type identity produce cross-runtime-stable view hashes, while identity receivers remain runtime-local. Receiver hash failure propagates. | [IRIS-V1-TYPES-C051](05-types-contracts-generics.md), [IRIS-V1-RUNTIME-C147](03-runtime-object-model.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V262` | none | `normative` |
| `D-242` | A statically named Contract Type's public hash is specification-stable within a language major and derives from canonical package identity, fully qualified Contract name, and major-version contract identity—not structural member shape or runtime allocation. Distinct nominal Contracts remain distinct even with identical declarations. Renaming/moving package identity changes Type identity/hash. Future anonymous/dynamic Contracts, if allowed, use runtime identity semantics. | [IRIS-V1-TYPES-C052](05-types-contracts-generics.md), [IRIS-V1-META-C004](08-modules-metaprogramming.md), [IRIS-V1-TYPES-C077](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V260` | none | `normative` |
| `D-243` | Publishable packages declare a globally unique reverse-domain-style `package_id` and `api_major`. Named nominal Type identity includes package ID, package API major, fully qualified name, Iris language major, type kind, generic arity, and closed argument identities as applicable. It excludes filesystem path, display name, source hash, and machine/build data. Manifestless local scripts receive runtime-local package identity: Types remain stable only within that runtime, have no cross-process stable public Type hash, and cannot define publishable/stable serialization contracts. Minor/patch compatibility remains separate. | [IRIS-V1-META-C003](08-modules-metaprogramming.md), [IRIS-V1-META-C004](08-modules-metaprogramming.md), [IRIS-V1-META-C010](08-modules-metaprogramming.md) | `IRIS-V1-META-V352` | none | `normative` |
| `D-244` | Releases sharing `(package_id, api_major)` define the same nominal Type identities. Breaking superclass/Contract/member/generic/native changes require a new API major. Stable hashes, plugins, and serialization rely on this; side-by-side behavior follows D-245. | [IRIS-V1-META-C004](08-modules-metaprogramming.md), [IRIS-V1-META-C005](08-modules-metaprogramming.md), [IRIS-V1-META-C010](08-modules-metaprogramming.md) | `IRIS-V1-META-V352` | none | `normative` |
| `D-245` | A runtime activates at most one package implementation revision for each `(package_id, api_major)`. Dependency resolution/linking selects one minor/patch satisfying all constraints before initialization; failure to unify aborts linking/startup. Same-major versions do not coexist with shared identity, override by load order, or gain implicit load-context identities. Coexisting incompatible implementations require different API majors and nominal identities. Hot upgrade remains separate. | [IRIS-V1-META-C005](08-modules-metaprogramming.md), [IRIS-V1-META-C010](08-modules-metaprogramming.md) | `IRIS-V1-META-V352` | none | `normative` |
| `D-246` | `Package.upgrade(...)` may replace the one active minor/patch implementation with a compatible newer revision of the same `(package_id, api_major)`. The runtime builds candidates for all affected Modules, generic definitions/closed constructions, Classes, methods, properties, native ABI obligations, and extension effects; validates static identities/contracts and initialization as one transaction; then atomically switches all active revisions and invalidates/deoptimizes dependencies. Any failure leaves the old package fully active. Existing instances retain their logical Class identities and begin using the newly active revisions for subsequent sends after commit; already-entered frames and retained Method/BoundMethod objects continue their captured Method identities. Iris does not automatically reinitialize/migrate existing instance state. Old-revision reclamation remains separate. | [IRIS-V1-META-C068](08-modules-metaprogramming.md), [IRIS-V1-META-C070](08-modules-metaprogramming.md) | `IRIS-V1-META-V353` | none | `normative` |
| `D-247` | A superseded package revision remains loaded while reachable from retained Methods/BoundMethods, Closures/code, active interpreter/JIT/construction frames, native handles/pins, revision reflection objects, JIT dependencies, debugger/profiler pins, or other artifacts explicitly tied to old code/metadata. Ordinary instances do not by themselves pin the old dispatch revision merely because they were created before upgrade; they follow their logical Class active revision. Only after all old-revision captures/execution reach zero may GC/loader reclaim metadata/code/caches and unload native libraries. | [IRIS-V1-META-C062](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C019](03-runtime-object-model.md) | `IRIS-V1-META-V353` | none | `normative` |
| `D-248` | A same-major package upgrade reuses existing module/class/shared/per-closed storage whose static contracts remain compatible and does not rerun their ordinary initializers. Newly introduced storage runs its new initializer in the candidate transaction. A package may define an explicit `upgrade(from_version, state/context)` migration hook to transform/validate candidate state. All runtime-owned candidate changes publish atomically with the package revision; hook/validation failure leaves the old package and state active. External side-effect responsibility remains separate. | [IRIS-V1-META-C069](08-modules-metaprogramming.md), [IRIS-V1-META-C068](08-modules-metaprogramming.md) | `IRIS-V1-META-V354` | none | `normative` |
| `D-249` | Runtime rollback covers only candidate package/module/Class/state/interning/MRO/contract/JIT/native-binding publication. Filesystem, database, network, external-process, native-internal, and other external effects are not automatically reversed. Migration hooks must use cleanup/compensation and should be idempotent/retry-safe. Failure propagates a normal ExceptionContext, leaves the old package active, and discards the candidate; uncompensated external effects remain the migration author's responsibility. | [IRIS-V1-META-C042](08-modules-metaprogramming.md), [IRIS-V1-META-C070](08-modules-metaprogramming.md) | `IRIS-V1-META-V354` | none | `normative` |
| `D-250` | After candidate validation/migration, commit brings runtime threads to safepoints, atomically publishes all package/module/Class active revisions and state plus dispatch/JIT dependency invalidation, then resumes execution. Threads never observe a partial package mix. Method frames entered before commit continue their captured old bodies/revisions; sends/lookups after resume use the newly active revisions. Commit does not wait for all old frames to finish; old revision reclamation follows D-247. | [IRIS-V1-META-C041](08-modules-metaprogramming.md), [IRIS-V1-META-C068](08-modules-metaprogramming.md) | `IRIS-V1-META-V353` | none | `normative` |
| `D-251` | Class/Module structural changes (member/property shape, Method add/remove/replace, Module composition, runtime-superclass/MRO revision, contract implementation, generic revision propagation, native structural metadata) validate off-line then commit atomically at a global safepoint with cache/JIT invalidation/deoptimization. Old entered frames continue old bodies; post-resume sends use new active revisions. Updating the value of an already-existing class/shared property is ordinary state mutation and does not itself require a structural safepoint, subject to future concurrency rules. | [IRIS-V1-META-C041](08-modules-metaprogramming.md), [IRIS-V1-META-C043](08-modules-metaprogramming.md) | `IRIS-V1-META-V355` | none | `normative` |
| `D-252` | Origin/open transaction bodies execute to completion on one thread and may not `await`, scheduler-yield with resumable transaction state, transfer/resume on another thread, share candidate mutation with child threads, or let candidate/transaction capability escape. They may call ordinary synchronous user/native code and perform IO, but rollback covers only runtime candidate state and external effects remain user responsibility. Violation is a compile error when statically known or `MetaTransactionError` dynamically. Nested transaction behavior remains separate. | [IRIS-V1-META-C037](08-modules-metaprogramming.md), [IRIS-V1-ASYNC-C018](07-async-resources-diagnostics.md) | `IRIS-V1-META-V355` | none | `normative` |
| `D-253` | Same-thread nested `open` calls join the outermost transaction group; inner opens never commit independently. All target candidates and cross-target MRO/contracts validate together and publish at one global safepoint, or all roll back. Reopening a target already in the group reuses its one candidate. The group remains synchronous, thread-confined, non-suspending, and non-escaping. Concurrent transaction conflicts remain separate. | [IRIS-V1-META-C038](08-modules-metaprogramming.md), [IRIS-V1-META-C043](08-modules-metaprogramming.md) | `IRIS-V1-META-V355` | none | `normative` |
| `D-254` | Candidate construction does not hold long-lived exclusive Class/Module locks. Each target records its base active revision. At commit, the group compares every base; the first valid committer may publish, while any group with an overlapping changed target raises ordinary catchable `MetaTransactionConflictError` and rolls back all candidates. Runtime never auto-merges, rebases, retries, or re-executes the block, avoiding duplicated external effects. User-directed retry remains separate. | [IRIS-V1-META-C039](08-modules-metaprogramming.md), [IRIS-V1-META-C043](08-modules-metaprogramming.md) | `IRIS-V1-META-V355` | none | `normative` |
| `D-255` | Iris/runtime provides no `open_retry`, automatic rebase/merge, or hidden block re-execution after `MetaTransactionConflictError`. User code may explicitly catch conflicts and choose compensation, backoff, retry count, and rerun, accepting responsibility for duplicated external effects and idempotency. | [IRIS-V1-META-C040](08-modules-metaprogramming.md), [IRIS-V1-META-C021](08-modules-metaprogramming.md) | `IRIS-V1-META-V355` | none | `normative` |
| `D-256` | Transaction context does not redirect instance dispatch to an unpublished candidate. Existing instances continue through their logical Class's currently published active revision for ordinary sends inside and outside the open block; candidate members are visible only through transaction-aware Class/Module meta/reflection reads. After atomic commit, subsequent sends from all existing instances use the new active revision. This preserves isolation and prevents helper call chains from seeing uncommitted dispatch. | [IRIS-V1-META-C036](08-modules-metaprogramming.md), [IRIS-V1-META-C043](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C009](03-runtime-object-model.md) | `IRIS-V1-META-V355` | none | `normative` |
| `D-257` | Open transactions expose candidate metadata/contracts/MRO/module/layout descriptors through meta/reflection only. They provide no wrapper or operation that dispatches candidate Methods/MRO against an existing instance, no automatic clone/migration, and no purity-based exception. Ordinary instances remain on published revisions. Candidate test-instance semantics are deferred. | [IRIS-V1-META-C036](08-modules-metaprogramming.md), [IRIS-V1-META-C043](08-modules-metaprogramming.md) | `IRIS-V1-META-V355` | none | `normative` |
| `D-258` | Every instance references stable logical Class. Ordinary sends, qualified Contract implementation lookup, MRO, and ancestry tests use current active revision. Open/upgrade affects existing/future instances after commit; entered frames and retained Methods continue captured identities. Instance state is not automatically migrated. | [IRIS-V1-RUNTIME-C009](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C011](03-runtime-object-model.md), [IRIS-V1-META-C043](08-modules-metaprogramming.md) | `IRIS-V1-META-V432`, `IRIS-V1-RUNTIME-V082` | none | `normative` |
| `D-259` | `A.new(...)` captures A's active revision at construction start so allocation and initialization never mix revisions mid-construction. If a new revision commits before completion, construction continues using the captured code/layout without retry. On success, the instance references logical Class A; subsequent ordinary sends use A's current active revision. Constructor side effects are never automatically repeated or compensated. | [IRIS-V1-RUNTIME-C056](03-runtime-object-model.md), [IRIS-V1-META-C043](08-modules-metaprogramming.md) | `IRIS-V1-META-V432`, `IRIS-V1-RUNTIME-V083` | none | `normative` |
| `D-260` | Iris provides no `new_current`, `new_checked`, constructor retry, revision lock, automatic post-construction reinitialization, or instance state migration. Applications coordinate construction versus structural metaprogramming when required. If the current active behavior expects state absent from an existing/newly completed instance, responsibility belongs to the metaprogrammer under D-263. | [IRIS-V1-RUNTIME-C059](03-runtime-object-model.md), [IRIS-V1-META-C043](08-modules-metaprogramming.md) | `IRIS-V1-META-V432`, `IRIS-V1-RUNTIME-V084` | none | `normative` |
| `D-261` | Every instance and revision of logical Class A has nominal Type A. `is A`, typed boundaries, generics, Contract/static-spine promises, and Type metadata treat revisions identically. Revision identity is explicit reflection only; runtime ancestry follows D-262. | [IRIS-V1-RUNTIME-C009](03-runtime-object-model.md), [IRIS-V1-TYPES-C045](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V432`, `IRIS-V1-RUNTIME-V082` | none | `normative` |
| `D-262` | `is` and runtime checked ancestry tests inspect the active revision current at the test. After a superclass/MRO evolution commit, existing and future instances observe the new runtime ancestry for subsequent tests/sends. The immutable declared superclass bound remains the permanent compile-time ancestry promise; stricter runtime ancestors require a current `is` check/narrowing. Already-running frames are unaffected until later operations. | [IRIS-V1-RUNTIME-C009](03-runtime-object-model.md), [IRIS-V1-TYPES-C028](05-types-contracts-generics.md), [IRIS-V1-TYPES-C079](05-types-contracts-generics.md) | `IRIS-V1-META-V432`, `IRIS-V1-RUNTIME-V100` | none | `normative` |
| `D-263` | Iris guarantees static-spine Contracts, superclass bounds, MRO acyclicity, atomicity, memory/GC/native-layout safety, and invalidation. It does not rerun constructors/migrate fields/resources/scan instances/restore business invariants. Existing instances follow new behavior with old state; users own reinitialization. Memory/GC/ABI-unsafe candidates are always rejected. | [IRIS-V1-META-C002](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C157](03-runtime-object-model.md) | `IRIS-V1-META-V356` | none | `normative` |
| `D-264` | V1 defines a conventional instance Method `migrate_revision(from, to)` that applications may implement and explicitly invoke on objects they themselves track after structural evolution. Runtime/open commit never enumerates live instances, never invokes this Method automatically, and provides no Class-wide live-instance enumeration. Applications choose object sets, timing, synchronization, retries, and compensation. Exact parameter types/selector protection remain separate. | [IRIS-V1-RUNTIME-C060](03-runtime-object-model.md), [IRIS-V1-IDENTITY-C115](01-language-identity.md), [IRIS-V1-IDENTITY-C116](01-language-identity.md) | `IRIS-V1-IDENTITY-V014`, `IRIS-V1-META-V433`, `IRIS-V1-RUNTIME-V085` | none | `normative` |
| `D-265` | The convention is an ordinary unqualified replaceable Method, no privileged path or special Contract, and never implicit. Applications own results. Parameters follow D-266. | [IRIS-V1-RUNTIME-C060](03-runtime-object-model.md), [IRIS-V1-META-C024](08-modules-metaprogramming.md) | `IRIS-V1-META-V433`, `IRIS-V1-RUNTIME-V085` | none | `normative` |
| `D-266` | Conventional signature is `migrate_revision(from: ClassRevision, to: ClassRevision) -> Nil`. `ClassRevision` is identity-bearing, runtime-owned/read-only, reflectable, `same?`-comparable, and usable as an identity Hash key. It exposes owner Class, revision number, static-spine reference, runtime superclass/MRO/modules, member/property metadata, layout/shape descriptor, and source/package revision metadata. Users cannot mutate/reactivate it. Holding one pins its metadata/code. Success returns Nil; failure raises ordinary exceptions. Revision numbering remains separate. | [IRIS-V1-RUNTIME-C016](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C060](03-runtime-object-model.md), [IRIS-V1-META-C097](08-modules-metaprogramming.md) | `IRIS-V1-META-V433`, `IRIS-V1-RUNTIME-V085` | none | `normative` |
| `D-267` | Each stable logical Class numbers revisions independently: origin revision is `1`; each successful structural publication affecting that Class receives the next integer; failed/rolled-back candidates consume no number. Numbers from different Classes have no ordering/equality meaning by themselves; use owner plus number or `same?`. Numbers are diagnostic/history metadata, not cross-runtime stable Type identity. Multi-Class group commit correlation remains separate. | [IRIS-V1-META-C061](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C017](03-runtime-object-model.md) | `IRIS-V1-META-V353` | none | `normative` |
| `D-268` | Each successfully published structural transaction group receives one runtime-wide strictly increasing `commit_id`, shared by all Class/Module revisions in that group. Failed/rolled-back groups receive/consume none. Per-Class `revision_number` remains independent. `commit_id` is runtime-local diagnostic/audit ordering metadata, not cross-process stable and not part of nominal Type identity. Revision metadata may additionally record package/source/initiator information. | [IRIS-V1-META-C061](08-modules-metaprogramming.md), [IRIS-V1-ASYNC-C046](07-async-resources-diagnostics.md) | `IRIS-V1-META-V355` | none | `normative` |
| `D-269` | Active revisions and old revisions pinned by Method/frame/native/tool/user `ClassRevision` references retain full executable metadata/code. Once unreferenced and inactive, heavy revision state may be reclaimed/unloaded. Reflection history retains a lightweight non-executable audit record containing owner, revision number, commit ID, package/source/initiator, timestamps/context, and change summary sufficient for diagnostics but not Method execution. Retention/storage policy may bound persisted audit records without pinning executable revisions. Rollback/reactivation from records remains separate. | [IRIS-V1-META-C062](08-modules-metaprogramming.md), [IRIS-V1-META-C063](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C019](03-runtime-object-model.md) | `IRIS-V1-META-V353` | none | `normative` |
| `D-270` | Historical revisions/audit records are never directly reactivated. `Class.rollback(target)` is a new structural transaction that reconstructs desired state from current active state plus available historical artifacts, validates current static spine/contracts/MRO/native constraints, and publishes with new per-Class revision number and runtime commit ID. History remains linear. External data/resources/instance state are not restored automatically. Lightweight audit records need not retain executable code; artifact requirements remain separate. | [IRIS-V1-META-C064](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C020](03-runtime-object-model.md) | `IRIS-V1-META-V357` | none | `normative` |
| `D-271` | Lightweight audit records retain an immutable artifact locator plus cryptographic digest. Rollback resolves the exact source/bytecode/native artifact through the active package store or an explicit user resolver and verifies the digest before reconstruction. Missing, inaccessible, or mismatched artifacts raise ordinary catchable `RevisionArtifactUnavailableError` and publish nothing; runtime never substitutes current/nearest source or approximate semantics. Digest algorithm/scope remain separate. | [IRIS-V1-META-C066](08-modules-metaprogramming.md), [IRIS-V1-META-C064](08-modules-metaprogramming.md) | `IRIS-V1-META-V357` | none | `normative` |
| `D-272` | Integrity digest is the complete 32-byte BLAKE3 hash of a canonical manifest covering relevant Iris source blobs, bytecode/IR, native binaries, dependency package IDs/API majors/exact revisions, Iris language/compiler/runtime ABI versions, and every semantics-affecting build option/path-to-content mapping. Locator is excluded so mirrors may change. Any mismatch aborts exact recovery. Canonical manifest serialization/path normalization remain package-format details. | [IRIS-V1-META-C067](08-modules-metaprogramming.md), [IRIS-V1-META-C066](08-modules-metaprogramming.md) | `IRIS-V1-META-V357` | none | `normative` |
| `D-273` | Rollback may use an older minor/patch implementation artifact of the same package API major, but reconstructs a candidate against the currently active static spine/contracts. Old bodies/state logic may be restored only if they satisfy every current member, Contract, generic, superclass-bound, visibility, and native obligation. Missing later contracts cause rollback failure; runtime does not remove current API or synthesize failing stubs. | [IRIS-V1-META-C065](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C021](03-runtime-object-model.md), [IRIS-V1-META-C064](08-modules-metaprogramming.md) | `IRIS-V1-META-V357` | none | `normative` |
| `D-274` | Minor/patch revisions may add ordinary typed methods/properties that do not invalidate existing callers or implementers. They may not change/remove existing signatures, constraints, visibility obligations, superclass bounds, generic arity/invariance, add required members to existing Contracts, or add new mandatory obligations to existing implementers. Such changes require a new package API major/nominal identity. | [IRIS-V1-RUNTIME-C021](03-runtime-object-model.md), [IRIS-V1-META-C065](08-modules-metaprogramming.md), [IRIS-V1-META-C068](08-modules-metaprogramming.md) | `IRIS-V1-META-V357` | none | `normative` |
| `D-275` | Contracts are pure static promises: member names/signatures/generic constraints/visibility and qualified slot identities only. They contribute no Method bodies to MRO, `super`, or dispatch. Behavior reuse belongs to Modules with explicit mixin/include and Self constraints. Existing Contract required members cannot gain compatibility-evolution defaults in minor releases; adding required members remains API-major breaking. | [IRIS-V1-TYPES-C042](05-types-contracts-generics.md), [IRIS-V1-META-C029](08-modules-metaprogramming.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V441`, `IRIS-V1-TYPES-V258` | none | `normative` |
| `D-276` | A Contract may inherit multiple named Contracts, forming the union of their static requirements without creating any implementation MRO or `super` path. Compatible same-name requirements merge into one obligation under method-compatibility rules; incompatible requirements make the declaration a compile-time error. Parent order never chooses a winner, and Contract inheritance cycles are compile errors. Exact keyword syntax remains separate. | [IRIS-V1-TYPES-C043](05-types-contracts-generics.md), [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md), [IRIS-V1-TYPES-C041](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V249` | none | `normative` |
| `D-277` | Syntax is `contract Child extends ParentA, ParentB { ... }`. `extends` uniformly denotes nominal subtype: Classes permit one superclass, Contracts permit one or more parent Contracts. Contract parent order has no winner/MRO meaning. `include` remains runtime Module composition only. | [IRIS-V1-TYPES-C041](05-types-contracts-generics.md), [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C046](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V192` | none | `normative` |
| `D-278` | Syntax is `class Sprite extends Node for Drawable, Serializable { ... }`. In a Class header, `for` declares immutable nominal Contract conformance in the static spine; individual implementing members use `impl`. In statement grammar, `for` remains the iteration keyword, with declaration context making the uses unambiguous. `satisfies` and legacy `implements` are not formal keywords. | [IRIS-V1-TYPES-C044](05-types-contracts-generics.md), [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md) | `IRIS-V1-META-V440`, `IRIS-V1-TYPES-V018` | none | `normative` |
| `D-279` | Modules cannot use header `for` to claim instance Contract conformance, and Module composition never automatically transfers nominal conformance to the host. A Module may provide `impl`-marked compatible bodies and constrain `Self` by a Contract; the final Class static spine must explicitly list the Contract with `for`. Dynamic Module removal is rejected if it leaves that declared Contract without a compatible implementation. | [IRIS-V1-TYPES-C044](05-types-contracts-generics.md), [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C013](02-lexical-grammar.md) | `IRIS-V1-META-V440`, `IRIS-V1-TYPES-V261` | none | `normative` |
| `D-280` | Canonical grammar order is `class Name<T...> extends Base for ContractA, ContractB mixin ModuleA, ModuleB where ... { ... }`. Each clause kind appears at most once, omitted clauses are skipped without reordering, and formatters emit this order. The sequence expresses nominal base, static Contract promises, static behavior composition, then generic constraints. `extends` remains single-Class, while `for`/`mixin` accept comma-separated lists. | [IRIS-V1-GRAMMAR-C052](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V161` | none | `normative` |
| `D-281` | Module grammar is `module M<T...> mixin A, B where Self: ContractExpr, T: ConstraintExpr { ... }`; it supports neither `extends` nor `for`. Contract grammar is `contract C<T...> extends A, B where T: ConstraintExpr { ... }`; it supports neither `for` nor `mixin`. Each allowed clause appears at most once and in shown order. `Self` is intrinsic only where defined for Module host constraints. | [IRIS-V1-GRAMMAR-C052](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V161` | none | `normative` |
| `D-282` | Grammar is `where T: A & B, U: C | D`. Each comma starts a different type-parameter/Self constraint assignment; each constrained parameter appears at most once. `&`, `|`, precedence, and parentheses exclusively compose the type expression for one parameter. Comma never means intersection/all-of. | [IRIS-V1-TYPES-C055](05-types-contracts-generics.md), [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C015](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V163` | none | `normative` |
| `D-283` | `class A { ... }` and `class A extends Object { ... }` have the same static spine/direct superclass; Object alone has no superclass. Origin/open distinction comes solely from `class` versus `open class`, not presence/order. No second root Class may be declared. Source formatting may preserve explicitness while reflection normalizes it. | [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md), [IRIS-V1-TYPES-C009](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-GRAMMAR-V193` | none | `normative` |
| `D-284` | A Contract without `extends` has no parent Contracts. A Module without `mixin` composes no Modules. A Module without explicit `where Self:` has implicit `Self: Object`; any other unconstrained generic parameter likewise has Object upper bound. No implicit Contract parent or NonNil constraint is added. Whether built-in NilClass may compose Modules remains separate. | [IRIS-V1-TYPES-C041](05-types-contracts-generics.md), [IRIS-V1-TYPES-C056](05-types-contracts-generics.md), [IRIS-V1-META-C053](08-modules-metaprogramming.md) | `IRIS-V1-META-V358` | none | `normative` |
| `D-285` | Nil's stable Class may statically mix and transactionally include/remove Modules like other Classes, subject to Module `Self` constraints, static-spine/Contract preservation, atomic revision commit, and JIT/cache invalidation. The unique nil singleton uses the Class's new active revision for subsequent sends; composition creates no new nil instances. Static visibility follows declarative versus programmatic open rules. | [IRIS-V1-RUNTIME-C148](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C149](03-runtime-object-model.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V359`, `IRIS-V1-RUNTIME-V101` | none | `normative` |
| `D-286` | Bool, Integer, Float32, and Float64 stable Classes may be opened, gain/remove dynamic members/Modules/properties, and replace compatible Method implementations like user Classes. Their instance value bits/mathematical representation/layout immutability and built-in static Contracts/native/GC safety cannot be broken. Primitive/interpreter/JIT fast paths depend on active revision and exact Method/slot versions and invalidate/deopt after commit. Static API visibility follows declarative/programmatic open rules. Runtime-superclass mutation remains separate. | [IRIS-V1-RUNTIME-C148](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C149](03-runtime-object-model.md), [IRIS-V1-META-C082](08-modules-metaprogramming.md) | `IRIS-V1-META-V359`, `IRIS-V1-RUNTIME-V101` | none | `normative` |
| `D-287` | Nil, Bool, Integer, Float32, and Float64 remain behaviorally open for compatible Method/Module/property-protocol changes, but their runtime superclass cannot be changed. Any declarative or programmatic attempt to invoke the structural `superclass=` operation on these protected Classes raises a meta-operation exception and publishes no candidate revision. Protection is an intrinsic Class/meta-API capability flag owned by the runtime, not a user-implementable `ValueSuperclass` Contract or unsafe escape hatch. This preserves tagged/unboxed representation, GC/native ABI, and primitive assumptions while keeping ordinary behavior open. | [IRIS-V1-META-C082](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C149](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C154](03-runtime-object-model.md) | `IRIS-V1-META-V359` | none | `normative` |
| `D-288` | Every syntax/API path that can request superclass mutation lowers to the same runtime structural operation and unforgeable capability check. Protection applies equally to `open class`, `Class#open`, aliases, reflection, Dynamic calls, qualified/unqualified meta messages, nested transaction groups, package upgrade/rollback, deserialization, native/plugin/host APIs, and indirect helper calls. No alternate method-table replacement, raw metadata write, custom setter, monkey patch, or unsafe user flag may bypass it. Attempting `superclass=` on a protected Class aborts the complete transaction group before publication; unrelated candidate targets in that group also roll back. Runtime/native bugs aside, user code can never acquire or fabricate the internal capability required to alter a protected superclass. | [IRIS-V1-META-C072](08-modules-metaprogramming.md), [IRIS-V1-META-C080](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C154](03-runtime-object-model.md) | `IRIS-V1-META-V359` | none | `normative` |
| `D-289` | Every Class static spine contains a read-only, reflectable `MetaCapabilities` policy describing which structural operations are permitted (e.g. method open/replacement, Module composition, dynamic members, runtime-superclass change, instance-shape/native-layout change). Every syntax, reflection, Dynamic, package, native, and host meta path invokes the same operation-level capability check. Absence raises ordinary catchable `MetaCapabilityError` carrying target, operation, policy origin, and reason, and aborts the transaction atomically. Built-ins are not name-based magic exceptions: they use the same policy. User Class origins may only narrow the language/default capability set, not grant runtime-internal powers or later widen their immutable policy. Exact declaration syntax remains separate. | [IRIS-V1-META-C072](08-modules-metaprogramming.md), [IRIS-V1-META-C076](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C152](03-runtime-object-model.md) | `IRIS-V1-META-V360` | none | `normative` |
| `D-290` | Class origin header order extends to `extends -> for -> mixin -> where -> meta`. Syntax `meta deny capability_a, capability_b` records immutable static-spine restrictions. Only denial/narrowing is source-expressible; omitted capabilities inherit defaults, and no source `allow` can grant unavailable runtime powers. Open/conditional code cannot change the policy. Reflection exposes it and all meta APIs enforce it through D-289. Exact capability vocabulary/dependencies remain separate. | [IRIS-V1-META-C074](08-modules-metaprogramming.md), [IRIS-V1-META-C075](08-modules-metaprogramming.md), [IRIS-V1-GRAMMAR-C052](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V165`, `IRIS-V1-META-V360` | none | `normative` |
| `D-291` | Vocabulary includes `method_set`, `method_body`, `property_set`, `property_body`, `modules`, `superclass`, `subclass`, `shape`, `class_state_set`, `class_state_write`, `instance_state`, and `native`. `instance_state` controls creation/recreation of undeclared per-instance ivars; deletion remains cleanup under D-306. Unknown names are compile errors. | [IRIS-V1-META-C073](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-292` | Denying one capability does not implicitly deny another. Each operation declares all required capabilities: Method slot/signature changes use `method_set`; compatible Method body replacement `method_body`; property/getter/setter slot/signature changes `property_set`; compatible accessor body replacement `property_body`; stored Class-wide instance storage additionally `shape`; native-backed storage additionally `native`; class/shared slot changes `property_set & class_state_set`; existing class/shared value writes `class_state_write`; undeclared per-instance ivar creation/recreation `instance_state`. Missing any requirement raises `MetaCapabilityError`. Reflection exposes policy and operation requirements. | [IRIS-V1-META-C080](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-293` | Method slot/signature changes require `method_set`; compatible Method body replacement `method_body`; property/accessor slot/signature changes `property_set`; compatible accessor body replacement `property_body`; stored instance-property changes additionally `shape`; Module composition `modules`; runtime-superclass change `superclass`; selecting a Class as declared/runtime superclass requires target effective `subclass`; class/shared slots `property_set & class_state_set`; existing class/shared value writes `class_state_write`; native involvement adds `native`. Future APIs declare requirements. | [IRIS-V1-META-C081](08-modules-metaprogramming.md), [IRIS-V1-META-C080](08-modules-metaprogramming.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-294` | A Class's effective capabilities are language/runtime defaults minus every deny inherited from its superclass chain minus its own origin denies. A subclass may further narrow but cannot re-enable a capability denied by an ancestor, preventing policy bypass and protecting inherited layout/native/static invariants. Reflection exposes local denies, inherited denies with origin, and effective set. Runtime-superclass revision interaction remains separate. | [IRIS-V1-META-C076](08-modules-metaprogramming.md), [IRIS-V1-META-C077](08-modules-metaprogramming.md), [IRIS-V1-GRAMMAR-C052](02-lexical-grammar.md) | `IRIS-V1-META-V360` | none | `normative` |
| `D-295` | A Class revision's effective MetaCapabilities subtract local origin denies, immutable declared-superclass-chain denies, and every deny from its current runtime-superclass/MRO Class chain. Runtime ancestry may further narrow but can never restore static-chain capabilities. Candidate validation computes the full set before applying/committing operations; operations losing authorization cause atomic failure. Reflection exposes local/static/runtime deny origins and final set. | [IRIS-V1-META-C076](08-modules-metaprogramming.md), [IRIS-V1-META-C077](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V360` | none | `normative` |
| `D-296` | A Module may declare immutable `meta deny` restrictions in its origin header. Any static mixin or dynamic include adds those denies, with Module identity as policy origin, to the host Class revision's effective MetaCapabilities. Removing the Module removes only that Module-sourced restriction; static superclass/local/other Module denies remain, so capabilities restore only when no remaining source denies them. Composition/removal recomputes policy transactionally before other operations/commit. Module open cannot alter its static deny policy. | [IRIS-V1-META-C078](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V360` | none | `normative` |
| `D-297` | A Contract may declare `meta deny` requirements as part of its static promise. Any Class using header `for Contract` must have an effective policy at least as restrictive; conformance permanently absorbs those denies into the Class static spine and they cannot disappear through runtime superclass/Module changes. Contract multiple inheritance unions deny requirements. Contracts never grant/allow capabilities. Reflection/diagnostics expose Contract origins. Header placement remains separate. | [IRIS-V1-META-C079](08-modules-metaprogramming.md), [IRIS-V1-TYPES-C045](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-META-V360` | none | `normative` |
| `D-298` | Class order is `extends -> for -> mixin -> where -> meta`; Module is `mixin -> where -> meta`; Contract is `extends -> where -> meta`. `meta deny` is static header syntax only and never appears in executable/conditional body code. | [IRIS-V1-GRAMMAR-C052](02-lexical-grammar.md), [IRIS-V1-META-C075](08-modules-metaprogramming.md) | `IRIS-V1-GRAMMAR-V165`, `IRIS-V1-META-V360` | none | `normative` |
| `D-299` | `method_set` and `method_body` are orthogonal. A Class/Contract/Module may deny API/member-set evolution while allowing compatible bug-fix/hot-upgrade body replacement, or deny body replacement while permitting other authorized member-set operations. Contract/static signature compatibility remains mandatory regardless of capability. No privileged “hotfix” bypass exists; permission follows these normal capabilities. | [IRIS-V1-RUNTIME-C154](03-runtime-object-model.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md), [IRIS-V1-META-C080](08-modules-metaprogramming.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-300` | `property_set` controls adding/removing/changing property/getter/setter slots and contracts; `property_body` controls compatible getter/setter implementation replacement. Stored instance property set changes additionally need `shape`; class/shared property slot changes additionally need class-state capability. This permits immutable property API with hotfixable accessors without special bypasses. | [IRIS-V1-RUNTIME-C154](03-runtime-object-model.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md), [IRIS-V1-META-C080](08-modules-metaprogramming.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-301` | `class_state_set` controls adding/removing/changing class/shared storage slots; `class_state_write` controls assignment/mutation of an existing slot's stored value through Class meta/property state APIs. This permits fixed registry shape with mutable counters/config, or fully read-only class state. Slot creation still also requires `property_set`; native involvement adds `native`. | [IRIS-V1-RUNTIME-C154](03-runtime-object-model.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md), [IRIS-V1-META-C080](08-modules-metaprogramming.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-302` | One `modules` capability controls Module include/mixin, removal, replacement, and ordering/recomposition because all alter MRO/dispatch and membership operations could otherwise emulate order changes. Iris does not split add/remove/order or define per-Module capability flags in v1. Static-spine/Contract/MRO validation remains independently mandatory. | [IRIS-V1-META-C055](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-303` | `shape` is required for revision-level typed storage, shared layout/shape descriptors, all-instance slots, native layout, and GC tracing structure. First assignment to an absent undeclared dynamic ivar does not require `shape` or alter Class-wide layout; it requires effective `instance_state` under D-304. Thus `meta deny shape` alone does not seal dynamic ivars, while `meta deny instance_state` prevents their creation/recreation. | [IRIS-V1-META-C083](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C154](03-runtime-object-model.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-304` | When effective `instance_state` is present, instances may create undeclared dynamic ivars under ordinary object rules. `meta deny instance_state` causes first assignment to an absent undeclared ivar (including recreation after deletion) to raise ordinary catchable `InstanceStateError`. It does not forbid constructor initialization or reads/writes of declared typed property/storage slots. Existing dynamic ivars created before policy tightening remain readable/writable and may be deleted for cleanup; deletion is state contraction and stays allowed, but the deleted slot cannot be recreated while denied. | [IRIS-V1-RUNTIME-C069](03-runtime-object-model.md), [IRIS-V1-META-C083](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-305` | Identity-less immutable values, including Integer, Float32, and Float64, cannot create, recreate, or retain undeclared per-instance dynamic ivars because such state would create observable identity or value-keyed global state. Missing reads remain side-effect-free and return nil under D-308; assignment that would create/recreate a slot raises `InstanceStateError`. Their effective Class policy intrinsically denies `instance_state` through the same MetaCapabilities model, enforced across source, reflection, Dynamic, native, and host paths. Identity-bearing nil/Bool singletons follow their own Class policies. | [IRIS-V1-META-C084](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C069](03-runtime-object-model.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-306` | After effective `instance_state` becomes denied, deleting an existing undeclared dynamic ivar is legal without migration privilege. Deletion does not auto-delete other instances' state and does not alter Class-wide shape. Subsequent assignment to that absent name is a new expansion and raises `InstanceStateError` while denial remains. Iris never automatically deletes old ivars on policy commit. | [IRIS-V1-RUNTIME-C070](03-runtime-object-model.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md), [IRIS-V1-META-C083](08-modules-metaprogramming.md) | `IRIS-V1-META-V361` | none | `normative` |
| `D-307` | An existing undeclared dynamic instance variable may hold any `Object` value, including nil, and later assignments may change runtime value type. `instance_state` governs slot creation only, not value typing or mutation. No first-write type locking or policy-tightening freeze occurs. Programs requiring stable types must declare typed properties/storage. Static read typing remains separate. | [IRIS-V1-RUNTIME-C068](03-runtime-object-model.md), [IRIS-V1-TYPES-C003](05-types-contracts-generics.md), [IRIS-V1-META-C083](08-modules-metaprogramming.md) | `IRIS-V1-META-V434`, `IRIS-V1-RUNTIME-V086` | none | `normative` |
| `D-308` | Reading undeclared `@x` has static type `Dynamic<Object>`. If the receiver lacks that slot, the read returns nil and does not create/expand instance state. Subsequent messages on the result use dynamic lookup. Stable/member-checked/non-nil behavior requires a declared typed property or explicit narrowing. External access remains separate. | [IRIS-V1-RUNTIME-C068](03-runtime-object-model.md), [IRIS-V1-META-C109](08-modules-metaprogramming.md), [IRIS-V1-TYPES-C003](05-types-contracts-generics.md) | `IRIS-V1-META-V434`, `IRIS-V1-RUNTIME-V086` | none | `normative` |
| `D-309` | `@x` denotes the single slot named `@x` on current receiver, independent of lexical declaring Class/Module. Parent, subclass, and composed Module Methods on the same receiver see/write the same slot. External `obj.@x` remains invalid, and `Dynamic<T>` does not enable it. External access uses properties/messages or permission-checked reflection. Raw ivars are implementation state, not Contract API. | [IRIS-V1-RUNTIME-C071](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C075](03-runtime-object-model.md), [IRIS-V1-META-C111](08-modules-metaprogramming.md) | `IRIS-V1-META-V434`, `IRIS-V1-RUNTIME-V086` | none | `normative` |
| `D-310` | A Closure created in an instance Method captures its current receiver and may continue reading/writing that receiver's named ivars after escape. This is ordinary receiver capture rather than an owner-specific field capability. Calling/passing the Closure does not expose external `obj.@x` syntax, and its receiver remains non-rebindable under D-311. | [IRIS-V1-RUNTIME-C072](03-runtime-object-model.md), [IRIS-V1-CONTROL-C028](04-bindings-callables-control-flow.md) | `IRIS-V1-META-V434`, `IRIS-V1-RUNTIME-V087` | none | `normative` |
| `D-311` | A Closure's captured receiver/private capability cannot be rebound through source, reflection, Dynamic, or ordinary native/plugin APIs, even to another instance of the same Class. Captured locals/receiver/capability remain fixed. Reusable logic for other receivers uses a Method, explicit parameter, Module, or newly created Closure. | [IRIS-V1-RUNTIME-C072](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C078](03-runtime-object-model.md) | `IRIS-V1-META-V434`, `IRIS-V1-RUNTIME-V087` | none | `normative` |
| `D-312` | User code accesses named slots only through unqualified `@name` on current `self`; `other.@name` is invalid even for the same Class. Slot identity is `(receiver, name)`, not `(declaring Class, name)`, so superclass/subclass/Module code on the same self shares it. Cross-instance access uses messages/properties/reflection. Runtime primitive payload access is separate. | [IRIS-V1-GRAMMAR-C011](02-lexical-grammar.md), [IRIS-V1-CONTROL-C008](04-bindings-callables-control-flow.md), [IRIS-V1-META-C111](08-modules-metaprogramming.md) | `IRIS-V1-META-V434` | none | `normative` |
| `D-313` | A private Method selector may be sent only from implementation code lexically authorized by the declaring logical Class (and separately authorized Modules), including explicitly to another receiver of that same logical Class. Subclass code, external callers, `Dynamic<T>`, and ordinary reflection cannot invoke it merely because the selector exists. Private Method access is Class-level behavioral collaboration, distinct from ivars which remain current-receiver-only. Module authorization remains separate. | [IRIS-V1-RUNTIME-C078](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C079](03-runtime-object-model.md), [IRIS-V1-META-C056](08-modules-metaprogramming.md) | `IRIS-V1-META-V434`, `IRIS-V1-RUNTIME-V088` | none | `normative` |
| `D-314` | Module composition grants no private Method access by default. A Class owner may explicitly authorize at each static/dynamic composition site (`mixin M private` or `include(M, access: private)`), recorded and committed/removed atomically. Authorized Module Methods may invoke host private Methods; the Module cannot self-grant/forge access. Raw current-receiver `@x` access is independent and follows D-309 for every composed Module Method. | [IRIS-V1-META-C056](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C050](03-runtime-object-model.md) | `IRIS-V1-META-V435` | none | `normative` |
| `D-315` | Every Module Method executing on a host receiver may read/write unqualified `@x` on that current receiver, whether or not its composition edge grants private Method access. `private` authorization affects private selectors only. `other.@x` remains invalid. Module authors and host integrators own receiver-slot name-collision consequences. | [IRIS-V1-META-C058](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C051](03-runtime-object-model.md) | `IRIS-V1-META-V435`, `IRIS-V1-RUNTIME-V089` | none | `normative` |
| `D-316` | Authorization is scoped to `(host logical Class, Module identity/closed construction, composition edge/revision)`. Granting M private access in host A never grants it in B, another generic instantiation, package, or future remove/reinclude edge. Each composition site states access anew; removal revokes that edge's capability atomically. Module global state does not retain host capabilities. | [IRIS-V1-META-C057](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C050](03-runtime-object-model.md) | `IRIS-V1-META-V435` | none | `normative` |
| `D-317` | A protected Method declared by Class A may be called from implementation code lexically belonging to A or a nominal subclass, with receiver restricted to current self or an instance in that permitted hierarchy. External callers and `Dynamic<T>` do not bypass protection. A Module composed into a host revision may call host protected Methods by default as part of that implementation layer, without private authorization; this does not grant private access. Exact protected property/storage rules remain separate. | [IRIS-V1-META-C059](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C079](03-runtime-object-model.md) | `IRIS-V1-META-V435`, `IRIS-V1-RUNTIME-V089` | none | `normative` |
| `D-318` | Protected property getters/setters follow D-317 and form explicit inherited API. Raw `@x` is not a visibility-declared field: parent, child, and composed Module code on the same receiver all access the same `(receiver, @x)` slot, while external/cross-receiver direct syntax remains invalid. Stable subclass contracts should use protected properties; direct slot coupling is a deliberate dynamic implementation choice. | [IRIS-V1-META-C059](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C071](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C079](03-runtime-object-model.md) | `IRIS-V1-META-V435`, `IRIS-V1-RUNTIME-V089` | none | `normative` |
| `D-319` | Lookup starts with the logical Class's own active members, then composed Modules in reverse composition order (most recently composed closest), then recursively the runtime superclass MRO. Header `mixin A, B` is applied left-to-right and yields `Class, B, A, Super...`; sequential `include(A); include(B)` yields the same order. `super` advances to the next implementation in this linearization. Nested composition expansion/dedup remains separate. | [IRIS-V1-RUNTIME-C046](03-runtime-object-model.md), [IRIS-V1-META-C053](08-modules-metaprogramming.md), [IRIS-V1-META-C060](08-modules-metaprogramming.md) | `IRIS-V1-META-V435`, `IRIS-V1-RUNTIME-V090` | none | `normative` |
| `D-320` | Nested Module composition expands deterministically according to D-319, then each closed Module identity appears at most once at its closest-to-Class occurrence. Example: B mixes A and Class mixes A then B => `Class, B, A, Super`. Re-including an already-present closed Module is idempotent: it creates no duplicate, does not move/reorder the existing occurrence, and causes no structural revision unless other edge metadata changes. Explicit reordering remains separate. | [IRIS-V1-RUNTIME-C046](03-runtime-object-model.md), [IRIS-V1-META-C060](08-modules-metaprogramming.md), [IRIS-V1-META-C054](08-modules-metaprogramming.md) | `IRIS-V1-META-V435`, `IRIS-V1-RUNTIME-V090` | none | `normative` |
| `D-321` | V1 provides no dedicated reorder API and repeated include does not move a Module. A user may reorder by removing and re-including within one open transaction; candidate validation/atomic commit, `modules` capability, Module/Self constraints, MRO, Contract implementation, MetaCapabilities, and composition-edge authorization all reapply. Re-inclusion creates a new edge and requires edge metadata such as private authorization to be stated again. | [IRIS-V1-META-C060](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C046](03-runtime-object-model.md) | `IRIS-V1-META-V340` | none | `normative` |
| `D-322` | V1 has no executable `included`, `method_added`, `inherited`, or equivalent callback during candidate construction/validation/commit. Such hooks could observe partial state, reenter transactions, and duplicate external effects after rollback/conflict. Iris instead exposes a read-only after-commit revision event stream after atomic publication. Events cannot veto or roll back the committed transaction; subscriber failure handling and payload/access remain separate. | [IRIS-V1-ASYNC-C045](07-async-resources-diagnostics.md), [IRIS-V1-META-C071](08-modules-metaprogramming.md) | `IRIS-V1-ASYNC-V028`, `IRIS-V1-ASYNC-V073` | none | `normative` |
| `D-323` | Any Iris code may subscribe to the read-only event stream. Events identify commit ID, targets, old/new Revision or audit identities, change summaries, initiator/source/package metadata, and permitted structural metadata. Payload is filtered through subscriber reflection permissions; private Method bodies, raw private data, native secrets, and inaccessible values are not exposed. Events provide no candidate mutation handle and cannot alter/rollback commit. Subscriber failure remains separate. | [IRIS-V1-ASYNC-C046](07-async-resources-diagnostics.md), [IRIS-V1-META-C071](08-modules-metaprogramming.md) | `IRIS-V1-ASYNC-V028`, `IRIS-V1-ASYNC-V074` | none | `normative` |
| `D-324` | Subscribers are invoked independently in subscription order. A failure creates a full ExceptionContext, is recorded on a runtime event-error channel and passed to a configurable handler, but does not rollback commit, affect other subscribers, propagate to the completed open/upgrade caller, or trigger retry. Default handling must diagnose rather than silently discard. Delivery scheduling remains separate. | [IRIS-V1-ASYNC-C048](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C056](07-async-resources-diagnostics.md), [IRIS-V1-META-C071](08-modules-metaprogramming.md) | `IRIS-V1-ASYNC-V020`, `IRIS-V1-ASYNC-V028`, `IRIS-V1-ASYNC-V075` | none | `normative` |
| `D-325` | Structural commit publishes at safepoint, resumes runtime threads, and lets open/upgrade return without waiting for subscribers. Immutable events enter a runtime queue ordered by `commit_id`; within one event/commit subscribers run in subscription order. Subscriber code never runs in the safepoint/commit path. A flush/wait API is available for tests and controlled shutdown. Backpressure/loss policy remains separate. | [IRIS-V1-ASYNC-C047](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C048](07-async-resources-diagnostics.md), [IRIS-V1-META-C071](08-modules-metaprogramming.md) | `IRIS-V1-ASYNC-V022`, `IRIS-V1-ASYNC-V023`, `IRIS-V1-ASYNC-V028`, `IRIS-V1-ASYNC-V076` | none | `normative` |
| `D-326` | Each subscriber has a bounded queue. A slow/full subscriber never blocks safepoint/commit/open. Dropped event ranges are never silent: the queue coalesces them into a `GapEvent(from_commit, to_commit)` and continues with newer events. Consumers may recover from audit history. Zero-loss audit requirements use a separately configured persistent host/runtime sink rather than unbounded in-memory queues. Recovery API/retention guarantees remain separate. | [IRIS-V1-ASYNC-C051](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C052](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C056](07-async-resources-diagnostics.md), [IRIS-V1-META-C117](08-modules-metaprogramming.md) | `IRIS-V1-ASYNC-V021`, `IRIS-V1-ASYNC-V028`, `IRIS-V1-ASYNC-V077` | none | `normative` |
| `D-327` | `RevisionHistory.events(from_commit:, to_commit:) -> Iterator<RevisionAuditEvent>` returns retained lightweight events in commit order. If any requested portion is no longer retained/available, it raises `AuditHistoryUnavailableError` rather than returning a partial sequence as complete or conflating absence with pruning. GapEvent consumers may use it; persistent sinks define stronger retention externally. Iterator follows Iteration/close rules. | [IRIS-V1-ASYNC-C053](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C054](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C055](07-async-resources-diagnostics.md) | `IRIS-V1-ASYNC-V024`, `IRIS-V1-ASYNC-V025`, `IRIS-V1-ASYNC-V028`, `IRIS-V1-ASYNC-V078` | none | `normative` |
| `D-328` | V1 has no first-class ReflectionCapability token, token argument, delegation, derivation, or revocation graph. Reflection calls are ordinary APIs (`Reflection.list_ivars/get_ivar/set_ivar/remove_ivar`) and authorization is ambient to the currently executing package/module context under a runtime policy configured by the external Host/CLI and constrained by package manifest permissions. Method visibility, Dynamic typing, Module private authorization, and MetaCapabilities do not themselves grant reflection. Denial raises `ReflectionAccessError`; creating/recreating a slot additionally checks target effective `instance_state`, and identity-less values remain prohibited. | [IRIS-V1-META-C100](08-modules-metaprogramming.md), [IRIS-V1-META-C101](08-modules-metaprogramming.md) | `IRIS-V1-META-V362` | none | `normative` |
| `D-329` | Runtime policy grants `inspect` (list/get) and `mutate` (set/remove) independently to an executing package. Scope may be own package, selected packages/Classes, or runtime-wide according to Host policy; mutate does not imply inspect. Every operation checks caller package identity, granted permission/scope, target Class `instance_state`, identity-less restrictions, and host/native safety. Ordinary Iris code and native extensions cannot enlarge policy; package manifests request but do not self-grant permissions. `ReflectionAccessError` reports caller package, operation, target scope, and denial origin without leaking inaccessible data. Finer first-class delegated tokens are deferred beyond v1. | [IRIS-V1-META-C102](08-modules-metaprogramming.md), [IRIS-V1-META-C103](08-modules-metaprogramming.md), [IRIS-V1-META-C108](08-modules-metaprogramming.md) | `IRIS-V1-META-V362` | none | `normative` |
| `D-330` | A package manifest may request `reflection.inspect` and/or `reflection.mutate` with scope `own_package` or an explicit list of package IDs. Requests are declarative requirements, never self-authorization. The Host/CLI must explicitly grant a subset; unspecified permissions default deny, and startup/link may fail if the package marks an ungranted request required. Runtime-wide reflection cannot be requested by a package and exists only by Host configuration for trusted tooling. Dependency propagation remains separate. | [IRIS-V1-META-C008](08-modules-metaprogramming.md), [IRIS-V1-META-C009](08-modules-metaprogramming.md), [IRIS-V1-META-C010](08-modules-metaprogramming.md) | `IRIS-V1-META-V421` | none | `normative` |
| `D-331` | Authorization for a Reflection API call is determined by the package owning the currently executing callable/call site that performs the call, not by caller stack, dependency importer, receiver owner, or any union/intersection of stack permissions. A privileged package cannot implicitly lend rights to an unprivileged helper; it must perform reflection itself and pass ordinary filtered results. This prevents confused-deputy escalation. Closure/Method package ownership remains separate. | [IRIS-V1-META-C104](08-modules-metaprogramming.md), [IRIS-V1-META-C103](08-modules-metaprogramming.md), [IRIS-V1-META-C108](08-modules-metaprogramming.md) | `IRIS-V1-META-V362` | none | `normative` |
| `D-332` | Method reflection calls use the package defining that exact Method body; Closure calls use the package of its creating code and retain it after escape/cross-package invocation; BoundMethod inherits its Method package. Extension-open Methods belong to the extension package, not target Class origin. Caller/receiver/stack do not alter rights. Hot-upgraded replacement Methods use the new body artifact's package identity. | [IRIS-V1-META-C105](08-modules-metaprogramming.md), [IRIS-V1-META-C104](08-modules-metaprogramming.md) | `IRIS-V1-META-V362` | none | `normative` |
| `D-333` | Origin body code executes under the origin source package's ReflectionPolicy. Declarative extension/open body code executes under the extension source package, never the target Class/Module package. Programmatic `Class/Module#open` runs the supplied block under that Closure's lexical definition package. Caller/transaction target does not lend permissions. | [IRIS-V1-META-C106](08-modules-metaprogramming.md), [IRIS-V1-META-C103](08-modules-metaprogramming.md) | `IRIS-V1-META-V362` | none | `normative` |
| `D-334` | Each native artifact/callable belongs to its manifest package identity and performs reflection under that package's Host grants, never caller/receiver/stack or ambient Host superuser rights. Host built-ins use a dedicated host package identity with explicit policy. Manifestless native artifacts default to no reflection unless Host loads them under an explicitly trusted package identity. Process-local code execution alone grants no reflection privilege. | [IRIS-V1-META-C107](08-modules-metaprogramming.md), [IRIS-V1-FFI-C018](09-native-host-ffi.md), [IRIS-V1-META-C103](08-modules-metaprogramming.md) | `IRIS-V1-FFI-V059`, `IRIS-V1-META-V363` | none | `normative` |
| `D-335` | Authorized `get_ivar(obj,name)` returns nil for absent slot and creates nothing. `set_ivar` returns the assigned value; creating/recreating an absent slot additionally requires effective `instance_state`. `remove_ivar` returns the removed old value and raises `InstanceVariableNotFoundError` when absent, avoiding nil ambiguity. `list_ivars` returns an immutable `Array<Symbol>` of currently existing names with unspecified order. Identity-less values and ReflectionPolicy checks still apply. Name validation remains separate. | [IRIS-V1-META-C109](08-modules-metaprogramming.md), [IRIS-V1-RUNTIME-C068](03-runtime-object-model.md) | `IRIS-V1-META-V362` | none | `normative` |
| `D-336` | Reflection raw-ivar operations require a Symbol whose canonical name begins with exactly one `@` followed by a valid ordinary identifier; Strings never implicitly convert. Source `@name` and Symbol `:@name` denote the same receiver slot, and `list_ivars` returns those Symbols. Empty names, repeated `@`, method suffix `?`, or source-inexpressible identifiers raise `InvalidInstanceVariableNameError`; reflection cannot create hidden-only ivars. General Symbol literal syntax remains separate. | [IRIS-V1-META-C110](08-modules-metaprogramming.md), [IRIS-V1-META-C111](08-modules-metaprogramming.md), [IRIS-V1-GRAMMAR-C011](02-lexical-grammar.md) | `IRIS-V1-META-V362` | none | `normative` |
| `D-337` | `:name`, operator forms such as `:+`, and ivar forms such as `:@x` create simple Symbols; `:"arbitrary text"` creates an arbitrary-text Symbol using String escape rules but no interpolation. Symbols are immutable and runtime-interned by exact canonical content. Raw-ivar APIs further restrict accepted Symbol content under D-336. Identity/equality/hash remain separate. | [IRIS-V1-GRAMMAR-C012](02-lexical-grammar.md), [IRIS-V1-COLLECTIONS-C063](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V298` | none | `normative` |
| `D-338` | Symbol equality is exact canonical-content value equality; `same?` raises `IdentityError`. Runtime interning/storage sharing is unobservable and not semantic identity. Symbols may be reconstructed across serialization/runtime, are valid Hash keys, and require stable content hashing. Case/Unicode canonicalization remains separate. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C064](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V299` | none | `normative` |
| `D-339` | Use BLAKE3 derive-key context exact ASCII `Iris Language v1 stable symbol hash`; input is canonical shortest ULEB128 of canonical UTF-8 byte length followed by those UTF-8 bytes. Reduce digest by first eight bytes interpreted little-endian to `0..2^64-1`. Domain is independent from String/numeric/singleton hashes. Canonical Unicode/case rules remain separate. | [IRIS-V1-COLLECTIONS-C085](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C091](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C064](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V300` | none | `normative` |
| `D-340` | Ordinary identifiers are case-sensitive and lexer-normalized to Unicode NFC before name/selector/Type identity. Simple Symbols reuse the corresponding normalized identifier/operator/ivar canonical content. Quoted arbitrary-text Symbols preserve the exact post-escape Unicode scalar sequence with no normalization; case and canonically equivalent sequences remain distinct there. Stable hashing encodes the resulting canonical content. Allowed identifier character classes remain separate. | [IRIS-V1-GRAMMAR-C009](02-lexical-grammar.md), [IRIS-V1-COLLECTIONS-C065](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C063](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V301` | none | `normative` |
| `D-341` | Before/with NFC canonicalization, identifier start is Unicode XID_Start or `_`; continuation is XID_Continue or `_`. Pattern_Syntax, Pattern_White_Space, controls, and default-ignorable code points are forbidden even if otherwise classified. CJK and other scripts are supported. Mixed-script/confusable identifiers are valid language syntax but tooling emits configurable security lint warnings. Suffix punctuation remains separate. | [IRIS-V1-GRAMMAR-C009](02-lexical-grammar.md), [IRIS-V1-COLLECTIONS-C042](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C063](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V302` | none | `normative` |
| `D-342` | Callable/property selector identifiers may carry at most one trailing `?` or `!`, included in selector identity. Ordinary locals/parameters/constants/Class/Module/Contract/type-parameter names cannot. `?` and `!` have no mandatory return/mutation semantics; they are naming conventions available to APIs and lint tools. Combined setter suffix rules remain separate. | [IRIS-V1-GRAMMAR-C010](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C019](02-lexical-grammar.md), [IRIS-V1-CONTROL-C034](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V318`, `IRIS-V1-GRAMMAR-V187` | none | `normative` |
| `D-343` | Getter declaration is `property fun name() -> T`; setter declaration is `property fun name=(value: T) -> Nil` (or its declared result contract). Property read `obj.name` sends the zero-argument property selector; assignment `obj.name = value` sends the one-argument `name=` selector. Full selectors may include suffix punctuation before `=`, including `ready?=` and `value!=`. Declaration/property-assignment grammar uses contextual longest-selector recognition, while expression `a != b` remains inequality. No hidden getter/setter is generated merely by naming. Assignment result remains separate. | [IRIS-V1-RUNTIME-C061](03-runtime-object-model.md), [IRIS-V1-CONTROL-C034](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V319`, `IRIS-V1-RUNTIME-V096` | none | `normative` |
| `D-344` | `obj.name = value` is a message expression equivalent to `obj.name=(value)` and yields that Method's actual declared/runtime result. Iris does not force the right-hand value or nil. Setters may return old value/status/other typed result; ordinary setters commonly return Nil by declaration. Chained assignment semantics remain separate. | [IRIS-V1-CONTROL-C035](04-bindings-callables-control-flow.md), [IRIS-V1-RUNTIME-C062](03-runtime-object-model.md) | `IRIS-V1-CONTROL-V011`, `IRIS-V1-CONTROL-V320` | none | `normative` |
| `D-345` | `a.x = b.y = v` parses/evaluates as `a.x=(b.y=(v))`; the outer setter receives the inner setter's actual result, not necessarily original v. Static checking uses the inner result type against the outer parameter; Dynamic paths use normal runtime guards/errors. Evaluation follows the expression tree once per receiver/RHS. Local/index mixed assignment remains separate. | [IRIS-V1-CONTROL-C035](04-bindings-callables-control-flow.md), [IRIS-V1-RUNTIME-C063](03-runtime-object-model.md) | `IRIS-V1-CONTROL-V321` | none | `normative` |
| `D-346` | Local/class/shared/raw-ivar assignment yields the actual value stored in the binding/slot. Property and index assignment yield their setter Method result (`name=`/`[]=`). All assignment forms are right-associative, so mixed chains pass each inner expression's actual result outward. Binding assignment is not customizable; message assignment remains dynamic dispatch. Compound assignment remains separate. | [IRIS-V1-CONTROL-C035](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C038](04-bindings-callables-control-flow.md), [IRIS-V1-RUNTIME-C064](03-runtime-object-model.md) | `IRIS-V1-CONTROL-V322` | none | `normative` |
| `D-347` | `target OP= rhs` is not an independent selector. It evaluates receiver/index/key and RHS once, reads target once, sends ordinary `OP` to the read value, then writes the operation result once through the target's normal binding/property/index setter. The compound expression yields that write-back operation's result: new stored value for bindings/raw ivars, setter Method result for property/index. Side effects are never duplicated. Supported OP set remains separate. | [IRIS-V1-CONTROL-C036](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C038](04-bindings-callables-control-flow.md), [IRIS-V1-RUNTIME-C064](03-runtime-object-model.md) | `IRIS-V1-CONTROL-V012`, `IRIS-V1-CONTROL-V323` | none | `normative` |
| `D-348` | Supported forms are `+=`, `-=`, `*=`, `/=`, `**=`, `&=`, `|=`, `^=`, `<<=`, and `>>=`, each using D-347 with its corresponding ordinary operator. No independent compound selector exists. Named infix methods such as `div`/`mod` do not acquire `div=`/`mod=` syntax. Removed rotate operators have no compound forms. Logical short-circuit assignment remains separate. | [IRIS-V1-GRAMMAR-C015](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C016](02-lexical-grammar.md), [IRIS-V1-CONTROL-C036](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V324`, `IRIS-V1-GRAMMAR-V188` | none | `normative` |
| `D-349` | `target &&= rhs` and `target ||= rhs` are control-flow assignment syntax, not Method/operator selectors. Target location/receiver/index is evaluated/read once. `&&=` evaluates/writes RHS only when current value is truthy; `||=` only when current value is falsy. No-write path yields the current value. Write path yields the normal write-back result (stored RHS for binding/raw ivar, setter result for property/index). RHS is never evaluated on the short-circuited path. Truthiness remains to be formally reaffirmed. | [IRIS-V1-CONTROL-C037](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C038](04-bindings-callables-control-flow.md), [IRIS-V1-RUNTIME-C098](03-runtime-object-model.md) | `IRIS-V1-CONTROL-V013`, `IRIS-V1-CONTROL-V014`, `IRIS-V1-CONTROL-V325` | none | `normative` |
| `D-350` | Initially every object inherits or defines ordinary dynamically replaceable `to_bool() -> Bool`; after permitted removal, D-353 fallback applies. Root Object returns true; Nil's Class returns false; Bool returns self, so false/true preserve their Bool values. Conditions evaluate operand once, send `to_bool` once, require actual Bool, and never recursively convert the result. Wrong return raises `TypeContractError`; exceptions propagate. Optimizers depend on exact Method/revision versions. | [IRIS-V1-RUNTIME-C093](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C094](03-runtime-object-model.md), [IRIS-V1-CONTROL-C039](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V015`, `IRIS-V1-CONTROL-V326`, `IRIS-V1-RUNTIME-V097` | none | `normative` |
| `D-351` | No Class/Module may install Method selectors for these tokens. `!x` sends `x.to_bool()` once and returns its Bool negation; `!!x` normalizes through two core negations without invoking a second custom conversion beyond each `!` operand's required truth test semantics. `a && b` tests a via `to_bool`; when false returns original a without evaluating b, otherwise evaluates/returns b. `a || b` tests a; when true returns original a, otherwise evaluates/returns b. RHS short-circuit is guaranteed and `&&=`/`||=` follow D-349. `&`, `|`, `^`, `~` remain overloadable messages. Error propagation from `to_bool` remains separate. | [IRIS-V1-RUNTIME-C028](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C097](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C098](03-runtime-object-model.md), [IRIS-V1-GRAMMAR-C045](02-lexical-grammar.md), [IRIS-V1-CONTROL-C040](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V327`, `IRIS-V1-RUNTIME-V110` | none | `normative` |
| `D-352` | If truth testing invokes `to_bool` and it raises, the same propagation continues without wrapping/coercing to false. Conditional bodies/RHS and logical-assignment writeback do not run. Any target-location evaluation already performed is not rolled back. A non-Bool normal return remains a `TypeContractError` under D-350. | [IRIS-V1-RUNTIME-C095](03-runtime-object-model.md), [IRIS-V1-CONTROL-C039](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V015`, `IRIS-V1-CONTROL-V328`, `IRIS-V1-RUNTIME-V098` | none | `normative` |
| `D-353` | Object initially provides `to_bool() -> Bool { true }`; Nil overrides false and Bool returns self. The Method may be removed when `method_set` permits. Truth testing performs normal zero-argument lookup; if absent, runtime invokes `method_missing(:to_bool, [], nil)` once. Result must be Bool or raises `TypeContractError`. Missing/recursive fallback terminates with ordinary message-missing failure and never recursively truth-tests fallback results. | [IRIS-V1-RUNTIME-C096](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C099](03-runtime-object-model.md) | `IRIS-V1-CONTROL-V362` | none | `normative` |
| `D-354` | Root Object declares ordinary dynamically replaceable `method_missing(selector: Symbol, arguments: Array<Object>, block: Closure?) -> Object`. Runtime invokes it after ordinary selector lookup fails, passing the exact selector Symbol, immutable/snapshot positional argument Array, and trailing block or nil. Object's default implementation raises `MessageNotFoundError` with receiver Class/revision, selector, arity, source location, and visibility context. The handler is not recursively re-entered for a missing `method_missing` lookup itself. Qualified Contract-slot behavior remains separate. | [IRIS-V1-RUNTIME-C033](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C099](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C100](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V099` | none | `normative` |
| `D-355` | `view..member()` never invokes ordinary `method_missing`, falls back to an unqualified selector, searches another Contract, or uses a dynamic Contract-missing hook. Absence/invalidation of the exact qualified slot raises `ContractDispatchError` with receiver Class/revision, Contract identity, selector/signature, source location, and current Contract/MRO metadata. This denotes a broken runtime Contract invariant rather than ordinary dynamic absence. | [IRIS-V1-RUNTIME-C030](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C033](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V081` | none | `normative` |
| `D-356` | If ordinary selector lookup finds a Method/property but the caller lacks private/protected visibility, dispatch raises `MethodVisibilityError` directly. It does not invoke `method_missing`, conceal the distinction as absence, or permit fallback to bypass/probe hidden members. `method_missing` handles only selectors genuinely absent from ordinary lookup. Arity/type mismatch remains separate. | [IRIS-V1-RUNTIME-C034](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C078](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C079](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V088` | none | `normative` |
| `D-357` | When ordinary lookup resolves a selector, arity mismatch raises `ArgumentError` and runtime argument/block Contract failure raises `TypeError`; neither invokes `method_missing`. Iris performs no overload fallback or implicit coercion after selector resolution. `method_missing` applies only to genuinely absent ordinary selectors. | [IRIS-V1-RUNTIME-C025](03-runtime-object-model.md), [IRIS-V1-CONTROL-C025](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V009`, `IRIS-V1-CONTROL-V329` | none | `normative` |
| `D-358` | One complete selector in one Class revision/MRO slot namespace maps to at most one Method. Iris has no overload by parameter type, return type, arity, generic constraints, static expected type, or runtime argument types; it performs neither compile-time overload resolution nor runtime multi-dispatch. Defining the same complete selector again is replacement and requires explicit `override`, not an overload. APIs use optional/variadic parameters, union/intersection types, generic Methods, narrowing, distinct selector names, receiver dynamic dispatch, or `method_missing` only for true absence. Contract-qualified selectors such as `Parser::process` and `Validator::process` are distinct namespaced selectors explicitly chosen with a Contract view and `..`, not overload candidates. Each qualified selector likewise has at most one Method. | [IRIS-V1-RUNTIME-C023](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C024](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V080` | none | `normative` |
| `D-359` | Dynamic `to_bool() -> Bool` controls only which original operand value is returned; its Bool result is not the expression value. Therefore the static result type is the normalized union of reachable operand result types, e.g. `String && Integer` or `String || Integer` yields `String | Integer` absent compile-time-unreachable constant branches. Arbitrary `to_bool` implementations are not type predicates and cause no implicit narrowing. Explicit `is`, nil comparisons, and other dedicated flow tests perform narrowing separately. Runtime/JIT may optimize branch reachability under version guards without changing the stable source type. | [IRIS-V1-CONTROL-C040](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C042](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V330` | none | `normative` |
| `D-360` | `if/while` and other condition contexts evaluate the source once, call `to_bool() -> Bool` once, and branch on that Bool result. The original variable/expression value and static type remain unchanged in either branch unless a separate explicit narrowing predicate applies; `String | Integer` does not become Bool. Explicit `to_bool`, `!`, and `!!` expressions yield Bool, while operand-returning `&&`/`||` retain union result types. | [IRIS-V1-CONTROL-C039](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C042](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V331` | none | `normative` |
| `D-361` | `!x` and `!!x` always have static type Bool. `target &&= rhs` and `target ||= rhs` have the normalized union of the no-write path's current target-read type and the write path's actual write-back result type. Binding/raw-ivar writes contribute the RHS value type; property/index writes contribute the setter Method return type. Proven unreachable paths may be eliminated through ordinary normalization. | [IRIS-V1-CONTROL-C040](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C037](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C038](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V332` | none | `normative` |
| `D-362` | Revived Iris does not lex/parse `;statement` as a compatibility or deprecated form. Legacy examples require migration. Statements use newline and/or conventional trailing separators under the future terminator rule. A leading semicolon is not an optional marker and produces syntax error except where another explicitly defined grammar later uses it. | [IRIS-V1-GRAMMAR-C008](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V167` | none | `normative` |
| `D-363` | A newline terminates when grammar can complete the statement/expression; parser-expecting-more contexts continue automatically. A backslash immediately followed by the physical newline sequence explicitly removes/continues that newline. `;` is a trailing separator permitting multiple same-line statements. Formatter prefers delimiter-based continuation and omits unnecessary semicolons/backslashes when safe. | [IRIS-V1-GRAMMAR-C004](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C007](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C008](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C047](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V168` | none | `normative` |
| `D-364` | One complete statement may end with one semicolon and one semicolon may separate same-line statements. A standalone semicolon, repeated separator (`;;`), or legacy leading `;statement` is a syntax error, with migration diagnostics where recognizable. Parser never silently treats these as empty statements. | [IRIS-V1-GRAMMAR-C008](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V167` | none | `normative` |
| `D-365` | `//` comments to physical line end; `/* ... */` block comments may nest. `///` is a documentation line comment and `/** ... */` a documentation block comment with nesting support. Ordinary comments act as token-separating whitespace; a line-comment newline still participates in statement termination. Documentation attachment rules remain separate. | [IRIS-V1-GRAMMAR-C006](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V170` | none | `normative` |
| `D-366` | A physical first line beginning `#!` (optionally after UTF-8 BOM) is treated as a non-documentation comment, e.g. `#!/usr/bin/env iris`. `#!` elsewhere is a lexical error. The rule is uniform for CLI/package/module source; formatter preserves/moves no shebang except keeping an existing one first. | [IRIS-V1-GRAMMAR-C005](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V171` | none | `normative` |
| `D-367` | Source accepts UTF-8 only, optionally one UTF-8 BOM at file start. UTF-16/local encodings/other BOMs and malformed UTF-8 are compile errors with byte offset and source position; lexer never substitutes U+FFFD. LF, CRLF, and isolated CR are recognized as physical newlines; formatter emits LF canonically. | [IRIS-V1-GRAMMAR-C003](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V172` | none | `normative` |
| `D-368` | `"..."` processes escapes and `${expr}` interpolation; `'...'` processes escapes but never interpolation. Prefix `r` on either quote form disables escapes and interpolation. Triple-double/triple-single quotes are multiline counterparts inheriting interpolation/no-interpolation respectively, and may also be raw-prefixed. Exact escapes, delimiter inclusion, indentation, and interpolation conversion remain separate. | [IRIS-V1-GRAMMAR-C032](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C035](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C036](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C048A](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V173` | none | `normative` |
| `D-369` | Supported escapes are `\\`, `\"`, `\'`, `\n`, `\r`, `\t`, `\0`, `\b`, `\f`, `\v`, `\xNN` (exactly two hex digits), and `\u{H...}` (1-6 hex digits). Unicode escape values must be valid scalar values, excluding surrogates and values above U+10FFFF. Unknown/incomplete escapes are compile errors. Exact `\x` byte-versus-Unicode semantics remain separate. | [IRIS-V1-GRAMMAR-C033](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V174` | none | `normative` |
| `D-370` | Strings never contain invalid UTF-8 or surrogate code points. `\xNN` denotes Unicode scalar U+0000..U+00FF (e.g. `\xFF` is U+00FF encoded as UTF-8 C3 BF), not a raw byte injection. `\u{}` covers other scalars. Arbitrary binary data belongs in ByteArray/bytes APIs. Index/length units remain separate. | [IRIS-V1-GRAMMAR-C034](02-lexical-grammar.md), [IRIS-V1-COLLECTIONS-C041](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V303`, `IRIS-V1-GRAMMAR-V189` | none | `normative` |
| `D-371` | `length`, integer `[]`, and ordinary slicing count Unicode scalar values/code points and never split UTF-8 sequences. Explicit `byte_length`/`bytes` expose encoded bytes, while `graphemes`/grapheme APIs expose Unicode grapheme clusters with their own versioned behavior. Units are never mixed implicitly. Complexity/caching and boundary errors remain separate. | [IRIS-V1-COLLECTIONS-C044](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C045](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C062](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V304` | none | `normative` |
| `D-372` | String equality is value-based over its Unicode scalar sequence and `same?` raises `IdentityError`. Concatenation, replacement, slicing, case conversion, and edits produce new String values. Runtime may intern, rope, share substrings, use small-string/copy-on-write representations, or fold constants without observable identity/storage. String Class behavior remains open but existing contents cannot mutate. Normalization/case equality remains separate. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C047](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V305` | none | `normative` |
| `D-373` | Built-in String equality compares exact Unicode scalar sequence, case-sensitively, with no implicit NFC/NFD/NFKC/NFKD normalization, case folding, locale, or grapheme equivalence. Canonically equivalent source sequences may be unequal. Explicit `normalize(form)`, `casefold`, and locale-aware APIs produce values for alternate comparisons. Hash must match exact equality. Algorithm remains separate. | [IRIS-V1-COLLECTIONS-C043](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V306` | none | `normative` |
| `D-374` | Use exact ASCII derive-key context `Iris Language v1 stable string hash`; input shortest ULEB128 of exact UTF-8 byte length plus exact valid UTF-8 bytes. Digest reduces by first-eight-bytes little-endian. No normalization/case folding occurs. String and Symbol domains remain distinct; container bucket mixing remains runtime-secret. | [IRIS-V1-COLLECTIONS-C085](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C087](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C091](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V307` | none | `normative` |
| `D-375` | Each `${expr}` evaluates expr once. If already String, append directly; otherwise send ordinary `to_string` dynamically. Missing lookup may use `method_missing`; normal result must be String or raises `TypeContractError`. Exceptions propagate and abort interpolation. Nil's standard representation is `"nil"`. Concatenation may be optimized without changing evaluation order. Default Object representation remains separate. | [IRIS-V1-COLLECTIONS-C048](06-collections-text-regex.md), [IRIS-V1-GRAMMAR-C032](02-lexical-grammar.md) | `IRIS-V1-COLLECTIONS-V308` | none | `normative` |
| `D-376` | Root implementation returns `<fully.qualified.ClassName>` using nominal Class identity. It omits memory address, identity hash, runtime ID, revision number, properties, and ivars, so it is stable across runs for stable package/type identity. Classes may dynamically replace it subject to normal Method rules. Detailed debug representation belongs to `inspect`. | [IRIS-V1-COLLECTIONS-C049](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V309` | none | `normative` |
| `D-377` | Root `inspect() -> String` delegates to current `to_string()` and does not enumerate ivars/properties, expose identity/address/revision, or bypass ReflectionPolicy. Classes may override it normally. Debuggers/profilers use separate structured permission-checked reflection rather than trusting/depending on inspect. Standard value/container Classes may provide richer safe inspect forms. | [IRIS-V1-COLLECTIONS-C049](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C050](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V310` | none | `normative` |
| `D-378` | `String#to_string` returns the String value itself. `String#inspect` returns a reparsable double-quoted escaped literal, escaping controls/backslash/quote/interpolation opener as needed while preserving printable Unicode; parsing it recreates an exact scalar-equal String and no interpolation executes. Interpolation of a String adds no quotes. | [IRIS-V1-COLLECTIONS-C049](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C050](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V311` | none | `normative` |
| `D-379` | If opening triple quote is immediately followed by newline, that newline is omitted. If closing delimiter is alone on its line, the immediately preceding final newline is omitted and the exact whitespace prefix before the closing delimiter is removed from every non-empty content line. Any non-empty line lacking that exact prefix is a compile error. Empty lines normalize accordingly. Inline triple-quoted forms perform no automatic indentation stripping. Tab/space prefix matching remains separate. | [IRIS-V1-GRAMMAR-C036](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V175` | none | `normative` |
| `D-380` | Closing-indent stripping compares raw prefix code points/characters exactly; tabs are not expanded to columns and never equal spaces. Each non-empty line must begin with the same tab/space sequence as the closing delimiter prefix. Formatter preserves that prefix composition and literal content. Semantics never depend on editor tab width. | [IRIS-V1-GRAMMAR-C036](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V176` | none | `normative` |
| `D-381` | Raw literals may place 0-255 `#` characters between `r` and the quote delimiter, with the exact same count after the closing delimiter, for single/double/triple quote forms (e.g. `r#"..."#`, `r#"""..."""#`). Only quote sequence plus matching fence closes; content performs no escapes or interpolation. Fence-count mismatch/overflow is lexical error. Raw multiline indentation remains separate. | [IRIS-V1-GRAMMAR-C035](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V177` | none | `normative` |
| `D-382` | Raw disables escapes/interpolation only. Triple-quoted raw forms apply D-379/D-380 first/final newline and exact-prefix dedent; `#` fences do not affect indentation. Exact whitespace preservation requires an inline/non-standalone-closing form rather than a second raw mode. | [IRIS-V1-GRAMMAR-C036](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C035](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V178` | none | `normative` |
| `D-383` | `${...}` in interpolated double/triple Strings contains a full expression grammar with balanced nesting, nested Strings/interpolations, and normal evaluation. In escaped forms `\${` emits literal `${`; raw/single-quoted forms never interpolate. V1 has no `${expr:format}` syntax; formatting uses explicit Methods before `to_string`. Evaluation/partial-result behavior remains separate. | [IRIS-V1-GRAMMAR-C032](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C048A](02-lexical-grammar.md), [IRIS-V1-COLLECTIONS-C048](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V312`, `IRIS-V1-GRAMMAR-V190` | none | `normative` |
| `D-384` | Each segment expression and its required `to_string` completes before the next interpolation. On any failure, later segments do not run, the exception propagates, prior side effects remain, and no partially built String is returned/observable. Runtime may builder/fuse/preallocate only while preserving ordering and failure semantics. | [IRIS-V1-COLLECTIONS-C048](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V313` | none | `normative` |
| `D-385` | Standard `String#+(other: Object) -> String` appends other directly if String, otherwise invokes its dynamic `to_string() -> String`, with normal errors/order; the Method remains replaceable. Lexically adjacent String literal segments concatenate at compile time/one expression without `+`, preserving each segment's own escape/raw/interpolation semantics and left-to-right interpolation effects. Cross-family adjacency remains separate. | [IRIS-V1-COLLECTIONS-C051](06-collections-text-regex.md), [IRIS-V1-GRAMMAR-C032](02-lexical-grammar.md) | `IRIS-V1-COLLECTIONS-V314` | none | `normative` |
| `D-386` | Double/single/raw/triple String literal segments may concatenate adjacently. Each parses under its own escape/interpolation/fence/dedent rules, then contributes in source order. Only String literal segments participate; Symbols, ByteArrays, and arbitrary expressions do not implicitly join. Whitespace/comments may separate segments. Newline adjacency remains separate under statement termination. | [IRIS-V1-GRAMMAR-C032](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C048A](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V179` | none | `normative` |
| `D-387` | A complete String segment followed by an ordinary terminating newline does not concatenate with a String on the next statement. Cross-line adjacency is allowed inside automatic continuation contexts or through D-388 explicit backslash continuation. No indentation-based inference applies. | [IRIS-V1-GRAMMAR-C004](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C047](02-lexical-grammar.md), [IRIS-V1-COLLECTIONS-C051](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V315` | none | `normative` |
| `D-388` | A backslash must be immediately followed by LF, CRLF, or CR; lexer removes both. No spaces, comments, or tokens may intervene, otherwise it is a lexical error outside a literal. It applies to any expression/statement, including `"a" \` newline `"b"`. Automatic delimiter/incomplete-grammar continuation remains available without backslash. | [IRIS-V1-GRAMMAR-C007](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-COLLECTIONS-V356`, `IRIS-V1-GRAMMAR-V191` | none | `normative` |
| `D-389` | `str[i]` indexes Unicode scalars and returns a one-scalar String. Nonnegative indices count from start; negative from end (`-1` last). Out-of-range returns nil without mutation/structural side effect, so static type is `String?`. V1 introduces no separate Character type for this operation. Slice semantics remain separate. | [IRIS-V1-COLLECTIONS-C009](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C044](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C045](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V316` | none | `normative` |
| `D-390` | `str[range]` slices Unicode scalar positions. Negative endpoints resolve from end; effective boundaries then clamp to `[0, length]`. Range open/closed endpoints determine included scalar boundaries. Empty or start-after-end ranges return empty String rather than nil/error/reverse. Static result is String. Step/reverse slicing remains separate. | [IRIS-V1-COLLECTIONS-C010](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C046](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V317` | none | `normative` |
| `D-391` | String `[]` slice has no step/stride or reverse-range semantics. It accepts only unit-forward Range behavior; unsupported stepped/reverse forms are statically rejected when known or raise `ArgumentError` dynamically. Start-after-end still yields empty String. Use `reverse()` or iterator transformations for other traversal. | [IRIS-V1-COLLECTIONS-C010](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C046](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V318` | none | `normative` |
| `D-392` | Immutable identity-less String defines no standard `[]=` and cannot mutate in place. Functional APIs such as `replace_range` return new Strings. Efficient accumulation/editing uses separate mutable identity-bearing `MutableString` (future TextBuffer optional), which owns mutation/alias/concurrency semantics and converts to immutable String. The former design name `StringBuilder` is not the formal standard type. User-added String `[]=` cannot mutate intrinsic String scalar content. | [IRIS-V1-COLLECTIONS-C047](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V319` | none | `normative` |
| `D-393` | Each call returns immutable String content at that moment; later MutableString mutation never alters prior snapshots. Runtime may share rope/buffer storage with copy-on-write/immutable segments, but sharing is unobservable and eager deep copy is not required. `to_string` does not clear/freeze/mutate the MutableString. | [IRIS-V1-COLLECTIONS-C053](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V320` | none | `normative` |
| `D-394` | `value[i]` uses String's Unicode-scalar/negative-index/nil-out-of-range semantics. `value[i]=replacement` requires a one-scalar String, raises `IndexError` out of range, mutates in place, and returns Nil. `value[range]=replacement` uses clamped unit-forward scalar Range semantics, accepts any String, may change length, mutates in place, and returns Nil. Aliases observe mutation; prior String snapshots remain unchanged. | [IRIS-V1-COLLECTIONS-C054](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C040](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V321` | none | `normative` |
| `D-395` | Prefix `m` applies to every String literal family: escaped/interpolated `m"..."`, escaped non-interpolated `m'...'`, multiline triple forms, and raw forms with fixed prefix order `mr#"..."#` / `mr#"""..."""#` and matching fences. Content parsing/interpolation/dedent follows the corresponding immutable String literal rules, then yields a new identity-bearing MutableString on every evaluation, even for identical text. Runtime may construct directly without an intermediate String. `rm...` is invalid. | [IRIS-V1-GRAMMAR-C037](02-lexical-grammar.md), [IRIS-V1-COLLECTIONS-C052](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V322` | none | `normative` |
| `D-396` | MutableString is mutable/identity-bearing; `same?` compares identity. Built-in `==` compares current exact Unicode scalar content and is cross-type equal to immutable String with identical content. Built-in `hash` raises `InvalidKeyError`, so it is not a Hash key by default. `to_string` returns immutable snapshot; `inspect` returns a reparsable escaped `m"..."` form. Equal/hash contract scope remains separate. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C055](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V323` | none | `normative` |
| `D-397` | If `a == b` and both public hash calls succeed, results must match. Equality does not require an intentionally unhashable value to provide a hash. MutableString may equal String by content while MutableString hash raises `InvalidKeyError` and cannot be inserted as a key. User-added MutableString hashing must honor String-compatible equality and assumes mutation/rehash responsibility. | [IRIS-V1-COLLECTIONS-C004](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V324` | none | `normative` |
| `D-398` | Standard `MutableString#+(other: Object) -> MutableString` obtains text directly from String, snapshots MutableString content, or fully invokes other `to_string() -> String`, then returns a fresh MutableString identity without mutating receiver. Standard `MutableString#<<`/`append(other: Object) -> MutableString` use the same conversion but append in place and return receiver identity. Conversion completes before mutation; failure leaves receiver unchanged. Source text is snapshotted before mutation, making self/alias append deterministic. | [IRIS-V1-COLLECTIONS-C056](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C058](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V325` | none | `normative` |
| `D-399` | Under D-347, `target += other` reads target, calls `MutableString#+(Object)` including its `to_string` conversion, then writes the fresh MutableString back. For local/raw-ivar storage the result is that new value; for property/index targets the result is the setter result. Existing aliases to the old MutableString observe no append. Explicit `<<`/`append` is required for in-place mutation. Conversion failure prevents writeback. | [IRIS-V1-COLLECTIONS-C057](06-collections-text-regex.md), [IRIS-V1-CONTROL-C036](04-bindings-callables-control-flow.md) | `IRIS-V1-COLLECTIONS-V326` | none | `normative` |
| `D-400` | `+`, `<<`, and `append` evaluate/convert the operand exactly once. Non-text `to_string` must return String or raises `TypeContractError`; exceptions propagate. `<<`/append mutate only after the complete source String is available, so conversion failure leaves the receiver content unchanged. If source is receiver or an alias to it, source content is snapshotted before append, e.g. `s << s` duplicates the original once. Allocation/resource failure during commit follows ordinary ResourceError and must not expose partial appended scalar content. | [IRIS-V1-COLLECTIONS-C058](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C056](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V327` | none | `normative` |
| `D-401` | `clear() -> MutableString` empties receiver in place and returns the same identity. `replace(other: Object) -> MutableString` evaluates/converts other through String/MutableString snapshot/`to_string` exactly once, then atomically replaces all content and returns self. Conversion/allocation failure leaves previous content unchanged and propagates the error. Prior immutable snapshots remain unchanged. | [IRIS-V1-COLLECTIONS-C058](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C056](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V328` | none | `normative` |
| `D-402` | String transforms such as `upcase/downcase/normalize` return new immutable Strings. MutableString non-bang counterparts return fresh MutableStrings without mutating receiver; `upcase!/downcase!/normalize!` mutate receiver atomically and return self. Failure exposes no partial transformed content. `!` remains a naming convention generally, but the standard library follows this rule. | [IRIS-V1-COLLECTIONS-C059](06-collections-text-regex.md), [IRIS-V1-GRAMMAR-C010](02-lexical-grammar.md) | `IRIS-V1-COLLECTIONS-V329` | none | `normative` |
| `D-403` | Concurrent unsynchronized read/write or write/write is unsupported at the logical API level and may raise `ConcurrentMutationError`/be detected by runtime, but must never cause undefined behavior, memory corruption, invalid Unicode, or partial committed content. Each public mutation publishes complete old-or-new content atomically to correctly synchronized observers. Users coordinate via Mutex/actors/higher-level concurrency. Iterator interaction remains separate. | [IRIS-V1-COLLECTIONS-C060](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V330` | none | `normative` |
| `D-404` | Scalar/grapheme iterators capture a content version. Append, clear, replace, index/range assignment, bang transforms, or any content change increments it; next advance on mismatch raises `ConcurrentModificationError`, even if scalar length is unchanged. No remove/replace-current or weak traversal exists. Stable traversal uses an explicit immutable `to_string` snapshot. | [IRIS-V1-COLLECTIONS-C061](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C062](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V331` | none | `normative` |
| `D-405` | `for x in text` and default iterator produce one-scalar immutable String values in scalar order, matching `length`/`[]`. Index pairs require `each_with_index`/enumerate; graphemes/code points/bytes use explicit APIs. MutableString iterator follows fail-fast D-404 and still yields immutable scalar values. | [IRIS-V1-COLLECTIONS-C062](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C040](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V332` | none | `normative` |
| `D-406` | String/MutableString `bytes` and `graphemes` do not eagerly allocate Arrays. `bytes` yields Integer 0..255 for exact UTF-8 encoding; `graphemes` yields immutable String clusters. Consumers obtain iterators/pipelines or explicitly materialize via `to_array()`. MutableString views capture content version and fail-fast under D-404. Unicode grapheme-version binding remains separate. | [IRIS-V1-COLLECTIONS-C044](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C062](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V333` | none | `normative` |
| `D-407` | Iris language major version 1 uses Unicode 17.0.0 data for identifier XID tables, grapheme segmentation, normalization, Unicode properties, case mapping, and casefold. OS/runtime library/package/Host configuration cannot change default results. Changing default Unicode data is language-major breaking unless exposed through a separately explicit versioned Unicode API. | [IRIS-V1-COLLECTIONS-C042](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C078](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C080](06-collections-text-regex.md), [IRIS-V1-LIBRARY-C024](10-serialization-standard-library.md) | `IRIS-V1-COLLECTIONS-V042`, `IRIS-V1-COLLECTIONS-V334`, `IRIS-V1-COLLECTIONS-V354` | none | `normative` |
| `D-408` | `Bytes` is immutable/identity-less and may be stably hashed; `ByteArray` is mutable/identity-bearing and default-unhashable. Both contain bytes 0..255, support negative index reads/out-of-range nil; ByteArray writes range-check and out-of-range raise `IndexError`. `b"..."` / `br#"..."#` (and multiline counterparts) produce Bytes; `mb"..."` / `mbr#"..."#` produce a fresh ByteArray identity. Prefix order is mutable `m`, bytes `b`, optional raw `r`, fence/quotes; alternatives are invalid. Non-ASCII source text contributes UTF-8 bytes; escaped byte literal `\xNN` injects one raw byte. `ByteArray#to_bytes` returns immutable snapshot; `Bytes#to_byte_array` returns mutable copy with COW sharing permitted. Equality/hash details remain separate. | [IRIS-V1-COLLECTIONS-C067](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md), [IRIS-V1-GRAMMAR-C038](02-lexical-grammar.md) | `IRIS-V1-COLLECTIONS-V335` | none | `normative` |
| `D-409` | Built-in `==` compares exact current byte sequence and permits Bytes/ByteArray cross-type equality. Bytes is identity-less (`same?` error) and hashable; ByteArray is identity-bearing (`same?` identity) and built-in-unhashable. Equal-implies-same-hash applies only when both hash calls succeed. User-added ByteArray hashing must match Bytes equality and assumes mutation/rehash risk. | [IRIS-V1-COLLECTIONS-C068](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V336` | none | `normative` |
| `D-410` | Use exact ASCII derive-key context `Iris Language v1 stable bytes hash`; input is shortest ULEB128 byte length plus exact byte content. Digest reduces to first eight bytes little-endian Integer. Domain is distinct from String/Symbol even when byte sequences coincide. Hash containers may apply runtime-secret mixing. | [IRIS-V1-COLLECTIONS-C085](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C087](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C091](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V337` | none | `normative` |
| `D-411` | Bytes Range slice returns Bytes; ByteArray Range slice returns an independent ByteArray snapshot identity (COW allowed). Both use negative endpoints, clamped unit-forward boundaries, and empty result for empty/reverse. `ByteArray[range]=replacement` accepts Bytes|ByteArray, snapshots aliasing source, may change length, atomically replaces, returns Nil, and exposes no partial content. | [IRIS-V1-COLLECTIONS-C070](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C040](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V338` | none | `normative` |
| `D-412` | `Bytes + (Bytes|ByteArray) -> Bytes` allocates immutable result. `ByteArray + (Bytes|ByteArray) -> ByteArray` allocates fresh mutable identity without changing receiver. `ByteArray <<`/`append` mutate in place, snapshot aliases, return self, and commit atomically; `+=` uses `+` then rebinds. String/Object are never implicitly encoded or `to_string`-converted for binary operations. | [IRIS-V1-COLLECTIONS-C071](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C072](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V339` | none | `normative` |
| `D-413` | `String#to_bytes` and `MutableString#to_bytes` return immutable UTF-8 Bytes snapshots. `Bytes#to_string` strictly decodes UTF-8; invalid sequences raise `EncodingError`. ByteArray snapshots then decodes equivalently. Lossy replacement/ignore behavior requires explicit decoding API/options such as `decode_utf8(errors: :replace)` and is never default. General encodings remain separate. | [IRIS-V1-COLLECTIONS-C073](06-collections-text-regex.md), [IRIS-V1-LIBRARY-C021](10-serialization-standard-library.md), [IRIS-V1-LIBRARY-C026](10-serialization-standard-library.md) | `IRIS-V1-COLLECTIONS-V340`, `IRIS-V1-LIBRARY-V004`, `IRIS-V1-LIBRARY-V009` | none | `normative` |
| `D-414` | Language core guarantees UTF-8 source/text-binary convenience conversion only. Standard library exposes explicit Encoding objects (at least UTF-8, UTF-16LE/BE, Latin-1) with strict default errors and explicit replace/ignore options; packages may add encodings. String remains Unicode scalar content and never retains source encoding. OS locale/code page is never implicitly selected. | [IRIS-V1-COLLECTIONS-C074](06-collections-text-regex.md), [IRIS-V1-LIBRARY-C022](10-serialization-standard-library.md), [IRIS-V1-LIBRARY-C025](10-serialization-standard-library.md), [IRIS-V1-LIBRARY-C026](10-serialization-standard-library.md) | `IRIS-V1-COLLECTIONS-V341`, `IRIS-V1-LIBRARY-V004`, `IRIS-V1-LIBRARY-V009`, `IRIS-V1-LIBRARY-V010`, `IRIS-V1-LIBRARY-V011` | none | `normative` |
| `D-415` | Iris has no separate Function runtime kind. Every named `fun` defines a Method on its lexical owner. Top-level/module-level `fun` defines a Method on that executable module's unique identity-bearing `main` receiver. BoundMethod captures a receiver plus Method; Closure is anonymous lexical code/capture. BoundMethod and Closure share ordinary callable typing after binding, while unbound Method remains reflective and requires explicit receiver/binding. | [IRIS-V1-CONTROL-C014](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C021](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V333` | none | `normative` |
| `D-416` | All source files in one module share its unique `main` object. Top-level `fun f` creates a private Method on main, and top-level `f()` is a privileged implicit send to main. Importers cannot call private top-level helpers. `export fun` explicitly publishes a public Method through the module object/API. Runtime-wide or per-file main objects do not exist. | [IRIS-V1-CONTROL-C012](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C013](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V334` | none | `normative` |
| `D-417` | Named Methods use `fun name(parameters) -> Return { body }`; placement determines instance/Module/main ownership, while Class Methods use `class fun`. Closure/trailing-block syntax is `{ |parameters| -> ReturnType body }`; multiline body starts after header newline and single-line uses semicolon (`{ |x: String| -> Nil; print(x) }`). Empty parameters use `{ || -> R ... }` or formatter-standardized unambiguous shorthand. Closure return annotation may be omitted when expected callable context uniquely supplies a closed type. Method annotation omissions follow D-452 and become `Dynamic<Object>`. No second arrow/lambda syntax exists. | [IRIS-V1-GRAMMAR-C049](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C051](02-lexical-grammar.md), [IRIS-V1-CONTROL-C015](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C016](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V007`, `IRIS-V1-CONTROL-V335` | none | `normative` |
| `D-418` | Declaration order is required positional; optional positional (`x: T = expr`); at most one positional rest (`*args: T`); required keyword-only (`key name: T`); optional keyword-only (`key name: T = expr`); at most one keyword rest (`**kwargs: V`); and trailing-block binding last (`&block: (P...) -> R`, optionally `= nil`). Calls spell keywords `name: value`. Iris has no positional-only/keyword-only separator tokens and no overloads. | [IRIS-V1-CONTROL-C022](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C023](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C027](04-bindings-callables-control-flow.md), [IRIS-V1-GRAMMAR-C053](02-lexical-grammar.md) | `IRIS-V1-CONTROL-V008`, `IRIS-V1-CONTROL-V336` | none | `normative` |
| `D-419` | Defaults evaluate on each invocation left-to-right and may reference receiver, module scope, and earlier parameters but not later ones. Parameter bindings are immutable. Rest arguments form a fresh `Array<T>` and keyword rest a fresh `Hash<Symbol,V>`; bindings cannot be rebound but container values retain ordinary mutability. Omitted optional block binds nil. Arity/category/Contract failures raise `ArgumentError`/`TypeError` after selector resolution. | [IRIS-V1-CONTROL-C024](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C025](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V337` | none | `normative` |
| `D-420` | Closures capture binding cells, not value snapshots. Immutable captures are read-only; explicitly mutable bindings are shared so outer/Closure updates agree. Closure captures its current receiver and receiver-name ivar access; receiver is not rebindable. Named Methods never capture dynamic body locals; explicit Closure installation is required. | [IRIS-V1-CONTROL-C028](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C029](04-bindings-callables-control-flow.md), [IRIS-V1-RUNTIME-C072](03-runtime-object-model.md) | `IRIS-V1-CONTROL-V006`, `IRIS-V1-CONTROL-V338` | none | `normative` |
| `D-421` | `return` inside a Closure ends only that Closure invocation. `break`/`continue` cannot target loops outside a Closure call boundary and are compile errors in that use. V1 has no Proc/lambda split, nonlocal-return Closure, or LocalJumpError model. | [IRIS-V1-CONTROL-C020](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C031](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V024`, `IRIS-V1-CONTROL-V037`, `IRIS-V1-CONTROL-V038`, `IRIS-V1-CONTROL-V339` | none | `normative` |
| `D-422` | Normal fallthrough returns the final expression; empty body returns nil. `return expr` exits early with expr and bare `return` returns nil. Every normal exit is checked against any explicit return Contract. Omitted annotation meaning belongs to the global type-default decision. | [IRIS-V1-CONTROL-C019](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C031](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V340` | none | `normative` |
| `D-423` | A trailing Closure is not a final positional argument and binds only to the final declared `&block` parameter. Existing Closure may be passed with `&closure`. A call has at most one block. Unexpected block raises `ArgumentError`; optional omission binds nil. The block's type is ordinary `(P...) -> R`; `&` marks call channel/binding. | [IRIS-V1-CONTROL-C030](04-bindings-callables-control-flow.md), [IRIS-V1-GRAMMAR-C050](02-lexical-grammar.md) | `IRIS-V1-CONTROL-V010`, `IRIS-V1-CONTROL-V341` | none | `normative` |
| `D-424` | Ordinary zero/nonzero positional/keyword calls use parentheses. Only property getter syntax omits them. A trailing block follows a completed call, including `method() { ... }`. This avoids ambiguity with control blocks, Hashes, named infix messages, and newline rules. | [IRIS-V1-GRAMMAR-C050](02-lexical-grammar.md), [IRIS-V1-CONTROL-C032](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V342` | none | `normative` |
| `D-425` | Bound callable types use `(P1, P2, ...) -> R`. Closure and compatible BoundMethod values assign using parameter contravariance, return covariance, and arity/mode/Contract compatibility. Unbound Methods use a distinct reified `Method<Self, (P...) -> R>`-style type and require explicit binding/receiver. Trailing block types are ordinary function types. | [IRIS-V1-CONTROL-C018](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C021](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V343` | none | `normative` |
| `D-426` | `let name[: T] = expr` declares immutable; `mut name[: T] = expr` or typed deferred `mut name: T` declares mutable. Bare `name = expr` only assigns an existing mutable binding and never implicitly declares, preventing typo-created locals. Parameters/catch bindings/iteration bindings default immutable. | [IRIS-V1-CONTROL-C003](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C007](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V001`, `IRIS-V1-CONTROL-V002`, `IRIS-V1-CONTROL-V005`, `IRIS-V1-CONTROL-V039`, `IRIS-V1-CONTROL-V040`, `IRIS-V1-CONTROL-V344` | none | `normative` |
| `D-427` | `let` requires initialization. `mut` may defer initialization only with explicit type and cannot be read before definite assignment; untyped/uninitialized mut is invalid. Annotation is mandatory runtime/static contract. Without annotation, initializer yields a precise local inferred type that remains fixed for later assignments and never auto-widens; use explicit union/Dynamic when needed. | [IRIS-V1-CONTROL-C004](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C005](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C007](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V001`, `IRIS-V1-CONTROL-V002`, `IRIS-V1-CONTROL-V003`, `IRIS-V1-CONTROL-V004`, `IRIS-V1-CONTROL-V039`, `IRIS-V1-CONTROL-V041`, `IRIS-V1-CONTROL-V345` | none | `normative` |
| `D-428` | Same lexical scope cannot redeclare a name. Nested scopes may shadow, with default lint warning. A new declaration's initializer resolves the outer binding because the new binding enters scope only after initialization. Closures capture the specific binding cell; later nested shadowing does not retarget capture. | [IRIS-V1-CONTROL-C010](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C028](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V346` | none | `normative` |
| `D-429` | `@name` is current-receiver raw dynamic ivar under D-309; `@@name` is a hierarchy-shared Class variable requiring explicit origin declaration; `$name` is a package-qualified runtime-global requiring explicit declaration. Sigils never create bindings merely by read/assignment except ordinary `@` dynamic slot creation governed by `instance_state`. Typed/property/Class-state alternatives remain available. | [IRIS-V1-CONTROL-C008](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C009](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C013](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V347` | none | `normative` |
| `D-430` | `class let @@x: T = expr` or `class mut @@x: T = expr` declares a slot owned by one logical Class and shared through its subclass hierarchy. Descendants resolve/read/write the nearest/unique declared ancestral slot but cannot redeclare/shadow the same `@@x`. Missing declaration is static error or dynamic access error. Let/mut/type/default initialization follows binding/Class initialization rules. | [IRIS-V1-RUNTIME-C073](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C074](03-runtime-object-model.md), [IRIS-V1-CONTROL-C008](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V348`, `IRIS-V1-RUNTIME-V091` | none | `normative` |
| `D-431` | Any package may declare `global let/mut $name[: T] = expr`; true identity is `(package_id, $name)`, unique within package and separately instantiated per IrisRuntime. Globals default package-private; `export global` plus explicit direct import/alias exposes them. No flat cross-package namespace, auto-merge, or process-global storage exists. Initialization follows package/module initialization transactions. | [IRIS-V1-CONTROL-C008](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C013](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V350` | none | `normative` |
| `D-432` | `const Name: T = expr` is immutable and evaluates during module initialization. Class, Module, Contract, Type aliases, and constants occupy one qualified namespace; kind/name collision is error. `A::B` resolves B visibly within A; leading `::A` starts at current package root. Unqualified resolution is lexical scope, current module/package declarations, then explicit imports; it never guesses runtime globals. | [IRIS-V1-CONTROL-C011](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C013](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V351` | none | `normative` |
| `D-433` | Identifier/call resolution first checks lexical bindings, constants/types, and imports. If a syntactic call `name(...)` has no callable binding, it sends ordinary `name` to current self/main and may reach `method_missing`. A non-call bare unresolved `name` raises `NameError`; it does not implicitly read a property/Method. Property access requires property syntax on an explicit/otherwise syntactically established receiver. Resolution never depends on whether an open-added Method currently exists. | [IRIS-V1-CONTROL-C011](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C013](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V352` | none | `normative` |
| `D-434` | Braced blocks, conditional branches, catch bodies, loops, and Closures create lexical scopes according to grammar. A `for` iteration creates a fresh immutable iteration binding cell each pass, so escaping Closures capture distinct values rather than a single final loop variable. Outer mutable cells remain shared when explicitly captured. Loop bindings do not leak after the loop. Class/Module executable-body scopes follow D-225. | [IRIS-V1-CONTROL-C010](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C045](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V022`, `IRIS-V1-CONTROL-V353` | none | `normative` |
| `D-435` | Every Class object is an instance of Class and may own ordinary receiver-name `@x` slots; `A` and subclass Class object `B` have independent `@x` state, with no inheritance sharing beyond ordinary messages. `@@y` is not an ivar/property of A's Class object: it is a separately declared hierarchy binding cell/metadata entity anchored to logical Class A and resolved/shared by A's subclass hierarchy under D-430. B may own its own Class-object `@x` but cannot shadow/redeclare A's `@@y`. Reflection, storage, lifecycle, policy, and serialization distinguish these categories. | [IRIS-V1-RUNTIME-C045](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C075](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C076](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V091` | none | `normative` |
| `D-436` | Instance and Class Methods lexically owned by a declaring Class or its subclasses may access the hierarchy's declared `@@name` cell directly. Lookup follows the Method's immutable static lexical Class hierarchy and does not change with runtime-superclass revisions. External `obj.@@x`/`A.@@x` syntax is invalid; public access uses Methods/properties. Composed Module lookup follows its host composition context. | [IRIS-V1-RUNTIME-C074](03-runtime-object-model.md), [IRIS-V1-CONTROL-C013](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V349`, `IRIS-V1-RUNTIME-V091` | none | `normative` |
| `D-437` | Syntax is `if condition { ... } else if condition { ... } else { ... }`. Each condition invokes `to_bool` once. Result type is normalized union of reachable final-expression branch types. Missing else contributes an implicit nil branch. Explicit predicates drive narrowing; arbitrary `to_bool` does not. | [IRIS-V1-CONTROL-C041](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C010](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V016`, `IRIS-V1-CONTROL-V354` | none | `normative` |
| `D-438` | `while condition { body }` performs one `to_bool` per iteration. Counting uses Range+for; no repeat/loop-if syntax. Natural or zero-iteration completion returns nil. `break expr` returns expr as loop result; bare break returns nil. Static loop type is union of reachable break values and natural Nil. `continue` has no value. | [IRIS-V1-CONTROL-C043](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C049](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V017`, `IRIS-V1-CONTROL-V018`, `IRIS-V1-CONTROL-V019`, `IRIS-V1-CONTROL-V355` | none | `normative` |
| `D-439` | Iterable yields an Iterator; loop calls `next() -> Iteration<T>`, exits on done, binds yielded values in a fresh immutable per-iteration scope, and closes Iterator on every exit. Destructuring mismatch raises `PatternMatchError` and closes. It does not lower to arbitrary `each`. `_` discards. | [IRIS-V1-CONTROL-C044](04-bindings-callables-control-flow.md), [IRIS-V1-COLLECTIONS-C011](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C012](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V342`, `IRIS-V1-CONTROL-V020`, `IRIS-V1-CONTROL-V021` | none | `normative` |
| `D-440` | `outer: while ...` / `outer: for ...`; `break outer: expr` and `continue outer` target that lexical loop, while unlabeled forms target nearest. The colon after the label makes labeled break syntactically distinct from unrestricted `break expr`; parsing never consults declared labels or a symbol table. Labels cannot duplicate in overlapping scope, label non-loops, or cross Closure boundaries. Labeled break values contribute to target loop result type. | [IRIS-V1-GRAMMAR-C053A](02-lexical-grammar.md), [IRIS-V1-CONTROL-C048](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C049](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V023`, `IRIS-V1-CONTROL-V024`, `IRIS-V1-CONTROL-V356`, `IRIS-V1-GRAMMAR-V013` | none | `normative` |
| `D-441` | `match value { pattern [if guard] => expression-or-block, ..., else => ... }` is source-order first-match with no fallthrough. Patterns: literal, nil/Bool, nominal `is T` with binding, union alternatives, Tuple/fixed-or-rest Array destructuring, binding, `_`; no arbitrary object deconstruction, regex, or user pattern protocol. Bindings immutable; guard uses `to_bool`, false discards bindings and continues. Alternatives bind identical names with compatible merged types. Match must be provably exhaustive or contain else; Dynamic/open-ended domains require else. Result is branch union. For destructuring uses only binding/Tuple/Array/rest/_ subset; mismatch raises `PatternMatchError`. Legacy switch/when is removed. | [IRIS-V1-CONTROL-C050](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C051](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C052](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C054](04-bindings-callables-control-flow.md), [IRIS-V1-GRAMMAR-C053B](02-lexical-grammar.md) | `IRIS-V1-CONTROL-V025`, `IRIS-V1-CONTROL-V026`, `IRIS-V1-CONTROL-V357` | none | `normative` |
| `D-442` | `A.new(args...)` is a Class-object Method that allocates a complete memory-safe instance, invokes its dynamic `initialize(args...)`, ignores/validates its Nil result, and returns the instance on success. Object provides default zero-argument initialize. Classes may override `class fun new`; standard allocation remains conceptually allocate+initialize. Iris has no Class-named constructor syntax or separate constructor overload system. | [IRIS-V1-RUNTIME-C054](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C055](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V083` | none | `normative` |
| `D-443` | If initialize raises, `new` propagates and returns no instance, but allocated object remains memory-safe/normal. Escaped self remains an ordinary instance; runtime does not poison, revoke, scan references, auto-close, run special finalization, or undo state/external effects. Cleanup is user responsibility through `try/finally`, `using`, explicit `close`, or compensation. | [IRIS-V1-RUNTIME-C058](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C056](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V026` | none | `normative` |
| `D-444` | Class instance Methods, Class-object singleton Methods, Module Methods, properties, and module-main Methods default private. Visibility prefixes are per declaration; no stateful sections. Contract requirements are public by default, with explicit protected requirements under D-457. `export fun` publishes public module API. `initialize` remains private. Modifier order is formatter-defined. | [IRIS-V1-RUNTIME-C077](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C078](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C079](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C080](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V088` | none | `normative` |
| `D-445` | `property name: T [= initializer] { get; private/protected/public set; ... }` creates revision-level typed instance storage and generated/default property Method selectors, with explicit accessor visibility (default private). Custom `property fun name()`/`name=(...)` may compatibly replace generated bodies. Stored-property initializer executes per instance under D-446. Contract exposure is through property Methods, never raw storage. Shape/property capabilities govern metaprogramming. | [IRIS-V1-RUNTIME-C065](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C076](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V092` | none | `normative` |
| `D-446` | After allocation, property initializers execute superclass-to-subclass and declaration order, with definite-initialization checks for later slots. Then only the final dynamic `initialize(args...)` is invoked. Parent initialize logic runs only via explicit `super(...)`; runtime never auto-calls every initializer. Initialization errors follow D-443. | [IRIS-V1-RUNTIME-C057](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C054](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V092` | none | `normative` |
| `D-447` | `super(args..., key..., &block)` calls the same complete selector after the current Method lexical owner in the receiver's current MRO (or Class-object singleton Method chain). Bare super/implicit argument forwarding is illegal; explicit splats forward. No successor raises `NoSuperMethodError` and never invokes `method_missing`. Qualified Contract Method super continues within its explicit qualified slot/MRO rules. | [IRIS-V1-RUNTIME-C081](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C014](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C044](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V093` | none | `normative` |
| `D-448` | `alias_method :new, :old` creates another slot referencing the same Method identity. `remove_method :x` removes only current-owner local slot and may expose ancestor implementation. `undef_method :x` installs a tombstone blocking ancestor lookup; an ordinary call then counts as absent and may invoke `method_missing`. All require `method_set`, explicit override intent where applicable, and open transaction atomicity. Retained Method objects survive. | [IRIS-V1-RUNTIME-C037](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C012](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C013](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V094` | none | `normative` |
| `D-449` | Origin names and constants are immutable qualified bindings. Ordinary or meta code cannot rebind them to another identity; package upgrade evolves stable logical identities via revisions. Alias locals may reference Class objects, but no `rebind_const` exists in v1. This preserves imports, static annotations, nominal hashes, and reflection identity. | [IRIS-V1-RUNTIME-C010](03-runtime-object-model.md), [IRIS-V1-CONTROL-C011](04-bindings-callables-control-flow.md), [IRIS-V1-CONTROL-C013](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V358`, `IRIS-V1-RUNTIME-V111` | none | `normative` |
| `D-450` | Capability vocabulary includes `subclass`. `class A ... meta deny subclass` prevents A from being selected as declared or runtime superclass; attempts fail with `MetaCapabilityError` through the unified meta boundary. Deny propagates/accumulates under ordinary capability rules and Contracts/Modules may require it. V1 has no separate `final` or `sealed` Class keyword. | [IRIS-V1-RUNTIME-C154](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C155](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V108` | none | `normative` |
| `D-451` | Every language Class is an object/instance of Class. `class fun` installs a singleton Method on that specific Class object. Sending to a Class object checks its singleton Methods, then corresponding logical runtime-superclass Class objects in order, then ordinary instance Methods supplied by Class; `super(...)` continues that chain. Class-object `@x` remains that object's own ivar. V1 exposes no separate metaclass declaration syntax, though reflection may model the chain. | [IRIS-V1-RUNTIME-C043](03-runtime-object-model.md), [IRIS-V1-RUNTIME-C044](03-runtime-object-model.md) | `IRIS-V1-RUNTIME-V095` | none | `normative` |
| `D-452` | Every omitted parameter or return annotation, regardless of public/protected/private/top-level status, has static/runtime contract `Dynamic<Object>`. Compiler may infer local facts inside a body for diagnostics/JIT but never exports them as signature metadata. Stable public promises arise only from explicit annotations. This avoids body/open changes silently altering callable ABI/contracts. | [IRIS-V1-TYPES-C003](05-types-contracts-generics.md), [IRIS-V1-TYPES-C007](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V253` | none | `normative` |
| `D-453` | Bare Dynamic is `Dynamic<Object>`. Entering `Dynamic<T>` semantically checks value satisfies reified T; thereafter arbitrary ordinary selectors may be sent without static member validation, while qualified Contract dispatch still requires explicit view/`..`. `T -> Dynamic<T>` is safe. Dynamic<S> widens safely to Dynamic<T> when S <: T; narrowing or conversion back to static T performs runtime guard. Dynamic never disables parameter/return/property/native boundary contracts. | [IRIS-V1-TYPES-C014](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V254` | none | `normative` |
| `D-454` | `value is T` returns Bool after checking nominal Classes, current runtime ancestry, Contracts, normalized unions/intersections, closed invariant generic arguments, callable/value/meta types, and other reified type forms; it drives true/false flow narrowing by intersection/exclusion. `value as T` preserves the same underlying object/value and raises `TypeError` on failure; impossible casts are compile errors and proven casts eliminate guards. `as Contract` produces a Contract view. `as? T` returns `T?`, yielding nil on failed runtime check; statically impossible safe casts are diagnosed, not silently encouraged. Casts never perform data conversion/copy and cannot cross invariant generic arguments. | [IRIS-V1-TYPES-C017](05-types-contracts-generics.md), [IRIS-V1-TYPES-C035](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V255` | none | `normative` |
| `D-455` | `type Name<T...> = TypeExpr [where ...]` creates no nominal/runtime wrapper identity; all uses expand/normalize to the target Type and reflect canonical identity. Generic aliases are allowed. Direct/indirect recursive aliases, including guarded container recursion, are compile errors in v1; recursive data uses nominal Classes/containers. Alias cycles never create equi-recursive types. | [IRIS-V1-TYPES-C015](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V256` | none | `normative` |
| `D-456` | Every normalized reified Type expression has an interned identity-bearing Type object. `same?`/`==` compare canonical Type identity; public hash is stable for publishable named/composite Types via package nominal IDs and domain-separated member hashes, runtime-local for local anonymous identities. Class/Contract/Module objects expose related `.type` metadata, but Type is not itself the Class object, allowing union/intersection/Dynamic/callable/Never/NonNil/generic constructions to share one reflection API. | [IRIS-V1-TYPES-C074](05-types-contracts-generics.md), [IRIS-V1-TYPES-C017](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V257` | none | `normative` |
| `D-457` | Contract bodies may declare instance Methods/properties, Class-object Methods/properties, and generic Methods with where constraints. They contain no Method bodies/defaults, stored state, initializers, raw ivars, or private requirements. Requirements are public by default; explicit protected requirements are permitted for hierarchy contracts. Implementations use `impl`/qualified slots and compatibility rules. | [IRIS-V1-TYPES-C042](05-types-contracts-generics.md), [IRIS-V1-TYPES-C053](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V258` | none | `normative` |
| `D-458` | `raise`, bare re-raise, statically proven nonterminating loops, and calls declared `-> Never` have Never type; Never paths do not widen result unions and aid exhaustive match/definite-return analysis. A `-> Never` callable that returns normally violates its Contract (`TypeError` dynamically; compile error when provable). Statically unreachable statements receive default compiler warning, remain type-checked for diagnostics, and warning may be promoted to error by policy. | [IRIS-V1-TYPES-C080](05-types-contracts-generics.md), [IRIS-V1-TYPES-C010](05-types-contracts-generics.md) | `IRIS-V1-TYPES-V259` | none | `normative` |
| `D-459` | `[a,b,...]` creates identity-bearing mutable `Array<T>`, with T inferred as normalized union of element static types or constrained by expected type. Empty `[]` requires expected closed `Array<T>` context; otherwise use `Array<T>.new()`, never implicit Dynamic. Negative indices count from end; out-of-range read returns nil without growth; out-of-range scalar write raises `IndexError`; append/insert explicitly grow. Generic Array remains invariant. | [IRIS-V1-COLLECTIONS-C023](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V343` | none | `normative` |
| `D-460` | `array[range]` returns independent Array snapshot using negative/clamped/unit-forward semantics. `array[range]=iterable` atomically materializes/type-checks replacement, may change length, returns Nil, and leaves array unchanged on failure. Built-in `==` compares elements in order through dynamic equality, `same?` compares identity, and hash raises `InvalidKeyError`. Any Array element replacement or structural mutation increments content version and causes active iterators to fail-fast. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C040](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V344` | none | `normative` |
| `D-461` | `%{}` is empty Hash literal. Every key position is an ordinary expression; bare identifier means its binding, never implicit Symbol (`:name` must be explicit). K/V infer independently as normalized unions or expected `Hash<K,V>` context; empty literal requires expected type or explicit `Hash<K,V>.new()`. Key must be hashable under existing rules. | [IRIS-V1-COLLECTIONS-C027](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V345` | none | `normative` |
| `D-462` | `hash[key] -> V?` returns nil on absence without insertion. `hash[key]=value` enforces K/V contracts, updates/inserts, and standard setter returns Nil. `fetch(key)` raises `KeyError` when absent or uses an explicit trailing-block/default form. `delete(key) -> V?` returns old value or nil. Hash iteration/order/rehash/fail-fast semantics remain those frozen in D-116-D-125: structural changes fail-fast, existing-value update is allowed and not-yet-yielded entries expose latest value. | [IRIS-V1-COLLECTIONS-C029](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C040](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V346` | none | `normative` |
| `D-463` | `a ..= b` is inclusive end; `a ..< b` excludes end. These tokens are binary Range operators distinct from prefix/view-call `..member` by grammar context. V1 literal endpoints are Integer only; general Range construction uses explicit APIs. Fully open/left-open intervals use constructors, not additional literal tokens. | [IRIS-V1-COLLECTIONS-C006](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C007](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V347` | none | `normative` |
| `D-464` | Default step is +1 when end >= start and -1 when end < start. Equal endpoints yield one value for closed and empty for half-open. `.by(step: Integer)` permits nonzero steps whose sign moves toward end; zero/wrong direction raises `RangeError`. Iteration respects endpoint openness and may skip endpoint when stride does not land exactly. Range is immutable/identity-less; equality/hash include endpoints, openness, and step. | [IRIS-V1-COLLECTIONS-C038](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V348` | none | `normative` |
| `D-465` | Literals are `(a,b)`, singleton `(a,)`, empty `()`, with reified `Tuple<T...>` type. Equality compares elements in order; hash succeeds iff all element hashes succeed and is stable/domain-separated; `same?` raises IdentityError. Integer index supports negatives and nil out of range. Tuples support returns, patterns, and Hash `(key,value)` iteration. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C021](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V349` | none | `normative` |
| `D-466` | `contract Iterable<T> { fun iterator() -> Iterator<T> }`; `contract Iterator<T> { fun next() -> Iteration<T>; fun close() -> Nil }`. Iteration is the reified built-in generic yield/done value under D-127-D-133. Iterator is mutable identity-bearing and closeable. `remove_current` is not in base Iterator Contract and remains concrete HashIterator capability. `for` uses these exact Contracts. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C011](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V350` | none | `normative` |
| `D-467` | Array yields T; Hash yields `Tuple<K,V>`; Bytes/ByteArray yield byte Integer; String/MutableString yield one-scalar String. Immutable values iterate stably. Array/ByteArray/MutableString content mutation causes fail-fast; Hash preserves its earlier exception allowing existing-value update during traversal while structural mutation fails-fast. Iterators remain lazy, strongly retain source until done/close, and use Iteration results. | [IRIS-V1-COLLECTIONS-C003](06-collections-text-regex.md), [IRIS-V1-COLLECTIONS-C040](06-collections-text-regex.md) | `IRIS-V1-COLLECTIONS-V351` | none | `normative` |
| `D-468` | `defer` is not a keyword or cleanup mechanism in the revived v1 language. Resource cleanup is expressed through explicit `try/finally`, iterator automatic close under the already-frozen traversal protocol, and explicit resource `close` calls/standard-library helpers. The earlier revival-review mention of defer/scope guard was only a candidate recommendation and is intentionally not adopted. Owner correction: 2026-07-24. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V359` | none | `normative` |
| `D-469` | `try {}` must be followed by at least one catch or finally; zero or more source-order catches precede optional final finally. Typed forms are `catch error: Type [, context] {}`, discard forms `catch _: Type, context`, and catch-all `catch [error [, context]] {}`/`catch {}` last. Raise forms are `raise value`, `raise value from ExceptionContext|nil`, and bare `raise`. Existing matching/binding/value/finally/cause rules apply. | [IRIS-V1-CONTROL-C068](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V360` | none | `normative` |
| `D-470` | Standard `contract Closeable { fun close() -> Nil }`. Standard ordinary Method/helper `using(resource: Closeable, &block)` invokes block, then closes through try/finally, returns block result, and combines block/close failures with existing primary+suppressed rules. Users may always write explicit try/finally. `using` is not a keyword/control construct. | [IRIS-V1-ASYNC-C057](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C032](07-async-resources-diagnostics.md) | `IRIS-V1-ASYNC-V014`, `IRIS-V1-ASYNC-V015`, `IRIS-V1-ASYNC-V016`, `IRIS-V1-ASYNC-V027`, `IRIS-V1-ASYNC-V079` | none | `normative` |
| `D-471` | Repeated `close()` calls succeed with no additional effect and return Nil; standard resources expose state separately if needed. This makes using, iterator cleanup, nested finally, and error paths safely composable. An implementation violating idempotence violates Closeable Contract. Close may still raise on the first actual cleanup failure; subsequent semantics must not duplicate released-resource side effects. | [IRIS-V1-ASYNC-C031](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C057](07-async-resources-diagnostics.md) | `IRIS-V1-ASYNC-V027`, `IRIS-V1-ASYNC-V080` | none | `normative` |
| `D-472` | V1 defines no Task/thread cancellation type, cancellation points, masking, or asynchronous interruption. Host cannot inject arbitrary asynchronous unwinding as language cancellation. Cooperative flags remain library/application patterns. A future cancellation model must preserve finally, using, and Iterator close, but no current `CancellationError` Contract exists. | [IRIS-V1-IDENTITY-C033](01-language-identity.md), [IRIS-V1-ASYNC-C017](07-async-resources-diagnostics.md) | metadata-only: explicit DEFERRED V1 cancellation has no executable v1 behavior | none | `DEFERRED V1` |
| `D-473` | Public typed getters are `value: Object`, `cause: ExceptionContext?`, `suppressed: ReadonlyArray<ExceptionContext>`, `original_stack: ReadonlyArray<StackFrame>`, `re_raise_sites: ReadonlyArray<RaiseSite>`, and `raise_location: SourceLocation`; collections/records are runtime-owned immutable views/values. Runtime protected channels remain authoritative despite dynamic getter replacement. Discarded contexts from finally override are emitted only through runtime diagnostic/event stream, not attached as an artificial context edge. | [IRIS-V1-CONTROL-C065](04-bindings-callables-control-flow.md) | `IRIS-V1-CONTROL-V361` | none | `normative` |
| `D-474` | A file may declare/open multiple Modules. Outside Module blocks only package/file imports, exports/re-exports, and package-level declarative metadata are allowed; ordinary executable statements and `fun` definitions must belong to a Module body/main receiver. Ownership/ReflectionPolicy/initialization therefore always has a Module context and does not depend on paths. | [IRIS-V1-META-C011](08-modules-metaprogramming.md) | `IRIS-V1-META-V415` | none | `normative` |
| `D-475` | Within a package, a qualified Module has one `module Name` origin declaration. Additional cross-file/same-file extension uses `open module Name`, never implicit merging of multiple origins. Files may contain multiple different origins/opens. This gives one static spine/main/init owner and auditable transaction order. | [IRIS-V1-META-C012](08-modules-metaprogramming.md) | `IRIS-V1-META-V416` | none | `normative` |
| `D-476` | Supported forms include `import pkg::Module`, `import pkg::Module as Alias`, and `from pkg::Module import Name, Other as Alias`. Imports are fixed before executable Module body statements and introduce only named modules/items. `*` wildcard import is absent. Imports do not implicitly re-export or activate transitive static extensions. Name collisions require alias or fail. | [IRIS-V1-META-C013](08-modules-metaprogramming.md) | `IRIS-V1-META-V417` | none | `normative` |
| `D-477` | `export` marks Class/Module/Contract/type/const/global/fun/open-extension API. Facades use explicit `export import`/`export from`. Re-export exposes names/contracts but does not count as the direct import required to activate another module's exported open Class/Module static extension; consumers directly import that extension module. Internal public/private and package export remain distinct. | [IRIS-V1-META-C016](08-modules-metaprogramming.md) | `IRIS-V1-META-V418` | none | `normative` |
| `D-478` | Static dependencies initialize before dependents. One Module executes its origin then open blocks in manifest-declared source/block order; direct import source order determines compatible extension override order but not dependency-before-dependent. Dependency/initialization cycles are compile/link errors; no partially initialized/lazy cyclic Modules exist. Initialization failure marks Module/dependents failed and runtime does not automatically retry; explicit package reload/upgrade is required. | [IRIS-V1-META-C017](08-modules-metaprogramming.md) | `IRIS-V1-META-V419` | none | `normative` |
| `D-479` | Manifest includes package_id, api_major, package version, Iris language range/major, ordered source/module entries, dependencies, permissions, native artifacts, and entry modules. Dependencies use SemVer-compatible ranges respecting API-major identity. Resolver writes an exact lockfile with selected versions/artifact digests; runtime/build consumes the lock result and does not fetch/resolve “latest” implicitly. | [IRIS-V1-META-C010](08-modules-metaprogramming.md), [IRIS-V1-META-C003](08-modules-metaprogramming.md) | `IRIS-V1-META-V420` | none | `normative` |
| `D-480` | `permissions` requests names such as `reflection.inspect`, `filesystem.read`, `network.connect`, `native.load`, with required/optional and scope/config metadata. Default is deny and Host grants a subset; ungranted required requests prevent load. Core specifies request-vs-grant semantics and package/callable ownership, while concrete filesystem/network/etc capabilities belong to standard library/Host contracts. Packages cannot self-authorize. | [IRIS-V1-META-C008](08-modules-metaprogramming.md), [IRIS-V1-META-C009](08-modules-metaprogramming.md), [IRIS-V1-META-C010](08-modules-metaprogramming.md) | `IRIS-V1-META-V421` | none | `normative` |
| `D-481` | `import` never accepts runtime strings/values. Permission-controlled `Package.load(id/version...)` may dynamically load/initialize a package/module and returns a `Dynamic<Module>`-bounded handle or reflection metadata. It cannot retroactively add names/types/static extensions to the already compiled namespace; use Dynamic/reflection/declared Contracts. Runtime recompilation of caller is not implicit. | [IRIS-V1-META-C020](08-modules-metaprogramming.md) | `IRIS-V1-META-V422` | none | `normative` |
| `D-482` | Class/Module/Contract/Method/Type/Revision reflection returns read-only snapshots or lazy immutable views of identity-bearing metadata objects, never mutable internal Maps/tables. Default views include only caller-package-visible members; privileged `all_*`/raw metadata requires ReflectionPolicy inspect scope. Structural mutation always uses open/meta transactions. | [IRIS-V1-META-C096](08-modules-metaprogramming.md) | `IRIS-V1-META-V423` | none | `normative` |
| `D-483` | Class exposes name/package/type/static_spine/active_revision/runtime-superclass/MRO/Contracts/Modules/Methods/properties/MetaCapabilities/open; Module exposes name/type/composed Modules/Methods/properties/meta/open; Contract exposes name/type/parents/requirements/meta; Method exposes selector/owner/visibility/signature/source/package/bind/call; Type exposes kind/arguments/members/subtype?/assignable?; Revision APIs follow prior decisions. Convenience queries belong in standard library and v1 does not expose compiler AST as general reflection. | [IRIS-V1-META-C097](08-modules-metaprogramming.md), [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V424` | none | `normative` |
| `D-484` | `obj.respond_to?(selector, include_non_public: false)` checks current active ordinary MRO slots and caller visibility; it never invokes/consults `method_missing`. Optional explicit `respond_to_missing?` is a separate dynamic hint and does not change respond_to? truth. Qualified Contract slots use Contract-view-specific `respond_to_contract?`; ordinary and qualified namespaces never merge. | [IRIS-V1-META-C099](08-modules-metaprogramming.md) | `IRIS-V1-META-V425` | none | `normative` |
| `D-485` | Inside Class/Module `open`, v1 core operations include define Method/property, alias/remove/undef Method, include/remove Module, runtime `superclass=`, and class/shared state slot/value APIs. All mutate candidate state, declare capability requirements, enforce override/Contract rules, and return candidate target or immutable metadata as specified; no raw table access or eval-string primary mutation path exists. Declarative open may lower to the same operations. Contract has no open. | [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V426` | none | `normative` |
| `D-486` | Programmatic open additions carry reflection metadata including dynamic-only static visibility, origin package/source, revision and commit. They may be discovered, replaced, or removed through normal meta APIs. Runtime cannot promote them into already compiled static Type APIs. Static visibility requires a declarative/exported open extension artifact and direct consumer import; such an artifact may reuse a compatible implementation but changes compiler inputs, not runtime type facts retroactively. | [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V427` | none | `normative` |
| `D-487` | One IrisRuntime executes Iris tasks on a single cooperative scheduler/event loop and shared heap; task interleaving occurs only at await/scheduler boundaries. `async fun`/async Closure invocation starts synchronously and runs until first incomplete await or completion. Awaiting an already-complete Awaitable continues synchronously; incomplete await registers continuation and returns control. Native no-GIL shared-memory threads are deferred beyond v1, but implementation architecture must not require a permanent GIL and future semantics remain unspecified. | [IRIS-V1-ASYNC-C057](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C019](07-async-resources-diagnostics.md) | `IRIS-V1-ASYNC-V001`, `IRIS-V1-ASYNC-V004`, `IRIS-V1-ASYNC-V005`, `IRIS-V1-ASYNC-V006`, `IRIS-V1-ASYNC-V026`, `IRIS-V1-ASYNC-V081` | none | `normative` |
| `D-488` | Source `async fun f(...) -> T` declares the awaited result T while invocation type/reflection result is `Task<T>`. No-result async Methods use `T = Nil`, yielding `Task<Nil>`; there is no separate `Task`, void, or fire-and-forget signature. Ordinary `return` in async body supplies T. Task is identity-bearing and represents eventual completion. | [IRIS-V1-ASYNC-C010](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C057](07-async-resources-diagnostics.md) | `IRIS-V1-ASYNC-V001`, `IRIS-V1-ASYNC-V002`, `IRIS-V1-ASYNC-V003`, `IRIS-V1-ASYNC-V026`, `IRIS-V1-ASYNC-V082` | none | `normative` |
| `D-489` | `await expr` is legal only in async Method/Closure and requires `Awaitable<T>`; standard `Task<T>` implements it. A Task may be awaited multiple times and all awaiters observe the same immutable completion value or ExceptionContext. Async exceptions are captured in Task and repropagated on await with async stack linkage. Unobserved failed Tasks enter runtime diagnostic stream rather than disappearing. V1 has no cancellation semantics. | [IRIS-V1-ASYNC-C057](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C019](07-async-resources-diagnostics.md) | `IRIS-V1-ASYNC-V005`, `IRIS-V1-ASYNC-V007`, `IRIS-V1-ASYNC-V008`, `IRIS-V1-ASYNC-V009`, `IRIS-V1-ASYNC-V010`, `IRIS-V1-ASYNC-V026`, `IRIS-V1-ASYNC-V083` | none | `normative` |
| `D-490` | `async` may annotate Methods and Closure headers. try/finally, using, ExceptionContext, and cleanup remain suspended/resumed correctly across await. `open`/revision transactions cannot await or escape under D-252. Ready continuations use deterministic FIFO enqueue order on the single runtime scheduler, while external IO completion ordering is not promised. No implicit blocking wait exists in Iris code; Host may drive/run the event loop through its API. | [IRIS-V1-ASYNC-C057](07-async-resources-diagnostics.md), [IRIS-V1-ASYNC-C062](07-async-resources-diagnostics.md) | `IRIS-V1-ASYNC-V006`, `IRIS-V1-ASYNC-V011`, `IRIS-V1-ASYNC-V013`, `IRIS-V1-ASYNC-V017`, `IRIS-V1-ASYNC-V018`, `IRIS-V1-ASYNC-V019`, `IRIS-V1-ASYNC-V022`, `IRIS-V1-ASYNC-V028`, `IRIS-V1-ASYNC-V084` | none | `normative` |
| `D-491` | C is the stable binary contract for runtime lifecycle, package load, calls, values, callbacks, extensions, and async posting. Rust provides ergonomic RAII/type-safe wrappers but its native ABI is not the compatibility contract. C++/other hosts consume C ABI or wrappers. Internal Rust/C++ objects, raw Iris pointers, layouts, and vtables never cross ABI. | [IRIS-V1-FFI-C006](09-native-host-ffi.md), [IRIS-V1-FFI-C003](09-native-host-ffi.md) | `IRIS-V1-FFI-V060` | none | `normative` |
| `D-492` | ABI exposes opaque runtime/context and IrisHandle identities managed in a runtime handle table. Handles strongly root values until explicit release; scoped/local handle frames bulk-release temporaries. Runtime destruction invalidates all handles. No raw managed object pointer escapes. Handles belong to one runtime and cannot be mixed or assumed numerically stable across runtimes/restarts. | [IRIS-V1-FFI-C032](09-native-host-ffi.md), [IRIS-V1-FFI-C006](09-native-host-ffi.md) | `IRIS-V1-FFI-V061` | none | `normative` |
| `D-493` | All APIs touching Iris handles/heap/Class/meta/scheduler run on the owning runtime event-loop thread. Native libraries may use worker threads for external work but cannot dereference/use Iris handles there. A thread-safe post/message API schedules data/completion onto runtime thread. Violations return thread-affinity error and never race the managed heap. Future no-GIL ABI requires a later ABI version. | [IRIS-V1-FFI-C006](09-native-host-ffi.md), [IRIS-V1-FFI-C012](09-native-host-ffi.md) | `IRIS-V1-FFI-V062` | none | `normative` |
| `D-494` | C ABI functions return IrisStatus and expose error/result through out handles, preserving raised Iris value and ExceptionContext graph. Native callbacks may create/raise Iris errors through ABI. Rust panic/C++ exception never crosses C ABI; wrappers catch/translate to NativeError where safe, while unrecoverable memory corruption may abort. Iris unwinding never longjmps across native frames. String-only error channels are insufficient. | [IRIS-V1-FFI-C054](09-native-host-ffi.md), [IRIS-V1-FFI-C021](09-native-host-ffi.md) | `IRIS-V1-FFI-V063` | none | `normative` |
| `D-495` | Native packages ship Iris-readable compile-time metadata for Classes/Contracts/Methods/properties/generics/MetaCapabilities and package identity, with digest/ABI requirements. Compiler imports it as static API. Runtime load verifies matching metadata/artifacts then binds implementations. Additional runtime-only registration creates dynamic-only members through open transactions/capabilities and never expands already compiled static APIs. | [IRIS-V1-FFI-C054](09-native-host-ffi.md), [IRIS-V1-FFI-C026](09-native-host-ffi.md) | `IRIS-V1-FFI-V064` | none | `normative` |
| `D-496` | Native-backed Classes register validated opaque payload descriptors/external handles with size/alignment/construct/drop/trace as applicable. Runtime controls allocation/lifetime; trace only reports managed handles/roots and drop runs at GC-safe context, cannot raise into Iris, and is final memory cleanup. Files/sockets/GPU/etc use idempotent Closeable for deterministic release; GC timing is not resource management. Raw extension-owned object-pointer conventions are prohibited. | [IRIS-V1-FFI-C030](09-native-host-ffi.md), [IRIS-V1-FFI-C032](09-native-host-ffi.md) | `IRIS-V1-FFI-V065` | none | `normative` |
| `D-497` | Native async Method obtains runtime-owned `Task<T>` completion source/opaque token. Worker threads perform external work and use only thread-safe post API with copied/owned data/status; runtime thread converts to Iris values/handles and completes Task exactly once. Duplicate completion errors; posts after runtime shutdown return closed status. Workers never resolve Task or touch heap directly. | [IRIS-V1-FFI-C037](09-native-host-ffi.md), [IRIS-V1-FFI-C016](09-native-host-ffi.md) | `IRIS-V1-FFI-V066` | none | `normative` |
| `D-498` | Runtime/extension negotiate required ABI major/minor/features and size-tagged structures/function tables. Major mismatch rejects load; compatible minors append fields/functions and use struct sizes/feature tests without reinterpreting old layouts. Symbol/Type/handle IDs are runtime-local unless specified. Rust wrapper crate pins ABI versions and supplies RAII handles, checked runtime ownership, panic barriers, and safe callback abstractions. | [IRIS-V1-FFI-C054](09-native-host-ffi.md), [IRIS-V1-FFI-C006](09-native-host-ffi.md), [IRIS-V1-FFI-C038](09-native-host-ffi.md), [IRIS-V1-FFI-C042](09-native-host-ffi.md) | `IRIS-V1-FFI-V067` | none | `normative` |
| `D-499` | Ordinary Iris scripts cannot directly use Host ABI, extension function tables, raw runtime handles, loader internals, or arbitrary native symbol APIs. All script-originated binary calls use the standard FFI subsystem under manifest/Host grants (at least `ffi.load`/`ffi.call` scopes). Runtime/package loader alone uses formal extension ABI. V1 FFI supports stable C ABI only; Rust/C++/other languages export C-compatible wrappers and representations. | [IRIS-V1-FFI-C006](09-native-host-ffi.md), [IRIS-V1-FFI-C054](09-native-host-ffi.md) | `IRIS-V1-FFI-V068` | none | `normative` |
| `D-500` | `FFI` is the standard namespace/service Class; `FFI.open(path, declarations: ...) -> FFI::Library` loads one authorized dynamic library. Library owns/pins native library lifetime, bound functions/callbacks/pointers, close state, and is idempotent Closeable. Multiple libraries are independent. Scripts call bound symbols as Methods on the Library object. | [IRIS-V1-FFI-C050](09-native-host-ffi.md), [IRIS-V1-FFI-C043](09-native-host-ffi.md) | `IRIS-V1-FFI-V069` | none | `normative` |
| `D-501` | `FFI.open(..., declarations: "lib.ffi")` loads verified declarations, or `library.bind(:symbol, (CType...) -> CType, options)` binds explicitly. Bound symbols become Library dynamic-only Methods with declared C calling convention, widths, encoding, ownership/lifetime, nullability, error/result, and callback metadata. Unbound symbols cannot be invoked and v1 exposes no signature-less `unsafe_call`. Sidecar and programmatic declarations have equivalent validation and Reflection metadata. | [IRIS-V1-FFI-C050](09-native-host-ffi.md), [IRIS-V1-FFI-C054](09-native-host-ffi.md) | `IRIS-V1-FFI-V070` | none | `normative` |
| `D-502` | Language core does not reflectively serialize arbitrary objects/ivars. Standard library supports JSON-compatible values and an Iris binary value format for primitives/immutable values/collections plus Classes explicitly participating through a `Serializable` Contract/representation. Class/Module/Method/Closure/Task/FFI pointers/native resources are not automatically serializable. ReflectionPolicy/private data is never bypassed. | [IRIS-V1-LIBRARY-C008](10-serialization-standard-library.md), [IRIS-V1-LIBRARY-C028](10-serialization-standard-library.md) | `IRIS-V1-LIBRARY-V001`, `IRIS-V1-LIBRARY-V002`, `IRIS-V1-LIBRARY-V003`, `IRIS-V1-LIBRARY-V008` | none | `normative` |
| `D-503` | Format has magic/version and compatibility metadata (including relevant language/Unicode/hash/schema versions), strict bounded lengths/counts, full input validation, and no trusted allocation from unvalidated sizes. It represents supported primitives, arbitrary Integer, Float widths/bits, String/Symbol/Bytes, Tuple/Array/Hash, and explicit Serializable representations. Exact byte/tag schema belongs to a dedicated format specification and is not further micro-designed in the language spec. | [IRIS-V1-LIBRARY-C007](10-serialization-standard-library.md), [IRIS-V1-LIBRARY-C008](10-serialization-standard-library.md), [IRIS-V1-LIBRARY-C009](10-serialization-standard-library.md), [IRIS-V1-CONFORMANCE-C044](12-conformance.md) | `IRIS-V1-CONFORMANCE-V010`, `IRIS-V1-LIBRARY-V005`, `IRIS-V1-LIBRARY-V006`, `IRIS-V1-LIBRARY-V007`, `IRIS-V1-LIBRARY-V008` | none | `normative` |
| `D-504` | V1 core/stable runtime packages include core object/type/Contract/reflection, collections/text/bytes/Regex, Async Task/event loop, IO/File/Path, Encoding/Unicode, JSON/IrisValue, FFI, Package, diagnostics/testing. HTTP, general networking protocols, cryptography APIs, databases, GUI, and similar facilities ship as separately versioned official standard packages, not language core ABI. Internal BLAKE3 use does not imply a public core crypto suite. | [IRIS-V1-LIBRARY-C028](10-serialization-standard-library.md), [IRIS-V1-LIBRARY-C032](10-serialization-standard-library.md) | `IRIS-V1-LIBRARY-V012`, `IRIS-V1-LIBRARY-V013`, `IRIS-V1-LIBRARY-V014` | none | `normative` |
| `D-505` | `/pattern/flags` creates Regex and supports `${expr}` interpolation by `to_string` then default Regex escaping; `r/.../flags` is raw/non-interpolated (fence details follow raw literal conventions). `text =~ regex -> Match?` returns immutable Match or nil; `text !~ regex -> Bool`. Regex equality/hash are by canonical pattern+flags and stable. | [IRIS-V1-GRAMMAR-C015](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md) | `IRIS-V1-COLLECTIONS-V042`, `IRIS-V1-COLLECTIONS-V352`, `IRIS-V1-COLLECTIONS-V353`, `IRIS-V1-COLLECTIONS-V354`, `IRIS-V1-COLLECTIONS-V355`, `IRIS-V1-GRAMMAR-V181`, `IRIS-V1-LIBRARY-V012`, `IRIS-V1-LIBRARY-V013` | none | `normative` |
| `D-506` | V1 specifies a deterministic linear-time-oriented engine language comparable to Rust-regex capabilities, excluding backreferences, lookbehind, and constructs requiring unbounded catastrophic backtracking. Flags are fixed/documented (`i`, `m`, `s`, `x`, Unicode behavior default/major-version-bound). Match is immutable and exposes full match, scalar/byte ranges, numbered/named captures without global match variables. Advanced PCRE-style engines belong in separate standard packages and use distinct types. | [IRIS-V1-GRAMMAR-C054](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C015](02-lexical-grammar.md) | `IRIS-V1-COLLECTIONS-V042`, `IRIS-V1-COLLECTIONS-V352`, `IRIS-V1-COLLECTIONS-V353`, `IRIS-V1-COLLECTIONS-V354`, `IRIS-V1-COLLECTIONS-V355`, `IRIS-V1-GRAMMAR-V181`, `IRIS-V1-LIBRARY-V012`, `IRIS-V1-LIBRARY-V013` | none | `normative` |
| `D-507` | High-to-low: primary/call/index/property/Contract-view `..`; exponent `**`; unary `+ - ~ !` with the already frozen exponent-vs-negation grammar; multiplicative `* /`; additive `+ -`; shifts `<< >>`; bitwise `&`; `^`; `|`; Range `..= ..<`; relational/type/regex `< <= > >= <=> =~ !~ is as as?`; equality `== !=`; named infix Method; `&&`; `||`; assignment/compound/logical assignment. D-022 named-infix placement and D-033 exponent rules remain authoritative. | [IRIS-V1-GRAMMAR-C040](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C041](02-lexical-grammar.md), [IRIS-V1-CONFORMANCE-C045](12-conformance.md) | `IRIS-V1-CONFORMANCE-V011` | none | `normative` |
| `D-508` | `**` and assignment families are right-associative; ordinary binary arithmetic/bitwise/logical/named-infix are left-associative as applicable. Range, relational, equality, `<=>`, regex match, `is`, `as`, and `as?` are non-associative and cannot chain (`a<b<c`, `a==b==c`, chained Range/casts require parentheses/`&&`). Call/member/index chains associate left through primary parsing. | [IRIS-V1-GRAMMAR-C041](02-lexical-grammar.md), [IRIS-V1-CONFORMANCE-C045](12-conformance.md) | `IRIS-V1-CONFORMANCE-V012` | none | `normative` |
| `D-509` | Reserved words are: `class module contract open extends for mixin where meta deny public protected private override impl property shared key async await fun let mut const global import from as export type if else while in break continue match try catch finally raise return is nil true false self super`. Legacy/unused `and or not repeat switch when groan order serve ignore defer implements satisfies interface goto retry redo static alias undef` are not keywords/reserved solely for history; alias/undef/remove functionality uses Method APIs. `new`, `using`, `close`, and `method_missing` remain ordinary Method names. | [IRIS-V1-GRAMMAR-C013](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C014](02-lexical-grammar.md), [IRIS-V1-MIGRATION-C009](11-migration-divergence.md) | `IRIS-V1-GRAMMAR-V010`, `IRIS-V1-GRAMMAR-V183` | none | `normative` |
| `D-510` | Lexer prefers `..=`/`..<` over `..`, `!=` over `!`+`=`, and other longest fixed operators. Parser recognizes `..identifier` as Contract-view call in primary/postfix context, `::` as qualified name/declaration slot, `=>` as match arm, `%{` as Hash literal opener, and `${` only inside interpolated String. `as?` parses as cast operator; method `?`/`!` suffix and property setter selectors (including `name!=`) use declaration/property contexts without changing expression token meanings. | [IRIS-V1-GRAMMAR-C017](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C018](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C019](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C020](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C021](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C022](02-lexical-grammar.md), [IRIS-V1-GRAMMAR-C023](02-lexical-grammar.md) | `IRIS-V1-GRAMMAR-V011`, `IRIS-V1-GRAMMAR-V184` | none | `normative` |
| `D-511` | Syntax `@decorator(args)` precedes Class, Module, Contract, Method, or property declarations. A decorator transforms the declaration candidate/metadata rather than replacing its nominal identity or kind. Multiple decorators execute in written top-to-bottom order. Result remains the same typed declaration category. V1 decorators are neither unrestricted Python replacement functions nor AST macros. | [IRIS-V1-META-C086](08-modules-metaprogramming.md), [IRIS-V1-META-C085](08-modules-metaprogramming.md) | `IRIS-V1-META-V428` | none | `normative` |
| `D-512` | Compiler/linker resolves each directly imported decorator and executes/validates a pure deterministic static planning phase that may contribute compiler-visible metadata/API transformations. Runtime construction/open applies its runtime transform inside the declaration's candidate transaction. Runtime-dependent/conditional programmatic decoration is dynamic-only and cannot alter already compiled static API. Static plan cannot depend on IO, time, random, ambient runtime state, or untracked environment. | [IRIS-V1-META-C087](08-modules-metaprogramming.md) | `IRIS-V1-META-V429` | none | `normative` |
| `D-513` | Decorators carry no inherent structural authority. Metadata attachment and compatible body wrapping use declared operation requirements (`method_body`, `property_body`, etc.); generated/removal changes require corresponding set/shape/state capabilities. They cannot change declaration kind/nominal identity, immutable superclass bound, Contract set/requirements, generic arity/invariance, package identity, or other forbidden static-spine facts. Contract decorators cannot hide/add required obligations outside explicit Contract source/static API. Target policy and caller/decorator package permissions all remain enforced. | [IRIS-V1-META-C081](08-modules-metaprogramming.md) | `IRIS-V1-META-V430` | none | `normative` |
| `D-514` | Built-in typed generic Decorator Contracts receive immutable declaration metadata plus a controlled transform context and return same-kind candidate transformations. Static phase is pure/deterministic; runtime phase is synchronous/non-awaiting and may execute code, but external side effects are not rollbackable and remain author responsibility. Origin/open/hot-upgrade rebuilds reapply declaration decorators in source order to new candidates; any transform/validation failure aborts the whole transaction. Reflection exposes ordered decorator identity, arguments, package/source, phases, and permission-filtered generated diff. | [IRIS-V1-META-C092](08-modules-metaprogramming.md) | `IRIS-V1-META-V431` | none | `normative` |
