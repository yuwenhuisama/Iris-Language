fun f(x) {
	;return (x**3 - 2*x - 5)
}

fun abs(x) {
	if(x < 0) {
		;return -x
	} else {
		;return x
	}
}

;a = 2.0
;b = 2.2
;c = 0.0
;d = c - b
;print("k\tk-1\tk+1\n")
if(abs(d) > 0.0005, 0) {
	;c = b - f(b) * (b - a) / (f(b) - f(a))
	;print(b, "\t", a, "\t", c, "\n")
	;d = c - b
	;a = b
	;b = c
}