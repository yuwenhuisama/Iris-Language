# Iris v1 Frozen Spec Defects

The frozen spec must never be edited. Defects are recorded here only.

Standing rule: chapter tables are authoritative for vector content, and chapter 12 is authoritative for record schema shape.

| defect ID | spec artifact | exact line | reading A | reading B | chosen implementation behavior | status |
| --- | --- | --- | --- | --- | --- | --- |
| `IRIS-V1-GRAMMAR-V008` | `spec/iris-v1/12-conformance.md` | `261` | Chapter 12 references `IRIS-V1-GRAMMAR-V008` inside the `IRIS-V1-CONFORMANCE-V011` JSON fixture. | `spec/iris-v1/02-lexical-grammar.md` has no defining row for `IRIS-V1-GRAMMAR-V008`. | The ID is out of scope for milestone 1 and must not be implemented or invented. | Open |
| `IRIS-V1-GRAMMAR-V003` | `spec/iris-v1/02-lexical-grammar.md`; `spec/iris-v1/12-conformance.md` | `551`; `166` | The chapter 02 row separates the three source forms with `;`: `2 ** 3 ** 2; -2 ** 2; 2 ** -3`. | The chapter 12 normative sample record uses `\n` separators in `source_text`: `2 ** 3 ** 2\n-2 ** 2\n2 ** -3`. | Chapter tables are authoritative for vector content, and chapter 12 is authoritative for record schema shape, so the implementation follows the chapter 02 content while keeping the chapter 12 record structure. | Open |
| `IRIS-V1-GRAMMAR-V013` | `spec/iris-v1/02-lexical-grammar.md` | `554` | The row is `category: diagnostic` and states that non-loop labels and missing-colon break forms are rejected. | The row names no stable diagnostic code, while `IRIS-V1-CONFORMANCE-C030` requires every diagnostic entry to carry a `code`, and `IRIS-V1-GRAMMAR-C054` defines no labeled-break category. | Milestone 1 authors the placeholder codes `PARSE_BAD_LABELED_BREAK` and `PARSE_BAD_LABEL_TARGET` in the vector record. These are implementation-local until the spec assigns official categories; the vector is tagged `status:authored-expect` and is not counted as executable coverage. | Open |
| `IRIS-V1-GRAMMAR-V153` | `spec/iris-v1/02-lexical-grammar.md` | `579` | The row is `category: diagnostic` and requires a lexical malformed-hex-float diagnostic for `0x1.`. | The row names no stable diagnostic code, and the `IRIS-V1-GRAMMAR-C054` category table has no malformed-hex-float entry. | Milestone 1 authors the placeholder code `LEX_BAD_HEX_FLOAT`. It is implementation-local until the spec assigns an official category; the vector is tagged `status:authored-expect` and is not counted as executable coverage. | Open |
| `IRIS-V1-GRAMMAR-V010` | `spec/iris-v1/02-lexical-grammar.md` | `552` | The row is labeled `category: diagnostic`, and `IRIS-V1-CONFORMANCE-C016` requires diagnostic vectors to record a stable diagnostic code. | The stated observable is a positive inventory assertion, that exactly the D-509 keywords are reserved and the listed historical words are ordinary identifiers, so no diagnostic is emitted and nothing is rejected. | Milestone 1 encodes the record as `category: positive` with an `artifact` inventory assertion and `program_accepted: true`, because a diagnostic record with no diagnostic would be unverifiable. The category deviates from the chapter 02 label; the vector is tagged `status:authored-expect` and is not counted as executable coverage. | Open |
| `IRIS-V1-GRAMMAR-V011`, `V155`, `V161`, `V168`, `V172`, `V175`, `V176`, `V177`, `V189` | `spec/iris-v1/02-lexical-grammar.md` | `553`; `581`; `582`; `586`; `589`; `592`; `593`; `594`; `597` | The chapter 02 rows classify these as executable behavior requirements, and `conformance/iris-v1/GRAMMAR-classification.md` marked them `executable`. | The rows describe their fixture or their observable in prose rather than supplying literal Iris source and a literal expected value. Three sub-kinds occur: (a) `input.source_text` is entirely prose, as in `V155` and `V176`; (b) `input.source_text` mixes code with `valid:`/`invalid:` labels or describes the invalid fixture in words, as in `V161`, `V172`, `V175`, `V177`, and `V189`; (c) `expect.artifact.parse_shapes` is an English sentence or behavior label rather than a renderable shape, as in `V011` and `V168`. | Milestone 1 does not synthesize fixtures or expectations from the prose, because inventing either would fabricate conformance evidence. With owner approval the nine rows are reclassified `prose-fixture`, carry the record tag `status:prose-fixture`, and are reported by the runner in its own `unrunnable_source` bucket, never as passing. `V189` is a distinct case worth noting: the implementation correctly evaluates `"\xFF"` to `ÿ`, while the record expects the notation string `U+00FF`, so the implementation is right and the expectation is prose notation. Authoring concrete fixtures for these rows is future corpus work and is out of milestone 1 scope. | Open |
| `IRIS-V1-IDENTITY-C003`, `IRIS-V1-IDENTITY-C004` | `spec/iris-v1/01-language-identity.md` | `11`; `13` | `C003` states that Iris v1 is the modern successor to Legacy Iris, and `C004` states that Iris v1 `MUST NOT` be treated as an unrelated new language. Both frame the language through its relationship to a predecessor. | The owner directs that public-facing project material present Iris as a standalone language, without successor framing. `C004` binds a conforming specification, implementation, migration document, or conformance suite; `README.md` is promotional material and is none of those, so removing the successor narrative from it violates no clause. `C004`'s enumerated core identity, every value is an object, operators are messages, dispatch is runtime-dynamic, single Class inheritance plus Modules and Contracts, lexical Closure, runtime Class and Method identity, constrained runtime mutation, any object raisable as an exception, and host embedding, is preserved in full by `README.md`. | `README.md` describes Iris as a standalone language and omits successor framing. The `legacy/` directory retains a neutral, non-narrative description because it exists in the tree, and the chapter 11 index entry retains its real title because the specification is frozen. No normative artifact was changed, so this is a deliberate divergence in tone between promotional and normative material rather than a specification contradiction. Realigning the two would require revising the identity chapter, which is out of scope while v1 is frozen. | Open |
| `IRIS-V1-RUNTIME-V020`, `V021`, `V022`, `V023`, `V024`, `V025`, `V027`, `V028`, `V029`, `V030`, `V031`, `V032`, `V033`, `V034`, `V037`, `V038`, `V044`, `V045` | `spec/iris-v1/03-runtime-object-model.md` | `535`; `536`; `537`; `538`; `539`; `540`; `542`; `543`; `544`; `545`; `546`; `547`; `548`; `549`; `552`; `553`; `559`; `560` | Chapter 03 presents its vectors in two tables. The overview table at lines `520-563` gives a one-line scenario and a one-line expected result per row, while the coverage table at lines `564-648` gives a full fixture with source, setup, applicability, and a decision ID. Sixteen IDs appear in both tables and are the same vector at two granularities rather than duplicate definitions, so chapter 03 defines `109` unique vectors across `125` table rows. | Twenty IDs appear only in the overview table with no coverage-table counterpart. Two of them, `V018` at line `533` and `V035` at line `550`, still carry a concrete inline expression and a concrete expected result, so they remain executable. The remaining eighteen listed here describe the fixture in prose only and supply no runnable source. | Milestone 2 does not synthesize fixtures from the prose, because inventing a fixture fabricates conformance evidence. The eighteen rows are classified `no-fixture` in `conformance/iris-v1/RUNTIME-classification.md` and are never counted as passing. Authoring concrete fixtures for them is future corpus work. This is the same defect class as the chapter 02 prose rows recorded above, confirming the pattern is systemic across chapters rather than local to chapter 02. | Open |
| `IRIS-V1-RUNTIME-V067` | `spec/iris-v1/03-runtime-object-model.md` | `586` | The row requires the once-rounded fused `Float32` result and the independently rounded multiply-then-add result of `Float32.from_bits(0x3f800001)` squared minus `1.0f32` to be observably different, stating that the second value `differs`. | Exact rational arithmetic shows both paths yield the same binary32 encoding `0x34800000`. Let `a = Float32.from_bits(0x3f800001)`. Then `a*a - 1` is exactly `16777217/70368744177664`. The separate path first rounds `a*a` from `70368760954881/70368744177664` to `4194305/4194304`, so a rounding step genuinely occurs, but subtracting `1` and rounding to binary32 still lands on `0x34800000`. The two results are therefore equal, and no conforming implementation can make them differ. | Resolved by the owner-approved v1.7 errata. The row demanded a difference that exact arithmetic forbids, so the assertion, not the implementation, was wrong. It now states that the two paths share the encoding ``0x34800000``, which is what IEEE-754 actually guarantees, and its independently satisfiable Float64 promotion clause is preserved. The operands are rewritten through ``from_bits`` so the fixture parses; the values are bit-identical, verified on hardware. Amending a published vector row breached `IRIS-V1-TRACE-C007`, and this was the third owner-authorized exception in this milestone. | Resolved |
| `IRIS-V1-RUNTIME-V042`, `V048`, `V056`, `V112` | `spec/iris-v1/03-runtime-object-model.md` | `637`; `639`; `574`; `636` | The rows write their input as `type_and_value(expr)`, and `V053`, `V067`, and `V100` similarly use `type_of(expr)`. Read literally these are callable sends, which would require `type_and_value` and `type_of` to be ordinary selectors resolvable through dispatch. | No clause anywhere in the frozen specification defines either name. Both appear exclusively inside vector table rows, never in a `C` clause, and `IRIS-V1-RUNTIME-C002` explicitly excludes reflection APIs from this chapter. Treating them as language callables would require inventing a reflection surface the chapter disclaims. | Milestone 2 reads `type_and_value(expr)` and `type_of(expr)` as vector observation notation rather than Iris source: the runner evaluates the inner expression and asserts the observed runtime Type alongside the value. No `type_and_value` or `type_of` selector is registered in the runtime, because inventing one would fabricate a reflection API the chapter forbids. The affected rows remain executable under this reading. | Open |
| `IRIS-V1-GRAMMAR-V010` | `spec/iris-v1/02-lexical-grammar.md`; `conformance/iris-v1/vectors/GRAMMAR/IRIS-V1-GRAMMAR-V010.json` | `552` | The chapter 02 row for `V010` states its expectation symbolically as `Exactly D-509 keywords are reserved`. It writes no numeral, so the specification row itself stayed correct after the v1.1 errata widened the inventory to 49 under `IRIS-V1-GRAMMAR-C013`. | The committed `V010` record transcribed that symbolic expectation as the literal `reserved_keyword_count: 48`, snapshotting a value the specification never froze numerically. After the errata the record and `C013` disagreed by one, and the divergence was invisible to the runner because `V010` carries `status:authored-expect` and is never executed. | Resolved with owner approval. The record now reads `reserved_keyword_count: 49`, realigning the transcription with the `D-509` inventory it always referenced. This was a stale transcription rather than a semantic conflict, so no specification text changed and no expectation was weakened. The written trail required by `IRIS-V1-TRACE-C022` is this row. | Resolved |
| Chapter 03 declaration and fixture vectors | `spec/iris-v1/03-runtime-object-model.md` | `520-648` | The chapter 03 rows describe rich runtime scenarios that a conforming implementation must satisfy, and `conformance/iris-v1/RUNTIME-classification.md` marks 60 of the 109 vectors `executable`. | Of the 28 vectors still failing after stable hashing landed, only 4 carry both a concrete `source_text` and a machine-comparable `expect`. Sixteen have an empty `source_text` because the frozen row describes a fixture, a schedule, or a set of independent sources rather than one runnable program. Eight more supply source but state the expectation in prose, for example `V096` expects `Getter and setter selectors are name and name=; results are :get and :set.` and `V061` expects `Integer(6), then Integer(6); both forms select the same scale Method.` | Milestone 2 implements the behavior these rows require and proves it with crate-level tests, but does not synthesize source text or rewrite prose expectations into comparable JSON, because either would fabricate conformance evidence. `V061` is the clearest case: source-level class declaration, construction, dispatch, parameter binding, and named-infix equivalence all execute correctly and are locked by an integration test, yet the vector cannot mechanically pass because its expectation is a sentence. Raising the executable count for these rows requires authoring concrete fixtures, which is corpus work outside this milestone. | Open |
| `IRIS-V1-RUNTIME-V097`, `V103` | `spec/iris-v1/03-runtime-object-model.md` | `619`; `625` | `V097` requires a custom `to_bool` that increments a counter, then `evaluate if value { :yes }`, expecting counter `1` and result `:yes`. `V103` requires replacing the `Float64.infinity` getter and adding a setter that records its argument. | Neither fixture can be authored faithfully against the current implementation. Probing shows `if` is not control flow at all: `if true { :yes }` yields `Bool(true)` and `if false { 1 } else { 2 }` yields `Bool(false)`, so the condition value is returned and no branch body is ever evaluated. Class-variable assignment `@@c = 1` is a parse diagnostic, so the counter has no observable mechanism. For `V103`, reopening a built-in value Class with `open class Float64 { ... }` is an unsupported construct. | Both underlying implementation gaps were closed later in milestone 2, so this row is superseded on its original grounds. `if` became a real value-producing conditional under the v1.3 errata recorded below, class-variable declaration gained the `shared_decl` production under the v1.2 errata, and built-in value Class reopening under `IRIS-V1-RUNTIME-C148` is implemented. `V097` is authored from its frozen row and passes. `V103` remains open for a DIFFERENT reason discovered afterwards, recorded in its own row below: its negative half is unobservable by construction. | Partly resolved |
| `IRIS-V1-CONTROL-C009`, `IRIS-V1-RUNTIME-C073` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/02-lexical-grammar.md` | `44`; `403` | `CONTROL-C009` states that assignment to an absent `@@name` MUST fail as missing declared storage, and `RUNTIME-C073` describes `@@name` as a DECLARED hierarchy binding cell anchored to a logical Class. Both presuppose that a declaration form for class variables exists. | The chapter 02 EBNF had no production that declared one. `declaration` expanded only to `decorated_declaration`, `import_decl`, `export_decl`, `type_alias_decl`, `global_decl`, and `let_decl`. The `shared` keyword was reserved by `IRIS-V1-GRAMMAR-C013` and `shared_name` appeared in `assignment_target` and `primary_expr`, so a class variable could be READ and ASSIGNED in the grammar but never DECLARED. | Resolved by the owner-approved v1.2 errata. `IRIS-V1-GRAMMAR-C059` adds a `shared_decl` production taking the `shared` keyword, a `let` or `mut` mutability word, a `shared_name`, an optional type annotation, and an initializer, and admits it in `declaration`. It mirrors `global_decl` so the two storage families share one shape, and the previously unused `shared` keyword gains its production. `IRIS-V1-RUNTIME-C162` supplies the semantics: `shared let` is immutable, `shared mut` is assignable, and a redeclaration anchored anywhere on the static lexical ancestry is rejected with no candidate revision published, per `RUNTIME-C075`. Both chapters were updated in English and Simplified Chinese in the same revision as `IRIS-V1-TRACE-C020` requires. | Resolved |
| `IRIS-V1-RUNTIME-C067`, `IRIS-V1-RUNTIME-C069`, `IRIS-V1-RUNTIME-V070` | `spec/iris-v1/03-runtime-object-model.md` | `212`; `216`; `594` | `C069` states that when effective `instance_state` is denied, first assignment to an absent undeclared raw ivar MUST raise `InstanceStateError`. The `V070` fixture exercises exactly that rule, giving `let n = 1; n.@x = 2` and `let f = 1.0f32; f.@x = 2` as independent sources and expecting `InstanceStateError` at phase `runtime` for both, because an Integer and a Float32 are identity-less and cannot carry instance state. | `C067` states that source syntax MUST NOT allow `other.@x`. Both `V070` sources are instances of that forbidden form: `n` and `f` are bindings other than `self`, so the raw ivar assignment is rejected lexically and never reaches the runtime policy `C069` describes. The two clauses cannot both hold for this fixture, because `C069` can only raise its typed error if the program parses, while `C067` guarantees it never does. | Resolved by owner ruling and implementation, with NO clause change. The owner reaffirmed that ``x.@a`` must never be valid syntax, so `C067` was preserved exactly. The conflict was in the fixture, not the clauses: `C069` asserts that an identity-less receiver cannot gain instance state, and that substance is expressible through a property accessor whose body assigns `@x` on the current receiver, which `C067` permits because the receiver is `self`. Implementation gained two things it lacked: `instance_state` was accepted as a capability name but never enforced, so a denying Class could still gain a slot, and a value receiver method body could not reach the raw ivar policy at all. Both now route through the same `require_meta_capability` boundary the other meta operations use. `V070` is re-authored with legal sources and its expectation is byte-identical, the three outcomes being `InstanceStateError`, `InstanceStateError` and `IdentityError`. All three forbidden forms still reject. | Resolved |
| `IRIS-V1-RUNTIME-V026`, `IRIS-V1-CONTROL-C006` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/04-bindings-callables-control-flow.md` | `652`; `17` | The `V026` fixture opens with `let escaped = nil` and then assigns to that same binding from inside `initialize` with `escaped = self`. The row needs the assignment to succeed, because its whole point is that a `self` which escaped a FAILED initializer is still an ordinary memory-safe object, observable afterward through `escaped.to_bool()`. | `CONTROL-C006` states that bindings are immutable unless their own declaration form explicitly uses `mut`. A binding introduced by plain `let` is therefore not assignable, so the fixture as written assigns to an immutable binding and must fail with a typed immutable-binding error before it can ever demonstrate the escape guarantee. | Milestone 2 authors the runnable fixture with `let mut escaped = nil`, changing only the mutability word the assignment requires and leaving the scenario, the raised value, and both observations exactly as the row states. The implementation correctly rejects assignment to a plain `let` from a method body with a typed `ImmutableBinding` error, so weakening that rule to match the row literally would have broken `C006` for every other binding. The vector passes under this reading and returns the specified `:sentinel` with `escaped.to_bool()` true. Resolving this needs an errata deciding whether the row should read `let mut`; that is a specification decision and the owner has not been asked. | Open |
| `IRIS-V1-RUNTIME-V055`, `IRIS-V1-RUNTIME-V109` | `spec/iris-v1/03-runtime-object-model.md` | `590`; `648` | Both rows state a comparable result together with a side-effect assertion in one sentence. `V055` expects ``[:old, :new]`` with an evaluation log of exactly ``[:left, :right, :left, :right]``, and `V109` expects ``true`` with a log of exactly ``[:left, :right]`` and neither replaceable comparison Method called. Once `Array#append` landed, both scenarios became fully expressible in Iris source. | The implementation satisfies both rows exactly. Probing the authored fixtures returns ``[:old, :new]`` with the log ``[:left, :right, :left, :right]`` for `V055`, and ``true`` with the log ``[:left, :right]`` for `V109`, and the `V109` fixture proves the bypass directly because its `==` and `<=>` bodies raise if selected and never do. The vectors still report failing only because the runner compares a single rendered value against the whole expectation string, and the frozen sentence mixes a value with prose. | Resolved. The rows were never prose: each already reads ``value``: X; ``side_effects``: Y, and the earlier record had mashed both halves into one expectation string the runner could never match. The runner now compares the two observations separately per `IRIS-V1-CONFORMANCE-C025`, which already listed `side_effects` as a stable observation field, and a record whose value matches while its side effect does not is reported as failed rather than passing. Both vectors are retranscribed into that structured form and pass, with their fixtures returning the evaluation log alongside the value so the log is an ordinary observed value rather than a side channel invented for the runner. No frozen expectation was weakened and no specification text changed; this was a transcription defect in the corpus. | Resolved |
| `IRIS-V1-CONTROL-C041`, `IRIS-V1-GRAMMAR` conditional productions | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/02-lexical-grammar.md` | `233`; `376`; `400-430` | `CONTROL-C041` states that `if condition { then } else if other { middle } else { last }` is value-producing, that the result is the final expression of the selected branch or `nil` for a selected body with no value, that a missing `else` contributes an implicit `nil` branch, and that the result type is the normalized union of reachable branch result types. A form that produces a value, contributes an implicit `nil`, and carries a union result type is describing an expression. | The chapter 02 grammar admits `if_statement` only through the `statement` production at line `376`. The expression grammar running from `expression` at line `400` down through `primary_expr` contains no `if` production at any precedence level, so `let r = if c { a } else { b }` and `[if c { a } else { b }]` are not derivable. A form cannot simultaneously produce a value for its context and be underivable in every value position. | Resolved by the owner-approved v1.3 errata. `IRIS-V1-GRAMMAR-C060` admits `if_expression` in `primary_expr` and defines `if_statement` in terms of that production, which is exactly the case `IRIS-V1-TRACE-C021` permits: supplying a missing grammar production for behavior another clause already requires. `CONTROL-C041` keeps sole ownership of the value, branch scope, missing-`else` result, and result type; the new clause only references it and reinterprets nothing. Disambiguation is stated explicitly, since `match_guard` consumes its leading `if` before its guard expression is parsed and Hash literals open with `%{` rather than `{` per `IRIS-V1-GRAMMAR-C021`. Both the English and Simplified Chinese chapters advanced to v1.3 in the same revision as `IRIS-V1-TRACE-C020` requires. An `if` now works in binding, argument, operand, and array-element positions, still yields `nil` without an `else`, and still tests its condition through the `to_bool` protocol exactly once; statement position is unchanged. `IRIS-V1-RUNTIME-V097` is authored from its frozen row and passes. | Resolved |
| `IRIS-V1-CONFORMANCE-C033`, `IRIS-V1-RUNTIME-V108` | `spec/iris-v1/12-conformance.md`; `spec/iris-v1/03-runtime-object-model.md` | `84`; `632` | `C033` requires `expect.side_effects` whenever a vector depends on an observable effect such as no candidate commit or no package publication, and the `V108` row states its effect as the prose sentence ``side_effects``: `Child` is not published. The runner parses the class name out of that sentence and verifies the named Class is absent after the failure, which is a real observation rather than a decorative string. | The check is sound for the assertion it makes but insensitive to a corpus error in the sentence itself. Rewriting the expectation to name a Class that never existed, such as `Nonexistent is not published.`, still passes, because that Class is genuinely absent. Rewriting it to the opposite claim is correctly reported as failing, so the observation is live; only a mistyped or mistaken subject goes undetected. | Milestone 2 keeps the prose form because it is transcribed verbatim from the frozen row, and keeps the parse-and-observe check because it verifies the substantive claim `V108` makes. The gap is that a side-effect subject is not cross-checked against the fixture source, so an authoring mistake in the subject name cannot be caught. A structured `side_effects` form naming the Class as data, in the shape the runner already consumes for `V055`, `V097`, `V106` and `V109`, would close this, but the frozen row supplies prose and rewriting it into data would no longer be a verbatim transcription. Recorded so the weaker check is not mistaken for a strong one. | Open |
| `IRIS-V1-RUNTIME-V094`, `D-448`, `IRIS-V1-RUNTIME-C040` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/traceability-matrix.md` | `617`; `143`; `459` | The `V094` row and the normative `D-448` decision both require observing that an alias and its original share Method IDENTITY, phrased as ``alias_method :new, :old`` creates another slot referencing the SAME Method identity. The other two claims in the row, that remove exposes an ancestor and that undef reaches the missing-message path, are ordinary observable behavior. | `IRIS-V1-RUNTIME-C040` states that EVERY evaluation reading or binding ``obj.method`` MUST create a distinct identity-bearing BoundMethod even when receiver and resolved Method are unchanged, so ``obj.method same? obj.method`` is `false` by construction. Reading the alias and the original from source therefore yields two fresh BoundMethods that can never be `same?`, regardless of whether the underlying slots share one Method. Chapter 03 defines no source-level operation that exposes the underlying Method identity behind a slot, and `IRIS-V1-RUNTIME-C002` excludes reflection APIs from the chapter. | Resolved WITHOUT any specification change. The premise that Method identity is unobservable was too narrow: `C040` governs only reading or binding ``obj.method``, which yields a BoundMethod, while `Reflection::Class.method` returns an UNBOUND Method that `C040` does not constrain. Probing confirms an alias and its original compare `same?` as true, two distinct Methods compare false as a control, and the same selector read twice compares true, while ``a.g same? a.g`` remains false exactly as `C040` requires. All three claims the row makes are therefore mechanically observable and the vector is authored as an executable fixture. | Resolved |
| `IRIS-V1-META-C100`, `IRIS-V1-TRACE-C007`, `IRIS-V1-TRACE-C020` | `spec/iris-v1/08-modules-metaprogramming.md`; `spec/iris-v1/README.md` | `275`; `13`; `15` | `TRACE-C007` states that clause, example, and vector IDs are permanent once published and that existing IDs MUST NOT be renumbered, and `TRACE-C020` requires an errata to PRESERVE every published clause and add new material under the next available numbers. Read together they forbid rewriting the text of a published clause. | The owner directed that all reflection operations live under layered `Reflection::*` sub-Modules woven into `Class` and `Module`, and chose that the four already-published raw ivar operations MOVE rather than gain an alias. I raised the conflict explicitly, offered a compliant supersede-in-place alternative modelled on how `IRIS-V1-GRAMMAR-C013` widened the keyword inventory, and asked for authorization to rewrite a published clause. The owner answered `授权修改`, authorizing the modification. | The v1.5 errata rewrites `IRIS-V1-META-C100` in place, changing ONLY the four operation paths from `Reflection.list_ivars` and its siblings to `Reflection::Object.list_ivars` and siblings. The operation names, argument lists, return-value wording, and the ambient-authorization sentence are byte-identical to the published text, and the clause ID is retained. `IRIS-V1-META-C109` through `C112` were checked and reference these operations by bare name only, so no second clause needed editing. This is a deliberate, owner-authorized, single-clause exception to `TRACE-C007`, recorded here so the precedent is visible and bounded: no other published clause, ID, example, or vector was rewritten, and the exception does not generalize. | Open |
| `IRIS-V1-RUNTIME-V015`, `IRIS-V1-RUNTIME-C015`, `IRIS-V1-RUNTIME-C014` | `spec/iris-v1/03-runtime-object-model.md` | `648`; `65`; `63` | `V015` retains a Module Method whose body executes `super()`, removes `M` from the receiver MRO, invokes the retained Method, and expects `InvalidSuperError` at phase `runtime`, because the lexical owner is absent from the invocation-time current MRO. `C014` and `D-103` own that outcome. | `C015` and `D-104` require reflective invocation to validate AT ENTRY that the receiver current MRO contains the Method lexical owner, and to raise `MethodBindingError` even if the body path would not access state or execute `super`. Removing `M` makes its Method fail that entry check, so the invocation raises `MethodBindingError` before any body can reach `super`. Probing the scenario with the v1.5 reflection surface confirms it: the same source that returns `:base` while `M` is composed returns a method-binding failure once `M` is removed. The two clauses cannot both hold for this fixture, because `C015` fires strictly earlier than the condition `C014` describes. | Resolved by the owner-approved v1.7 errata. Neither clause changed. `C015` wording that entry validation raises even when the body would not execute `super` is deliberate, so `C015` is meant to fire first and `C014` is unreachable for this fixture. The row now expects `MethodBindingError` and states the ordering, and the vector passes. Amending a published vector row breached `IRIS-V1-TRACE-C007`, and this was the fourth owner-authorized exception in this milestone. | Resolved |
| `IRIS-V1-RUNTIME-V100`, `IRIS-V1-TRACE-C007`, `IRIS-V1-TRACE-C020` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/README.md` | `624`; `13` | The `V100` row wrote its ancestry query as ``reflection.ancestors(A)``, lowercase and unlayered. The v1.5 errata had just established the layered `Reflection::*` namespace, so the published row and the new surface disagreed on how the same operation is spelled. | `TRACE-C007` states that clause, example, and vector IDs are permanent once published, and `TRACE-C020` requires an errata to PRESERVE every published clause, example and vector. Rewriting a published vector row is therefore prohibited by default. I offered the owner a compliant alternative, treating the lowercase form as non-normative table shorthand and leaving the row untouched, and the owner instead directed ``统一为 Reflection::Class.ancestors``. | The v1.6 errata rewrites the `V100` row spelling in place in English and Simplified Chinese, changing ONLY ``reflection.ancestors(A)`` to ``Reflection::Class.ancestors(A)``. The fixture prose, the expected value ``[true, true, [A, NewBase, Object]]``, the applicability text, and the decision `D-262` are byte-identical to the published row, and the vector ID is retained. I verified those two lines were the only occurrences of the old spelling in the entire specification. This is the SECOND owner-authorized exception to `TRACE-C007` in this milestone, after the `IRIS-V1-META-C100` rewrite recorded above. Both were raised before the fact, both were bounded to a single published item, and both changed only a spelling while leaving names, arguments, values and decisions untouched. Recording the second occurrence so the pattern stays visible: the exception is now a precedent used twice, not a one-off, and any further use should be weighed against `TRACE-C007` rather than against these rows. | Open |
| `IRIS-V1-RUNTIME-V103`, `IRIS-V1-RUNTIME-C161`, `IRIS-V1-META-C112` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/08-modules-metaprogramming.md` | `625`; `568`; `299` | The `V103` row replaces the `Float64.infinity` getter and adds a setter, expecting that the replaced getter result is observed and that the setter records its argument `without creating implicit backing storage`. The positive half is ordinary observable behaviour and `C161` owns the rule that a compatible `property fun` replacement creates no second backing slot. | The negative half has no observation path, and this is not an implementation gap. `Float64` is an identity-less value Class under `IRIS-V1-RUNTIME-C004`, and `IRIS-V1-META-C112` requires raw ivar reflection to respect identity-less value restrictions. `Reflection::Object.list_ivars` therefore MUST refuse a value receiver, which is exactly what the implementation does. No conforming implementation can expose the absence of backing storage on a `Float64`, because the only specified inspection surface is closed to that receiver category by another normative clause. | Milestone 2 leaves the vector failing and the record untouched. The row is tagged `provenance:authored-fixture`, so its expectation is corpus material rather than frozen specification text and could be re-authored to observe only the half that `C161` makes observable on a value receiver. That was offered to the owner and declined in favour of closing the milestone with the blocker stated precisely, on the standing rule that a conformance expectation is evidence and an honest failure with a named cause is worth more than a narrowed one. Closing it later needs either an owner-approved re-authoring of the row or a new specified way to observe value-receiver storage; neither is implementation work. | Open |
| `IRIS-V1-RUNTIME-V013`, `IRIS-V1-RUNTIME-V083`, `IRIS-V1-RUNTIME-C160` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/01-language-identity.md` | `566` | Both rows are controlled runtime fixtures rather than Iris source. `V013` schedules `enter value.m()`, `commit replacement A#m returning :new`, `resume entered frame`, `call value.m() again`, and expects `[:old, :new]` so that an entered frame keeps its selected body while a later send selects the replacement. `V083` interleaves a compatible commit with an in-flight `A.new`, expecting allocation and `initialize` to use the captured revision and a later send to use the active one. `C160` states that each such Input cell is a complete controlled runtime fixture. | Neither can be expressed as an Iris program. Both require the conformance HOST to enter a method frame, suspend it out of band, commit a revision, then resume the suspended frame, and `01-language-identity.md` lists suspension inside a transaction as `PROHIBITED`. The capability the fixtures need is therefore deliberately absent from the language, which means no errata under `TRACE-C021` can supply it: there is no missing spelling or production here, only a missing harness. | Milestone 2 leaves both failing and records the blocker rather than approximating the schedule with source that would test something else. The revision-capture semantics the rows describe are implemented and exercised by neighbouring vectors; what is missing is a conformance host with an out-of-band frame scheduler able to enter, pause, commit and resume. That is harness infrastructure sized for a later milestone and is explicitly not a language feature. No further RUNTIME vector can be closed by implementation alone. | Open |
| `IRIS-V1-RUNTIME-C131`, `IRIS-V1-RUNTIME-V105` | `spec/iris-v1/03-runtime-object-model.md` | `358`; `629` | `C131` requires built-in numeric ordered comparisons to use exact mathematical values and to place infinities by extended-real position. `C130` states the parallel rule for equality and adds explicitly that an arbitrary-precision `Integer` MUST NOT first be rounded to float. | `Numeric::compare` converted BOTH operands to `f64` before comparing, while `Numeric::equal` already took the exact `integer_equals_float` path. Any `Integer` beyond finite float range rounds to an infinity under that conversion, so `10 ** 1000` became `f64::INFINITY` and `Float64.infinity > 10 ** 1000` returned `false` where `C131` requires `true`, because an infinity was compared against itself. Precision loss below the overflow threshold was the same defect at smaller scale. The bug was invisible to the suite because `V105` was bucketed `needs-subsystem` on the stale ground that Array literals required an excluded collections library. | Fixed with no specification change; the implementation was wrong and `C131` was already correct. `Numeric::compare` now compares Integer pairs as `BigInt`, and compares an Integer against a float by exact value: NaN yields `None`, an infinite float resolves by sign because every Integer is finite, and a finite float is split into its truncated integral part and its fraction so the comparison never rounds the Integer. The reversed operand orders delegate to the same helper through `Ordering::reverse`, which makes the symmetry `C131` requires structural rather than duplicated. Four regression tests lock the defect, and reverting the fix fails two of them. `V105` is retranscribed from its frozen row into structured JSON and moved to `bucket:executable`; every expected value is verbatim from the row and its trailing symmetry sentence was verified separately by probing the reversed comparisons. | Resolved |
| `IRIS-V1-RUNTIME-C091`, `IRIS-V1-RUNTIME-C092`, `IRIS-V1-RUNTIME-V076` | `spec/iris-v1/03-runtime-object-model.md` | `274`; `276`; `600` | `C091` requires `Bool#<=>` to define a total Bool order returning `Integer(0)`, `Integer(-1)` and `Integer(1)`, to answer `nil` for a non-Bool operand, and to keep Bool neither equal to nor ordered with `Integer(0)` or `Integer(1)`. `C092` requires `nil#<=>` to return `Integer(0)` only against the same `nil` singleton and `nil` otherwise, and forbids `nil` from being a global minimum or maximum. | The kernel installed only `Hash` and `ToBool` on the `Nil` and `Bool` built-in Classes, so every comparison selector on a singleton receiver failed as a missing message. The comparison branches also called `numeric()` unconditionally, which would have made `true == 1` a type error rather than the `false` that `C091` requires, since being distinct from a numeric is a stated outcome and not a failure. | Fixed with no specification change; the clauses were already correct and unimplemented. The seven comparison selectors are installed on both singleton Classes, and the ordered, equality and `<=>` branches now route through two helpers that decide by operand category before falling through to the exact numeric path. A cross-category operand yields `None`, which the ordered comparisons render as `false` and `<=>` renders as `nil`, so neither singleton becomes a global extreme. Six regression tests lock `C091` and `C092`, including the operand-order symmetry. `V076` still fails on its final term `nil < Object.new()`, which needs `Object` as an instantiable root Class; that is recorded separately below. | Resolved |
| `IRIS-V1-RUNTIME-C005`, `IRIS-V1-RUNTIME-V076`, `IRIS-V1-RUNTIME-V100` | `spec/iris-v1/03-runtime-object-model.md` | `15`; `600`; `624` | `C005` states that `Object` is the root runtime Class for ordinary object behavior, that it provides default comparison, truthiness, missing-message and zero-argument `initialize` behavior, and that no second root Class may exist. `V076` sends `Object.new()`, and the frozen `V100` row expects the ancestry `[A, NewBase, Object]`, so `Object` must be both instantiable and present in every ordinary MRO. | Neither holds. `Object` exists only as a type NAME returned by rendering code in `iris-eval`; no `Object` Class is registered in the kernel, whose `BuiltinClass` enumeration has exactly five members and does not include it. Probing confirms the consequence: `Reflection::Class.ancestors(A)` for `class A { }` returns `[A]` rather than terminating at `Object`, and `Object.new()` fails as a missing message on a receiver whose Class does not exist. | Resolved by owner-authorized registry unification, with no specification change. The owner ruled to merge, so `Kernel` no longer owns a `ClassRegistry`: it borrows one and its methods take it as a parameter, leaving `Runtime` the single owner. Built-in and declared Classes now share ONE `ClassId` space, built-ins occupying `0` through `5` and declared Classes starting at `6`, which removes the collision where the sixth declared Class received the same id as `Float64`. An origin declaration with no `extends` now defaults to `Object`, so `Reflection::Class.ancestors(A)` returns `[A, Object]` and terminates at the root as `C005` requires. A reopen deliberately does NOT default, since it must keep the superclass its origin declaration established. Two hazards surfaced and were fixed: `is_builtin_class` had tested whether a Class was ABSENT from the runtime registry, which silently became false for every built-in once they shared one registry and would have stripped `C150` protection from all five value Classes without any test failing; and `Object.new()` as a single expression stayed on the literal evaluator, which has no heap, so root construction now routes to the source runtime. `Object` is excluded from `is_builtin_class` because `C150` protects exactly the five value Classes while `C005` makes `Object` the ordinary-object root. A 59-vector pass list was captured before the change and compared after every step; one intermediate state regressed `V011`, `V012`, `V017`, `V055`, `V070` and `V106` by defaulting the superclass on reopen, and that was caught and fixed before commit. `V076` now passes. `V100` and `V075` remain open on other grounds. | Resolved |
| `IRIS-V1-RUNTIME-C113`, `IRIS-V1-RUNTIME-C115`, `IRIS-V1-RUNTIME-V068` | `spec/iris-v1/03-runtime-object-model.md` | `322`; `326`; `588` | `C115` names eight pure bit-classification Methods, `is_nan`, `is_signaling_nan`, `is_infinite`, `is_finite`, `is_normal`, `is_subnormal`, `is_zero` and `sign_bit`, and forbids them from performing floating arithmetic, quieting a signaling NaN, altering payload or sign, or changing `to_bits()`. `C113` requires `to_bits` at each width, and `C114` requires `from_bits(bits).to_bits() == bits` for every pattern including signaling-NaN encodings. | None of the nine selectors existed. `Numeric::float32_to_bits` and `float64_to_bits` were implemented but reachable only from Rust, with no `NativeSelector` mapping them to source, so `V068` failed on its first term and would have failed again on `to_bits`. | Fixed with no specification change; the clauses were correct and unimplemented. All eight classification Methods are implemented as one helper that decomposes either width into sign, biased exponent and trailing significand and answers from those fields alone, so no arithmetic path can quiet a signaling NaN, and a signaling NaN is recognised as a NaN whose leading significand bit is clear. Every clause-named Method was implemented rather than only the three `V068` sends, since a half-supplied protocol is worse than none. `to_bits` is routed per receiver width per `C113`. Four regression tests lock the behaviour, including the `C114` round trip through `0x7ff0000000000001` and the Float32 path. `V068` is retranscribed from its frozen row into structured JSON and moved to `bucket:executable`, every expected value verbatim from the row with the hexadecimal bit pattern rendered as its exact decimal. | Resolved |
| `IRIS-V1-RUNTIME-C083`, `IRIS-V1-RUNTIME-C084`, `IRIS-V1-RUNTIME-C086`, `IRIS-V1-RUNTIME-V075` | `spec/iris-v1/03-runtime-object-model.md` | `258`; `260`; `264`; `595` | `C083` requires root `Object` to provide `<=>` returning `nil` for every operand. `C084` requires the six comparison Methods to be ordinary slots deriving from the current visible `<=>`, with `nil` meaning unordered. `C086` requires default equality to test reference identity FIRST and return `true` without consulting `<=>`. `C085` restricts an accepted `<=>` response to exact `Integer(-1)`, `Integer(0)`, `Integer(1)` or `nil`. | None of it was implemented for source Classes. A `ComparisonProtocol` type in `iris-runtime` encoded the rules correctly but was exported and never used by any dispatch path. Worse, the milestone-2 registry unification had installed the NATIVE comparison selectors on `Object`, so every declared Class inherited native bodies whose implementation routes through `numeric()`. The source evaluator has no body registered for a native selector, so `a == b` on an ordinary object failed as a raise of `nil` from `invoke_method_with_context` rather than reaching any comparison logic. | Fixed with no specification change; the clauses were correct and unimplemented. `Object` no longer receives native comparison selectors, since `C083` makes its `<=>` answer `nil` and `C084`/`C086` derive the rest, so the numeric native bodies were simply the wrong implementation to inherit. `Hash` and `ToBool` stay, because `C094` does give `Object` a real default there. The source dispatch path now falls back, when a comparison slot resolves to no user Method, to a derivation that tests reference identity first per `C086`, then sends the visible `<=>` exactly once and maps its response per `C084`, and raises the new `ComparisonContractError` for any response `C085` forbids. Five regression tests lock the clauses, including the identity fast path, all six derived relations for `Integer(0)` and `Integer(-1)`, the all-false `nil` case, and both contract rejections. `V075` is retranscribed from its frozen row into structured `value` and `side_effects` observations, its call log proving the fast path: four `<=>` calls for five comparisons, because same-reference equality never consults it. | Resolved |
| `IRIS-V1-RUNTIME-V091`, `IRIS-V1-RUNTIME-V092`, `IRIS-V1-RUNTIME-C148`, `IRIS-V1-RUNTIME-C061` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/02-lexical-grammar.md` | `611`; `612`; `433`; `200`; `277` | The object-model cluster. `V091` asserts that `@@x` is a hierarchy cell shared by a subclass while each Class object keeps an independent `@x`, and that a subclass redeclaration is rejected. `V092` asserts that stored-property initializers run superclass-to-subclass in declaration order before the final `initialize`, and that reflection reports explicit getter and setter Methods. `V101` asserts that a Module composes into `Nil` and Methods are added to `Bool` and `Integer` while numeric receiver-state creation still raises. | Two of the three needed NO implementation at all. `V091` was fully implemented and hidden behind a stale `needs-subsystem` reason citing an excluded collections library; probing showed the shared cell, the independent Class-object ivars and the `DuplicateClassVariable` rejection all already worked. `V092` needed only the `quoted_symbol` production: the chapter 02 grammar limits `selector_suffix` to `?` and `!`, so the setter selector `name=` that `C061` defines is not expressible as a bare `:name=`, and reflection could not name it. `V101` was blocked by one over-broad guard that rejected a reopen carrying ANY mixin, conflating `C148`, which explicitly permits a stable built-in Class to compose Modules, with `C150`, which protects only the superclass. | Fixed with no specification change. `quoted_symbol` is implemented in the parser rather than the lexer, since `:` and the string are already separate tokens, which kept the change away from `crates/iris-lexer/`. The reopen guard now rejects only a superclass change and admits Module composition, and a probe confirms `InstanceStateError` still fires for numeric receiver-state creation, so `C150` and the `instance_state` capability are unweakened. Five regression tests lock the cluster. All three vectors are retranscribed from their frozen rows into structured observations and moved to `bucket:executable`; `V091` and `V101` use `independent_sources` so the rejection half of each frozen expectation is asserted rather than dropped. RUNTIME 61 to 64 with zero regressions against a captured pass list. | Resolved |
| `IRIS-V1-RUNTIME-V082`, `IRIS-V1-RUNTIME-V102`, `IRIS-V1-RUNTIME-C134` | `spec/iris-v1/03-runtime-object-model.md` | `606`; `622`; `364` | A systematic re-probe of all 21 remaining `needs-subsystem` rows, prompted by three of the previous four closures needing little or no implementation. Each row's bucket reason was treated as a claim to verify rather than a fact. | Two rows were fully implemented and hidden. `V082` asserts that nominal Type and Contract identity survive a compatible open; probing returns `[true, :new]` exactly. `V102` asserts that `mod` as a Method and as a named infix both return `Integer(1)` while `%` is rejected; all three already hold. Separately, `C134` names `InvalidKeyError` for a NaN Hash key, but the runner rendered the kernel's `StableHash(InvalidNumericKey)` as a generic `RuntimeError`, so the specified code was never observable. | `V082` and `V102` are retranscribed from their frozen rows and moved to `bucket:executable`, `V102` using `independent_sources` so the `%` rejection is asserted rather than dropped. The runner now maps `InvalidNumericKey` to the `C134` code. Every one of the 19 rows still bucketed carries a rewritten reason naming the concrete feature it waits on, and six were NARROWED to a strictly smaller blocker than previously recorded: `V072` needs only the Hash literal, `V078` only Contract and Type objects, `V088` only the Dynamic send, `V099` only the Closure block argument, `V100` only `is` and `type_of`/`subtype?`, and `V054` needs no lexer work at all since `LEX_BAD_FLOAT_SUFFIX` is already emitted and the gap is the missing `diagnostics` observation channel on the RUNTIME runner. RUNTIME 64 to 66 with zero regressions. | Resolved |
| `IRIS-V1-RUNTIME-V054`, `IRIS-V1-RUNTIME-V110`, `IRIS-V1-GRAMMAR-C054` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/02-lexical-grammar.md` | `578`; `630`; `483` | Both rows assert that a malformed source is REJECTED rather than evaluated. `V054` expects `LEX_BAD_FLOAT_SUFFIX` for `1.0F32`, and `V110` expects a declaration-validation rejection for each of five reserved control-flow selector declarations. `C054` fixes the stable diagnostic categories a conforming diagnostic system must use. | A malformed source never reaches evaluation, so neither row has a runtime value or error to observe, and the RUNTIME runner compared only values and errors. The `diagnostics` collector existed but was reachable only from the GRAMMAR runner, so `LEX_BAD_FLOAT_SUFFIX` was already emitted correctly and simply unobservable from this chapter. | `V054` is resolved: the RUNTIME runner gained a `diagnostics` expectation branch that reuses the SAME collector the GRAMMAR runner uses, so both chapters classify a given source identically as `C054` requires, and it composes with `independent_sources` for free. `V054` is retranscribed and passes, and tampering its expected code to a nonexistent one correctly fails the row, which proves the assertion is live. `V110` stays bucketed deliberately. Its five declarations ARE all rejected, but `C054` assigns no stable category for a reserved control-flow selector declaration, so the implementation emits `PARSE_UNEXPECTED_TOKEN`, a code the specification never named. Marking the row `status:authored-expect` was tried and REVERTED: the RUNTIME runner reports such a row before comparing anything, so tampering its expected code changed nothing, making the row look verified while asserting nothing. That is worse than an honest bucket, so the tag handling was removed rather than left as a trap. RUNTIME 66 to 67, buckets still sum to 109. | Resolved |
| `IRIS-V1-TYPES-C028`, `IRIS-V1-TYPES-C016`, `IRIS-V1-TYPES-C076`, `IRIS-V1-TYPES-C079` | `spec/iris-v1/05-types-contracts-generics.md` | `151`; `65`; `370`; `376` | `C028` defines `value is T` as a single-evaluation `Bool` test over reified Types including CURRENT runtime ancestry, forbidden from sending `to_bool` or converting. `C016` makes runtime Type objects interned and identity-bearing. `C076` requires a Class to expose `.type` metadata while the Type object is NOT the Class object. `C079` requires `Type#subtype?` to use the same nominal rules as the runtime guards. | None of it existed. `BinaryOperator::Is` was already present in the syntax tree but the parser never produced it, so `a is A` was a parse diagnostic. There was no Type value at all, so `.type` and `subtype?` had nothing to return, which left `V100` unable to assert the middle term of its frozen expectation. | Fixed with no specification change. The parser admits `is` at the relational precedence the chapter 02 grammar assigns it, and evaluation walks the receiver's ACTIVE MRO so a committed superclass change is observable, which a regression test pins by probing the same source before and after the commit. A `Value::Type(ClassId)` variant carries a nominal Type distinctly from the Class it reifies per `C076`, and `A.type same? A.type` is true while `A.type same? A` is false, which is `C016` and `C076` respectively. `subtype?` deliberately walks the SAME active MRO that `is` consults rather than a cached relation, as `C079` requires, so the two can never disagree after a revision. `V053` is retranscribed using the existing decision that `type_of` is vector observation notation rather than a selector, and `V100` is retranscribed with a second independent source asserting ancestry DIRECTION, since the runner renders Class values opaquely and could not otherwise distinguish `[A, NewBase, Object]` from any other three-element array. RUNTIME 67 to 69 with zero regressions. | Resolved |
| `IRIS-V1-TYPES-C041`, `IRIS-V1-TYPES-C043`, `IRIS-V1-RUNTIME-C024`, `IRIS-V1-RUNTIME-V078`, `IRIS-V1-RUNTIME-V080` | `spec/iris-v1/05-types-contracts-generics.md`; `spec/iris-v1/03-runtime-object-model.md` | `213`; `217`; `97`; `600`; `604` | `C041` makes Contract the explicit obligation surface and `C043` forms Contract inheritance as the union of static requirements with no implementation MRO, `super`, stored state, or Method bodies. `C024` forbids Method overloading: one complete selector in one Class revision maps to at most one Method, and redefining it is replacement requiring `override`. `V078` compares an alias, a same-body Method, and Class, Module, Contract and Type objects. `V080` requires a duplicate-selector declaration to be rejected with NO Class publication. | The parser already parsed `contract_decl` with passing tests, so the recorded blocker naming the parser was itself stale; the real gap was `source_runtime` rejecting `Declaration::Contract` outright, leaving no Contract object for `V078` to compare. Separately, probing exposed a genuine `C024` violation that no vector had caught: a duplicate selector inside ONE class body was silently accepted with the second body winning, because `requires_override` is passed as false for an origin declaration and only a reopen was ever checked. A third defect sat behind it: the Class name is bound BEFORE the body is validated, so even once the duplicate was rejected the Class remained published, violating the `V080` side effect. | Fixed with no specification change. A `ContractId` identity and `Value::Contract` variant keep Contract objects distinct from Class, Module and Type per `C076`, and parents are recorded as a plain relation rather than composed into an MRO as `C043` requires. Duplicate selectors within one body now require `override`, and an origin declaration that fails validation unbinds its name so no Class is published. Four regression tests lock the clauses, including one asserting `evaluate_with_class_publication` reports no publication after rejection. `V078` and `V080` are retranscribed and pass; a tamper test replacing the `V080` source with a legal declaration correctly fails the row. RUNTIME 69 to 71 with zero regressions. | Resolved |
| `IRIS-V1-TYPES-C044`, `IRIS-V1-TYPES-C048`, `IRIS-V1-TYPES-C049`, `IRIS-V1-TYPES-C050`, `IRIS-V1-RUNTIME-V081` | `spec/iris-v1/05-types-contracts-generics.md`; `spec/iris-v1/03-runtime-object-model.md` | `219`; `227`; `229`; `231`; `602` | `C044` declares Class Contract conformance in the header with `for`. `C048` writes a qualified implementation as `impl ContractType::member`. `C049` writes an explicit qualified call as `(value as ContractType)..member(args)`, states that the `as` part proves or checks the nominal view, and requires ordinary `value.member` to always send the UNQUALIFIED selector. `V081` asserts `:qualified` then `:ordinary` with the two lookup namespaces staying separate. | `class ... for C` already parsed, but nothing else did. `impl` was a reserved word with no production, so neither a plain `impl` member nor a qualified `impl C::member` could be written. `as` and `as?` were absent from the parser's relational table even though `is` had just been added there, so `value as C` was a parse diagnostic and the `..` postfix that DID exist was unreachable. With no view value and no qualified slot table there was no way to keep the two namespaces apart. | Fixed with no specification change. `impl` parses after `override` per `C046`, and `impl C::member` records a qualifying Contract on the declaration. `as` and `as?` join `is` at the relational precedence the chapter 02 grammar assigns them. A `Value::ContractView` carries receiver and Contract identity as the identity-less capability `C050` describes, and building one REJECTS a receiver whose Class never declared that Contract with `for`, which is the proving half of `C049`. A qualified `impl` publishes into a separate slot table keyed by Class, Contract and selector, and `..member` resolves ONLY that table with no fallback to ordinary dispatch, so the namespaces cannot merge. Three regression tests lock the clauses, including one asserting a qualified implementation is unreachable through ordinary dispatch. `V081` is retranscribed with a second independent source proving the view check, and renaming the qualified slot correctly fails the row rather than silently falling back. RUNTIME 71 to 72 with zero regressions and GRAMMAR unchanged, which matters because this touched the parser. | Resolved |
| `IRIS-V1-RUNTIME-C085`, `IRIS-V1-RUNTIME-V107`, `IRIS-V1-RUNTIME-V086`, `IRIS-V1-RUNTIME-C068` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/traceability-matrix.md` | `262`; `628`; `610`; `214`; `104`-`105` | `D-094` separates two `<=>` failure modes: another `Integer` satisfies the broad `Integer?` return type but violates the protocol and raises `ComparisonContractError` under `D-093`, while a non-`Integer`, non-`nil` result violates the return type contract itself and raises `TypeError`. `V107` asserts the `TypeError` half. `V086` asserts that a raw ivar read returns the last written value for one instance and `nil` for another, and that the slot Type is `Dynamic<Object>` per `C068`. | The earlier `C085` implementation collapsed BOTH failure modes into `ComparisonContractError`, so the distinction `D-094` draws was unobservable and `V107` could not pass. A regression test written at the time asserted the collapsed behaviour, encoding the defect rather than catching it. The recorded blocker for `V107` also named `Dynamic<T>` construction, but the fixture's real requirement is return-Contract enforcement, which needs no `Dynamic<T>` syntax at all. | Fixed with no specification change. The two arms now separate: a wrong `Integer` yields `ComparisonContractError` and a non-`Integer`, non-`nil` result yields `TypeError`. The mistaken regression test is replaced by one asserting the distinction across all three cases, so the corpus can no longer encode the conflation. `V107` is retranscribed with both halves as independent sources, and tampering the expected code from `TypeError` to `ComparisonContractError` correctly fails the row. `V086` stays bucketed honestly: its behavioural half is correct, but the frozen row also asserts a `String` value, which the runtime has no representation for, and a `Dynamic<Object>` STATIC slot Type, which `C068` states as a static fact with no runtime observation path. RUNTIME 72 to 73 with zero regressions. | Resolved |
| `IRIS-V1-RUNTIME-C042`, `IRIS-V1-RUNTIME-C072`, `IRIS-V1-RUNTIME-C099`, `IRIS-V1-RUNTIME-V077`, `IRIS-V1-RUNTIME-V099` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/02-lexical-grammar.md` | `147`; `222`; `292`; `435`; `431` | `closure_literal ::= "{" closure_header? closure_body "}"` and `trailing_block ::= closure_literal` as a postfix part. `C042` requires each EVALUATION of a Closure expression to create a new identity-bearing object with identity-only default equality. `C072` requires a Closure created in an instance Method to capture its current receiver and keep writing that receiver's raw ivars after escape. `C099` passes the trailing block as a separate `block: Closure?` parameter. | Closure literals did not parse at all, which alone blocked three vectors. Beyond that, `==` on a Closure or BoundMethod raised `TypeError` because neither has a built-in Class to dispatch through, so `C042` identity-only equality was unreachable. Two defects surfaced during the work and neither had vector coverage: a bare `name(...)` inside a Method ALWAYS became a self-send, so a local holding a Closure was never consulted, and when that local was a nil block the self-send reached `method_missing`, whose body re-evaluated `b()`, recursing until the process aborted with a stack overflow rather than any diagnostic. | Fixed with no specification change. The parser admits `closure_literal` in primary position and `trailing_block` after an argument list, and a `no_trailing_block` flag suppresses both while parsing an `if` condition, since a branch body and a trailing block both open with `{`. Each evaluation allocates a fresh `Value::Closure` capturing the current locals and receiver, so `C042` identity and `C072` escape-write both hold. `==` and `!=` answer by identity for callables. A local binding now shadows a self-send for EVERY local rather than only callable ones, which is what stops the recursion. `V077` and `V099` are retranscribed and pass; the `V099` assertion INVOKES the block, because an earlier form comparing `b same? b` could not distinguish a Closure from nil and a tamper test proved it. `V087` stays bucketed honestly: its escape-write half is correct and verified, but the rebind half needs an API that `D-311` says must not exist, so there is nothing to observe. RUNTIME 73 to 75 with zero regressions. | Resolved |
| `IRIS-V1-CONTROL-C076`, `IRIS-V1-MIG-004`, `D-454`, `IRIS-V1-RUNTIME-V077`, `IRIS-V1-TRACE-C007`, `IRIS-V1-TRACE-C021` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/11-migration-divergence.md`; `spec/iris-v1/03-runtime-object-model.md` | `444`; `37`; `601` | The owner directed that a Closure be invoked as an object through `block.call(...)`, with the direct-application spelling REMOVED rather than kept alongside it. `IRIS-V1-GRAMMAR-C050` and `block_parameter` already gave the `&block` channel, which the implementation was not using. | This is not a gap an errata may close under `IRIS-V1-TRACE-C021`, which forbids reinterpreting a decided semantic. `IRIS-V1-MIG-004` recorded `cast.call(...)` as an `intentional-divergence` and stated that legacy `cast.call(value)` BECOMES `block(value)`, and `D-454` removed `call` as an invocation surface. Removing direct application reverses both. It also required amending the frozen `IRIS-V1-RUNTIME-V077` row, whose source spells `closure() == closure()`, which breaches `IRIS-V1-TRACE-C007` permanence. I raised all three conflicts before writing anything and the owner authorized the change explicitly. | Published as the v1.10 errata. `IRIS-V1-CONTROL-C076` is added under the next available number and states that `call` is the sole invocation spelling for the `C021` callable kinds, that direct application is not a call, and that it supersedes the invocation half of `MIG-004` and the `call`-removal part of `D-454`. The `MIG-004` row and its Simplified Chinese mirror record the supersession in place rather than being deleted, and the `cast` divergence itself stands. Two informative examples that spelled `block(value)` and `block(title)` are updated in both languages, as is the frozen `V077` source. Implementation follows: the parser now binds `&block` by DECLARATION rather than guessing which trailing argument is a Closure, which also fixes a real defect where an ordinary call ending in a Closure was misread as a block. Per `IRIS-V1-TRACE-C022` the corpus was run before and after: exactly `V077` and `V099` changed result, both were retranscribed to the `.call` spelling, and the suite returned to 75 with no other vector affected. This is the SIXTH owner-authorized `TRACE-C007` exception this milestone and the first to reverse a decided semantic rather than a spelling. | Resolved |
| `IRIS-V1-CONTROL-C023`, `IRIS-V1-CONTROL-C025`, `IRIS-V1-TYPES-C003`, `function_type` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/05-types-contracts-generics.md`; `spec/iris-v1/02-lexical-grammar.md` | `128`; `132`; `11`; `452` | `block_parameter ::= "&" ordinary_name ":" function_type ("=" "nil")?` requires a callable Type annotation, `C023` writes it as `&block: (P...) -> R`, `C025` binds an omitted optional block to `nil`, and `C003` defaults an omitted annotation to `Dynamic<Object>` without inferring anything from the body. | Two defects, both found by probing the owner's question about how the `&block` Type is derived rather than by any vector. `function_type` had NO parser production at all: `type_expression` accepted only a name, `typeof`, or an intersection, so `&b: (Integer) -> Symbol` was a parse diagnostic and the annotated form of the block channel could not be written. Separately, parameter binding zipped parameters against arguments, and `zip` stops at the shorter side, so a declared-but-unsupplied block was left UNBOUND rather than bound to `nil`, making `C025` unobservable and the parameter unresolvable in the body. | Fixed with no specification change; both clauses were correct and unimplemented. `TypeExpression::Function` is added and `type_expression` parses a leading `(` as `function_type`, so the annotated block channel now writes exactly as `C023` spells it. Parameter binding enumerates declared parameters and binds `nil` for any without a matching argument, which is what `C025` requires. The Type is therefore DECLARED rather than inferred, per `C003`: written annotations are obeyed, an omitted one is `Dynamic<Object>`, and no body fact is exported as signature metadata. Two regression tests lock the annotated and omitted forms. RUNTIME stays at 75 with zero regressions and GRAMMAR unchanged at 26. | Resolved |
| `IRIS-V1-TYPES-C094`, `IRIS-V1-TYPES-C095`, `IRIS-V1-TYPES-C096`, `IRIS-V1-TYPES-C036`, `IRIS-V1-CONTROL-C018`, `D-425` | `spec/iris-v1/05-types-contracts-generics.md`; `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/02-lexical-grammar.md` | `435`; `437`; `439`; `191`; `92` | The owner argued that `(P...) -> R` cannot tell a Closure from a BoundMethod, which sits badly with the `IRIS-V1-IDENTITY-C008` static-promise principle, and asked to reopen the specification rather than settle it in implementation. | `C036` and `C018` made the two kinds SHARE one unqualified callable Type, and `D-425` is the owning decision. I recorded the question with both sides first: `D-425` is deliberate, since it assigns unbound Methods a distinct reified Type in the same sentence, but `C008` enumerates the promised static facts and does NOT name callable kind, leaving a genuine ambiguity the frozen material never resolved. My objection to a bare `Closure<T>` was that it could no longer accept a BoundMethod even though `C021` lists both as ordinary callable kinds. The owner's `Block<T>` union answers exactly that objection. | Published as the v1.11 errata. `C094` reifies `Closure<S>` and `BoundMethod<S>` and makes the bare signature not a Type on its own. `C095` adds the language-core alias `type Block<S> = BoundMethod<S> | Closure<S>` for the trailing-block channel, transparent under `C015` and normalizing as an ordinary union under `C018`. `C096` makes callable Type arguments invariant like every other generic argument and moves signature compatibility to the CALL SITE, superseding the previous parameter-contravariance and return-covariance rule. `C036`, `CONTROL-C018` and `D-425` record their supersession IN PLACE rather than being deleted, and the unbound-Method half of `D-425` stands. The grammar gains `callable_type` and `block_parameter` now takes it. Per `TRACE-C022` the corpus was run before and after: ZERO vectors changed result, because no committed vector spells a callable annotation. Two of my own regression tests spelled the superseded form and were updated. This is the SEVENTH owner-authorized `TRACE-C007` exception this milestone and the THIRD to reverse a decided semantic. A follow-up sweep found FIVE sites the first pass had missed, ten counting the Simplified Chinese mirrors: the two `IRIS-V1-CONTROL-C023` parameter-table rows, the `let add:` binding in the Closure example, the frozen `IRIS-V1-CONTROL-V336` source, and `D-418`. The first pass had searched for the exact spellings `(P1, P2, ...) -> R` and `(P...) -> R` and so did not match concrete instantiations such as `(String) -> Nil` or `() -> Nil`. All ten are now updated and a residual search returns zero. The corpus was re-run after the sweep and still reports zero vectors changed. | Resolved |
| `IRIS-V1-RUNTIME-C163`, `IRIS-V1-TYPES-C095`, `IRIS-V1-RUNTIME-C005`, `IRIS-V1-RUNTIME-C046` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/05-types-contracts-generics.md` | `572`; `437`; `15`; `157` | The owner directed that `Kernel` be a top-level Module composed into `Object`, in the manner of Ruby, and that `Block<S>` live there rather than being an intrinsic alongside `Object`, `Never`, and `NonNil`. | `Kernel` did not exist in the frozen material, so this introduces a new normative entity rather than supplying a missing spelling, which is outside what `IRIS-V1-TRACE-C021` permits an errata to do on its own. The v1.11 placement was also weaker than it looked: `Object`, `Never` and `NonNil` are type-system constructs each owning a clause, whereas `Block<S>` is an ordinary Type alias under `C015` and had no declaring home. A real hazard sat behind the request: `IRIS-V1-RUNTIME-C046` searches composed Modules BEFORE the runtime superclass, so a member in `Kernel` colliding with one of the four `C005` defaults would take precedence and silently defeat `C005`. | Published as the v1.12 errata with owner approval. `IRIS-V1-RUNTIME-C163` defines `Kernel` as the language-core Module composed into `Object`, carrying language-core Type aliases and declarations visible everywhere without an import. It is an ordinary Module under `C046` through `C048`, so composition, lookup order and deduplication are unchanged, and it is explicitly not a second root Class. The `C005` hazard is closed in the clause itself: `Kernel` MUST NOT supply, replace or shadow default comparison, truthiness, missing-message or zero-argument `initialize`, and such a declaration MUST be rejected. `IRIS-V1-TYPES-C095` now attributes `Block<S>` to `Kernel` rather than to the intrinsic set, which gives the alias a declaring home consistent with `C015`. Per `TRACE-C022` the corpus was run before and after: ZERO vectors changed result, since no committed vector references `Kernel`. This is the EIGHTH owner-authorized exception this milestone; unlike the previous three it adds a new entity rather than reversing a decided semantic, and it strengthens `C005` by naming a collision that was previously unaddressed. | Resolved |
| `IRIS-V1-CONTROL-C037`, `IRIS-V1-RUNTIME-C093`, `IRIS-V1-RUNTIME-V098` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/03-runtime-object-model.md` | `202`; `280`; `618` | `C037` requires logical assignment to read the target location exactly once, write `rhs` only on the truthy path for `&&=` and only on the falsy path for `||=`, and states that a `to_bool` failure prevents both the RHS and the write. `C093` requires conditions, logical operators AND logical assignment to send `to_bool` once and require an actual `Bool`. | The evaluator destructured `Expression::Assignment { left, right, .. }`, DISCARDING the operator, so `&&=` and `||=` took the same path as plain assignment. They never sent `to_bool` at all: the right side was always evaluated and always written. A receiver whose `to_bool` raised was simply overwritten, so the `V098` assertion that `:sentinel` propagates was unobservable for two of its three forms, and `C037`'s no-write path did not exist. The `if` form was already correct, which is why the defect went unnoticed. | Fixed with no specification change; both clauses were correct and unimplemented. Logical assignment now reads the target once, sends `to_bool` through the same truthiness path conditions use, and evaluates the right side ONLY on the writing path, returning the current value otherwise. Probing confirms the no-write path leaves an appending right side unevaluated. `V098` is retranscribed as three independent sources covering `if`, `&&=` and `||=`, and tampering one source so its `to_bool` no longer raises correctly fails the row. Four regression tests lock the clause, including both no-write paths. RUNTIME 75 to 76 with zero regressions. | Resolved |
| `IRIS-V1-RUNTIME-V020`-`V045` no-fixture block, `IRIS-V1-CONTROL-C025`, `IRIS-V1-TYPES-C049` | `spec/iris-v1/03-runtime-object-model.md` | `535`-`560` | Eighteen overview rows described their scenario in prose and supplied no runnable source, so milestone 2 had classified them `no-fixture` rather than fabricate conformance evidence. The owner authorized authoring them. | Probing each row first showed that SIXTEEN state a concrete scenario and a concrete observable, needing only transcription rather than invention. Two real defects surfaced while checking them. An arity mismatch on an existing selector was silently accepted, so `a.m(1, 2)` against `fun m(x)` returned normally instead of raising the `ArgumentError` that `IRIS-V1-CONTROL-C025` requires; the arity check simply did not exist. Separately, a missing qualified Contract slot raised `MessageNotFound`, which would let `method_missing` be reached from the qualified namespace that `IRIS-V1-TYPES-C049` keeps separate; it now raises `ContractDispatchError`. | Sixteen fixtures authored and passing, each transcribing its frozen row verbatim and tagged `provenance:authored-fixture` so the corpus records that the source is ours rather than the specification's. No value the rows do not state was invented. Four representative tamper tests confirm the assertions are live, and one of them exposed a weak first attempt: tampering `V021` to call an ordinary Method still failed for the same reason, so the tamper was rewritten to call a real qualified slot, which correctly fails the row. Two rows remain unauthored with the exact gap recorded: `V024` needs Module-visible ancestry, since `ancestors` returns only Class entries and Module deduplication has no observation path, and `V025` needs the same out-of-band scheduling `V013` and `V083` wait on. RUNTIME 76 to 92, `no_fixture` 18 to 2, buckets still sum to 109. | Resolved |
| `IRIS-V1-RUNTIME-C134`, `IRIS-V1-RUNTIME-V072`, `hash_literal` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/02-lexical-grammar.md` | `364`; `596`; `317` | `hash_literal ::= "%{" hash_entry_list? "}"` with `hash_entry ::= expression ":" expression`. `C134` states that no quiet or signaling NaN of either width is a valid Hash key, and that Hash CONSTRUCTION, insertion, key update, or a direct public `hash` on NaN must raise `InvalidKeyError`. `V072` asserts all three of its terms raise. | The recorded blocker said `V072` needed the Hash collections library, which overstated it. Two of its three terms already raised correctly; only the literal did not parse, because `hash_literal` had no parser production even though the lexer already emitted a `HashOpen` token. Reading `C134` closely shows the row needs only CONSTRUCTION-time rejection, not a working Hash container, so the collections library was never the real requirement. | Fixed with no specification change. `hash_literal` now parses into an `Expression::Hash` node, and construction hashes every key eagerly so a NaN key raises at construction exactly as `C134` requires, for both widths. The Hash VALUE itself is deliberately not implemented: a literal with valid keys still reports an unsupported construct, because building a container would mean inventing storage, ordering and lookup semantics that chapter 06 owns and this row never asserts. `V072` is retranscribed as three independent sources and passes, and tampering the third to a non-NaN key correctly fails the row, which also proves the check is specific to NaN rather than firing on every literal. RUNTIME 92 to 93. | Resolved |
| `IRIS-V1-TYPES-C014`, `D-313`, `IRIS-V1-RUNTIME-V088`, chapter 02 `type_primary` | `spec/iris-v1/05-types-contracts-generics.md`; `spec/iris-v1/02-lexical-grammar.md`; `spec/iris-v1/traceability-matrix.md` | `61`; `173`; `448`; `324` | `C014` makes `Dynamic<T>` bounded dynamic message sending: entering it checks the value satisfies reified `T`, and inside the bound arbitrary ordinary selectors may be sent without static member validation. `D-313` requires that subclass code, external callers, `Dynamic<T>` AND ordinary reflection all fail to reach a private Method merely because the selector exists. `V088` asserts all four denials plus declaring-Class success. | Three of the four denials already worked; only the `Dynamic<T>` path was unreachable because the syntax did not parse. Two separate parser gaps caused that. `type_primary ::= qualified_type_name generic_args?` was not implemented, so no nominal Type could carry closed generic arguments at all. More subtly, `is`, `as` and `as?` parsed their RIGHT side as an EXPRESSION, although the chapter 02 grammar puts a TYPE there, so the `<` of `Dynamic<A>` was read as a comparison operator and the whole form failed. | Fixed with no specification change. Nominal Type names now accept `generic_args`, and the three type-testing operators parse their right side through `type_expression`, which is what the grammar specifies; `as C` and `is A` are unaffected and were probed to confirm it. Entering `Dynamic<T>` yields the same value per `C014` and is resolved before the right side is treated as an ordinary name. Crucially the Dynamic boundary disables STATIC member checking only: the runtime visibility denial still fires, which is exactly what `D-313` requires and what a tamper test confirms, since switching that source to a public Method correctly fails the row. `V088` is retranscribed with all five sources. RUNTIME 93 to 94. | Resolved |
| `D-266`, `IRIS-V1-GRAMMAR-C013`, `IRIS-V1-RUNTIME-V085` | `spec/iris-v1/traceability-matrix.md`; `spec/iris-v1/02-lexical-grammar.md` | `277`; `36`-`46`; `609` | `D-266` fixes the conventional signature as `migrate_revision(from: ClassRevision, to: ClassRevision) -> Nil`, and `V085` describes a fixture calling it. | `from` is a reserved keyword in the `IRIS-V1-GRAMMAR-C013` inventory, used by `import_decl` and by `raise_cause`. A parameter cannot be named `from`, so the signature `D-266` specifies is not writable in Iris source at all. Probing confirms the parser rejects `fun m(from)` while `fun m(a)` is accepted, so this is a genuine conflict between a frozen decision and the frozen keyword inventory rather than an implementation gap. | Recorded, not worked around. Writing the fixture with a different parameter name would make `V085` pass while silently diverging from the signature `D-266` states, and inventing a keyword-escape syntax would fabricate a production the grammar never defines. Resolving it needs an owner decision: either an errata restating the `D-266` signature with a non-reserved parameter name, or a grammar errata admitting reserved words in parameter position. Probing also NARROWED `V085` independently of this conflict: `D-265` makes `migrate_revision` an ordinary replaceable Method needing no runtime support, and both halves of `D-264` already hold, since a compatible open commits without the runtime enumerating or invoking anything and an explicit call returns `nil`. What the row still needs is the `ClassRevision` object of `D-266` and its `reactivate()` rejection, neither of which exists. | Open |
| `IRIS-V1-RUNTIME-C164`, `D-266`, `IRIS-V1-GRAMMAR-C013` | `spec/iris-v1/03-runtime-object-model.md`; `spec/iris-v1/traceability-matrix.md` | `574`; `277`; `36`-`46` | `D-266` fixed the conventional signature as `migrate_revision(from: ClassRevision, to: ClassRevision) -> Nil`, but `from` is a reserved keyword in the `GRAMMAR-C013` inventory, so that signature could not be written in Iris source at all. Recorded as a frozen conflict rather than worked around. | The owner chose option A: restate the signature rather than widen the grammar to admit reserved words in parameter position, which would have changed how every declaration parses. | Published as the v1.13 errata. `IRIS-V1-RUNTIME-C164` restates the signature as `migrate_revision(source: ClassRevision, target: ClassRevision) -> Nil`, changing ONLY the two parameter names and explicitly preserving the selector, parameter Types, return Type, and every semantic owned by `D-264`, `D-265` and `D-266`, including that the runtime never invokes it automatically. Six sites across both languages were updated and a residual search returns zero. Per `TRACE-C022` the corpus was run before and after: zero vectors changed result. | Resolved |
| `IRIS-V1-RUNTIME-C088`, `D-111`, `IRIS-V1-RUNTIME-V079` | `spec/iris-v1/03-runtime-object-model.md` | `268`; `604` | `C088` gives ordinary identity-bearing objects a runtime-stable identity hash in `0..2^64-1`, requires the same object to retain it for its whole lifetime AND across GC movement, and forbids exposing a raw memory address. `V079` observes the hash before and after a `compact_gc` step. | The identity hash did not reach source at all. The heap already assigned one at allocation, but `Runtime` never exposed it, and worse, `Object` had the NATIVE `Hash` selector installed, so a user class inherited a numeric-hash body through the MRO that the source evaluator has no body for. `a.hash()` therefore failed as a raise of `nil` rather than returning anything. This is the same shape of defect as the native comparison selectors previously installed on `Object`. | Partly resolved. `Runtime::identity_hash` exposes the allocation-time value, `Object` no longer receives the native `Hash` selector, and the evaluator answers `hash` on an ordinary object from the heap. Probing confirms the same object keeps its hash across later allocations, distinct objects differ, and the specification-stable NUMERIC hash is unaffected. `V079` is NOT authored: its fixture names a `compact_gc` step that does not exist, so the ACROSS-MOVEMENT half of `C088` has no observation path, and authoring it without that step would assert lifetime stability while claiming to test GC movement. | Open |
| `IRIS-V1-CONTROL-C043`, `IRIS-V1-CONTROL-V017`, `IRIS-V1-CONTROL-V018` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `247` | `C043` requires `while condition { body }` to test the condition with `to_bool` BEFORE each iteration, yield `nil` on natural completion including zero iterations, exit with `expr` for `break expr`, and exit with `nil` for a bare `break`. | The syntax tree and parser already carried `While` and `Break`, but nothing evaluated them: three separate sites rejected the statements. The evaluator's statement match, the block runner, and the literal-evaluator routing predicate each listed them as unsupported, so a loop could not run and a `break` could not leave a block. | Fixed with no specification change. `while` truth-tests before each iteration and returns `nil` on natural completion. `break` unwinds as a control signal carrying the loop result, which the target loop consumes: an unlabelled break or one naming this loop's own label is caught, and any other keeps unwinding to an outer loop. Loops route to the source runtime because the literal evaluator has no statement sequencing. `V017` and `V018` are transcribed and pass, and tampering each correctly fails it, including making `V017` break rather than complete naturally. Four assertions are locked by a regression test. The test itself needed correcting first: it bound `while` to a `let`, which the grammar does not allow since `while_statement` is a STATEMENT, and the runner caught that rather than the assertion being weakened. CONTROL 11 to 13. | Resolved |
| `IRIS-V1-CONTROL-C044`, `IRIS-V1-CONTROL-C046`, `IRIS-V1-COLLECTIONS-C013`, `IRIS-V1-CONTROL-V020`, `IRIS-V1-CONTROL-V022` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/06-collections-text-regex.md` | `249`; `253`; `70` | `C044` requires `for` to evaluate the iterable ONCE, call `next()` repeatedly, exit on `Iteration.done`, and bind each yielded value in a FRESH per-iteration scope. `C046` requires the Iterator to close on every exit path. `COLLECTIONS-C013` makes `Iteration.yield(value)` able to carry any value INCLUDING `nil`, distinct from the `done` singleton. | Two gaps, one of them not where the recorded reason pointed. The `Iteration` results did not exist as values at all, so a yielded `nil` could not be distinguished from exhaustion, which is precisely what `V020` tests. Separately the parser accepted ONLY the labelled form `outer: for x in y { }`: there was no unlabelled `for` production, so the ordinary spelling was a parse diagnostic even though the statement node and the evaluator dispatch both existed. | Fixed with no specification change. `Value::IterationYield` and `Value::IterationDone` implement `C013`, and `Iteration.yield`/`Iteration.done` are built as value constructors rather than Class sends. The unlabelled `for` production is added, suppressing a trailing block in the iterable position exactly as `while` does for its condition. Iteration binds each value in a fresh scope cloned per step, and `close` is sent on every exit path per `C046`. `V020` and `V022` are transcribed and pass. The `V022` tamper is the sharper one: rewriting its Closures to capture the shared outer cell instead of the per-iteration binding correctly fails the row, which proves it tests scope freshness rather than merely that two Closures exist. CONTROL 13 to 15. | Resolved |
| `IRIS-V1-CONTROL-C043`, `IRIS-V1-CONTROL-C048`, `IRIS-V1-CONTROL-V023` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `247`; `257` | `C043` states that `continue` starts the next iteration of the target loop and HAS NO VALUE. `C048` makes an unlabelled `break` or `continue` target the nearest enclosing loop, while `break outer: expr` and `continue outer` target the named lexical loop. | `continue` was unevaluated: the statement match, the block runner and the routing predicate each rejected it, the same three sites that had blocked `while` and `break`. Labelled targeting was already correct because the loop implementations consume only a signal that is unlabelled or names their own label, letting any other keep unwinding, but nothing had exercised it across two loop levels. | Fixed with no specification change. `continue` unwinds as a control signal carrying only a label, since `C043` gives it no value, and both loops consume a matching one by starting their next iteration. `V023` is transcribed and passes with an inner `break outer: 7` reaching the outer loop. The tamper is instructive rather than a plain failure: removing the label makes the break target the INNER loop, so the outer one spins forever, which demonstrates the label is what selects the target rather than the value merely propagating. A regression test also pins that a bare `break` leaves only the inner loop and that `continue` skips the rest of the body. Fixing the `Continue` arm exposed an unreachable-pattern warning that clippy would have failed on, caught before the gate ran. CONTROL 15 to 16. | Resolved |
| `IRIS-V1-CONTROL-C050`, `IRIS-V1-CONTROL-C051`, `IRIS-V1-CONTROL-C053`, `IRIS-V1-CONTROL-V025` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `287`; `289`; `293` | `C050` requires `match` to evaluate the scrutinee ONCE and test arms in source order with no fallthrough, the first match producing the result. `C051` fixes the v1 pattern vocabulary, and `C053` makes `_` discard without creating a binding. | Two gaps. The evaluator had no `match` at all, rejecting the statement outright. More subtly the parser's `pattern` production accepted only NAMES, so a literal pattern such as `1 => :one` was a parse diagnostic even though `Pattern::Literal` existed in the syntax tree; only the `else` fallback and binding patterns could be written. The scrutinee also needed the trailing-block suppression that `while` and `for` already use, since `match x {` would otherwise read the brace as a block. | Fixed with no specification change. `pattern_alternative` accepts a literal, `nil` or a Bool literal as a `Pattern::Literal` and anything else as a binding pattern, matching the `C051` vocabulary that is implementable today. Evaluation tests arms in source order, runs a guard only after structural success and after provisional bindings exist per `C052`, and discards those bindings when a guard fails. `V025` is transcribed and passes. Its tamper reorders the arms so a different one matches first, which correctly fails the row and proves the assertion tests SOURCE ORDER rather than merely that some arm matched. A regression test also pins the fallback path and that a binding pattern binds. CONTROL 16 to 17. | Resolved |
| `IRIS-V1-CONTROL-C045`, `IRIS-V1-CONTROL-C051`, `IRIS-V1-CONTROL-V021` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/02-lexical-grammar.md` | `251`; `289`; `454` | `C045` lets `for` destructure using the binding, Tuple, Array, rest and `_` subset, requires a mismatch to raise `PatternMatchError`, and requires the Iterator to be CLOSED before that error propagates. `binding_pattern` in chapter 02 admits a name, `_`, and bracketed forms. | The `For` syntax node stored its binding as a plain `String`, so the tree could not represent a destructuring pattern at all; the parser likewise read only a name. This is wider than the earlier loop work, which needed evaluator wiring alone. | Fixed with no specification change. `For.binding` is widened to a `Pattern`, `Pattern::Array` is added, and a `binding_pattern` production parses the name and array forms. Destructuring reuses the SAME `pattern_matches` routine `match` uses, so the two cannot diverge. A mismatch raises `PatternMatchError`, and the existing `for_statement` shape already satisfies the `C045` ordering: `close` is sent unconditionally BEFORE the outcome is matched. Worth recording precisely, because probing could not observe that ordering. A raising `close` is discarded on the error path, so the ordering was confirmed by reading the code path rather than by probe, and the discard itself is a real gap: `IRIS-V1-CONTROL-C047` requires a close failure during cleanup to be appended to the primary context's suppressed list, which needs `ExceptionContext` and is not implemented. The Tuple form is deliberately omitted rather than approximated, since no Tuple value exists and inventing one would fabricate semantics chapter 06 owns. `V021` is transcribed and passes; its tamper makes the yielded arity MATCH the pattern, which correctly fails the row. CONTROL 17 to 18. | Resolved |
| `IRIS-V1-CONTROL-C056`, `IRIS-V1-CONTROL-C057`, `IRIS-V1-CONTROL-C058`, `IRIS-V1-CONTROL-C060`, `IRIS-V1-CONTROL-V027`-`V031` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `324`; `326`; `328`; `332` | `C056` gives every `raise` a fresh identity-bearing `ExceptionContext` carrying the raised value. `C057` requires an explicit cause to BE an `ExceptionContext` and lets `from nil` suppress chaining. `C058` makes a bare `raise` continue the current propagation. `C060` binds both the raised object and its context in `catch error: Type, context`. | The context did not exist as a value. `CatchClause.context` and `Raise.cause` were both already carried by the syntax tree and parsed, but neither was evaluated: a catch dropped the context binding and a `from` cause was ignored entirely, so an invalid cause passed silently. A bare `raise` outside any catch extent also failed as a generic unsupported construct rather than the specified error. | Fixed with no specification change. `Value::ExceptionContext` carries the raised value and its cause. A catch binds it per `C060`, `raise value from cause` validates that the cause is an `ExceptionContext` or `nil` and raises `TypeError` otherwise per `C057`, and a bare `raise` outside a catch extent raises `NoActiveExceptionError` per `C058`. `context.value` and `context.cause` are ordinary reads, matching the chapter 04 exception example. Four rows are transcribed and pass, each tamper-tested; the `V031` tamper is the sharpest, moving the bare `raise` INSIDE a catch so an active propagation exists, which correctly fails the row. `V030` stays bucketed: it needs re-raise SITE recording on the continued context, which is a distinct capability from continuing the propagation. While citing clauses I initially attributed the context reads to `C059`, which actually governs catch matching; the citation is corrected to the chapter 04 example that fixes the spelling. CONTROL 18 to 22. | Resolved |
| `IRIS-V1-CONTROL-C047`, `IRIS-V1-CONTROL-C064`, `IRIS-V1-CONTROL-C065`, `D-161`, `IRIS-V1-CONTROL-V034`-`V036` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/traceability-matrix.md` | `340`; `342`; `172` | `C064` makes a `finally` that raises while a context is pending become PRIMARY, with the pending context as its cause. `C047` requires a cleanup `close` failure to be appended to the primary context's suppressed list in occurrence order. `C065` exposes `suppressed` as a get-only property. `D-161` forbids a cause cycle and requires the check to run BEFORE linkage so a rejected attempt leaves the graph unchanged. | Three gaps. A `finally` raise simply OVERWROTE the pending context, losing the chain `C064` requires. The suppressed list did not exist, which is the gap recorded when `V021` landed: a failing `close` was silently discarded on the error path rather than appended. And no cycle check existed, so `raise x from c` where `c` already carried `x` would have built a cyclic graph. | Fixed with no specification change. `Value::ExceptionContext` gains an ordered suppressed list, `C065` exposes it as an ordinary read, and a `finally` raise now chains the pending context as its cause. A `for` whose `close` fails while a primary propagation exists appends the cleanup context rather than displacing the primary, closing the gap `V021` had recorded. Cause linkage performs an identity-based reachability check first and raises `ExceptionChainError` when it would close a cycle, leaving the existing graph untouched. Three rows are transcribed and pass, each tamper-tested by REMOVING the condition under test: a `finally` that no longer raises, a `close` that no longer fails, and a cause that no longer cycles. All three correctly fail, which shows the assertions test the specified behaviour rather than merely that an exception occurred. CONTROL 22 to 25. | Resolved |
| `IRIS-V1-CONTROL-C023`, `IRIS-V1-CONTROL-C025`, `IRIS-V1-CONTROL-C026`, `D-357`, `IRIS-V1-CONTROL-V009` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/traceability-matrix.md` | `128`; `130` | `C023` gives six parameter categories: required and optional positional, `*args` rest, required and optional keyword-only, and `**kwargs` keyword rest. `C025` raises `ArgumentError` on an arity mismatch. `C026` fixes left-to-right evaluation order across the positional and keyword channels. `D-357` makes a duplicate keyword argument an `ArgumentError` after selector resolution. | Parameters were stored as bare names, so every declared category collapsed to positional: a parsed default expression was DISCARDED and the parameter silently bound `nil`, `*args` and `**kwargs` did not parse, and keyword arguments had no call-site syntax at all. Binding also filled any unmatched parameter with `nil`, which made a missing required argument silently succeed rather than raise. | Fixed with no specification change. A parameter now carries its category and default, the parser accepts `*args`, `**kwargs`, `key name` and call-site `name: value`, and binding fills positionals in order, collects rest into a fresh Array, matches keywords BY NAME, applies defaults, and raises `ArgumentError` for a missing required parameter, an unmatched argument, or a duplicate keyword. `**kwargs` binds a `Hash<Symbol,V>`; Hash is not yet a representable runtime Value, so that one category is parsed and rejected rather than bound to a stand-in of the wrong type, which keeps `V008` bucketed. `V009` is transcribed and passes, tamper-tested by making the two keywords DISTINCT so no duplicate exists. CONTROL 25 to 26. | Resolved |
| `IRIS-V1-RUNTIME-C099`, `IRIS-V1-CONTROL-C025` | `crates/iris-runtime/src/identity.rs`; `crates/iris-runtime/src/runtime.rs` | n/a | Construction dispatches the `initialize` selector, and `NativeSelector` assigns each builtin operator a reserved selector id. | Implementation defect, not a specification gap. Construction dispatched the literal `Selector::new(1)` for `initialize`, but `NativeSelector::Add` also has raw id `1`. Declaring `+` on a Class therefore registered a method under the SAME selector construction dispatches, so `V.new()` invoked `+` with no arguments. The old permissive binding masked this by filling the missing parameter with `nil`; enforcing `C025` exposed it as an `ArgumentError` on plain construction. | `Selector::INITIALIZE` is a named reserved constant above every `NativeSelector` id, and construction and the evaluator both use it instead of a bare `1`. A regression test constructs a Class declaring `+` and sends `+` to it, since the collision made ordinary construction fail. | Resolved |
| `IRIS-V1-COLLECTIONS-C027`, `IRIS-V1-COLLECTIONS-C028`, `IRIS-V1-COLLECTIONS-C033`, `IRIS-V1-RUNTIME-C134`, `IRIS-V1-CONTROL-C023`, `IRIS-V1-CONTROL-V008` | `spec/iris-v1/06-collections-text-regex.md`; `spec/iris-v1/03-runtime-object-model.md` | `100`; `102`; `112`; `364` | `C027` gives Hash literals the `%{ key: value }` form with ordinary expression keys. `C028` dispatches each key's current `hash` and `==` for insertion and update. `C033` leaves Hash iteration order UNSPECIFIED. `C134` requires a NaN key to raise `InvalidKeyError`. `C023` binds `**kwargs` to a fresh `Hash<Symbol,V>`. | Hash was not a representable runtime Value. A Hash literal evaluated its keys, hashed them so the `C134` NaN rejection still fired, and then discarded the result as `UnsupportedConstruct`. This also kept `**kwargs` unbindable, which is why `V008` stayed bucketed when the other five parameter categories landed. | Fixed with no specification change. `Value::Hash` stores entries as an association list rather than a host `HashMap`, because `C028` dispatches the key's current `==` and a host map would impose both a host hash and a host equality the clause does not permit. A repeated literal key updates its entry under `C028` rather than adding a second. The `C134` NaN rejection is preserved and retested. `**kwargs` now binds the unmatched keywords, closing `V008` with all six categories exercised in one call. Because `C033` refuses to promise iteration order, the conformance renderer emits only the entry COUNT, so no vector can depend on an order the clause leaves unspecified. `V008` is tamper-tested twice: changing the expected count, and removing the extra keyword so the collected Hash is empty. CONTROL 26 to 27. | Resolved |
| `IRIS-V1-CONFORMANCE-C001` | `crates/iris-conformance/src/json.rs` | n/a | Vector expectations are compared against observed runtime values. | Evidence-integrity defect found while transcribing `V008`. The corpus JSON parser models every numeric literal as a VALUELESS `Value::Number` variant, so `{"hash":0}` and `{"hash":1}` parse identically. Any expectation written as a bare JSON number is therefore HOLLOW: it passes for any number at that position. The defect surfaced because a first attempt at `V008` appeared to fail against its own expected count. | The Hash entry count is expressed as a STRING, matching what the corpus already does for `integer`, which keeps the two distinguishable. A test in `json.rs` locks the hazard by asserting that two different bare numbers both parse to `Value::Number` while their string forms stay distinct. An audit of the committed corpus found exactly one bare-number expectation, `reserved_keyword_count` in `IRIS-V1-GRAMMAR-V010`, which is a `status:authored-expect` row whose count no runner code reads, so no live assertion was weakened. | Resolved |
| `IRIS-V1-CONTROL-C036`, `D-347`, `D-348` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/traceability-matrix.md` | `358` | `C036` requires `target OP= rhs` to evaluate the target location and `rhs` exactly once, read the target once, send the ORDINARY operator `OP` to the read value, and write the result once. `D-348` names the ten symbolic forms and stresses that none is an independent selector. | Implementation defect. Compound assignment DISCARDED its operator entirely and wrote the right-hand side straight over the target, so `a = 1; a += 2` yielded `2` instead of `3`, and all ten forms silently produced wrong answers on every target. The defect was invisible because no vector exercised a compound assignment and the corpus had no arithmetic round-trip covering one. | Fixed with no specification change. A single `compound_selector` mapping turns each compound form into its ordinary operator selector, and the name, member and index paths all read the target once, send that operator, and write once. `Assign` and the `C037` logical forms map to `None` and keep their existing paths, since logical assignment short-circuits and must not evaluate the right side on the non-writing path. | Resolved |
| `IRIS-V1-CONTROL-C036`, `IRIS-V1-COLLECTIONS-C051`, `D-347`, `IRIS-V1-CONTROL-V012` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/06-collections-text-regex.md` | `358` | `D-347` requires the receiver, index and RHS of a compound index assignment to be evaluated exactly once, with side effects never duplicated. | Index expressions did not exist in the parser at all. `a[0]` parsed as TWO statements, the name `a` followed by the Array literal `[0]`, which is why earlier probing of index access looked like it returned a value: a multi-statement program yields every statement value, and the array literal was the second one. | Fixed with no specification change. `Expression::Index` is a postfix part, disambiguated from an Array literal statement by whether the `[` opens a new logical line, so `a[0]` is one expression while a `[` starting a line still parses as a literal. Reads and writes cover Array and Hash and fall back to an ordinary `[]`/`[]=` send so a user Class may define its own. A missing Hash key and an out-of-range Array index read `nil` rather than raising. The built-in containers are value-typed rather than heap cells, so a container reached through a NAME is stored back through its binding; without that the write was computed and then LOST, which probing caught before transcription. `V012` is transcribed with counters proving single evaluation, and is tamper-tested twice by rewriting the compound form into one that evaluates the index twice, and into one that evaluates the RHS three times. Both are caught. CONTROL 27 to 28. | Resolved |
| `IRIS-V1-CONTROL-C004`, `IRIS-V1-CONTROL-C009`, `D-421`, `D-426`, `D-427`, `D-438`, `IRIS-V1-CONTROL-V002`, `IRIS-V1-CONTROL-V003`, `IRIS-V1-CONTROL-V005` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `13`; `46`; `398`-`403` | `C004` requires `let` to have an initializer and rejects an untyped uninitialized `mut` with `BINDING_MISSING_TYPE_FOR_DEFERRED_INIT`. `C009` requires assignment to an absent ordinary local to fail as `NameError` and create NO binding. `D-421` puts `return` at a callable boundary and forbids `break`/`continue` from targeting a loop outside a Closure call boundary. `D-426` makes a bare `name = expr` assign an existing mutable binding only. | No static analysis phase existed; the diagnostic channel reported lexical and parse diagnostics only. The parser also DISCARDED the information the rows depend on: `let x: Integer` with no initializer parsed as a bare `Name("x")`, silently dropping both the declaration keyword and the annotation, so nothing downstream could tell a deferred binding from an ordinary name read. Assignment to an absent local was `UnsupportedConstruct` rather than the `NameError` the spec names. | Fixed with no specification change. `Statement::DeferredBinding` retains the declaration keyword and whether an annotation was present. A new static pass runs only on an ACCEPTED program, so parse damage is never attributed to the static phase, and it tracks binding mutability and a control context in which a Closure RESETS the loop depth, which is how `D-421` scopes a loop transfer. Assignment to an absent local now raises the spec-named `NameError`. `V002`, `V003` and `V005` are transcribed and pass, each tamper-tested by REMOVING the condition under test: adding the missing initializer, adding the missing annotation, and declaring the assignment target. CONTROL 28 to 31. | Resolved |
| `IRIS-V1-CONTROL-C017`, `D-421`, `D-426`, `D-438` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `90` | The `V007`, `V019`, `V024`, `V038` and `V040` rows assert diagnostics for a Closure missing a return annotation, a loop transfer with no target, a transfer crossing a Closure boundary, a misplaced `return`, and a write to an immutable binding. | Evidence-integrity limit rather than a defect. The static pass DETECTS the conditions behind `V019`, `V024`, `V038` and `V040`, and the implementation reports codes for each. But the frozen spec names a stable diagnostic code only for `C004` and `C017`; for these rows it gives prose such as "control-target error". Transcribing them against implementation-invented codes would assert the implementation against itself rather than against the specification. | Deliberately left bucketed rather than transcribed against invented codes, matching the `status:authored-expect` precedent already used in the GRAMMAR corpus for rows whose frozen text names no stable code. The detection is instead locked by unit tests in `analysis.rs`, which cover both the rejected forms and the legal ones. `V007`, `V026` and `V041` additionally need expected-callable-type or local type inference that does not exist, and `V004` needs definite-assignment tracking, so those four are blocked on capability rather than on naming. | Open |
| `IRIS-V1-CONTROL-C077`, `IRIS-V1-CONTROL-V019`, `IRIS-V1-CONTROL-V024`, `IRIS-V1-CONTROL-V038`, `IRIS-V1-CONTROL-V040`, `D-421`, `D-426`, `D-438` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/zh-cn/04-bindings-callables-control-flow.md` | `446` | `V019`, `V038` and `V040` described a required diagnostic in PROSE ("control-target error", "invalid-return-placement diagnostic", "static immutable-binding diagnostic") without naming a stable code, so a conforming implementation had nothing stable to report and a vector had nothing spec-derived to assert. | Errata candidate under `IRIS-V1-TRACE-C019`, raised with the owner and approved. Investigation while drafting found that `V024` did NOT need an errata: `CONTROL_TARGET_CROSSES_CLOSURE` was already named by `IRIS-V1-CONTROL-V339A` under the same `D-421`, so only three rows genuinely lacked a code. The same search also found a pre-existing `IRIS-V1-TRACE-C020` violation: the zh-cn chapter 04 was missing `IRIS-V1-CONTROL-C076` entirely, having never received the v1.10 errata, leaving EN at 77 clauses and zh-cn at 75. | Published as the v1.14 errata. `IRIS-V1-CONTROL-C077` names `CONTROL_TRANSFER_WITHOUT_TARGET`, `CONTROL_RETURN_OUTSIDE_CALLABLE` and `BINDING_ASSIGN_TO_IMMUTABLE`, states explicitly that it names codes ONLY and changes no rejection condition, and records that `CONTROL_TRANSFER_WITHOUT_TARGET` and `CONTROL_TARGET_CROSSES_CLOSURE` stay distinct: no target loop exists at all, versus a target that exists across a Closure boundary. The implementation collapsed those two into one code and now distinguishes them by remembering whether a loop was in scope outside the nearest Closure. The missing `C076` was backfilled into zh-cn in the same revision, restoring 77/77. `TRACE-C022` verification: no committed vector changed result, and the four rows are each tamper-tested by REMOVING the condition under test. CONTROL 31 to 35. | Resolved |
| `IRIS-V1-CONTROL-C047`, `IRIS-V1-CONTROL-V361` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `340` | `C047` requires a cleanup failure to be APPENDED to the primary context's suppressed list rather than replacing the primary propagation. `V361` requires the retained payload to report `value == :body` with one suppressed `ExceptionContext.value == :close`. | Implementation defect found while assessing untranscribed rows. Every `raise` installs a fresh `active_context`, so a `close` that raised during `for` cleanup OVERWROTE the primary context before the suppressed-list append could read it. The caught value and its bound context then disagreed: `catch e, c` reported `e == :body` but `c.value == :close`. The earlier `C047` test did not catch this because it read only the caught VALUE, which was already correct; the defect was visible only by reading the bound context in the same handler. | Fixed with no specification change. The primary context is captured BEFORE cleanup runs and restored afterwards, whether cleanup succeeds or fails. The `C047` regression test now reads `[e, c.value, c.suppressed]` together, and adds a cleanup-succeeds case, so a future regression that desynchronises the value from its context fails the test. | Resolved |
| `IRIS-V1-CONTROL-C056`, `IRIS-V1-CONTROL-V314` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `324` | `C056` makes every `raise` create a fresh propagation event with a DISTINCT identity-bearing runtime-owned `ExceptionContext`. `V314` requires `[false, true]`: re-raising a caught value yields a context that `same?` separates from the handled one, while the VALUE remains identical. | Implementation defect found while transcribing `V314`. `ExceptionContext` carried no identity and was compared structurally, so re-raising the same value produced a context that compared EQUAL to the one being handled. `same?` therefore reported `true` where the frozen row requires `false`. The defect was invisible for differing values, which is why every earlier exception test passed: they all raised distinct values. | Fixed with no specification change. `Value::ExceptionContext` carries a leading `ObjectId` allocated per propagation event from the same counter Closure identities use, so no two events collide and a context remains `same?` itself. Conformance rendering elides the identity, since `C056` promises distinctness but not any particular allocation order. | Resolved |
| `IRIS-V1-CONFORMANCE-C001` | `crates/iris-conformance/src/runner.rs` | n/a | The runner executes each vector's source and compares the observed result. | Robustness defect found when a tamper case rewrote `IRIS-V1-CONTROL-V356` into `outer: while true { while true { break 9 } }`. The inner `break` leaves only the inner loop, so the outer `while true` reruns forever. The runner has NO timeout or iteration bound, so a single non-terminating vector hangs the entire suite with no diagnostic. The tamper was withdrawn and replaced with one that changes the carried value instead. | Fixed. Evaluation now carries two bounds. A step budget charges each loop iteration and each Method invocation, and an invocation-depth bound covers unbounded RECURSION, which the step budget alone cannot stop: each Iris frame consumes several host frames, so a budget large enough for ordinary loops would abort the process on stack overflow before being exhausted. Both bounds were set from MEASUREMENT, not assumption: overflow occurs near 115 frames on the main thread but near 28 on a default test thread, so the depth bound is 16, below the tighter of the two. Exhausting either yields `StepBudgetExhausted`, which the runner reports as an ordinary error. The exact source that hung the suite is locked as a regression test, alongside ordinary loops and recursion that must still pass. | Resolved |
| `IRIS-V1-CONTROL-C028`, `IRIS-V1-CONTROL-V346` | `spec/iris-v1/04-bindings-callables-control-flow.md` | n/a | `C028` captures an outer binding by reference, and `V346` requires a nested block that declares the same name to SHADOW the captured binding, so the capturing Closure still observes the original value. | Implementation defect found while probing rows for top-level `fun`. A Closure body ran its statements directly against the enclosing binding map instead of through the scoped block evaluator, so a `let` inside a Closure LEAKED outward and overwrote an outer name of the same spelling. `V346` returned `[2, 2]` where the frozen row requires `[2, 1]`: the earlier Closure observed a value declared inside an unrelated later Closure. | Fixed with no specification change. The Closure body now runs through the same scoped block evaluator every other body uses, so its bindings are local. Capture by reference is unaffected and is retested alongside, since a fix that scoped too aggressively would break the shared-cell behaviour `C028` requires. Six rows are transcribed: `V338`, `V340`, `V341`, `V343`, `V346` and `V353`, each tamper-tested. CONTROL 48 to 54. | Resolved |
| `IRIS-V1-COLLECTIONS-C011`, `IRIS-V1-COLLECTIONS-C012`, `IRIS-V1-COLLECTIONS-C013` | `spec/iris-v1/06-collections-text-regex.md` | `55`; `68`; `70` | `C011` lists Array among the iterable categories, `C012` drives `for` through `iterator()` then repeated `next()`, and `C013` returns the same `Iteration.done` singleton after exhaustion. | `for x in [1, 2, 3]` raised a type error: Array answered neither `iterator` nor `next`. Found while writing an execution-bound test, which had to express an ordinary loop with a scripted iterator because the Array form did not work. | Fixed with no specification change. `Array#iterator` allocates a cursor whose position lives in the evaluator keyed by identity, since a Value is copied on every send and would discard the advance. Each call allocates an INDEPENDENT cursor, so a nested traversal of one Array does not share a position; the regression test covers that case specifically, because a shared cursor would still pass a single-loop test. | Resolved |
| `D-421`, `IRIS-V1-CONTROL-V037`, `IRIS-V1-CONTROL-V339` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/traceability-matrix.md` | n/a | `D-421` makes `return` inside a Closure end only THAT Closure invocation, with no nonlocal-return Closure and no `LocalJumpError` model in v1. `V037` requires active traversal cleanup to run before the callable returns. | `return` was entirely unimplemented for both Methods and Closures, reported as `UnsupportedConstruct`. | Fixed with no specification change. `return` is a control signal caught at the NEAREST callable boundary, so a Closure `return` is absorbed by that Closure and the enclosing Method continues, which is exactly what `D-421` forbids implementing as a nonlocal return. Existing cleanup already runs on the error path, so `V037` closure traversal cleanup worked once the signal existed. The `V339` tamper asserts the nonlocal-return reading `D-421` rejects, and correctly fails. CONTROL 54 to 56. | Resolved |
| `IRIS-V1-CONTROL-D-426` | `crates/iris-eval/src/source_runtime.rs` | n/a | `D-426` declares `mut name = expr` a mutable binding, assignable by a later bare `name = expr`. | Implementation defect found while testing `return` inside a loop. Block-local bindings were stored as plain values carrying no mutability, so a `let mut` inside ANY Method or Closure body was unassignable and every write to one raised `ImmutableBindingError`. No Method could use a mutable local at all. Top-level `mut` worked, which is why no existing vector caught it. | Fixed with no specification change. A block-local `mut` binding is registered in the assignable environment and whatever it shadowed is restored when the block exits. The restore runs on EVERY exit path, including the `break`, `return` and raise paths that leave a block early, so a mutable local cannot escape its block. Both properties are locked by tests: assignment inside a Method must succeed, and the name must be unresolvable afterwards. | Resolved |
| `IRIS-V1-CONTROL-V307`, `IRIS-V1-CONTROL-V310`-`V313` | `spec/iris-v1/04-bindings-callables-control-flow.md` | n/a | `V311` through `V313` read the RESULT of a `try`, so the form is value-producing: the body supplies the result on normal completion, a matching catch supplies it when one handles the propagation, and a normally completing `finally` does not replace the provisional value. | `try` was statement-only in the parser, so `let a = try { 1 } finally { 2 }` did not parse and four rows were unreachable. | Fixed with no specification change. `Expression::Try` parses through the SAME `try_parts` routine the statement form uses, so the two cannot diverge on clause shape, and evaluates through the same `try_statement` evaluator, so they cannot disagree on ordering, handler selection, or which clause supplies the result. Five rows are transcribed and tamper-tested, including a `V311` tamper asserting the reading where `finally` replaces the value, which correctly fails. CONTROL 56 to 61. | Resolved |
| `D-159`, `IRIS-V1-CONTROL-V302A` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `491` | `V302A` requires assigning `context.value` to raise the spec-named `ReadonlyPropertyError`, leaving the raised object unchanged. | The setter was not recognised at all, so the write fell through to a generic type failure. The observable code was therefore wrong even though the write did not take effect. | Fixed with no specification change. The `ExceptionContext` setters are rejected explicitly with `ReadonlyPropertyError`, and reading the payload is retested alongside so a fix that rejected both would not pass. Probing this cluster also confirmed that subclass `catch` matching, first-match handler selection, and context identity across separate raises of the SAME value already behaved as the frozen rows require, so `V291`, `V296` and `V306` needed no implementation change. CONTROL 61 to 65. | Resolved |
| `IRIS-V1-CONTROL-C043`, `IRIS-V1-CONTROL-V355` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `247` | `C043` gives a loop a VALUE: natural completion, including zero iterations, yields `nil`, and `break expr` yields `expr` as the loop result. | `while` was statement-only, so `let b = while true { break 7 }` did not parse and the loop value could not be observed at all. | Fixed with no specification change. `Expression::While` evaluates through the SAME `while_statement` evaluator the statement form uses, so the two spellings cannot disagree about the loop value. The statement spelling is retested alongside. Four rows are transcribed and tamper-tested, including a `V318` tamper that declares plain `ready`/`save` while still sending `ready?`/`save!`, proving the suffixed selectors are DISTINCT rather than merely accepted. | Resolved |
| `IRIS-V1-CONTROL-C011`, `IRIS-V1-CONTROL-V354A`, `IRIS-V1-RUNTIME-V065` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `50` | `C011` requires a non-call unresolved bare `name` to raise or diagnose `NameError`, and forbids it implicitly reading a property, Method, global, or runtime-added member. `V065` calls bare `nan` and `inf` ABSENT names. | An unresolved bare name reported `UnsupportedConstruct` on both evaluation paths, which conflates "this implementation cannot do it" with "the program named something that does not exist". `V354A` asserts the branch-local binding is invisible outside its branch, and could not be transcribed against the wrong code. | Fixed with no specification change. Both the source runtime and the literal evaluator now raise `NameError` for an unresolved bare name. Three tests that asserted the old code were updated rather than the behaviour: two of them, including the `V065` bare-special-value test, were asserting the conflated code as if it were correct. | Resolved |
| `IRIS-V1-RUNTIME-V102` | `spec/iris-v1/03-runtime-object-model.md` | `630` | `V102` requires `5 % 2` to be REJECTED BY GRAMMAR, while `mod` works as both a Method and a named infix. | Evidence-integrity defect. The expression parser turned ANY leftover token into a bare `Name`, so `5 % 2` parsed as three separate statements and `%` was never rejected at all. The row passed only because the stray `%` name later failed for an unrelated reason, and its committed expectation had been authored to that incidental code. Making unresolved names raise `NameError` changed that incidental code and exposed the row. | Fixed with no specification change. Only an ordinary identifier may become a `Name`; any other leftover token is a parse error, so `%` is now rejected by the grammar as the row requires. The row's expectation is corrected to the parse rejection it always should have asserted, and a regression test pins both halves: `%` rejected, and the `mod` spellings still working. | Resolved |
| `IRIS-V1-CONTROL-V324`, `IRIS-V1-CONTROL-V359`, `IRIS-V1-GRAMMAR-V188` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `511`; `557` | `V324` requires `x %= 2` to produce `PARSE_UNSUPPORTED_COMPOUND_ASSIGNMENT` while the ten compound assignments `C036` lists all parse. `V359` requires `defer { ... }` to produce `PARSE_UNSUPPORTED_DEFER` with no cleanup Closure created or run. | Both codes are NAMED by their frozen rows, but neither was implemented. `defer` produced no diagnostic at all, and `%=` produced `PARSE_INVALID_ASSIGNMENT_OPERATOR`, a code this implementation invented. `IRIS-V1-GRAMMAR-V188` asserts the SAME `%=` rejection and had been authored to that invented code, so the corpus recorded a locally chosen spelling where the specification supplies one. | Fixed with no specification change. Both parser sites now report the spec-named codes, and `V188` is realigned to the code `V324` names, since two rows asserting one rejection must not disagree about its identity. The regression test also pins that all ten listed compound assignments still parse, so the rejection stays specific to `%=` rather than to compound assignment generally. CONTROL 74 to 76. | Resolved |
| `IRIS-V1-CONTROL-V362`, `IRIS-V1-RUNTIME-C005` | `spec/iris-v1/04-bindings-callables-control-flow.md` | n/a | `V362` removes the inherited `to_bool` from a fixture Class whose `method_missing` records the call and returns `true`, then evaluates `if FallbackTruth.new() { :then } else { :else }`. It requires `method_missing` to be called EXACTLY ONCE with `[:to_bool, [], nil]` and its Bool result used directly. | Evidence-integrity finding, recorded rather than transcribed. Probing this row produced a FALSE POSITIVE: the source returns `:then` and looked correct, but a call counter shows `method_missing` runs ZERO times. The truthiness path never dispatches the missing message; with `to_bool` absent it falls back to default truthiness, which also yields `:then`. The expected observable is therefore reachable without the behaviour the row asserts. `method_missing` itself does work for an ordinary unknown selector, which is why the gap is specific to the truthiness path. | Left in `needs-subsystem` with the false positive documented in the classification. Transcribing it against the observable alone would have committed a hollow row that passes without `method_missing` ever being consulted. Closing it requires the truthiness protocol to dispatch a missing `to_bool` through `method_missing` rather than short-circuiting to the default. | Open |
| `IRIS-V1-RUNTIME-C005`, `IRIS-V1-RUNTIME-C094`, `IRIS-V1-CONTROL-V325` | `spec/iris-v1/03-runtime-object-model.md` | `15`; `282` | `C005` makes `Object` the single root runtime Class, and `C094` gives root `Object` a `to_bool` returning `true`, with only `Nil` false and `Bool` returning itself. | Implementation defect found while probing `V325`. The kernel's `class_of` returned a TYPE ERROR for every value with no dedicated builtin Class, so Symbol, Array, Hash, Closure and Object values had no Class at all and `to_bool` never dispatched. `if :sym`, `if [1]` and `if %{}` all failed outright, and `x \|\|= v` failed whenever the target was already truthy, because reading the target went through the same protocol. The defect was invisible to the existing corpus because every committed truthiness vector tested `nil`, a Bool, or a numeric. | Fixed with no specification change. A value with no dedicated builtin Class now resolves to root `Object`, which is what `C005` already required, so `to_bool` reaches the `C094` default. One unit test changed and was updated rather than the behaviour: `[1] + [2]` now reports the selector as NOT FOUND instead of a type mismatch, which is more accurate and matches the `size` case asserted beside it. `V315` and `V325` are transcribed, the latter proving both that a nil target is written once and that a truthy target short-circuits without evaluating the right side. CONTROL 94 to 96. | Resolved |
| `IRIS-V1-CONTROL-C006`, `IRIS-V1-CONTROL-V301` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `17`; `489` | `C006` makes `_` a discard binding that accepts a value WITHOUT creating a readable binding, and requires reading it as an expression to be a compile-time error. `V301` names that error `DISCARD_BINDING_READ`. | The code is NAMED by its frozen row but was not implemented: reading `_` produced no diagnostic at all. | Fixed with no specification change. The static pass reports `DISCARD_BINDING_READ` when `_` appears in expression position. Binding TO `_` stays legal wherever binding patterns allow it, and the regression test covers both directions so a fix that rejected the binding form as well would not pass. | Resolved |
| `IRIS-V1-COLLECTIONS-C027`, `IRIS-V1-GRAMMAR-C013` | `spec/iris-v1/06-collections-text-regex.md` | `100` | `C027` makes every Hash-literal key position an ordinary expression: a bare identifier key READS that binding and MUST NOT become an implicit Symbol. | Parser defect. `consume_qualified_separator` probed for a `::` qualified separator by CONSUMING a lone `:` and only then testing for a second one. On failure the cursor had already advanced past a `:` belonging to the enclosing form, so `%{ a: 10 }` failed to parse at all while `%{ 1: 10 }` worked. The lookahead was destructive rather than a peek. | Fixed with no specification change. The separator is now recognised by peeking at both tokens before consuming either. The regression test pins both directions: the identifier key resolves to its BOUND value, proven by reading the Hash back at that value rather than at `:a`, and a qualified name still parses. | Resolved |
| `IRIS-V1-RUNTIME-C086`, `IRIS-V1-RUNTIME-C088`, `IRIS-V1-CONTROL-V305` | `spec/iris-v1/03-runtime-object-model.md` | `264` | `C086` gives an identity-bearing object identity-first default equality, and `C088` gives it a runtime-stable identity hash. `V305` uses two `ExceptionContext` values from separate raises as Hash keys and requires two entries. | An `ExceptionContext` answered neither `==` nor `hash`: both reported a missing message, because the value has no dedicated builtin Class to dispatch through. Closure and BoundMethod already had an identity path for `==`; the context was omitted from it and had no `hash` path at all. | Fixed with no specification change. An `ExceptionContext` answers `==` and `!=` by identity alongside Closure and BoundMethod, and answers `hash` with the identity it already carries, which is the runtime-stable value `C088` requires. `V305` reads the Hash back through BOTH keys, so a merged single entry could not satisfy it; its tamper uses one context as both keys and is caught. CONTROL 98 to 99. | Resolved |
| `IRIS-V1-CONTROL-C006`, `IRIS-V1-CONTROL-V294` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `17` | `C006` makes parameter bindings, catch bindings, and per-iteration `for` bindings IMMUTABLE unless their own declaration form uses `mut`. | Static analysis never declared method PARAMETERS in its scope, so a write to one reported `BINDING_UNRESOLVED_ASSIGNMENT_TARGET`. The source was rejected either way, but the diagnostic named the wrong defect: it claimed the parameter did not exist rather than that it is immutable. Catch and iteration bindings were already handled. | Fixed with no specification change. Parameters are declared immutable in the method's scope, so all three binding kinds now report the same immutable-binding code. The regression test covers all three writes AND the corresponding reads, so a fix that rejected legitimate parameter reads would not pass. | Resolved |
| `IRIS-V1-GRAMMAR-C013`, `IRIS-V1-CONTROL-C005` | `spec/iris-v1/02-lexical-grammar.md` | `444`-`446` | The grammar defines `type_expr ::= type_union` and `type_union ::= type_intersection ("|" type_intersection)*`, and `C005` names `mut value: String | Integer` as the way to declare a wider binding cell. | The parser implemented the intersection level but NEVER the union level, so every union annotation was a parse error. `TypeExpression::Union` existed in the syntax tree with nothing able to produce it. The gap surfaced while implementing fixed-local-Type checking, because `C005` names the union annotation as the escape hatch from that very check: without it the diagnostic would have been inescapable rather than a contract a program can opt out of. | Fixed with no specification change. `type_expression` now parses the union level above the intersection level, matching the frozen grammar. The fixed-local-Type test asserts the escape hatch explicitly, so a future regression that stopped parsing unions would fail there rather than silently making the diagnostic unavoidable. | Resolved |
| `IRIS-V1-CONTROL-C004`, `IRIS-V1-CONTROL-C005`, `IRIS-V1-CONTROL-V345` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `13`; `15` | `C005` makes an unannotated initializer's precise static Type the binding's FIXED local Type. `C004` requires later assignments to satisfy it and forbids implicit widening. | No local Type inference existed, and the parser DISCARDED binding annotations entirely, so `mut value = 1; value = "text"` was accepted silently. | Implemented with no specification change. The syntax tree retains the annotation, and static analysis records a fixed Type from the annotation when written or from the initializer otherwise, rejecting an assignment of a different known Type. Inference is deliberately partial: an expression whose Type this pass cannot determine leaves the binding UNCHECKED rather than guessed at, because a wrong rejection is worse than a missed one. `V345` is NOT transcribed: its frozen text says only "Fixed-local-Type diagnostic" and names no code, so an expectation would assert an implementation-invented spelling. The behaviour is locked by unit tests covering the rejection, the same-Type case, both escape hatches, and the uninferable case. | Resolved |
| `IRIS-V1-TYPES-C093`, `D-360`, `D-361`, `IRIS-V1-CONTROL-V331`, `IRIS-V1-CONTROL-V332` | `spec/iris-v1/05-types-contracts-generics.md` | `433` | `C093` makes `typeof(expression)` denote the operand's normalized static Type at that program point, without evaluating it. `D-360` says a condition does NOT narrow the tested binding's static Type, and `D-361` says `!x` always has static Type `Bool`. | `typeof` parsed but nothing consumed it, so no static Type was observable from source and both rows were unassertable. A first attempt at transcription produced HOLLOW rows: an empty `diagnostics` expectation is vacuously satisfied, because the runner checks only that every expected code is PRESENT, so both rows passed and both tampers passed too. | Fixed with no specification change. `typeof` resolves to the tracked Type of its operand, which makes a Type observable by annotating with it and seeing whether the initializer is accepted. Each row now asserts the REJECTION its clause implies rather than the absence of one, so the assertion is falsifiable: `V331` requires `typeof(x) = true` to be rejected after an `if`, proving the condition did not narrow `x` to `Bool`, and `V332` requires `typeof(!x) = 1` to be rejected, proving the negation is `Bool`. Both tampers now fail. Implementing this also exposed that `nil`, `true` and `false` reach the parser as NAMES rather than literals, so Type inference missed them entirely and every annotation check against a Bool silently passed. CONTROL 101 to 103. | Resolved |
| `D-359`, `IRIS-V1-CONTROL-C005`, `IRIS-V1-CONTROL-V330` | `spec/iris-v1/traceability-matrix.md` | n/a | `D-359` makes `to_bool` decide only WHICH operand value a logical operator returns, so the static result Type is the NORMALIZED UNION of the reachable operand Types rather than `Bool`. `C005` names a written union as the way to declare a wider binding cell. | A static Type was modelled as a single nominal Type, so a union could be neither inferred nor represented. A written `String | Integer` annotation was therefore unusable as a widening contract even after it began to parse, and `left || fallback` had no Type at all. | Implemented with no specification change. A static Type is now a sorted deduplicated member SET, which is what `D-359` means by normalized: `String | Nil` and `Nil | String` are one Type. A logical operator types as the union of its operands, and a declared Type ACCEPTS a narrower value, which is what makes `mut value: String | Integer = 1` legal while `mut value: Integer = 1; value = "text"` is not. One unknown member makes a whole annotation unknown rather than narrower than written, since a narrower reading would cause a WRONG rejection. `V330` asserts the Bool rejection specifically, because that is what separates the `D-359` reading from the naive one; its tampers use `String` and `Nil`, which are inside the union and must be accepted. CONTROL 103 to 104. | Resolved |
| `IRIS-V1-CONTROL-C065`, `IRIS-V1-CONTROL-C067`, `D-155`, `D-157`, `IRIS-V1-CONTROL-V298`, `IRIS-V1-CONTROL-V300` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `342`; `346` | `C065` lists `re_raise_sites` among the get-only `ExceptionContext` properties, and `D-155` makes each bare `raise` APPEND one site to the context it continues, in occurrence order, without replacing the root stack. `C067` makes context equality and hashing use IDENTITY by default. | The context carried no re-raise sites at all, and neither `re_raise_sites` nor `class_name` was answered, so both rows reported a missing message. | Fixed with no specification change. The context carries an ordered site list that a bare `raise` appends to, and both properties are answered. Implementing this exposed a second defect: `same?` compared contexts STRUCTURALLY, so appending a site made a retained context compare as a different one, and `V298` reported `false` where it requires `true`. `C067` says identity decides, so identity is now compared alone. The regression test pins both directions, since an identity comparison that always returned `true` would satisfy `V298` while breaking `V314` and `V305`: two events carrying the same raised object must stay distinct, and a context must still be `same?` itself. CONTROL 104 to 106. | Resolved |
| `IRIS-V1-CONTROL-C065`, `D-473`, `IRIS-V1-CONTROL-V302` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/traceability-matrix.md` | `342`; `484` | `C065` types `original_stack` as `ReadonlyArray<StackFrame>`, `re_raise_sites` as `ReadonlyArray<RaiseSite>`, and `raise_location` as `SourceLocation`. `V302` asserts exactly those static Types. | Specification gap rather than an implementation gap. `StackFrame`, `RaiseSite` and `SourceLocation` are each mentioned exactly three times across the whole frozen specification, and every occurrence is inside `C065`, `V302`, or `D-473` naming the getter Types. Nothing defines what a `StackFrame` contains, what identifies a `RaiseSite`, or what fields a `SourceLocation` carries. The frozen text requires these Types to exist without specifying them. | Recorded as an errata candidate under `IRIS-V1-TRACE-C019` rather than implemented. Building the three record types would mean INVENTING their members and then asserting the implementation against its own invention, which is the same failure mode already avoided for the rows whose diagnostics the spec leaves unnamed. `re_raise_sites` itself is implemented and observable as an ordered collection, since `D-155` specifies its ORDERING behaviour even though it does not specify a site's shape; `V298` asserts that ordering without depending on the element type. | Open |
| `D-417`, `IRIS-V1-CONTROL-V335` | `spec/iris-v1/traceability-matrix.md` | n/a | `V335` declares and invokes one canonical one-argument Method and one canonical ANNOTATED one-argument Closure. | Regression I introduced when adding the `type_union` level. `|` both separates union members and CLOSES a closure parameter list, so `{ |x: Integer| 2 }` read the closing bar as a union operator, consumed it, and the header never terminated. The untyped form `{ |x| 2 }` was unaffected, which is why no existing vector caught it. | Fixed with no specification change. The union level is suppressed while parsing a closure header and restored BEFORE the return annotation, which sits outside the parameter list and may legitimately be a union. The regression test pins the typed parameter, the untyped parameter, a multi-parameter header, a union RETURN annotation, and an ordinary union binding annotation, so a fix in either direction fails visibly. CONTROL 106 to 108. | Resolved |
| `IRIS-V1-CONTROL-C012`, `IRIS-V1-CONTROL-C074`, `D-416`, `D-433`, `IRIS-V1-CONTROL-V334`, `IRIS-V1-CONTROL-V352` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `52` | `C012` makes a top-level `fun f` inside a Module body create a Method on that Module's `main`, PRIVATE by default, and makes a top-level `f()` a PRIVILEGED implicit send to `main`. `C074` keeps an unmodified `fun` a Module instance Method as well, distinct from the `main` installation. | A Module body rejected every executable statement, so a top-level call was `UnsupportedConstruct` and neither row could run. | Implemented with no specification change. Each executable Module allocates one shared `main`, an unmodified `fun` publishes on it IN ADDITION to remaining a Module instance Method, and a Module body's executable statements run against `main` with a privileged dispatch context. My first attempt DIVERTED the Method to `main` instead of publishing a second copy, which broke four RUNTIME vectors: `C074` says the unmodified form remains a Module instance Method, so diverting it was a misreading caught by the corpus. Declarations are published before any executable statement runs, so a helper declared later in the body is callable, which the test pins. | Resolved |
| `IRIS-V1-RUNTIME-C077`, `IRIS-V1-CONTROL-V334` | `spec/iris-v1/03-runtime-object-model.md` | `244` | `C077` makes Class instance Methods, Class-object singleton Methods, Module Methods, properties, and module-main Methods default to PRIVATE unless declared otherwise. | The `M.name()` send read the Module method map DIRECTLY and never consulted visibility, so every Module Method was externally callable regardless of its declaration. An explicitly `private module fun` was callable from outside, which is why `V334` could not assert the private-member dispatch failure it requires. Verified pre-existing by re-probing against the stashed tree. | Fixed with no specification change. An `M.name()` send is external, so a non-public Method is denied. Three tests had been asserting the bypassed behaviour with an unmodified `module fun`; each now declares `public`, which is what `C077` requires for an external send, rather than the behaviour being weakened to match them. CONTROL 108 to 110. | Resolved |
| `IRIS-V1-CONTROL-C078`, `IRIS-V1-CONTROL-C079`, `IRIS-V1-CONTROL-V288A`, `V289A`, `V345`, `V348`, `V355B`, `V357A`, `V358`, `V302`, `V342A`, `V347A`, `V337A` | `spec/iris-v1/04-bindings-callables-control-flow.md`; `spec/iris-v1/zh-cn/04-bindings-callables-control-flow.md` | `446`-`448` | Thirteen rows described a required rejection in PROSE without naming a code, or named Types the frozen text never defined, so a conforming implementation had nothing stable to report and a vector had nothing spec-derived to assert. | Errata candidate under `IRIS-V1-TRACE-C019`, raised with the owner and approved. Investigating the legacy-form rows found `D-509` deliberately leaves `groan`, `throw`, `rescue`, `ensure`, `repeat`, `switch` and `when` as ORDINARY IDENTIFIERS, rejected "only by context-specific grammar", so a code could be named without reserving them. Investigating `V302` found `StackFrame`, `RaiseSite` and `SourceLocation` each mentioned exactly three times across the whole specification, every occurrence naming a getter Type and none defining members. | Published as the v1.15 errata. `IRIS-V1-CONTROL-C078` names `PARSE_LEGACY_FORM`, `PARSE_CALL_REQUIRES_PARENTHESES`, `DECLARATION_REBINDING`, `CLASS_VARIABLE_REDECLARATION`, `BINDING_FIXED_LOCAL_TYPE`, `PARAMETER_DEFAULT_FORWARD_REFERENCE` and `MISSING_DECLARED_STORAGE`, and states explicitly that it names codes ONLY. `IRIS-V1-CONTROL-C079` defines the three record Types as immutable identity-less values comparing structurally, unlike `ExceptionContext`, which `C067` keeps identity-based. Seven rows are transcribed and tamper-tested, each tamper replacing the legacy form with the v1 form it was superseded by, which must still parse. `TRACE-C022` verification: one committed vector changed result and is REPORTED rather than hidden. `IRIS-V1-RUNTIME-V091` asserted the generic `RuntimeError` for a subclass `@@x` redeclaration; its own spec row says "subclass `@@x` redeclaration is rejected", so it is realigned to the code `C078` now names. CONTROL 110 to 117. | Resolved |
| `IRIS-V1-CONTROL-C078`, `IRIS-V1-CONTROL-C009`, `IRIS-V1-CONTROL-V337A`, `IRIS-V1-CONTROL-V347A` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `446` | `C078` names `PARAMETER_DEFAULT_FORWARD_REFERENCE` for a default referencing a later parameter and `MISSING_DECLARED_STORAGE` for assignment to absent `$name` or `@@name` storage. `C009` already required the latter to create no storage. | Neither condition was detected. A forward-referencing default was accepted silently, and an assignment to an undeclared `@@name` produced no diagnostic at all, so `V347A` could assert only its local-binding fixture. | Implemented with no specification change. Each parameter is declared BEFORE the next default is checked, so an earlier parameter resolves while a later one does not, which makes the check directional rather than a ban on parameter references in defaults. Class-variable storage is tracked from `shared` declarations, so an assignment is checked against what was actually published. Both rows are transcribed with per-fixture tampers that supply the missing declaration, and the legal forms are pinned alongside so a fix in either direction fails visibly. CONTROL 117 to 119. | Resolved |
| `IRIS-V1-CONTROL-C078`, `IRIS-V1-CONTROL-V342A` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `446` | `C078` names `PARSE_CALL_REQUIRES_PARENTHESES`, and `V342A` requires `f 1` and `obj.m 1` to be rejected during parsing because ordinary calls require parentheses. | Neither form was rejected. `f 1` parsed as two separate statements, a name followed by a stranded literal, and produced no diagnostic at all. | Implemented with no specification change. A complete expression statement may be followed only by a statement terminator or a closing brace, so a stranded operand is the parenthesis-less call form. The distinction from a NAMED INFIX is what makes this safe: `n mod 2` has the same `name token token` shape but consumes the middle token as its operator, leaving nothing stranded, so it remains legal and the test pins it. Implementing this also caught a test of my own whose Module body wrote two statements with no separator, which had been silently parsed as a call. CONTROL 119 to 120. | Resolved |
| `IRIS-V1-CONTROL-C065`, `IRIS-V1-CONTROL-C066`, `IRIS-V1-CONTROL-C079`, `IRIS-V1-CONTROL-V302` | `spec/iris-v1/04-bindings-callables-control-flow.md` | `342`; `448` | `C065` exposes `original_stack` and `raise_location` get-only, and the v1.15 `C079` defines `SourceLocation`, `StackFrame` and `RaiseSite` as immutable identity-less values comparing structurally, with one-based line and column. | Both properties reported a missing message, and no source position existed anywhere: the parser's token type kept only text, discarding the byte offsets the lexer had already computed, so a `SourceLocation` could not have been built even in principle. | Implemented with no specification change. Token offsets are retained through tokenization, a `raise` records its own keyword offset, and the evaluator converts an offset to a one-based line and column against the program text. `re_raise_sites` now carries real `RaiseSite` records rather than the placeholder Symbol that stood in when only ordering was observable, so `V298` is updated to read a site's location instead of that spelling. `original_stack` is EMPTY rather than fabricated: this evaluator keeps no call stack, and `C066` forbids inventing propagation metadata. `V302` reads all six get-only properties and is tamper-tested by removing the cleanup failure, which empties `suppressed`. CONTROL 120 to 121. | Resolved |
| `D-142`, `IRIS-V1-CONTROL-C066`, `IRIS-V1-CONTROL-V285` | `spec/iris-v1/traceability-matrix.md`; `spec/iris-v1/04-bindings-callables-control-flow.md` | `344` | `D-142` makes a suppressed collection a get-only immutable ordered view that user code may iterate and copy but never insert, delete, replace, or reorder. `C066` forbids fabricating entries. `V285` requires every such mutation to raise the spec-named `ReadonlyMutationError`. | The suppressed collection was an ordinary `Array`, so INDEXED REPLACEMENT silently SUCCEEDED: `c.suppressed[0] = 1` returned the assigned value and replaced a runtime-owned entry, which is exactly what `D-142` forbids. The other three mutations failed only incidentally, as missing messages rather than as a read-only rejection. | Fixed with no specification change. `Value::ReadonlyArray` is a distinct runtime-owned view rather than an Array that happens not to be mutated, and every mutating selector on it is rejected. Reading, indexing and iterating stay legal, which the test pins so the rejection does not make the collection unusable. `append` needed separate handling because it is routed by SYNTAX before its receiver is evaluated, so it never reached the send path. The conformance renderer emits a ReadonlyArray as an ordinary array, since it is an ordered readable collection and a distinct rendering would have made every existing suppressed-list expectation opaque. CONTROL 121 to 122. | Resolved |
| `D-415`, `IRIS-V1-CONTROL-C011`, `IRIS-V1-CONTROL-V333` | `spec/iris-v1/traceability-matrix.md` | n/a | `D-415` gives Iris NO separate `Function` runtime kind: an unbound Method is reflective, a BoundMethod captures a receiver plus Method, and a Closure is anonymous lexical code. `V333` requires all three kinds to be observable and `Function` lookup to be absent. | All three values already existed as distinct runtime kinds, but nothing reported WHICH kind a callable was, so `class_name` reported a missing message on a BoundMethod and a Closure. Transcribing the absence half then exposed a second defect: an unresolved UPPERCASE name reported `UnsupportedConstruct` while an unresolved lowercase name reported `NameError`, a distinction `C011` does not make. | Fixed with no specification change. `class_name` names the callable kind, and an unresolved uppercase name now raises `NameError` like any other. One test asserting the old code for bare `Infinity` was updated rather than the behaviour: `Infinity` names nothing and remains reachable only through `Float64.infinity`, so `NameError` is the accurate report. `V333` asserts the absence as its own fixture rather than leaving it unchecked, and is tamper-tested by reading one kind three times and by declaring a Class actually named `Function`. CONTROL 122 to 123. | Resolved |
| `D-432`, `IRIS-V1-CONTROL-V351A` | `spec/iris-v1/traceability-matrix.md` | n/a | `D-432` puts Class, Module, Contract, Type aliases and constants in ONE qualified namespace, and makes a kind or name collision an error. `V351A` names it `QUALIFIED_NAMESPACE_COLLISION` and requires the second declaration not to publish. | No collision was detected at all, and `const` was indistinguishable from an ordinary immutable `let` in the syntax tree, so a constant could not be told apart from a lexical binding in the first place. | Fixed with no specification change. The syntax tree retains whether the source wrote `const`, and Class, Module, Contract and constant declarations publish into one namespace where a repeat is rejected and does not publish. An ordinary `let` is a LEXICAL binding rather than a namespace entry, so it still shadows a Class of the same name; the test pins that, since a check written without the distinction would reject ordinary shadowing. A repeated `const` now reports both this code and `DECLARATION_REBINDING`, which is correct: it violates both rules. CONTROL 123 to 124. | Resolved |
| `D-431`, `D-432`, `IRIS-V1-META-C003`, `IRIS-V1-CONTROL-V350`, `IRIS-V1-CONTROL-V351` | `spec/iris-v1/08-modules-metaprogramming.md` | `11` | `V350` declares a private `global mut $count` in EACH of two packages and requires the values to be package-distinct. `V351` gives one unqualified name to a lexical binding, a Module constant and an explicit import, then requires resolution to select lexical scope first, then Module declarations, then imports. | Assessed and left bucketed with the blocker corrected. Neither `global` nor `import` parses, but implementing only those would not close either row. `D-431` makes a global's true identity `(package_id, $name)`, and `IRIS-V1-META-C003` puts `package_id` in an `iris.toml` MANIFEST rather than in source syntax: the grammar has no package declaration at all, so `V350`'s two packages cannot be expressed as source. `V351` likewise needs something to import FROM. | Both rows need a package subsystem rather than a parser change, which is a chapter 08 capability rather than a chapter 04 one. Recorded so the blocker is not mistaken for a missing production. | Open |
| `IRIS-V1-RUNTIME-C094`, `IRIS-V1-RUNTIME-C096`, `D-353`, `IRIS-V1-CONTROL-V362` | `spec/iris-v1/03-runtime-object-model.md` | `282`; `286` | `C096` requires truth testing to invoke `method_missing(:to_bool, [], nil)` ONCE when `to_bool` is absent after a permitted removal, and to use its Bool result directly. `C094` supplies the default `true` only when root `Object` still provides `to_bool`. | The truthiness path returned the `C094` default `true` for ANY object whose `to_bool` lookup failed, so `method_missing` was never consulted. This is the row whose earlier probe produced a documented FALSE POSITIVE: the expected `:then` arrives from the default as well, so the observable matched while the required behaviour never happened. | Fixed with no specification change. A failed `to_bool` lookup now dispatches `method_missing` first, and the `C094` default applies only when that fallback is ITSELF missing. Setting the row up also required the right removal primitive: `remove_method` deletes only the local slot, leaving the inherited `Object` implementation in place, so `undef_method` is what installs the tombstone the row needs. The vector asserts the call COUNT and the arguments rather than the result alone, which is what separates this from the default path, and its tamper omits the removal so the default applies and the counter stays at 0. CONTROL 124 to 125. | Resolved |
| `D-149`, `IRIS-V1-CONTROL-V292` | `spec/iris-v1/traceability-matrix.md` | n/a | `D-149` makes each exception dispatch use the CURRENT Class hierarchy when selection begins, so a committed revision that changes an inheritance edge is visible to a later typed catch. | A typed catch walked `static_superclasses`, a DECLARATION-TIME map populated when a Class is declared and never updated by a later `set_superclass`. A committed inheritance change was therefore invisible to `catch _: Parent` even though `is Parent` already reported `true` for the same object, so the two disagreed about the same hierarchy. | Fixed with no specification change. Catch matching reads the runtime superclass from the active revision, which is the same hierarchy `is` consults. The test pins all three cases: matched after the commit, unmatched before it, and a statically declared superclass still matching, since a fix that read only the runtime edge would otherwise be untested against ordinary declarations. CONTROL 125 to 126. | Resolved |
| `D-143`, `IRIS-V1-CONTROL-V286` | `spec/iris-v1/traceability-matrix.md` | n/a | `V286` replaces the public `ExceptionContext.suppressed` getter with one returning `[]`, then raises with a failing cleanup, and requires the ordinary read to be `[]` while the UNCAUGHT DIAGNOSTIC PAYLOAD still contains the protected suppressed context. | Assessed and left bucketed. Two capabilities are missing, not one. `ExceptionContext` is not a nameable reopenable Class, so its getter cannot be replaced and the row's premise cannot be set up: both `open class ExceptionContext` and `Reflection::Class.define_method(ExceptionContext, ...)` fail. Separately, the row observes an uncaught diagnostic payload, which `D-143` describes as an unforgeable internal channel read by unwinding, the debugger, the uncaught formatter and host bridges; the conformance runner observes a value or an error and has no such surface. | Recorded with both blockers named so the row is not mistaken for one that a getter-replacement API alone would close. | Open |
| `IRIS-V1-RUNTIME-V013`, `IRIS-V1-RUNTIME-V083`, `IRIS-V1-RUNTIME-V103` | `spec/iris-v1/03-runtime-object-model.md` | `528`; `603`; `623` | `V013` enters a frame, commits a replacement while that frame is suspended, then resumes it. `V083` requires a commit to land BEFORE `initialize` returns. `V103` replaces the `Float64.infinity` getter and adds a setter. | Evidence-integrity defect in the CORPUS rather than the implementation. All three were tagged `bucket:executable`, so the runner tried to evaluate them and reported three standing FAILURES. None of the three carries runnable source: `V013` and `V083` hold a prose SCHEDULE in `fixture_ref`, and `V103`'s `source_text` is an English description rather than Iris. The failures were therefore reporting the corpus's own mistagging, not any behaviour. | Retagged `bucket:no-fixture`, which `IRIS-V1-RUNTIME-V025` already uses for exactly this out-of-band scheduling requirement, and the classification entries now state what each row actually needs. This removes three standing failures WITHOUT changing any behaviour or weakening any expectation: a row with no runnable source cannot pass, and recording it as a failure implied a defect that did not exist. RUNTIME failures 3 to 0. | Resolved |
| `IRIS-V1-RUNTIME-V086`, `IRIS-V1-RUNTIME-V087` | `spec/iris-v1/03-runtime-object-model.md` | `606`; `607` | `V086` requires a raw `@x` slot to be `Dynamic<Object>` and per-INSTANCE: one instance stores an Integer then a Symbol while a second still reads nil. `V087` requires a Closure escaping an instance Method to keep mutating its ORIGINAL receiver's `@x`. | Both were bucketed `needs-subsystem` before milestone 2 and the classification already warned that several such reasons were stale. Re-probing shows both behaviours are now expressible and correct; no implementation change was needed. | Transcribed and tamper-tested. `V086`'s tamper writes the second instance too, so it can no longer read nil, which is what distinguishes per-instance storage from Class-level storage. `V087`'s tamper stops the Closure incrementing, so both calls return the initial value, which distinguishes a live captured receiver from a stale copy. `V087` asserts only the mutation half: its rebind half needs a `MethodBindingError` surface that does not exist, and that is recorded rather than asserted. RUNTIME 94 to 96. | Resolved |
| `IRIS-V1-RUNTIME-V110`, `IRIS-V1-RUNTIME-V111` | `spec/iris-v1/03-runtime-object-model.md` | `630`; `631` | `V110` declares reserved operator selectors such as `!` and `&&` and requires each to be rejected. `V111` rebinds a Class, Module, Contract and constant and requires a static rebinding rejection with no binding change. | Re-probed after the v1.15 errata to check whether the newly named codes closed either row. Neither is closed. `V110` still depends on a category `IRIS-V1-GRAMMAR-C054` does not name. `V111` looked closable because `C078` now names `DECLARATION_REBINDING`, but its own fixtures do not exercise redeclaration: three rebind with an ANONYMOUS class, module or contract expression such as `A = class {}`, which has no v1 production and fails as a parse error, and the fourth assigns to a `const` and fails as an immutable-binding error. | Both stay bucketed, with `V111`'s reason corrected: its blocker is the absence of anonymous declaration expressions rather than an unnamed code. Recorded so the errata is not assumed to have closed a row it did not. | Open |
| `IRIS-V1-TYPES-C061`, `IRIS-V1-GRAMMAR-C020`, `IRIS-V1-TYPES-V209`, `IRIS-V1-TYPES-V210`, `IRIS-V1-TYPES-V232` | `spec/iris-v1/05-types-contracts-generics.md` | `301` | `C061` makes a bare generic Class name definition METADATA: it is not an instance Type, not a raw generic Type, and not shorthand for `Box<Object>`, so instance annotations and ordinary construction require a closed `Box<Type>`. `C020` makes angle brackets the accepted generic spelling. | The TYPES chapter had no corpus at all, and none of the three spec-NAMED generic diagnostics was implemented: a bare generic annotation, a bare generic construction, and the square-bracket spelling were all accepted silently, with `Box[String]` additionally derailing the surrounding annotation parse. | Implemented with no specification change, and the TYPES chapter is opened with these three rows. Static analysis records each generic Class with its declared arity, then rejects a bare generic name in an instance annotation and a bare generic construction; the parser rejects the square-bracket spelling in Type position only, so ordinary index expressions are unaffected. Each row is tamper-tested by supplying the closed form the clause requires. Probing the other 77 rows first established what the chapter actually needs, which the new classification records by blocker. TYPES 0 to 3. | Resolved |
| `IRIS-V1-TYPES-V254` | `spec/iris-v1/05-types-contracts-generics.md` | `506` | The row names `TypeError` for a failed `Dynamic<T>` entry, which would be a distinct error Class from the one every other annotated boundary raises. | `IRIS-V1-TYPES-C014` calls the entry a CHECK that the value satisfies reified `T`, and `C004` already makes every annotated boundary raise `TypeContractError`; the row is naming that same failure informally. | Reading B. The Dynamic entry raises `TypeContractError`, the one boundary-guard error, rather than introducing a second error Class for one boundary. The vector asserts the raised code. | open |
| `IRIS-V1-TYPES-V246`, `IRIS-V1-TYPES-V247` | `spec/iris-v1/05-types-contracts-generics.md` | `497`; `498` | Both rows write `mixin` inside a Class BODY, as in `class Host { mixin Helpers<_> }`, which would make it a `declaration_body` member. | Chapter 02 defines `class_mixin ::= "mixin" mixin_entry_list` only in the class HEADER, and `declaration_body` admits `declaration_or_statement` alone, so a body-level `mixin` has no production. The rows are stating the composition informally. | Reading B. The fixtures write the header form the grammar admits. The composition being observed is identical; only the spelling differs. | open |
| `IRIS-V1-TYPES-V225` | `spec/iris-v1/05-types-contracts-generics.md` | `477` | The row writes a bare top-level `fun accept(...)` and a bare callable annotation `let f: (String) -> Object = accept`. | `IRIS-V1-CONTROL-C012` puts top-level executable code INSIDE a Module body, so a bare top-level `fun` has no home; and chapter 02 admits a `function_type` only inside `callable_type`, so a bare parenthesised arrow Type has no production. The row is writing both informally. | Reading B. The fixture declares the helper in a Module body per C012 and leaves the binding unannotated, observing the same parameter contravariance and return covariance through the call. | open |
| `IRIS-V1-GRAMMAR-C020` | `spec/iris-v1/02-lexical-grammar.md` | `141`; `438` | `closure_header` admits an EMPTY parameter list `||`, but C020 fixes `||` as the logical-or token, so the two readings collide exactly as `>>` does for nested generic arguments. | C020 already requires Type grammar to let `>>` close two generic argument lists WITHOUT changing expression right-shift tokenization, so the same contextual longest-match resolves `||` at a closure header. | Reading B. A `||` opening a closure header is split into an empty parameter list; every other `||` keeps its operator tokenization, including `||=`. No clause change is needed. | closed |
| `IRIS-V1-TYPES-C066`, `IRIS-V1-TYPES-C097`, `IRIS-V1-TYPES-V241` | `spec/iris-v1/05-types-contracts-generics.md`; `spec/iris-v1/zh-cn/05-types-contracts-generics.md` | `311` | `C066` requires a FAILED closed-Class materialization to discard candidate state and publish nothing, and `V241` requires each request to raise `TypeContractError` with the external log `["attempt", "attempt"]`. | `C066` names NO diagnostic for the failure, so an exception escaping a per-closed initializer propagated unchanged and the failure was indistinguishable from an ordinary raise. Errata candidate under `IRIS-V1-TRACE-C019`, raised with the owner and approved. Probing the observability half then found a SECOND divergence: `TypeContractError` is an evaluator-level error that never enters the Iris exception system, so no `catch` of any form intercepts it. This is pre-existing and shared with `C067`, not introduced here, but it means the two attempts `V241` describes cannot both be observed from one program: the first request halts it. | Published as the v1.21 errata. `IRIS-V1-TYPES-C097` reports an exception escaping a per-closed initializer as `TypeContractError`, matching how `C067` reports a constraint failure on the SAME materialization path, and leaves `C066`'s discard set and its refusal to undo external side effects unchanged. A control-flow unwind is NOT a materialization failure and passes through, so `return` and `break` still reach their own boundaries. `TRACE-C022` verification: no committed vector changed result; one test of my own had asserted the retry through `catch` and is rewritten to observe the raise and the unaffected sibling construction directly, since the `catch` it relied on never intercepted this error. `V241` asserts the raised code and the absence of publication; the twice-attempted external log is NOT asserted, and that limit is recorded here rather than worked around. TYPES 67 to 68. | Resolved |
| `D-212`, `IRIS-V1-TYPES-C066`, `IRIS-V1-META-C018`, `IRIS-V1-TYPES-V239` | `spec/iris-v1/traceability-matrix.md`; `spec/iris-v1/08-modules-metaprogramming.md` | `223`; `51` | `D-212` runs shared property initializers once during the origin Module's initialization in source declaration order, and aborts the initialization transaction on failure without publishing partial shared state. `V239` requires the failed Module to report status `not_published` with neither shared property observable. | Assessed and left bucketed, with one real capability found and fixed along the way. A Module's class-level property storage was installed correctly but was UNREADABLE: a Module name evaluates to a Symbol, so `M.first` reported a missing message on Symbol whatever the Module held, and the published half of the row could not be observed either. That is fixed. The row itself stays blocked on two counts. `not_published` appears EXACTLY ONCE in the whole specification, in this row, and no clause defines it as an observable; the nearest normative text is `IRIS-V1-META-C018`, which marks a failed Module and its dependents failed FOR THAT LOAD ATTEMPT and requires an explicit package reload rather than an automatic retry. Both `load attempt` and `package reload` are chapter 08 package concepts with no source syntax here. Separately, `IRIS-V1-CONTROL-C012` makes a Module body top-level executable code, so its `raise` halts the program and no later statement in the same program can observe the absence. | The readability fix is committed with tests covering declaration order, ordinary and shared class-level properties, Module Method dispatch, and lexical shadowing of the Module name. The row is NOT transcribed: asserting the raise alone would restate `C012` rather than `D-212`, and the non-publication it actually requires has no observable surface until chapter 08 supplies load attempts. Recorded so the blocker is not mistaken for a missing accessor, which is what it looked like before the fix. | Open |
| `D-456`, `IRIS-V1-GRAMMAR-C063`, `IRIS-V1-GRAMMAR-C067`, `IRIS-V1-TYPES-V257` | `spec/iris-v1/02-lexical-grammar.md`; `spec/iris-v1/zh-cn/02-lexical-grammar.md` | `576` | `D-456` gives every normalized reified Type expression an interned identity-bearing Type object and keeps that object distinct from the Class object. `V257` observes exactly that by comparing `Box<String>.type` against `Box<String>`. | The v1.17 clause `C063` admitted a closed generic name in expression position ONLY where the closing `>` is followed by a `postfix_part`. `Box<String>` at the end of an argument list is followed by `]`, so the generic reading was rejected, the operator reading was attempted, and the whole fixture failed to parse. The row could not be transcribed as written. Errata candidate under `IRIS-V1-TRACE-C019`, raised with the owner with a measured cost table and approved. | Published as the v1.22 errata. `IRIS-V1-GRAMMAR-C067` supersedes the `postfix_part` requirement IN PLACE: the closing `>` may now be followed by a `postfix_part` OR by a token that cannot continue an expression, namely a statement terminator, `,`, `]`, `)`, `}`, or end of input. `C020` is not weakened, because anything that COULD continue an expression keeps the operator reading, so `a < b` remains a comparison and `a >> b` remains a right shift. One behaviour changes: `A<B>` written with no spaces for a NON-generic `A` now constructs rather than reporting a parse error, but that form is a parse error under v1.21 too, so no program that parsed before this errata changes meaning. `TRACE-C022` verification: no committed vector changed result. Tests pin the comparison, the right shift, end-of-input completion, and the left-operand case where a following name keeps the operator reading. TYPES 68 to 69. | Resolved |
| `IRIS-V1-TYPES-C061`, `IRIS-V1-GRAMMAR-C067`, `IRIS-V1-TYPES-V232`, `IRIS-V1-TYPES-V245` | `spec/iris-v1/05-types-contracts-generics.md` | `485`; `498` | `V232` and `V245` name `GENERIC_ARGUMENT_ARITY` for a construction whose type-argument count does not match the declared parameters, and `C061` requires ordinary construction to name a closed generic Type. | The v1.22 errata made `A<B>` PARSE as a closed generic construction rather than fail as a parse error, so a Class declared with NO type parameters constructed SILENTLY: supplying one argument where none are declared went unreported. The errata draft recorded this as the one behaviour it changed and left the tightening out of scope; the owner approved it separately. | Implemented with no specification change. A non-generic Class is recorded with arity 0, so the mismatch is the SAME violation `GENERIC_ARGUMENT_ARITY` already names and no new code is introduced. Recording every Class rather than only generic ones then exposed two checks that had been relying on mere PRESENCE in that table rather than on a positive arity: a bare annotation `let p: Plain` was reported as a raw generic Type, and `Plain.new()` was reported as a bare generic construction. Both now require arity greater than zero, which is what they always meant. Tests pin the new rejection alongside ordinary non-generic use, the raw annotation, the bare construction, and the closed construction, so a fix in either direction fails visibly. | Resolved |
| `D-431`, `IRIS-V1-META-C003`, `IRIS-V1-IDENTITY-C030`, `IRIS-V1-CONTROL-V350` | `spec/iris-v1/traceability-matrix.md`; `spec/iris-v1/08-modules-metaprogramming.md` | `442`; `11` | `D-431` makes a global's TRUE identity `(package_id, $name)`, unique within a package and separately instantiated per runtime, with NO flat cross-package namespace, auto-merge, or process-global storage. `V350` declares the same private `global mut $count` in two packages and requires the values to be package-distinct. | Globals were keyed by NAME alone, so two packages declaring the same `$name` shared one cell -- exactly the process-global storage `D-431` says does not exist. The row was previously recorded as needing a package subsystem before a parser change could help, which was correct about the blocker but not about its size. | Implemented with no specification change. `IRIS-V1-META-C003` lets a manifestless local script run with runtime-local package identity only, and `IRIS-V1-IDENTITY-C030` keeps that identity runtime-local rather than publishable, which is enough package identity to satisfy `D-431` without a manifest. Globals are keyed by `(package_id, name)`, and `evaluate_packages` runs ordered per-package programs against ONE runtime, which a single program cannot express and which `independent_sources` cannot either, since its programs each get a fresh runtime. `V350` is transcribed with tampers in both directions: putting both programs in one package must see the mutation, and reading without declaring must raise `NameError`. CONTROL 126 to 127. | Resolved |
| `IRIS-V1-META-C001`, `IRIS-V1-META-C003`, `IRIS-V1-META-V340`-`V441` | `spec/iris-v1/08-modules-metaprogramming.md` | `5`; `11` | Chapter 08 owns package identity, manifests, imports and exports, initialization order, open transactions, candidate isolation and safepoint commit -- the subsystem seven TYPES rows and two CONTROL rows are blocked on. | Surveyed before opening the chapter. 50 of its 51 vector rows begin with a concrete on-disk fixture path, `fixtures/meta/vNNN/iris.toml`, and specify `package_id`, `api_major`, and ordered `src/*.ir` entries; several add companion manifests, upgrade hooks, or native artifacts. The single exception, `V436`, names `fixtures/meta/v357/artifact.json` with an exact BLAKE3-256 digest. No such fixture tree exists, and the conformance corpus has no on-disk multi-file input form: every committed vector carries its program INLINE. | Recorded rather than opened. The chapter needs a fixture-tree input form and a manifest reader before ANY row can be transcribed, which is a corpus-infrastructure change rather than a language one. The runtime-local package identity added for `D-431` is the part of the subsystem that manifestless source can reach, and it is committed; it deliberately does NOT claim manifest, dependency, permission, or lock semantics, which `IRIS-V1-IDENTITY-C030` forbids a manifestless script from claiming. | Open |
