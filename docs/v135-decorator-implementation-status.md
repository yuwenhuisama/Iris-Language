# v1.35 Decorator Implementation Acceptance Status

Status: implementation in progress. Initial synchronous/asynchronous suites pass again after repairing canonical Task integration; broader target and replay tests remain red. Complete publication, Type and GC acceptance remains pending. No passing v1.35 conformance claim is made here.

## Authority And Readiness Rules

The current English [META-C127 through META-C139](../spec/iris-v1/08-modules-metaprogramming.md#v135-decorator-invocation-protocol) and [CONFORMANCE-C076](../spec/iris-v1/12-conformance.md#v135-decorator-coverage-obligations) are the acceptance source, read directly for this document. [TRACE-C023](../spec/iris-v1/README.md#revision-procedure) bounds the approved exception: only C125's unconditional static wrong-kind rejection phase is superseded. Historical proposals and existing implementation behavior do not replace these clauses.

Below, `I` means implementation readiness and `T` means complete acceptance-test evidence. An unchecked box means pending, including where older related behavior already exists. Rows are local checklist labels, not allocated conformance-vector or example IDs. Each positive case needs its negative counterpart, exact clause anchors, applicable reference/VM comparisons, error category and phase, entry/effect counters, and publication/revision/commit observations. Native, package, scheduler and GC cases need their actual surfaces; a source-only approximation does not complete those gates. Backend refusal is missing support, not equivalence or a passing negative test.

### Canonical v1.36 Fixture And Frozen-Vector Policy

1. Canonical v1.36 syntax: All new and updated fixtures across CLI, evaluator, and VM test suites must use canonical v1.36 language syntax. This includes top-level `impl Class for Contract` declarations, purely declarative origin Class and Module bodies, dynamic modifications restricted to `open` blocks, explicit instance field declarations (`let @field` or `mut @field`), array literals prefixed with `%[]`, the `is?` type test operator, and explicit non-null `!` operators alongside disambiguated `foo!` selector invocations.
2. Frozen vector baseline: Historical conformance records META V428 through V431 and V361 in `conformance/iris-v1/vectors/META/` remain immutable, frozen, and preserved byte-for-byte. Their existing narrower fixtures and classifications must not be adjusted or retagged.
3. Uncovered clause visibility: Documentation citations and status ledger entries do not close or satisfy CONFORMANCE-C057. Uncovered normative clauses remain reportable gaps until backed by executable conformance vectors.

The initial acceptance-preparation task wrote only this document. Subsequent implementation changes are recorded separately below. Temporary build products and baseline probes are isolated outside the workspace. Baseline evidence is explicitly from the mutable user working tree, not clean HEAD; unrelated `iris-builtins` and parser work must be preserved.

## Implementation Progress

- Shared foundation: `crates/iris-runtime/src/decorator_protocol/` implements immutable Rust records, persistent operations, structural argument snapshots, payload patch validation through backend-supplied Type predicates, scope bookkeeping and Value visitors. The coordinator independently ran `cargo test --locked -p iris-runtime`: 125 tests passed, including 21 new foundation tests. This does not complete source bindings, phase execution, candidate publication, exact backend Type checking or Task delivery.
- Initial public regression suite: `crates/iris-cli/tests/decorator_protocol.rs` contains 19 source specimens tested separately on both engines (38 executions, 19 pairs). The tests-first baseline originally reported 38 failures and no passes; all specimens passed a parser precheck.
- Previous interim check (superseded, historical): The coordinator rebuilt and ran the initial suite in `iris-v135-parent-check` yielding **8 passed, 30 failed, 0 ignored** on an earlier partial slice. Both backends passed `ArgumentChanges` availability, non-Symbol-key rejection, and typed outside-phase cases.
- Earlier synchronous-slice coordinator verification (historical):
  - Command: `CARGO_TARGET_DIR=/var/folders/tq/hgbd_kk13_1bgdj9c8983s8h0000gn/T/opencode/iris-v135-parent-check cargo test --locked -p iris-cli --test decorator_protocol -- --test-threads=1`
  - Result: **38 passed, 0 failed, 0 ignored**.
  - All 19 specimens now run green across both the reference evaluator and the VM.
  - VM integration tests run: `CARGO_TARGET_DIR=/var/folders/tq/hgbd_kk13_1bgdj9c8983s8h0000gn/T/opencode/iris-v135-parent-check cargo test --locked -p iris-vm --quiet`
  - Result: **165 passed, 0 failed, 0 ignored** across all 12 established test suites (`iris_vm` unit tests: 48, `binding_boundaries`: 14, `boundary_blockers`: 12, `decorator_core`: 13, `decorator_remaining`: 12, `decorator_sync`: 10, `module_shutdown`: 5, `native_packages`: 14, `open_boundaries`: 11, `open_signature_shapes`: 9, `readonly_array_guards`: 4, `small_stack`: 3).
- Scope boundaries of current passing tests:
  - The passing 38 CLI tests represent 19 specific specimens evaluated across 2 engines, covering core sync class/method wrapper flows, argument patch snapshots, basic purity checks, zero-attempt contracts, and rollback on invalid capability or wrong kind.
  - They do not represent full clause-by-clause satisfaction of the 92-row matrix.
  - AST focused test suites (`candidate_declarations`: 8, `sync_wrappers`: 10, `decorator_phases`: 14, `decorator_core`: 12) provide delegate evidence for specific AST behaviors but have not been independently full-checked against all matrix clauses.
  - A broader prior agent report noted a 735 / 11 / 1 differential or unit profile without final comprehensive green status.
- Coordinator checkpoints use `/Users/huisama/.cargo/bin/cargo` and the same `iris-v135-parent-check` target. Before the later Task identity integration, `test --locked -p iris-cli --test decorator_protocol --test decorator_async --quiet` passed **38 synchronous and 20 asynchronous tests**, with no failures or ignored tests. These are 29 specimens, not complete clause coverage.
- After repairing a misplaced Rust test left by a cancelled VM worker, the unfiltered `test --locked -p iris-vm --quiet` passes **194 tests**, including the existing small-stack tests. The cancellation did not roll back its earlier code; its pending origin-publication work was not completed.
- Independently checked evaluator integration suites pass **9 continuation, 5 async-lifecycle and 2 async-adapter tests**. They exercise retained AST execution, pending Task awaits, owner identity, structured unfinished-work diagnostics, repeated failure contexts and abandoned bridge reporting. The latest broader delegate run reports **734 passed, 14 failed, 1 ignored**; this is not a green full-suite result.
- Shared provisional Class-origin APIs pass **10 independently checked tests**. Canonical callable-Type interning passes **9 independently checked tests**; the runtime owner reports **157 total runtime tests passed**. These are additive APIs, not proof of backend adoption. Provisional transactions own Class metadata, not Module mutations or backend side tables.
- Backend origin-publication adoption is pending. A real VM probe still reports revision 2 for an origin with an empty Method decorator. Integration exposed a missing candidate-aware capability check because ordinary authorization requires a published revision. The new checked candidate mutation API passes **11 independently checked tests**, preserving active/candidate policy intersection and inherited/Module denial origins. The coordinator reran the complete runtime suite: **168 passed**. Public active lookup and ordinary reflection still reject provisional targets; backend adoption remains a separate gate.
- AST decorated-Class origin adoption has subsequently landed. The coordinator independently ran **7 passing `decorated_origins` integration tests**; four internal tests additionally cover commit/revision consumption and metadata cleanup in the delegate run. The source name is withheld until commit and failed transforms preserve author effects. Provisional class-object storage, broader declaration groups and other target kinds remain separate gaps. VM adoption is still being verified; the revision-2 observation above records its pre-adoption baseline.
- Closure binding/alias analysis now walks canonical parameters and defaults in declaration order. The coordinator independently reran **5 passing `closure_analysis` tests**. This does not complete ordinary Closure invocation or callable-Type reification in either backend.
- VM decorated-origin adoption subsequently passes **208 independently checked VM tests**, including nine origin regressions. Computed-property support then adds nine passing tests, bringing the independently rerun VM total to **217**. Getter/setter chains use `property_body`, reject absent accessors and preserve the checked setter result. These counts supersede the delegate's lower totals; no existing tests were credited as removed.
- Provisional Class-object raw storage now has Runtime-level commit/rollback APIs. The coordinator independently ran **6 storage and 4 GC tests**. Storage is not yet wired into every backend declaration path, and immutable hierarchy class-variable initialization is a different prerequisite.
- The synchronous and async CLI regression suites were independently rerun after the latest deliveries: **58 passed**. AST computed properties and VM Contract decorators are separate implementation tasks; the full target matrix remains incomplete.
- Subsequent integration exposed a new AST failure after canonical Task registration: `bridge_identity` rejects a valid typed bridge with `TypeContractError`; the async suite is **19 passed, 1 failed** at this checkpoint. Exact Task result metadata must be completed rather than restoring permissive checks. The latest all-target run is **8 execution cases passed, 12 failed**, plus a passing parser-only check: computed accessors and absent-accessor rollback pass on both engines, and the initial Contract specimen passes on VM.
- The first all-target checkpoint was **2 execution tests passed, 18 failed**, plus one passing parser-only test. All ten specimens parse. Its protected-access and computed-property failures have since been repaired; the newer counts above supersede this baseline. Parser acceptance and silent omission are not target support.
- A subsequent unfiltered VM run also detects `task_type_tests_and_casts_preserve_exact_adapter_results` failing with `Kernel(Type)`: reification now selects canonical Kernel Task, while one type-test path still compares an engine-local Task identity. The former 217-pass count is historical until this integration mismatch is repaired. The formal spec audit still passes: 14 artifacts, 629 normative paragraphs, no violations.
- Independently checked additions include **8 VM Contract-decorator tests**, **13 evaluator callable-Type tests**, **7 canonical Task-registration tests**, and **9 parser accessor-metadata tests**. The five new evaluator Task-value tests are **2 passed, 3 failed** before the production repair: exact async Closure signatures work, but valid Task bindings and Task-value tests do not. `cargo check --locked --workspace --all-targets` passes at this checkpoint; compilation is not semantic acceptance.
- The Module transaction design has been reviewed: use genuine Module candidates and immutable revisions beneath the existing registry coordinator, validate mixed Class/Module groups before allocating a shared commit, and retain backend-owned canonical undecorated artifacts separately from wrapper state. This is an implementation design, not completed Module or replay support.
- Latest independent verification supersedes the Task regression checkpoints: **58/58 core CLI tests**, **5/5 AST Task-contract tests**, **219/219 VM tests**, and **201/201 runtime tests** pass. AST records exact awaited-result Types before eager execution; VM uses the canonical Kernel Task identity consistently. No covariance or completion-based Type inference is used.
- Genuine Module candidates, immutable Module revisions, checked additions/replacements and mixed Class/Module commit receipts are now available in the runtime. The 201-test runtime result includes 16 new Module tests. Existing Module composition/policy edits and Class composition with unpublished Modules remain explicitly unsupported; backend Module adoption and canonical replay are not completed by this substrate.
- The new replay suite independently reports **7 execution tests passed, 1 failed**, plus four passing parser checks. Reference passes all four specimens; VM still rejects decorated declarative open. Historical rollback, upgrade and closed-materialization replay remain untested at their required surfaces.
- The stored-property positive specimen now explicitly declares `public get; public set;`. The prior unqualified accessor members default to private under RUNTIME-C077/D-445 and could not legitimately establish external-access success. Expected wrapping effects are unchanged; this local test correction does not change the frozen corpus or accessor semantics.
- Module source adoption now executes both addition and member-wrapper fixtures on both engines. The latest independent target suite is **13 execution cases passed, 7 failed**, plus one parser check. Remaining failures are AST Contract reflection/phases, both stored-property wrappers, both generic class-object wrappers and both qualified wrappers. The coordinator also reran **10 evaluator Module tests**, **9 VM Module integration tests**, **9 runtime private-owner tests**, the complete **210-test runtime suite**, and the **58 core CLI regressions** successfully. Same-Module private access is now owner-specific; a host composition grant cannot expose unrelated Module or ancestor-Class private methods.
- A wider evaluator library checkpoint reports **750 passed, 17 failed, 1 ignored**. Besides the previously recorded 14 failures, two VM dynamic-property differential cases now fail (`backends_initialize_published_stored_properties_on_new_instances`, `backends_share_dynamic_stored_property_slots_with_raw_ivars`), and `a_module_reopen_adds_to_its_target` reaches a new `module decorator target` refusal. These require investigation independently of the passing core decorator fixtures; they are not credited as historical compatibility failures.
- The next independent target checkpoint is **15 execution cases passed, 5 failed**, plus one parser check. AST Contract phases/reflection and VM generated stored-property wrappers now pass their specimens. The coordinator separately ran **10 AST Contract integration tests**, **13 VM stored-property integration tests**, and the complete **224-test runtime suite** successfully; the latter includes 14 provisional Class-variable payload/GC tests. Remaining target failures are AST stored-property wrapping and both generic class-object/qualified targets. Core CLI protocol/async regressions remain **58/58 passed**. The three wider VM regressions above remain assigned for repair, not dismissed by these passing subsets.
- Latest independent target verification is **16 execution cases passed, 4 failed**, plus one parser check: stored-property wrapping now passes on both engines. **15 evaluator stored-property tests** and **6 exported-decorator parser tests** pass. All three previously recorded dynamic-property/Module-open differential regressions now pass when rerun individually; their earlier failures remain historical evidence. Generic class-object and qualified Method wrappers remain red on both engines, and full replay/mixed-publication acceptance remains incomplete.
- A subsequent independent evaluator library run reports **755 passed, 14 failed, 1 ignored**, confirming the three added regression failures are no longer present in that run. During the next qualified-wrapper edit, a separate CLI rebuild encountered an incomplete `WrapperChain.qualifier` initializer; no successful build or semantic result is inferred from that transient failure. Release verification must rerun after writers finish.
- Qualified-slot delivery has subsequently passed independent checks: **8 evaluator and 6 VM integration tests**, and both qualified target specimens. The target suite is now **18 execution cases passed, 2 failed**, plus one parser check; only generic class-object Method cases remain red in that initial suite. **58 core CLI cases** pass again after rebuilding. Closed generic qualified views and source-level qualified retention remain separate limitations, not implied by non-generic fixture success.
- The runtime transaction-only Module overlay now permits Class origins/opens to compose staged Module origins and validates final transitive MRO/policy before shared publication. The coordinator independently reran all **236 runtime tests** successfully. This supersedes the earlier provisional-Module composition limitation; provisional Class parents and existing Module policy/composition changes are still not supported by that slice.
- The latest target checkpoint is **19 execution cases passed, 1 failed**, plus one parser check. The AST generic singleton specimen passes; VM currently supports only non-generic singleton wrapping and still rejects the generic specimen. The added channel suite independently reports **12 execution cases passed, 4 failed**, plus eight parser checks: AST consumes a positional Closure as a required block and rejects a matching block replacement, while both engines mishandle invalid next-call channels. Explicit source `&expr` is a separate missing parser/backend prerequisite; these passing subsets do not cover explicit-nil provenance.
- Latest independent evaluator library checkpoint: **757 passed, 14 failed, 1 ignored**. The same older decorator/generic/argument-channel failures remain visible; no full-library green claim is made.
- The initial target suite now independently passes **20 execution cases plus its parser check**: VM generic singleton binding has landed. Core protocol/async suites still pass **58/58**. The additional channel suite is **15 execution cases passed, 1 failed**, plus eight parser checks; only VM invalid next keyword/trailing-block classification remains red. These results do not complete generic owners, generic qualified targets, async VM generics, explicit source block arguments or the 92-row matrix.
- After the AST dedicated-channel/transparent-Block correction, the broader library checkpoint is **751 passed, 20 failed, 1 ignored**. Two newly introduced callable tests incorrectly expected nominal Block rejection or positional-as-block acceptance and require correction against TYPES-C095/CONTROL-C030. Other pre-existing test expectations and remaining implementation gaps must be separated before the final gate; no blanket pre-existing-failure claim is made.
- The stalled VM next-channel task was recovered in a bounded follow-up. Independent CLI verification now passes **103 tests** across protocol (38), async (20), targets (21 including parser check), and channels (24 including parser checks). Invalid keyword/trailing-block next calls produce `ArgumentError`, preserve async failed-bridge delivery, and do not leak block state into retries. The larger replay, generic-owner, native and 92-row acceptance gates remain incomplete.
- AST origin storage adoption now passes **11 independently rerun integration tests**: metadata and staged raw/shared payloads commit through Runtime together, and rollback releases private state without undoing external effects. Initializers no longer run during static planning. Prepublication shared-cell reads/reassignment and mixed provisional parents remain separate limitations.
- Canonical block transport migration is planned as a compile-green foundation, independent backend consumers, then parser cutover. Explicit `&expr`, duplicate-block errors and explicit-nil provenance must work without NUL pseudo-keywords or source-text reconstruction before that gate closes.
- The dormant transport foundation is now present: `Expression::BlockArgument` and `Value::BlockArgument` preserve the channel across cloning, and recursive traversal/GC follow the operand. The coordinator independently verified the workspace all-target compile and **3 runtime transport tests**. Parser emission remains unchanged; evaluator and VM consumer migrations are separate work in progress, so explicit `&expr` support is not yet claimed.
- Both engine consumer migrations have subsequently landed. Evaluator binders use the typed wrapper without NUL-keyword transport; VM emits `MakeBlockArgument`, scans channels in order, and no longer uses `Send.block` or ambient tail-block flags. The coordinator independently rebuilt and reran **103/103 CLI checks** successfully. The parser cutover and removal of temporary source-reconstruction adapters are now in progress; public explicit-block syntax is still a separate acceptance gate.
- Parser cutover has subsequently landed. Explicit `&expr` and trailing Closures now produce canonical block nodes; source metadata preserves explicit-block spans and operand edges. Independent checks pass **113 CLI tests** (the previous 103 plus ten source-block cases), the full parser suite and workspace all-target compilation. Searches found no remaining `SourceBlocks`, `trailing_blocks`, `call_channels::prepare` or NUL block marker. This closes that transport cutover, not the remaining splat, BoundMethod/Block, generic-owner, replay or final conformance gates.
- VM declarative decorated Class-open replay now passes independently: **125 CLI checks** across the prior five suites plus replay, and **4 dedicated VM replay integration tests**. Each reopen has an exact artifact target and rebuilds from its new undecorated body; retained and sibling chain cases pass. Historical Host rollback, package upgrade, Module replay and the full materialization matrix remain unimplemented or unverified. Failed-transform escaped ordinary captures require separate regression coverage; candidate rollback must not revoke author side effects.
- Canonical ordinary core-error boxing has passed its focused identity tests but the delegate reports **five additional library observation failures** and uncaught conformance rendering that loses the error name after evaluator teardown. This is assigned for repair, not accepted as a clean regression result or dismissed as legacy behavior.
- Host error observation now captures registry-derived `CoreErrorKind`, original Value and matching context before teardown. CLI/conformance use `evaluate_host`; value-preserving APIs retain their raised-object contract. The coordinator independently ran **14 evaluator host/error-identity integration tests**, the CLI host-error test and **4 escaped-capture callback-open tests** successfully. Four additional broad-library differences remain reported by the owner; host observation repair is not a full-suite pass.
- VM initial Class-state publication now uses Runtime staged initialization and group promotion, including immutable cells; owner reports eight new tests. Declarative failed-open escaped-capture coverage is still pending: callback-open success does not prove that `apply_reopen` may safely rewind Closure identities or delete escaped ordinary captures. A direct internal regression is assigned for that separate path.
- Declarative escaped-capture revocation was reproduced and repaired. The coordinator independently reran **2 internal VM regressions**: after late wrong-type/raised failures, captured closures remain callable across compacting GC, retain shared state, and never reuse later allocation identities. Candidate revision/signature rollback is unchanged; blanket Closure deletion and counter rewind are removed. Independent CLI verification passes **129 checks** including the four callback-open escaped-effect cases. Global rooting of all Closure records remains a separate GC reclamation gap.
- AST BoundMethod values now pass exact decorated original/replacement block admission without rebinding identity or receiver. This is partial: ordinary block-parameter annotation checking and canonical `BoundMethod<S>`/transparent `Block<S>` union reification still need completion; shared callable-Type correction is assigned before backend adoption.
- The shared correction is now available: canonical `CallableKind::BoundMethod` plus `intern_block_alias` produce the ordinary sorted `BoundMethod<S> | Closure<S>` union without an alias identity. **7 runtime alias tests** pass independently. The old draft `CallableKind::Block` remains only until both engine migrations finish; its presence is not compliant alias support. Backend migration and ordinary block-signature enforcement are in progress.
- Both engine alias migrations have subsequently landed: the coordinator independently ran **14 evaluator Block/BoundMethod tests**, **12 VM canonical-callable tests**, and all **129 CLI checks** successfully. Ordinary block annotations now use exact signature admission, and matching bound-callable values preserve identity/receiver. An earlier VM test from this implementation cycle incorrectly demanded rejection of a matching BoundMethod block; it is being replaced with a valid positive case plus a wrong-signature negative case, not by weakening signature checks. Draft nominal Block-kind removal is assigned now that engine references are gone.
- The draft nominal Block kind has now been removed, and the two resulting unreachable engine match arms were removed without changing other branches. Independent verification passes **129 CLI checks**, **9 AST historical-artifact rollback tests**, and **10 VM async-generic tests**. The async-generic implementation currently covers class-object methods on nongeneric owners only. Historical rollback currently supports a constrained host-provided instance-method artifact; its locator-based revision selection requires separate contract review, and it does not establish full historical replay coverage.
- The rollback review found the mandatory locator `@revision` suffix unsupported by META-C063/C066: revision identity and locator are separate host metadata. Explicit targets must resolve through owner/revision-keyed host input while locators remain opaque; the unkeyed no-argument compatibility path remains separate. Correction is assigned without changing the specification.
- A coordinator-run generic capture probe found a real evaluator defect: Integer/String/Integer calls produce `1, s, 3` on AST versus `1, s, 2` on VM. AST closed type applications still share wrapper captures; earlier generic-fixture success did not prove materialization isolation. A regression suite is being added before repair. Separately, **10 VM generic-instance integration tests** pass independently; generic owners and qualified generic targets remain outside that slice.
- Host revision selection now uses additive `PackageHistory`/`RevisionArtifact` metadata keyed independently of locator spelling. The coordinator independently reran **13 selection and 9 historical rollback tests** successfully. No-argument unkeyed compatibility is preserved, while unmapped explicit targets fail without guessing.
- VM decorated Module-open replay now passes **6 independently rerun integration tests**, covering fresh shared Module/main state, retained chains, wrong signatures and phase ordering. Historical Module rollback, upgrades, generic Module decorators and composition/policy changes remain separate gaps. The four new AST materialization-isolation regressions are all red before their assigned production repair.
- AST materialization repair now passes all **4 original isolation regressions independently**. Closed chains have separate identities and captures. This exposes a phase discrepancy: AST executes origin plus closed transforms, while VM currently defers the generic runtime transform; the initial generic CLI output expects only one transform. Normative review is pending before changing either behavior or that test expectation.
- Three new VM GC tests reproduce real reclamation failures after removing only user roots. Reachable captures and expired-next permission checks pass, but unreachable Closure captures and completed-next invocation objects remain retained by global bookkeeping roots. A reachability-based engine-root repair is assigned; survival-only GC checks are not reclamation acceptance.
- Both engine GC attempts identified a missing runtime traversal seam and delivered additional tests rather than a collector fix. The evaluator tests expose six retention defects and two premature collections; a temporary root-copy approach was reverted because it changed observable Array share counts. Runtime now provides `trace_with_edges` and `collect_with_edges`, including implicit Class roots and typed Method/BoundMethod observations before a fixed-point sweep. **7 new runtime edge-expansion tests pass independently**. Engine adoption is newly assigned; the reclamation and active-frame defects remain open until those tests pass.
- The recovered normative review confirms generic decorators transform at **origin and each new closed materialization**, with no rerun when a closed Method is reused (META-C087/C093/C127/C138). VM origin deferral is incorrect; its correction and the initial single-transform test expectation update are assigned together. AST's restored origin-plus-closed timing is not to be reverted to fit that earlier test.
- VM precise engine tracing now passes **14 independently rerun GC tests**, including reclamation, nested callers and ready-task roots. The AST GC worker was cancelled at its tool limit but left production changes; **17 GC-filtered unit tests independently pass** on disk. A fresh bounded recovery task must verify the full AST regression/share-count behavior before declaring that delivery complete.
- AST recovery found and fixed collection of earlier wrappers while a later generic transform runs. The coordinator independently reran **20 GC-filtered AST tests** successfully. Conservative unused lexical captures and reclamation of container backing cycles remain residual concerns; passing heap-identity tests alone do not close them.
- Generic VM phase timing is now corrected to origin/open plus each fresh closed materialization. **4 phase tests and all 129 CLI acceptance checks pass independently**, with the generic fixture now expecting both required metadata pairs. Counts are `1,2,3,3` for origin/Integer/String/reused Integer. This supersedes the earlier single-transform expectation without changing frozen clauses or corpus vectors.
- AST generic-owner support now passes **10 independently rerun integration tests** for closed owner arguments, distinct method bindings, captures, async calls and retained revisions. Inherited generic substitutions, qualified owners and generic-owner declaration/property decorators remain explicit gaps.
- The first VM multi-bundle code-ownership implementation was **not delivered**: its incomplete prototype was reverted. Probes demonstrated retained methods using the wrong bundle's function index and retained closures failing after another bundle loads. A smaller MethodBody-decoding consolidation is assigned before resuming actual ownership and historical VM rollback. No retained-code or VM historical rollback API is claimed from that failed attempt.
- The smaller VM decoder checkpoint has landed: all production MethodBody conversions now use `resolve_method_body`, and its new same-program retained-method test passes independently. It still resolves local indices, not multi-bundle ownership. Actual code-owner linking is newly assigned after this checkpoint; the earlier wrong-bundle probes remain unmet acceptance requirements.
- AST Module historical rollback passes **6 independently rerun public integration tests**, using opaque host artifacts and preserving retained current Module/mixin behavior. The owner reports four additional transaction-level tests. State, composition, generic/native/qualified history, Module-level transforms and package upgrade remain outside that slice.
- Disk exhaustion was avoided by removing only two confirmed completed-task Cargo targets (`iris-v135-block-foundation`, `iris-v135-block-cutover`), freeing approximately 8.4 GiB. Workspace sources and the main verification target were retained; subsequent builds should reuse existing targets.
- Retained VM code ownership has now landed after the decoder checkpoint. **11 ownership tests independently pass** for reordered bundles, retained Methods/Closures and suspended Tasks; executable references resolve to retained owner Programs and linked identity mappings. Bundles remain retained for the Machine lifetime. This is the prerequisite for artifact reconstruction, not a claim that VM host-history rollback or package upgrade is implemented.
- AST host-keyed package-upgrade replay now passes **10 independently rerun integration tests**, staging existing method-only Class/Module candidates with fresh Upgrade transforms and one structural commit. New owners/storage, member-set changes, generics, composition, native/qualified members and owner-level transformations still reject. Broader 24-library-failure status remains unresolved; upgrade subset success does not close full C068/C138 coverage.
- VM host-history Class rollback now passes **16 independently rerun tests**, using retained bundles, opaque owner/revision keys and digest-verified source without executing historical top-level statements. The supported slice is parentless nongeneric instance-method Classes with unchanged method sets; default/no-argument history, Modules, inheritance, state/accessors, generic/qualified/native history and upgrades remain outside it.
- AST direct generic-owner qualified wrappers now pass **6 independently rerun source integration tests**, preserving closed Contract arguments, owner/method binding separation and async guards. Inherited owner remapping, native owners and multiple applications of the same Contract definition remain unsupported; those gaps are not covered by the initial target suite.
- Subsequent independent tests pass **10 AST historical accessor/singleton scenarios** and **9 VM generic-owner integration cases**. Historical stored-property initializers/state, qualified/native replay and inherited generic-owner substitution remain separate work.
- Latest independently rerun evaluator library result is **791 passed, 28 failed, 1 ignored**. In addition to previously recorded conflicts, VM `Hash.rehash` block dispatch and annotated `Bytes` return paths fail; these two concrete integration regressions are assigned for repair. Generic construction/header differences remain separately visible. The 28 failures must not be summarized as proven pre-existing failures.
- A further disk-pressure cleanup removed only two confirmed completed acceptance Cargo targets (`iris-v135-target-acceptance`, `iris-v135-async-acceptance`), freeing approximately 1.9 GiB; sources and the main verification target remain untouched.
- The rehash/Bytes and stored-history workers timed out after reporting disk-full link errors, but their changes remained on disk. Recovery independently passes **13 VM native-boundary regression tests**, **10 AST stored-property history tests**, both original rehash/Bytes differential regressions, and **129 CLI checks**. The failed task status was not treated as code rollback or completion evidence. A completed channel-acceptance Cargo target was cleaned to free about 1.2 GiB; no sources or primary verification cache were removed.
- Stored-property history now preserves existing instance state while restoring historical initializer bodies for future construction, rebuilding accessor caches and retaining construction snapshots. It still does not establish generic/native/composition or all package-upgrade replay coverage. The full-library run must be repeated after ongoing work; the previous 28-failure checkpoint is not a current green result.
- AST generic Method history now passes **10 independently rerun tests** on nongeneric owners. VM closed-qualified decorators pass **8 independently rerun public-source tests**, and all **129 CLI checks** still pass. Generic inherited owners, native qualified methods and the broader history/upgrade matrix remain unimplemented or unverified; the VM owner's full-suite run timed out and is not credited as green.
- Disk space again reached about 259 MiB. The completed-task external `iris-v135-vm-sync` Cargo cache was removed (approximately 11.9 GiB); the main verification target and sources were preserved. Subsequent tasks must reuse the main target rather than recreate this duplicate cache.
- VM Module host-history rollback now passes **13 independently rerun integration tests**, and **129 CLI checks** still pass. The supported slice retains exact method-only Module/decorator code, old Method caches and structural identity; generic/native/composed history and decorated `module fun` remain unsupported. A newly stale VM generic-refusal test still fails the full suite and is assigned to be replaced with real supported-execution plus invalid-input coverage.
- The broad remaining-gap exploration was not accepted as evidence: it cited superseded history entries and treated valid protocol rejections as implementation gaps. Remaining work must be tied to current code paths and actual tests, not that report's stale claims about already-passing aliases, channels, generic targets or GC.
- Unimplemented and pending areas:
  - Current verification checkpoint: AST qualified history passes 7 public tests and 2 internal retained-chain/atomicity tests independently. VM computed-accessor/singleton history passes 11 tests, alongside 16 Class-history and 13 Module-history tests. These slices do not close generic, native, composition, stored-state or full upgrade history.
  - Native declaration signatures now retain scalar source annotations, with exact i64 range and module-local resource checks still owned by linked ABI metadata. All 13 native-host tests and the real native-library integration through both engines pass independently. Resource annotations project to Object; this is a metadata prerequisite, not native wrapping completion or permission relaxation.
  - AST F-bound/constructor regressions pass 6 tests, plus 16 related closed-owner/qualified tests. Complete substituted Contract arguments are checked, and ordinary initializers receive owner type bindings. The two old raw-Comparable positive fixtures remain unchanged and conflict with TYPES-C060/C084; their VM acceptance is not excused by that fixture defect.
  - The latest evaluator library rerun remains 795 passed, 27 failed, 1 ignored. Failures include stale positional-block and transform-only decorator fixtures, plus unresolved backend generic/reflection/error-observation differences. No blanket pre-existing-failure or full-suite-green claim is made. The 129 CLI checks and workspace all-target check passed at the preceding checkpoint.
  - Nested trailing-block parsing now passes a bounded six-input regression through parse, parse_with_source and parse_editor; the full parser suite passes. The original nested-call CLI probe independently prints v/true in both engines. Task transcript recovery failed, so the earlier timeout's precise root cause and production-change attribution remain unverified; do not credit a specific parser fix from the completion notification alone.
  - Independent manual review reproduced and verified the correction of a VM capability defect in shared stage_members: an absent accessor now requires PropertySet, while an existing candidate-visible accessor requires PropertyBody. The seven new property_capabilities tests cover local/inherited/Module slots, separate getter/setter additions, and static signature rejection. The original CLI bypass now reports PropertySet denial; the allow-add/deny-body counterpart prints 1. Historical accessor/Class/Module regressions remain passing.
  - This bounded checkpoint independently passes the full VM and CLI suites, including all 129 decorator/channel checks, the full parser and runtime suites, 76 AST history integration tests, and workspace all-target check. Native-host all-target Clippy passes. Evaluator library failures remain 795 passed / 27 failed / 1 ignored as recorded above. Rust LSP is unavailable (installation declined), and no full protocol, full workspace test, or final 92-row acceptance claim is made. VM-TO-BOOL-01 remains deferred until decorator completion.
  - The 27 evaluator-library failures were subsequently repaired without deleting or ignoring tests. Invalid positives were replaced by current-spec inputs while retaining explicit negatives for positional Closure/block-channel confusion, missing decorator protocol members, raw/wrong-closed generics, undefined decorators, and private properties. Core ArgumentError/TypeError catches now preserve canonical object identity and ExceptionContext; non-core VM failures retain their original structured errors across nested cleanup. The current independently rerun evaluator result is **836 passed, 0 failed, 1 ignored**, with all evaluator integration targets passing.
  - Follow-up fixes add exact generic-construction admission and owner bindings in both engines, declaration-local stored-property visibility, VM generic stored/computed property execution, and Type rather than Contract-object metadata for reflected Contract requirement annotations. The latest independent VM/CLI/parser/runtime joint run passes. These results close the former 27-failure checkpoint, not the full 92-row decorator matrix.
  - The two follow-up VM gaps are now closed. All three construction paths validate runtime owner bindings and exact generic admission before materialization, allocation, property initialization, or `initialize`; raw aliases reject with zero effects while valid closed/nested aliases construct. `Array<Integer>` and `Hash<Symbol, Integer>` now resolve to their canonical builtin Class values, matching the reference engine's existing Class and `.type` identity semantics; unknown generic names still raise NameError.
  - Final independent verification for this repair wave passes the complete evaluator suite (**836 library tests, 1 intentionally ignored measurement, and every integration target**), the complete VM/CLI/parser/runtime joint suite, workspace all-target check, and the specification audit (**14 artifacts, 37 ledger rows, 629 normative paragraphs, 16 labelled informative paragraphs, no violations**). Real CLI probes pass in both engines for dedicated block channels, canonical core errors and contexts, invalid causes, generic alias admission, builtin generic lookup, and private/public generic properties. This closes the 27-failure repair wave only; it does not mark the 92-row decorator acceptance matrix complete, and VM-TO-BOOL-01 remains deferred.
  - The first independent gate review rejected two uncovered generic-admission edges. Both are now repaired and independently reverified: `Closure<S>`, `BoundMethod<S>`, and transparent `Block<S>` arguments are structurally reconstructed from canonical callable metadata for direct and aliased generic construction; raw or statically invalid generic construction evaluates receiver/operands exactly once before raising, while allocation, property initialization, defaults, and constructor bodies remain unentered. Operand exceptions retain precedence over admission errors. The full workspace test run exits 0 after these fixes; VM library Clippy with `-D warnings` and `cargo fmt --all -- --check` pass. Combined VM/evaluator Clippy remains blocked only by three pre-existing evaluator style diagnostics in continuation/adapters.rs, decorator_metadata.rs, and native.rs.
  - The second independent gate review confirmed those two fixes but found that callable generic bounds still compared unnormalized source structure. Admission now canonicalizes callable parameter and result types with the VM normalization laws before enforcing invariant equality: reordered/deduplicated unions, `NonNil` and nominal intersection absorption, and nested callable signatures admit direct and aliased construction, while different parameter types, result types, and callable kinds still reject. Dedicated positive/negative tests and exact CLI reproductions cover each case. A serial full-workspace rerun exits 0; an earlier parallel workspace-test/all-target-check run exposed only a transient native fixture file race, and the isolated target plus the serial rerun both pass.
  - The third gate review showed that the first normalizer revision crossed mixed operators and removed `NonNil` without proof. The corrected implementation follows TYPES-C018/C022/C023 in both compile admission and runtime reification: only the same composition operator flattens; mixed union/intersection nodes stay compact; deduplication precedes marker handling; `Nil` is removed from a union intersected with `NonNil`; and `NonNil` is removed only when the remainder is provably non-nil. `Object & NonNil` therefore remains distinct from `Object`, while `(String | Nil) & NonNil` equals `String`, duplicate `NonNil` is idempotent, and `Nil & NonNil` is `Never`. `TypeAtom::Intersection` provides the missing symmetric runtime representation for an intersection nested inside a union, with exhaustive admission, reflection, reconstruction, equality, and hashing coverage. The focused serial matrix passes 102 tests and 18 direct/alias CLI probes; TYPES V214 and V221 pass without corpus changes; the full serial workspace run exits 0 with evaluator **836/0/1** and VM library **161/0**. Workspace all-target check, VM library Clippy with denied warnings, formatting, diff whitespace, and the specification audit pass.
  - All-target coverage across module, contract, property, and class-object targets.
  - Complete async signature/Type, lifecycle and GC coverage beyond the initial passing cases.
  - Replay across all five reasons (`:origin`, `:open`, `:upgrade`, `:rollback`, `:closed_materialization`).
  - Canonical undecorated artifacts and target-specific rebuilds.
  - Complete callable types, exact signature checking, and full `Block<S>` admission.
  - Moving GC stress and survival surfaces for wrapper closures, captured chains, and pending tasks.
  - All 92 rows in the clause checklist remain unchecked pending full, independent evidence.

## Clause-By-Clause Checklist

### IRIS-V1-META-C127: Core Records, Construction And Phase Extent

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 127.1 | Kernel exposes `Invocation`, `InvocationSignature`, `InvocationParameter`, `ArgumentChanges`, `DecoratorContext`, `DecoratorProtocolError` as core named Types, without new keywords or callable kinds. First five are noncallable immutable records; only `ArgumentChanges` has a public constructor. Check unqualified/Kernel availability, rejected public construction of the other records and immutable surfaces. | [ ] | [ ] |
| 127.2 | All five ordinary Contracts (`ClassDecorator`, `ModuleDecorator`, `ContractDecorator`, `MethodDecorator`, `PropertyDecorator`) require both `plan(declaration, arguments) -> Plan` and `transform(declaration, arguments, context) -> Transformation`. Test each missing member and each correctly admitted target; do not substitute locally weakened Contracts. | [ ] | [ ] |
| 127.3 | Fresh zero-argument instance for each executed phase/application/replay/closed materialization; application arguments go only to phase members, never `initialize`. Observe instance identity, constructor arguments and phase arguments separately; a phase returning empty still has the required member. | [ ] | [ ] |
| 127.4 | Static purity covers construction, `initialize`, synchronous helpers and the entire transitive planner graph. Whitelisted declaration/import/package/tracked configuration inputs succeed; IO, time, randomness, ambient/untracked/mutable globals, load races and unspecified ordering fail statically with `IRIS-DECORATOR-NONDETERMINISTIC`, before transform or publication. Direct-global scanning alone is insufficient. | [ ] | [ ] |
| 127.5 | `Plan.empty`/`Transformation.empty` are property reads with permanent read-only `kind`; exactly `:class`, `:module`, `:contract`, `:method`, `:property`. Factories/fluent operations obtain kind from runtime-owned phase data, including constructor and synchronous-helper extent. Retained reads do not retag or authorize construction; outside-phase construction fails. | [ ] | [ ] |
| 127.6 | Runtime-only immutable `DecoratorContext` exposes exactly `kind: Symbol` and `reason: Symbol`; cover `:origin`, `:open`, `:upgrade`, `:rollback`, `:closed_materialization`. No candidate, mutation operation, original body or grant token; retaining context does not extend phase authority. Declaration input remains its immutable C097/C123 view, not a mutable Class handle. | [ ] | [ ] |

### IRIS-V1-META-C128: Persistent Operations And Capability Boundaries

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 128.1 | Factories and identical fluent Methods return same-kind, ordered, persistent inert descriptions. Compare factory/fluent results, unchanged prior values and multiple appended operations; no wrapper/body executes until invocation and no operation installs before complete validation and atomic publication. | [ ] | [ ] |
| 128.2 | Exact required positional forms: `add_method(selector, body)`, `wrap_method(wrapper)`, `wrap_getter(wrapper)`, `wrap_setter(wrapper)`. Reject wrong arity, keywords, rest/trailing-block forms and non-Symbol selector. Accept body/wrapper as Object then inspect full ordinary Closure metadata, not invariant generic casts. | [ ] | [ ] |
| 128.3 | Class/Module addition creates a new private ordinary instance/declared-member slot under `method_set`; body retains full input/result, sync/async contract and lexical captures/receiver. Reject collisions, implicit override/impl, signature/visibility/Contract/static-spine/policy/native violations. No addressed-member/batch wrapping, composition or privileged replacement. | [ ] | [ ] |
| 128.4 | Method wrapping uses `method_body`, retaining exact slot, visibility, full signature, generics and obligations. A `method_set` denial alone permits compatible wrapping; body denial rejects. | [ ] | [ ] |
| 128.5 | Property getter/setter wrapping uses `property_body`, never a `method_body` loophole; combines existing getter and setter in one result without storage/layout/Type change. Missing accessor is candidate-validation `ArgumentError`; no absent setter creation. Test deny-set versus deny-body separately. | [ ] | [ ] |
| 128.6 | Contract accepts empty only, preserving ordered descriptive decorator reflection. Reject executable body/member/requirement changes and arbitrary metadata APIs; no special permission bypass. | [ ] | [ ] |

### IRIS-V1-META-C129: Assembly And Invocation Order

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 129.1 | Phase execution remains A then B for written `@A()` then `@B()`; invocation is A(B(original)), with entries A/B/body and exits B/A. Reverse source reverses nesting. Source application order then fluent append order governs layers; getter/setter chains are independent. | [ ] | [ ] |
| 129.2 | Later layers insert inside earlier layers immediately before the original, without rerunning prior decorators or redecorating an accumulated chain. Other operations keep candidate/conflict order. Logged/Cached/Retry enters logging once, cache hit skips inner work and retry repeats only inner suffix. | [ ] | [ ] |
| 129.3 | `next` closes over the exact inner suffix; no selector redispatch, `super` substitution or public original-Method handle. Exercise intervening replacement/removal/undef and ordinary/qualified namespace isolation. | [ ] | [ ] |

### IRIS-V1-META-C130: Original Call Preparation

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 130.1 | Before even zero-attempt/cache-hit wrappers, evaluate receiver/operands in CONTROL-C033 order, resolve visibility/current owner and one exact ordinary or Contract-qualified slot with closed bindings, then bind/validate all original inputs. Invalid arity, duplicate/unknown keyword, block, parameter or default prevents every wrapper/body. Preserve ordinary failure precedence. | [ ] | [ ] |
| 130.2 | Defaults evaluate once left-to-right in original lexical environment. Retries never reevaluate receiver, operands, splats, keyword expansion, block creation or defaults, reselect slot or reinfer generics. Counter tests include failures; a default-created mutable object retains identity and earlier shared mutations. | [ ] | [ ] |
| 130.3 | Outer Invocation is created only after preparation succeeds; sync failures raise normally. Async receiver/operand failure versus failed-Task binding/default/input preparation follows C136, not a cache repair path. | [ ] | [ ] |

### IRIS-V1-META-C131: Complete Immutable Invocation Views

Every field row below needs exact Type/order/identity observations, write rejection, and absence of binding cells, executable handles, candidates and private-state grants. Containers are runtime-created immutable Arrays/Hashes using explicit element storage, not generic covariance; immutability is shallow. Each layer gets a fresh record; provenance is fixed through nesting and retries.

| Field gate | Exact public contract | I | T |
| --- | --- | --- | --- |
| 131.receiver | `receiver: Object` is the exact selected receiver relation, never wrapper `self`. | [ ] | [ ] |
| 131.slot | `slot: Tuple<Object, Symbol, Type?, Symbol>`: declaring logical Class/Module, selector, closed qualifying Contract Type or nil, then `:method`/`:getter`/`:setter`; descriptive, not invocable. | [ ] | [ ] |
| 131.signature | `signature: InvocationSignature`; fully substituted selected contract. | [ ] | [ ] |
| 131.generics | `owner_type_arguments`, `method_type_arguments`: `Array<Type>`, respective declaration order, empty when nongeneric. | [ ] | [ ] |
| 131.positional | `positional: Hash<Symbol, Object>` of fixed bound positional names in declaration order, including defaults. | [ ] | [ ] |
| 131.keywords | `keywords: Hash<Symbol, Object>` of declared keyword-only names in declaration order, including defaults. | [ ] | [ ] |
| 131.rest | `rest: Array<Object>` in binding order, empty without positional rest. | [ ] | [ ] |
| 131.keyword-rest | `keyword_rest: Hash<Symbol, Object>` of unmatched entries in incoming encounter order, empty without that channel. | [ ] | [ ] |
| 131.block | `block: Object`: current exact block or nil, separate from all containers. | [ ] | [ ] |
| 131.original-positional | `original_positional: Array<Object>` after source splat expansion, call order, before defaults. | [ ] | [ ] |
| 131.original-keywords | `original_keywords: Array<Tuple<Symbol, Object>>` after expansion, encounter order, before defaults. | [ ] | [ ] |
| 131.original-block | `original_block: Object`: original block identity or nil; `block_omitted: Bool` distinguishes no supplied block from explicit nil. | [ ] | [ ] |
| 131.provenance | `omitted`, `defaulted`: `Array<Symbol>`, fixed positional/keyword names in declaration order; block omission uses its own field. | [ ] | [ ] |
| 131.replaced | `replaced: Array<Symbol>`: unique, cumulative explicitly replaced names, declaration order, including rest/keyword-rest/block binding names; equal-value replacement still counts. | [ ] | [ ] |
| 131.signature-fields | `InvocationSignature.parameters: Array<InvocationParameter>` in complete CONTROL-C022 declaration order; `result: Type` is exact closed R, awaited R for async; `is_async: Bool` is declared mode. | [ ] | [ ] |
| 131.parameter-fields | `InvocationParameter.name: Symbol`; `category: Symbol` exactly `:positional`, `:keyword`, `:rest`, `:keyword_rest`, `:block`; `type: Type` is closed fixed Type/rest-element/keyword-rest-value/exact `Block<S>`; `optional: Bool` records omission, false for rest categories. No default expression/handle is exposed. | [ ] | [ ] |

### IRIS-V1-META-C132: ArgumentChanges Construction

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 132.1 | `ArgumentChanges.empty` is immutable and available inside/outside phases. `ArgumentChanges.new()` is an ordinary class-object Method returning the same no-change meaning, without an identity/cache guarantee. Zero positional parameters, exactly five optional keyword-only fields in the order below, no rest/trailing block. | [ ] | [ ] |
| 132.positional | `positional: Hash<Symbol, Object>`: absent retains all fixed slots; supplied patches only listed declared positional names. | [ ] | [ ] |
| 132.keywords | `keywords: Hash<Symbol, Object>`: absent retains declared keyword-only slots; supplied patches only those names. | [ ] | [ ] |
| 132.rest | `rest: Array<Object>`: absent retains incoming positional rest; supplied replaces the whole sequence. | [ ] | [ ] |
| 132.keyword-rest | `keyword_rest: Hash<Symbol, Object>`: absent retains incoming keyword rest; supplied replaces entire unmatched map in supplied encounter order. | [ ] | [ ] |
| 132.block | `block: Object`: absent forwards exact incoming block identity/nil; supplied replaces the separate block channel subject to C133. | [ ] | [ ] |
| 132.2 | Absence is internal field-presence metadata, not nil or a public sentinel. Test each field separately and all five together; explicit nil is supplied, not omitted. Constructor snapshots container structure/Symbol keys/element references immediately; later source-container edits cannot change record, but shared value mutations remain shared. No target binding or deep copy. | [ ] | [ ] |
| 132.3 | Unknown/duplicate constructor keywords yield `ArgumentError`; wrong container shape/non-Symbol key yields `TypeError`. Hash duplicate-key resolution precedes this API; no recovery of discarded entries. No mutable update/merge surface. Reuse a record against a different signature to demonstrate per-call validation. | [ ] | [ ] |

### IRIS-V1-META-C133: Replacement Validation And Attempt Isolation

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 133.1 | Derive a complete payload from this layer's incoming snapshot, patch fixed slots by Symbol name, then validate the entire original exact closed signature before any inner marker increments. Unknown/wrong-channel names and supplied absent channels raise `ArgumentError`, including empty rest/keyword-rest/block replacement. | [ ] | [ ] |
| 133.2 | Rest/keyword-rest replace whole containers; unknown keywords require original keyword-rest channel. Reject declared positional/keyword names smuggled into keyword rest. Fields remain disjoint regardless of source/Hash order. Required/fixed/rest-element/keyword-rest-name-and-value contracts are all revalidated. Value failures are `TypeError`. | [ ] | [ ] |
| 133.3 | Missing patch entries retain incoming/default values; explicit nil is type-checked. Nested inner same-name patch wins; successive calls of one next each start from its incoming basis, never the preceding attempt. Success modifies only new inner payload/cumulative `replaced`; rejection leaves all records unchanged; original/default/omission provenance remains fixed. | [ ] | [ ] |
| 133.4 | Block absence forwards exact identity; non-nil replacement satisfies invariant original closed `Block<S>` without rebinding; nil only for optional block. Cover required, optional nil, optional non-nil, no-channel, matching replacement for incoming nil and mismatched signature. Do not create a channel, change receiver/selector/qualified slot or generic substitutions. | [ ] | [ ] |
| 133.5 | New immutable record, fresh parameter cells and rest containers at each entered layer and original-body attempt. Escaping parameter captures/container edits do not cross attempts; shared object effects do. Change-computation effects are not rolled back, including rejected attempts. No defaults/splats/generic inference rerun. | [ ] | [ ] |

### IRIS-V1-META-C134: Exact Wrapper And Next Signatures

| Callable gate | Complete admission/call contract | I | T |
| --- | --- | --- | --- |
| 134.sync-wrapper | Ordinary synchronous Closure, exactly two required positional parameters `invocation: Invocation`, `next: Closure<(ArgumentChanges) -> Object>`, declared result Object; no optional/keyword/rest/block parameters. | [ ] | [ ] |
| 134.async-wrapper | Ordinary async Closure, exactly required positional `invocation: Invocation`, `next: Closure<(ArgumentChanges) -> Task<Object>>`; awaited result Object, invocation result `Task<Object>`; no extra categories. | [ ] | [ ] |
| 134.sync-next | Fresh ordinary Closure with one optional positional `changes: ArgumentChanges`, default `ArgumentChanges.empty`, declared/invocation result Object. No keywords/rest/trailing block. | [ ] | [ ] |
| 134.async-next | Fresh async-mode bridge Closure with the same optional parameter/default; awaited result Object, invocation result `Task<Object>`. Use full signature metadata for optionality; do not invent optional/default syntax inside `Closure<S>` or an overload/type alias/fourth callable kind. | [ ] | [ ] |
| 134.attempts | Valid calls allow zero, once or sequential multiple next calls, including retry after failure and distinct changes each time. Zero still validates initial input and result; retries repeat only captured inner suffix, not outer callbacks/preparation. | [ ] | [ ] |

### IRIS-V1-META-C135: Exact Result And Callback Admission

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 135.1 | Target sync/async mode admits only matching exact C134 Closure kind/parameters/result annotation. Provable mismatch rejects statically, otherwise ordinary `TypeError` at candidate validation. Reject BoundMethod conversion, covariance and silent mode adaptation. | [ ] | [ ] |
| 135.2 | Runtime-owned adapter specialized to exact closed target signature checks inputs and successful R at every wrapper, including zero-attempt cache result; original return guard remains. Invalid original/inner result fails before outer sees success; outer may catch ordinary `TypeError` and recover with valid R. | [ ] | [ ] |
| 135.3 | Check `Never`, nilability, invariant closed generics, getter and setter results. Setter assignment yields checked setter result, not automatically assigned input. A synchronous Method returning a Task-valued R stays sync and is not awaited. | [ ] | [ ] |

### IRIS-V1-META-C136: Async Typed Bridges And Eager Execution

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 136.1 | Transform registers async Closure synchronously/non-awaitingly; callback neither executes nor awaits inside transaction. Every invocation creates a fresh caller-visible `Task<R>`, distinct from callback/inner Tasks even for forwarding/cache hits. Every accepted next call creates a fresh `Task<Object>` bridge. | [ ] | [ ] |
| 136.2 | Real invariant adapters await callback Object then guard exact awaited R; bridges await inner `Task<R>` and expose only checked Object. No Task covariance casts. Every layer checks completion before exposure. | [ ] | [ ] |
| 136.3 | Preparation/callbacks/bridges start eagerly until completion/failure/first incomplete await; completed awaits introduce no scheduling/fairness hop. Observe order with ready and incomplete Tasks, not sleeps. | [ ] | [ ] |
| 136.4 | Receiver/operand errors remain pre-Task; binding/default/input setup fails outer Task before wrapper entry. Invalid attempt payload fails returned bridge before inner entry and is catchable by await. Repeat awaits preserve completion identity and propagated original ExceptionContext root; a new result-guard failure creates its own context. Preserve chapter 07 no-cancellation rules. | [ ] | [ ] |

### IRIS-V1-META-C137: Owner Scope, Settlement And Unfinished Work

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 137.1 | Permission belongs to sync activation or owning async callback Task, not admission adapter. Same-owner synchronous helpers succeed; owner suspension/resumption retains permission. Foreign async helper fails even in its eager prefix; bridge checks invoking Task before internal switching. Sync owner records entering Task identity or no Task. | [ ] | [ ] |
| 137.2 | One in-flight attempt per layer; mark before starting inner code. Sync return/raise or async bridge settlement clears it; retry after success/failure settlement succeeds. Reentrant/eager-prefix/overlap, foreign-Task and post-completion calls fail before new inner work. Retaining/passing Closure preserves captures, not permission. | [ ] | [ ] |
| 137.3 | Callback normal completion with unfinished attempt fails admission with `DecoratorProtocolError`; exceptional completion preserves primary context and emits C139 lifetime diagnostic. Started inner work continues under its own scopes with no implicit cancellation, join, waiting or retroactive abortion. | [ ] | [ ] |
| 137.4 | Settlement tracking is not failure observation; abandoned inner failures remain reportable under ASYNC-C027/C028 after adapter failure propagation. Original ordinary block/replacement-block capture has no next-specific scope restriction. | [ ] | [ ] |

### IRIS-V1-META-C138: Identity, Tombstones, Replay And Target Coverage

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 138.1 | Publication creates new Method identity, preserving logical Class/Module identity. Retained Method/BoundMethod executes old exact chain/cache; future lookup gets new chain. Current lexical-owner loss raises `MethodBindingError` before cache-hit entry. | [ ] | [ ] |
| 138.2 | Removal and undef/tombstones affect future lookup, not captured next suffix or valid-owner retained Method. Test ordinary/qualified isolation and closed qualified slot keys; original `super(args...)` uses current MRO despite retained code. | [ ] | [ ] |
| 138.3 | Original lexical owner/package stays original; wrapper and replacement-block Closures keep their own lexical receiver/package, never borrowing caller/target/original-block reflection authority. Cross-package allow/deny probes prevent accidental grants. | [ ] | [ ] |
| 138.4 | Application captures/caches/counters persist over normal calls; separate applications and rebuilt/closed-materialized chains get fresh state. Deliberately shared objects stay shared, without cloning or implicit synchronization. | [ ] | [ ] |
| 138.5 | All five reasons (origin/open/upgrade/rollback/closed materialization) rebuild each ordered application once from exact canonical undecorated artifact, never active wrapped body. Rollback verifies historical artifact/current spine/policy, publishes new revision and fresh caches. Constructor/transform external effects remain author responsibility, not magically rolled back. | [ ] | [ ] |
| 138.6 | Support actual replaceable ordinary, Module/main, class-object, operator, generic and qualified Methods; generated accessors use Property operations. Native-backed compatible bodies use same managed admission with exact ABI metadata; native structural changes still require `native`. Async targets retain exact contracts. | [ ] | [ ] |
| 138.7 | Runtime-protected, otherwise nonreplaceable or native-unsafe entries reject atomically. Ordinary `protected` visibility is not runtime body protection: compatible wrapping remains possible subject to existing access/capabilities. No fabricated primitive slot, AST API or Class/Module batch wrapping. | [ ] | [ ] |
| 138.GC | Supporting implementation acceptance: exercise the existing moving-GC surface with active/retained old chains, wrapper captures, snapshots/changes, next closures and suspended callback/bridge Tasks. Live references and identities survive collection; expired execution permission is not revived; released chains do not remain rooted solely by completed bookkeeping. This derives lifecycle safety from existing GC rules, not a new META-C138 API. | [ ] | [ ] |

### IRIS-V1-META-C139: Failure Phase And Protocol Error Categories

| Gate | Implementation requirement and acceptance observations | I | T |
| --- | --- | --- | --- |
| 139.kind | Target-Contract mismatch, wrong-kind Plan/Transformation, retained tagged value used for another kind and inadmissible operation report `IRIS-DECORATOR-KIND`, severity error, static when provable; otherwise runtime candidate validation. Failed origin publishes nothing; existing target retains revision/members/commit, whole group rolls back and no revision/commit is consumed. Preserve provable V428 static lane. | [ ] | [ ] |
| 139.purity | Constructor/transitive-plan impurity remains `IRIS-DECORATOR-NONDETERMINISTIC`, severity error, static, no transform/publication. C125 phase exception weakens no other guarantee. | [ ] | [ ] |
| 139.error | Catchable core `DecoratorProtocolError` has read-only `category: Symbol`, `owner: Object` (owning next Closure or nil for phase-less construction), no inaccessible arguments. Argument shape stays `ArgumentError`; payload/result Type stays `TypeError`. | [ ] | [ ] |
| 139.outside | `:outside_phase`: context-derived Plan/Transformation construction without active phase raises synchronously; owner nil. Retained record/context is not authorization. | [ ] | [ ] |
| 139.expired | `:expired`: owner activation/Task completed; precedence is expired before foreign Task before overlap. Sync raises, async returns fresh failed `Task<Object>` without inner attempt. | [ ] | [ ] |
| 139.foreign | `:foreign_task`: caller Task differs from recorded owner; same mode-dependent delivery, including foreign helper eager prefix. | [ ] | [ ] |
| 139.overlap | `:overlap`: owning layer has in-flight attempt; same mode-dependent delivery. Rejection changes no existing in-flight mark and enters no inner body. | [ ] | [ ] |
| 139.unfinished | `:unfinished_inner`: normal early callback completion becomes failed admission `Task<R>`; already-failing callback keeps primary failure plus structured diagnostic. Started inner attempt continues. | [ ] | [ ] |
| 139.diagnostic | Exceptional unfinished case emits `IRIS-DECORATOR-PROTOCOL`, severity error, phase runtime, category `:unfinished_inner`, owning callback Task identity, next Closure identity, in-flight bridge Task identity and primary ExceptionContext, without mutating primary context. | [ ] | [ ] |
| 139.expression | Operand/change-construction errors occur at ordinary expression site before next Task exists. Failures discovered inside async next, including scope rejection, produce its fresh failed bridge Task; validate category/delivery independently of incidental NameError/TypeError from a missing API. | [ ] | [ ] |

### IRIS-V1-CONFORMANCE-C076: Coverage Accounting

| Formal coverage row | Checklist mapping and required evidence family | I | T |
| --- | --- | --- | --- |
| C127/C139 | 127.1-127.6, 139.kind/purity/outside: all kinds, phases, constructors, purity, tags, reasons and rollback. | [ ] | [ ] |
| C128/C129 | 128.1-128.6, 129.1-129.3: persistent operation equivalence/order, independent accessors, Logged/Cached/Retry. | [ ] | [ ] |
| C130/C131 | 130.1-130.3 and every 131 field: initial failures before cache, ordinary order/defaults and exact immutable snapshots. | [ ] | [ ] |
| C132/C133 | Every 132 field, 132.1-132.3, 133.1-133.5: constructor presence/snapshot/shape, all channels/blocks, inner validation and fixed provenance. | [ ] | [ ] |
| C133/C134 | 133.3-133.5 and all 134 rows: zero/once/retry, exact next call signature, fresh cells/containers/captures, retained shared effects. | [ ] | [ ] |
| C135 | 135.1-135.3: callback admission, exact per-layer R, recovery, Never/generics/setters/sync Task values. | [ ] | [ ] |
| C136 | 136.1-136.4: synchronous registration, eager typed adapters, fresh Task identities, failures and repeat awaits. | [ ] | [ ] |
| C137/C139 | 137.1-137.4 and 139 category/diagnostic/expression rows: owner helpers, expiry/foreign/overlap, settlement, unfinished work and abandoned failures. | [ ] | [ ] |
| C138 | 138.1-138.5 plus 138.GC: caches/capture isolation, replay, retained identities, owner/tombstone/qualified/super/authority boundaries. | [ ] | [ ] |
| C128/C138/C139 | 128.2-128.6, 138.6-138.7, 139.kind: capabilities, no accessor creation, all target surfaces, protected visibility versus protected replacement. | [ ] | [ ] |
| Reporting gate | Preserve META-V428/V429/V430/V431/V361 bytes/IDs/classifications; distinguish changed semantic lane from unchanged incomplete fixture. Missing coverage stays visible under C057; approval is not coverage. Exact anchors/backend applicability/failure phase/side effects follow C016/C033/C047/C049. No checklist row or temporary probe is counted as a corpus record or full-clause pass. | [ ] | [ ] |

## Fresh Working-Tree Baseline

### Provenance And Reproduction

Evidence directory: `/var/folders/tq/hgbd_kk13_1bgdj9c8983s8h0000gn/T/opencode/iris-v135-acceptance.yv1bx4` (macOS resolves this to `/private/var/...`; both identify the same directory). It was allocated with `mktemp -d` after listing the approved parent, and listed before Cargo created its previously nonexistent `target` directory. The CLI was freshly built there, not reused from workspace `target` or another agent's target.

Working directory: `/Users/huisama/Project/Iris-Language`. Build observed 2026-09-08 16:57:56Z through 16:58:10Z; library tests through 16:58:26Z; probes at 16:58:47Z through 16:58:48Z (local work date 2026-09-09). Toolchain: `cargo 1.97.1 (c980f4866 2026-06-30)` and `rustc 1.97.1 (8bab26f4f 2026-07-14)`.

`source-before.json` and `source-after.json` record timestamps and SHA-256 hashes for Cargo manifests/lock/toolchain, selected text extensions throughout `crates`, and current formal-spec files. `source-drift.json` has no changed or removed sampled files during build/test. This is a before/after observation, not an immutable snapshot, an exhaustive workspace inventory, or proof that parallel edits never occurred. Build/test provenance remains mutable working tree. No HEAD, branch or clean-tree claim is made.

Fresh `target/debug/iris` SHA-256: `6a78a868bbfc1b71f6fa175aaa6ccead104394487eafbe5c3bad89c10de9ddcd` (`binary-sha256.txt`). `acceptance-driver.mjs` records each subprocess's command, arguments, working directory, CARGO_TARGET_DIR, timestamps, exit status, signal, stdout and stderr. It generates probes only in its own temporary directory. Baseline and probes are separate modes; probes run after build and workspace library tests.

To repeat in another fresh isolated directory, list the parent first, allocate a new directory and place the saved driver there. The driver derives its evidence/target location from its own path; it does not install packages or edit workspace files. The executed Cargo commands are exactly:

```sh
ls /var/folders/tq/hgbd_kk13_1bgdj9c8983s8h0000gn/T/opencode
evidence=$(mktemp -d /var/folders/tq/hgbd_kk13_1bgdj9c8983s8h0000gn/T/opencode/iris-v135-acceptance.XXXXXX)
ls "$evidence"
export CARGO_TARGET_DIR="$evidence/target"
/Users/huisama/.cargo/bin/cargo build --locked -p iris-cli
/Users/huisama/.cargo/bin/cargo test --locked --workspace --lib
```

To rerun the saved driver against its recorded directory (not a new fresh-build claim):

```sh
node /var/folders/tq/hgbd_kk13_1bgdj9c8983s8h0000gn/T/opencode/iris-v135-acceptance.yv1bx4/acceptance-driver.mjs baseline
node /var/folders/tq/hgbd_kk13_1bgdj9c8983s8h0000gn/T/opencode/iris-v135-acceptance.yv1bx4/acceptance-driver.mjs probes
```

These reruns overwrite logs in that temporary directory; retain the original evidence first if comparing later mutable sources. `node --check` passed for the driver. JavaScript LSP was unavailable (installation previously declined), and Markdown has no configured diagnostic server; no clean LSP result is claimed.

### Build And Library Results

Fresh CLI build: exit 0, no warnings observed, `build.stdout`, `build.stderr`, `build.json`. Workspace library tests: exit 0, **1,134 passed, 0 failed, 1 ignored**, `workspace-lib.stdout`, `workspace-lib.stderr`, `workspace-lib.json`.

| Library | Passed | Ignored |
| --- | --- | --- |
| iris-abi | 50 | 0 |
| iris-builtins | 0 | 0 |
| iris-conformance | 32 | 0 |
| iris-eval | 739 | 1 |
| iris-lexer | 45 | 0 |
| iris-native-host | 8 | 0 |
| iris-native-sdk | 0 | 0 |
| iris-package | 0 | 0 |
| iris-parser | 101 | 0 |
| iris-runtime | 15 | 0 |
| iris-syntax | 8 | 0 |
| iris-vm | 36 | 0 |

The ignored test is `whole_program_tests::measure_corpus_agreement` ("measurement, run explicitly"). `--lib` does not run integration tests, binary tests, doctests or the entire conformance corpus; zero library tests in a crate is not evidence for that crate's new protocol. No package/native lifecycle or GC acceptance scenario was executed in this preparation task.

Manual terminal QA used this same binary in tmux: `--help` rendered usage; `--vm -e 'print(1 + 2)'` printed `3`; `-e 'ArgumentChanges.new(unknown: 1)'` reported `unknown name`. The automated CLI driver independently exercised both backends and captures the exact outputs below.

### Minimal New-Protocol Red Probes

`probes.json` contains eight exact source programs and both backend observations (16 executions), with individual `<probe>-<backend>.json/.stdout/.stderr` files and `<probe>.iris` sources. Only the two arithmetic controls matched their expectations. All fourteen new-protocol executions failed their coarse expectations; no clause is accepted. All new-protocol executions exited 1 with empty stdout.

| Probe / source | Required first observation | Fresh reference result | Fresh VM result |
| --- | --- | --- | --- |
| `control`: `print(1 + 2)` | stdout `3`, exit 0 | As expected | As expected |
| `changes-empty`: `print(ArgumentChanges.empty same? nil)` | stdout `false`, exit 0 | `unknown name` | `NameError` |
| `changes-new`: `print(ArgumentChanges.new() same? nil)` | stdout `false`, exit 0 | `unknown name` | `MessageNotFound`, ArgumentChanges/new |
| `changes-unknown-field`: `ArgumentChanges.new(unknown: 1)` | `ArgumentError` from existing constructor | `unknown name` | `MessageNotFound`, ArgumentChanges/new |
| `plan-outside-phase`: `Plan.empty` | catchable `DecoratorProtocolError`, `:outside_phase`, owner nil | `unsupported construct` | `NameError` |
| `transformation-outside-phase`: `Transformation.empty` | same typed outside-phase error | `unsupported construct` | `NameError` |
| `wrap-factory-outside-phase`: typed Closure passed to `Transformation.wrap_method` | same typed outside-phase error | `Transformation has no method wrap_method` | `MessageNotFound`, Transformation/wrap_method |
| `method-next-once`: full MethodDecorator using built-in Contract, both phase members and typed wrapper calling `next.call()`; target returns 7 | stdout `7`, exit 0, then instrument exactly one body entry | `unsupported construct` | compile refusal: `class contract unbound` |

These observations demonstrate red availability/admission baselines, not successful argument or scope guards. In particular, an unknown-constructor probe may produce NameError, MessageNotFound or incidental TypeError on another mutable baseline: none proves C132's `ArgumentError` guard while the constructor itself is absent. An outside-phase missing-factory error likewise is not `:outside_phase`. The typed next fixture fails before next semantics can be tested; it is not evidence of an implemented continuation or a continuation-specific defect. The driver's diagnostic string matching is deliberately coarse and is not a replacement for future category/owner/phase assertions.

### Source Evidence Behind The Baseline

These references describe inspected paths, not a promise that every repository implementation path was audited:

- [CLI source](../crates/iris-cli/src/main.rs): reference execution and independent VM compile/verify/run; VM refusal is reported instead of evaluator fallback.
- [Decorator static analysis](../crates/iris-parser/src/analysis.rs): `check_decorator_determinism` scans bodies named `plan` for globals; it does not establish constructor/transitive purity. `check_decorator_targets` collects top-level Class/Module/Contract applications; full five-target/membership acceptance remains pending.
- [Evaluator source runtime](../crates/iris-eval/src/source_runtime.rs): `run_decorator_transform` constructs with zero arguments but supplies `Value::Class(class)` as both declaration and context; inspected path skips absent transforms. `apply_transformation` handles staged additions with reconstructed public/unannotated synchronous Methods, not the exact C128/C135 closure boundary. `send` exposes legacy Transformation empty/kind/add_method, not the new wrapping protocol.
- [Runtime decorator metadata](../crates/iris-runtime/src/decorator.rs): existing metadata/identity-violation model is not an Invocation/next layer model.
- [VM declarations](../crates/iris-vm/src/compile/declarations.rs): method-decorator unsupported handling remains an explicit backend gap; the minimal fixture currently stops earlier at unbound built-in Contract.
- [Built-in catalog](../crates/iris-builtins/src/types.rs) and [catalog coverage tests](../crates/iris-builtins/tests/coverage.rs): inspected inventory contains Transformation and its older surface. Catalog presence is not executable protocol support; absence/search results are corroborated by actual CLI probes, not used alone as proof.

## Preserved Corpus Limitations

The current [v1.35 defect ledger](spec-defects-v1.md#v135-approved-decorator-errata) records source/record mismatches; its audit counts are historical evidence, not rerun results from this task. No conformance fixture or expectation was changed here.

| Preserved record | Why it does not complete this matrix |
| --- | --- |
| [META-V428](../conformance/iris-v1/vectors/META/IRIS-V1-META-V428.json) | Provable target mismatch remains static. Local plan-only ModuleDecorator fixture does not test built-in two-member admission or a runtime-only wrong-kind returned Transformation; the newly permitted candidate-validation phase needs a separate test. |
| [META-V429](../conformance/iris-v1/vectors/META/IRIS-V1-META-V429.json) | Local plan-only Contract and mutable-global fixture are narrower than constructor/transitive purity; code-only diagnostic expectation does not prove phase/severity/no-transform/no-publication. Clock versus global fixture discrepancy remains recorded. |
| [META-V430](../conformance/iris-v1/vectors/META/IRIS-V1-META-V430.json) | Transform-only Contract and one-positional-plus-trailing-block add_method do not match required complete admission and two-positional API. Earlier rejection can prevent intended capability check; do not call that a passing method_set denial. |
| [META-V431](../conformance/iris-v1/vectors/META/IRIS-V1-META-V431.json) | Same incomplete Contract/argument shape; external generated-method calls assume public members although minimal C128 addition is private. Order metadata/addition fixture does not prove wrapper nesting, replay, await refusal or cache lifetime. |
| [META-V361](../conformance/iris-v1/vectors/META/IRIS-V1-META-V361.json) | Existing handwritten method/property capability probes do not prove wrap-method versus wrap-property capabilities, deny-set-allowed wrapping, protected-visibility admission or native-body safety. |

## C127/C139 Implementation Evidence Map

Every row below maps a C127 or C139 matrix item to current runtime paths, existing or planned tests, required evidence form, and its concrete first red scenario. All items remain pending. This map tracks execution targets, not completed claims.

| Gate | Current source paths | Existing or planned test paths | Required evidence form | Concrete first red scenario |
| --- | --- | --- | --- | --- |
| 127.1 | `crates/iris-runtime/src/decorator_protocol/signature.rs`, `crates/iris-runtime/src/decorator_protocol/changes.rs`, `crates/iris-runtime/src/decorator_protocol/phase.rs`, `crates/iris-runtime/src/decorator_protocol/errors.rs`, `crates/iris-runtime/src/decorator_protocol/invocation.rs`, `crates/iris-eval/src/source_runtime/decorator_core.rs`, `crates/iris-vm/src/machine/decorator_core.rs` | existing: `crates/iris-cli/tests/decorator_protocol/argument_changes_available.iris`, `crates/iris-cli/tests/decorator_protocol/immutable_snapshots.iris`, `crates/iris-eval/tests/decorator_core.rs`, `crates/iris-vm/tests/decorator_core.rs` | Type reification on both backends, property read verification, mutation rejection | Read `Invocation.new()` or `DecoratorContext.new()`, verify `MessageNotFound` or non-constructible refusal; read `ArgumentChanges.empty`, verify immutability via `changes.block = nil` raising `ReadonlyMutationError`. |
| 127.2 | `crates/iris-eval/src/source_runtime/decorator_core.rs`, `crates/iris-eval/src/source_runtime/decorator_phases.rs`, `crates/iris-vm/src/machine/decorator_phases.rs`, `crates/iris-vm/src/compile/core_contracts.rs` | existing: `crates/iris-eval/tests/decorator_phases.rs`, `crates/iris-eval/tests/contract_decorators.rs`; add: `crates/iris-cli/tests/decorator_protocol/missing_phase_member.iris` | Both evaluator and VM contract validation, phase dispatch, missing member diagnostics | Apply decorator missing either `plan` or `transform`, verify rejection; supply both members with exact signatures on all five targets, verify successful phase invocation. |
| 127.3 | `crates/iris-eval/src/source_runtime/decorator_phases.rs`, `crates/iris-vm/src/machine/decorator_phases.rs`, `crates/iris-vm/src/machine/callback_methods.rs` | existing: `crates/iris-cli/tests/decorator_protocol/phase_instances.iris`, `crates/iris-eval/tests/decorator_phases.rs` | Identity observation of fresh decorator instances across phases, empty transform acceptance | Decorator instance mutates `@seen` during `initialize`, verify `@seen` resets between `plan` and `transform` runs; decorator arguments reach phase methods rather than `initialize`. |
| 127.4 | `crates/iris-parser/src/analysis.rs`, `crates/iris-eval/src/source_runtime/decorator_planning.rs`, `crates/iris-vm/src/machine/decorator_phases.rs` | existing: `crates/iris-cli/tests/decorator_protocol/impure_initialize.iris`, `crates/iris-cli/tests/decorator_protocol/impure_plan.iris`, `crates/iris-eval/tests/decorator_phases.rs` | Static diagnostic emission, phase termination before runtime mutation | Call IO, `print`, or read ambient state inside `initialize` or helper called by `plan`, verify static `IRIS-DECORATOR-NONDETERMINISTIC` with zero candidate mutation. |
| 127.5 | `crates/iris-runtime/src/decorator_protocol/phase.rs`, `crates/iris-runtime/src/decorator_protocol/transformation.rs`, `crates/iris-eval/src/source_runtime/decorator_phases.rs`, `crates/iris-vm/src/machine/decorator_core.rs` | existing: `crates/iris-cli/tests/decorator_protocol/plan_outside_phase.iris`, `crates/iris-cli/tests/decorator_protocol/transformation_outside_phase.iris`, `crates/iris-eval/tests/decorator_core.rs`, `crates/iris-vm/tests/decorator_core.rs` | Property access on empty records, catchable protocol error with nil owner outside phase | Evaluate `Plan.empty` or `Transformation.empty` outside active phase, verify raised `DecoratorProtocolError` with category `:outside_phase` and owner `nil`; verify read-only `kind` inside phase matches target. |
| 127.6 | `crates/iris-runtime/src/decorator_protocol/phase.rs`, `crates/iris-eval/src/source_runtime/decorator_phases.rs`, `crates/iris-eval/src/source_runtime/decorator_core_tests.rs`, `crates/iris-vm/src/machine/decorator_core_tests.rs` | existing: `crates/iris-cli/tests/decorator_protocol/phase_instances.iris`, `crates/iris-eval/src/source_runtime/decorator_core_tests.rs`, `crates/iris-vm/src/machine/decorator_core_tests.rs` | Immutable `DecoratorContext` field reads, invalid mutation rejection, reason coverage | Inspect `context.kind` and `context.reason` across `:origin`, `:open`, `:upgrade`, `:rollback`, `:closed_materialization`; attempt `context.kind = :class` to observe `ReadonlyMutationError`. |
| 139.kind | `crates/iris-parser/src/analysis.rs`, `crates/iris-eval/src/source_runtime/decorator_phases.rs`, `crates/iris-vm/src/machine/decorator_phases.rs`, `crates/iris-vm/src/machine/module_decorators.rs` | existing: `crates/iris-cli/tests/decorator_protocol/wrong_target_kind.iris`, `crates/iris-cli/tests/decorator_protocol/wrong_kind_rollback.iris`, `crates/iris-eval/tests/decorator_phases.rs`, `crates/iris-vm/tests/decorator_contract.rs` | Diagnostic code `IRIS-DECORATOR-KIND`, static vs candidate validation phase, candidate rollback | Apply `MethodDecorator` to class declaration, verify static `IRIS-DECORATOR-KIND`; return wrong-kind `Transformation` from runtime `transform`, verify candidate rollback without revision bump. |
| 139.purity | `crates/iris-parser/src/analysis.rs`, `crates/iris-eval/src/source_runtime/decorator_planning.rs`, `crates/iris-vm/src/machine/decorator_phases.rs` | existing: `crates/iris-cli/tests/decorator_protocol/impure_initialize.iris`, `crates/iris-cli/tests/decorator_protocol/impure_plan.iris`, `crates/iris-eval/tests/decorator_phases.rs` | Diagnostic code `IRIS-DECORATOR-NONDETERMINISTIC`, static error severity, target preservation | Access mutable global or trigger I/O inside transitive plan helper, verify `IRIS-DECORATOR-NONDETERMINISTIC` error at static phase and no publication of decorated target. |
| 139.error | `crates/iris-runtime/src/decorator_protocol/errors.rs`, `crates/iris-eval/src/source_runtime/decorator_core.rs`, `crates/iris-vm/src/machine/decorator_core.rs` | existing: `crates/iris-eval/tests/decorator_core.rs`, `crates/iris-vm/tests/decorator_core.rs`; add: `crates/iris-cli/tests/decorator_protocol/protocol_error_properties.iris` | Catchable error class identity, read-only category and owner attributes | Catch `DecoratorProtocolError` from invalid protocol invocation, verify `.category` is Symbol, `.owner` is Object or nil, and constructor arguments cannot be accessed or mutated. |
| 139.outside | `crates/iris-runtime/src/decorator_protocol/phase.rs`, `crates/iris-runtime/src/decorator_protocol/transformation.rs`, `crates/iris-eval/src/source_runtime/decorator_core.rs`, `crates/iris-vm/src/machine/decorator_core.rs` | existing: `crates/iris-cli/tests/decorator_protocol/plan_outside_phase.iris`, `crates/iris-cli/tests/decorator_protocol/transformation_outside_phase.iris`, `crates/iris-eval/tests/decorator_core.rs`, `crates/iris-vm/tests/decorator_core.rs` | Synchronous catchable `DecoratorProtocolError`, category `:outside_phase`, nil owner | Call `Plan.empty` or `Transformation.wrap_method(nil)` at top-level script scope, verify caught error has `.category == :outside_phase` and `.owner same? nil`. |
| 139.expired | `crates/iris-runtime/src/decorator_protocol/scope.rs`, `crates/iris-eval/src/source_runtime/continuation/adapters.rs`, `crates/iris-eval/src/source_runtime/wrapper_execution.rs`, `crates/iris-vm/src/machine/decorator_async.rs` | existing: `crates/iris-cli/tests/decorator_async/expired.iris`, `crates/iris-cli/tests/decorator_async.rs`; add: `crates/iris-eval/tests/async_decorator_expired.rs` | Precedence over foreign task, sync exception or async failed bridge Task | Retain `next` closure beyond wrapper callback completion, call `next.call()`; verify sync raises `DecoratorProtocolError(:expired)` and async returns failed `Task<Object>` without entering inner body. |
| 139.foreign | `crates/iris-runtime/src/decorator_protocol/scope.rs`, `crates/iris-eval/src/source_runtime/wrapper_execution.rs`, `crates/iris-vm/src/machine/decorator_async.rs` | existing: `crates/iris-cli/tests/decorator_async/foreign_eager.iris`, `crates/iris-cli/tests/decorator_async.rs`; add: `crates/iris-eval/tests/async_decorator_foreign.rs` | Task ID comparison between caller and owner, failed bridge Task | Pass `next` into an unrelated async worker task, invoke `next.call()`; verify rejection with `:foreign_task` category before inner method body executes. |
| 139.overlap | `crates/iris-runtime/src/decorator_protocol/scope.rs`, `crates/iris-eval/src/source_runtime/wrapper_execution.rs`, `crates/iris-vm/src/machine/decorator_async.rs` | existing: `crates/iris-cli/tests/decorator_async/overlap.iris`, `crates/iris-cli/tests/decorator_async.rs`; add: `crates/iris-eval/tests/async_decorator_overlap.rs` | In-flight flag preservation, rejection without inner reentry | Invoke `next.call()` while a prior attempt from the same layer remains pending, verify `:overlap` error and confirm existing attempt completes undisturbed. |
| 139.unfinished | `crates/iris-runtime/src/decorator_protocol/scope.rs`, `crates/iris-eval/src/source_runtime/continuation/adapters.rs`, `crates/iris-vm/src/machine/task_adapters.rs` | existing: `crates/iris-cli/tests/decorator_async/unfinished_normal.iris`, `crates/iris-cli/tests/decorator_async.rs`; add: `crates/iris-eval/tests/async_decorator_unfinished.rs` | Admission failure `Task<R>`, continued inner execution without cancellation | Async wrapper returns result while inner `next.call()` attempt is still in flight; verify outer admission fails with `DecoratorProtocolError(:unfinished_inner)` while inner task completes. |
| 139.diagnostic | `crates/iris-eval/src/source_runtime/continuation/adapters.rs`, `crates/iris-vm/src/machine/task_adapters.rs` | existing: `crates/iris-eval/tests/async_decorator_lifecycle.rs`; add: `crates/iris-cli/tests/decorator_async/unfinished_exceptional_diagnostic.iris` | Structured lifetime diagnostic tuple emission without altering primary exception | Exceptional wrapper exits while inner attempt runs; verify primary exception is preserved and diagnostic tuple `IRIS-DECORATOR-PROTOCOL` contains task and closure IDs. |
| 139.expression | `crates/iris-eval/src/source_runtime/wrapper_execution.rs`, `crates/iris-vm/src/machine/decorator_wrappers.rs`, `crates/iris-vm/src/machine/decorator_async.rs` | existing: `crates/iris-cli/tests/decorator_async/invalid_initial.iris`, `crates/iris-cli/tests/decorator_async/bad_patch.iris`; add: `crates/iris-eval/tests/decorator_expression_errors.rs` | Ordinary expression site failure vs failed next bridge Task distinction | Construct invalid patch arguments at expression site to verify immediate `ArgumentError` or `TypeError` before `next.call()` returns, and verify scope failure inside async next produces failed bridge. |

## Actionable First Red Tests

1. Port `changes-empty.iris` and `changes-new.iris` into an authorized implementation test wave: first require available immutable no-change records, then all five constructor fields, presence/snapshot isolation and exact argument/type errors. Keep the unknown-field negative red until the positive constructor is demonstrably available (127.1, all 132 rows).
2. Use `plan-outside-phase.iris`, `transformation-outside-phase.iris` and `wrap-factory-outside-phase.iris`; assert catchable error category/owner, not generic failure. Pair each with all-kind inside-phase success, constructor/helper extent and retained-tag rejection (127.5-127.6, 139.outside/kind).
3. Use `method-next-once.iris` without local substitute Contracts. Establish both required built-in members and exact callback signature first, then verify invocation fields, body-entry count and `next.call()` optional default. A VM refusal stays visible until genuinely implemented (127.2, 131, 134, 135).
4. Add instrumented zero-attempt, one-attempt and retry-after-failure siblings: invalid source call never enters a cache, defaults/operands run once, attempts revalidate every channel and return boundary, and Logged/Cached/Retry repeats only its inner suffix (129-135).
5. Add deterministic async Task-controlled cases for eager prefix, fresh bridge identity, owner suspension, foreign eager helper, overlap, expiry and unfinished normal/exceptional completion with continued inner work/unobserved failures. Then validate replay/retained/tombstone/qualified/native/GC boundaries on their real surfaces (136-139).

Readiness remains pending for every row until those tests observe the specified behavior on applicable backends with failure-phase and side-effect evidence. A green legacy library suite, documentation publication or feature-name catalog entry does not promote a checkbox.
