{"schema_version": "iris-v1-vector-schema-1", "id": "IRIS-V1-CONTROL-V004", "name": "Definite assignment before write", "category": "positive", "source": {"chapter": "CONTROL", "artifact": "spec/iris-v1/04-bindings-callables-control-flow.md", "clauses": [], "decisions": []}, "input": {"kind": "fixture", "fixture_ref": "frozen row: Definite assignment before write"}, "applicability": {"interpreter": "required", "jit": "required", "native": "not_applicable", "reason": "Blocked by evaluator frame pressure, now INDEPENDENTLY REPRODUCED rather than carried forward. A second implementation was written from scratch: `DeferredBinding` creates an empty cell, `Binding` carries a `deferred` flag the first assignment clears, and an unresolved read separates DefiniteAssignmentError from NameError. It WORKS - `mut x: Integer; x = 5; x` answers 5, a read before assignment answers DefiniteAssignmentError, and reassignment answers the new value - but the non-termination test overflows the host stack, which is the same failure the first attempt hit. Two placements were tried: a guarded match arm on the recursive name path, and a fold into that arm's existing tail with the decision made out of line. Both overflowed; the baseline without the change passes, so the change is the cause. The recursive expression path has no headroom for either shape, so closing this needs the frame reduction recorded under IRIS-V1-CONTROL-C033 rather than another placement attempt."}, "expect": {"status": "DefiniteAssignmentError."}, "tags": ["bucket:needs-subsystem"]}
