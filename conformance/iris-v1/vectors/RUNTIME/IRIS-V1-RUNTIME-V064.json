{"schema_version": "iris-v1-vector-schema-1", "id": "IRIS-V1-RUNTIME-V064", "name": "Shift resource quota", "category": "negative", "source": {"chapter": "RUNTIME", "artifact": "spec/iris-v1/03-runtime-object-model.md", "clauses": ["IRIS-V1-RUNTIME-C160"], "decisions": ["D-038"]}, "input": {"kind": "fixture", "fixture_ref": "{memory_limit_bytes: 1048576, source: \"1 << 1000000000\"}"}, "applicability": {"interpreter": "required", "jit": "required", "native": "not_applicable", "reason": "Requires the resource-limit quota harness, confirmed by measurement rather than assumed. The row's fixture pairs a memory_limit_bytes with `1 << 1000000000` and expects ResourceError or MemoryLimitError. Probing the source half alone did not fail fast: it ran unbounded until the harness timed out after 30 minutes, which is the clearest possible evidence that no limit is being enforced. Grepping confirms the cause: no memory_limit, quota, ResourceError or MemoryLimitError surface exists anywhere in the evaluator. The row needs a harness that IMPOSES the declared limit, since without one the shift is simply an unbounded allocation rather than a refusal."}, "expect": {"error": "ResourceError or MemoryLimitError in runtime phase."}, "tags": ["bucket:needs-subsystem"]}
