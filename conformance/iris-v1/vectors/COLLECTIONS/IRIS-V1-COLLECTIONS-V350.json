{"schema_version": "iris-v1-vector-schema-1", "id": "IRIS-V1-COLLECTIONS-V350", "name": "Contract requirement return types reflect as normalized Types", "category": "positive", "source": {"chapter": "COLLECTIONS", "artifact": "spec/iris-v1/06-collections-text-regex.md", "clauses": ["IRIS-V1-COLLECTIONS-C013"], "decisions": ["D-466"]}, "input": {"kind": "source", "source_text": "contract Numbers extends Iterable<Integer> { fun iterator() -> Iterator<Integer> } module Q { public fun run() -> Object { [Reflection::Contract.requirement(Numbers, :iterator)[:return_type], Reflection::Contract.requirement(Iterator<Integer>, :next)[:return_type], Reflection::Contract.requirement(Iterator<Integer>, :close)[:return_type]] } } Q.run()"}, "applicability": {"interpreter": "required", "jit": "required", "native": "not_applicable", "reason": "Supported. D-466 fixes the traversal Contracts as `contract Iterable<T> { fun iterator() -> Iterator<T> }` and `contract Iterator<T> { fun next() -> Iteration<T>; fun close() -> Nil }` and says `for` uses these EXACT Contracts, so they are now built in rather than requiring each program to declare the Contract its own `for` loop already relies on. Three gaps were closed: the built-in names, a closed generic naming a CONTRACT (C061 interns one Contract per generic definition, so `Iterator<Integer>` is the same Contract as bare `Iterator`), and requirement reflection exposing the declared return Type. The row's own `contract Numbers for Iterable<Integer>` spelling is ungrammatical - `contract_decl` fixes the clause as `extends` - which is recorded in docs/spec-defects-v1.md, so the observation uses `extends`."}, "expect": {"value": {"array": [{"opaque": true}, {"opaque": true}, {"opaque": true}]}}, "tags": ["bucket:executable"]}
